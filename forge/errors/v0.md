# v0 错误记录

# 记录规则

- 本文件集中记录 v0 大版本下的小版本错误。
- 每个错误仍需包含错误信息、出现阶段、原因分析、解决方案、是否已解决。
- 小版本更新只追加错误小节，禁止创建新的小版本错误目录。

## v0.1.7

### error-001

# 错误信息

`cargo check` 首次执行时报出 `E0308` 类型不匹配：`EvolutionError::Io` 需要 `PathBuf`，但 `append_record` 中传入了 `&Path`。

# 出现阶段

代码编译验证。

# 原因分析

`append_record` 接收 `&Path`，读取文件失败时错误记录需要拥有路径所有权，旧写法使用 `path.clone()` 只能得到 `&Path`。

# 解决方案

将错误路径改为 `path.to_path_buf()`，并保持 `fs::read_to_string(path)` 使用借用路径读取。

# 是否已解决

是。后续 `cargo check` 和 `cargo test` 均已通过。

### error-002

# 错误信息

`cargo fmt --check` 首次执行发现 `src/layout.rs` 和 `src/lib.rs` 存在格式差异。

# 出现阶段

格式验证。

# 原因分析

手工调整测试和布局代码后，部分链式路径调用未按 rustfmt 规则收敛。

# 解决方案

执行 `cargo fmt` 统一格式化。

# 是否已解决

是。已重新执行 `cargo fmt --check` 并通过。

## v0.1.8

# 错误信息

本轮实现阶段未出现新增错误。新增错误测试用于确认 workspace 根目录出现未规划文件时会被验证流程拒绝。

# 出现阶段

布局验证设计。

# 原因分析

如果 major workspace 长期复用但根目录不受约束，生成文件容易直接堆积在 `workspaces/v0`，导致查找困难和边界混乱。

# 解决方案

在 `layout` 中新增 workspace 根目录白名单校验，并要求文件进入固定职责目录继续分层。

# 是否已解决

是。`validation_rejects_unplanned_workspace_root_entries` 已覆盖该错误路径。

## v0.1.9

# 错误信息

本轮实现阶段未出现新增错误。新增错误测试用于确认 Markdown 文档出现 Emoji 时会被验证流程拒绝。

# 出现阶段

文档规范验证。

# 原因分析

如果只在规则中声明禁止 Emoji，后续文档仍可能无意引入 Emoji，导致风格和审计格式不稳定。

# 解决方案

在 `documentation` 校验中增加 Emoji 检测，并补充 `validation_rejects_markdown_emoji` 测试。

# 是否已解决

是。`cargo test` 已覆盖该错误路径。

## v0.1.10

# 错误信息

首次测试时，`runtime_executes_command_inside_version_workspace` 使用未规范化的 temp 路径断言 `run_dir`，而 Runtime 返回的是 canonical workspace 路径，导致断言失败。

# 出现阶段

单元测试验证。

# 原因分析

Runtime 为了防止工作区逃逸，会对 workspace 执行 canonicalize。测试直接使用 temp 根目录拼接路径，没有与 Runtime 返回路径保持同一规范。

# 解决方案

将测试改为验证 `run_dir` 位于 `report.workspace` 下，并检查父目录为 `runs`，不再混用未规范化路径。

# 是否已解决

是。修复后 `cargo test` 通过，24 个测试全部成功。

## v0.1.11

# 错误信息

本轮未出现功能错误。`cargo fmt --check` 首次发现测试代码中 `index.jsonl` 读取语句的换行格式不符合 rustfmt。

# 出现阶段

格式验证。

# 原因分析

手工新增索引测试时链式调用换行未按 rustfmt 规则收敛。

# 解决方案

执行 `cargo fmt` 统一格式，并重新运行 `cargo fmt --check`、`cargo test`。

# 是否已解决

是。格式、编译、测试和 SelfForge 验证均已通过。

## v0.1.12

# 错误信息

本轮未出现功能错误。`cargo fmt --check` 首次发现 Runtime 和 CLI 新增代码存在 rustfmt 换行差异。

# 出现阶段

格式验证。

# 原因分析

手工添加 `runs` 查询入口和索引解析时，部分链式调用和闭包格式未按 rustfmt 规则排列。

# 解决方案

执行 `cargo fmt` 统一格式，并重新运行格式、编译、测试和 SelfForge 验证。

# 是否已解决

是。全部验证已通过。

## v0.1.13

# 错误信息

本轮未出现功能错误。文档整理阶段发现 `forge/tasks/v0.md` 的 `v0.1.13` 任务段落曾插入到旧版 `v0.1.7` 记录中间，影响归档可读性。

# 出现阶段

文档归档。

# 原因分析

追加任务记录时使用的补丁上下文过于通用，匹配到了早期版本中相同的候选生成计划段落。

# 解决方案

删除误插入段落，并使用包含 `v0.1.12` 任务描述的精确上下文重新追加 `v0.1.13` 任务记录。

# 是否已解决

是。已重新检查 `forge/tasks/v0.md` 中 `v0.1.13` 的位置。

## v0.1.14

# 错误信息

本轮发现运行索引查询会读取同一 major 工作区内其他小版本的记录，导致 `runs --current` 可能显示旧小版本运行。

# 出现阶段

运行索引查询测试。

# 原因分析

小版本共用 `workspaces/v0/sandbox/runs/index.jsonl`，但 Runtime 查询只按 major 工作区读取文件，没有再按索引行中的 `version` 字段过滤。

# 解决方案

在 Runtime `query_runs` 中增加 `entry.version == 请求版本` 过滤，并补充同一 major 工作区不同小版本运行记录的测试。

# 是否已解决

是。`cargo test` 和真实 `runs --current --failed` 验证均已通过。

## v0.1.14 运行错误 run-1777557380568972400-000

# 错误信息

- 运行编号：run-1777557380568972400-000
- 版本：v0.1.14
- 程序：`C:\Users\xjuun\Space\Projects\SelfForge\target\debug\selfforge.exe`
- 参数：`unknown-command-for-error-archive`
- 退出码：Some(2)
- 是否超时：false
- 标准输出字节：0
- 标准错误字节：463
- 报告文件：`workspaces/v0/sandbox/runs/run-1777557380568972400-000/report.json`

# 出现阶段

Runtime 受控执行验证

# 原因分析

运行索引显示该命令出现非零退出或超时。详细原因需要结合运行报告、标准输出和标准错误继续分析。

# 解决方案

这是功能验证用失败运行，归档能力已验证，后续无需修改程序行为。

# 是否已解决

是。验证依据：v0.1.15 已通过 cargo test 与 cargo run -- validate，错误归档能力验证完成

## v0.1.15

# 错误信息

本轮未出现功能错误。已使用 `resolve-error` 将 v0.1.14 的验证用运行错误标记为已解决。

# 出现阶段

错误生命周期验证。

# 原因分析

上一轮为了验证错误归档能力，故意执行了未知命令并生成失败运行记录。该错误属于功能验证样例，不代表当前系统行为缺陷。

# 解决方案

执行 `resolve-error --version v0.1.14 --run-id run-1777557380568972400-000`，并记录 `cargo test` 与 `cargo run -- validate` 通过作为验证依据。

# 是否已解决

是。
## v0.1.30 最终错误记录

# 错误信息

本轮未发现功能错误。

# 出现阶段

Agent 会话事件时间线实现、兼容性测试、真实 `agent-evolve` 提升和最终会话审计阶段。

# 原因分析

新增事件字段均使用默认值兼容旧会话和旧索引。真实执行已生成带 9 条事件的 Agent 会话，并成功将 v0.1.30 提升为当前稳定版本。

# 解决方案

继续保持事件时间线内嵌于同一个会话 JSON。后续若将 Runtime 运行记录或错误归档关联到事件，必须以引用字段扩展事件结构，禁止创建平行事件存储体系。

# 是否已解决

是。当前版本无未解决错误，`errors --current --open --limit 5` 返回空记录。
## v0.1.31 最终错误记录

# 错误信息

本轮未发现功能错误。

# 出现阶段

Agent 运行记录关联实现、Runtime 运行引用测试、真实 `agent-evolve` 提升和最终验证阶段。

# 原因分析

新增 `agent-run` 复用了 Supervisor 与 Runtime 受控执行，并通过测试覆盖成功运行、非零退出失败运行和旧事件兼容读取。真实执行已成功将 v0.1.31 提升为当前稳定版本。

# 解决方案

继续保持运行证据由 Runtime 生成，Agent 会话只保存引用。后续自动验证编排必须复用 `agent-run` 或同等应用层用例，禁止绕过 Runtime 直接执行测试。

# 是否已解决

是。当前版本无未解决错误，`errors --current --open --limit 5` 返回空记录。
## v0.1.29 最终错误记录

# 错误信息

本轮未发现功能错误。

# 出现阶段

Agent 跨版本会话查询实现、测试验证、真实 `agent-evolve` 提升和最终审计查询阶段。

# 原因分析

新增 `agent-sessions --all` 复用了同一 major 工作区内的既有会话索引，并通过测试覆盖了版本过滤、跨版本查询、最新摘要去重、零 limit 边界和版本提升后的查询场景。真实执行已成功将 v0.1.29 提升为当前稳定版本。

# 解决方案

继续保持 Agent 会话只写入 `workspaces/vMAJOR/artifacts/agents/` 的规则。后续若新增更多审计视图，必须优先读取既有聚合索引，禁止创建小版本索引碎片。

# 是否已解决

是。当前版本无未解决错误，`errors --current --open --limit 5` 返回空记录。

## v0.1.16

# 错误信息

本轮未出现功能错误。

# 出现阶段

错误列表查询验证。

# 原因分析

新增 `errors` 只读查询能力未发现实现错误。

# 解决方案

继续保持测试和真实命令双重验证。

# 是否已解决

是。

## v0.1.17

# 错误信息

测试阶段出现一处 Rust 模式匹配字段绑定写法错误，导致首次 `cargo test` 编译失败。

# 出现阶段

测试验证。

# 原因分析

测试断言中将 `ref` 写在结构体字段名前，正确写法应放在字段绑定冒号之后。

# 解决方案

将 `ref run_id: error_run_id` 修正为 `run_id: ref error_run_id`，并重新执行格式、编译和测试。

# 是否已解决

是。修复后 `cargo test` 通过，32 个测试全部成功。

## v0.1.18

# 错误信息

本轮未发现功能错误。

# 出现阶段

进化前置检查、测试验证和候选提升。

# 原因分析

`preflight`、单元测试、真实 `advance`、`cycle` 和 `validate` 均通过，未出现需要修复的实现缺陷。

# 解决方案

继续保持测试和真实命令双重验证。后续若 `preflight` 报告未解决错误，必须先解决或明确归档处理后再继续进化。

# 是否已解决

是。本轮无未解决错误，`errors --current --open --limit 5` 返回空记录。

## v0.1.19

# 错误信息

本轮未发现功能错误。

# 出现阶段

AI 配置实现、测试验证和候选提升。

# 原因分析

`ai-config`、单元测试、真实 `advance`、`cycle` 和 `validate` 均通过，未出现需要修复的实现缺陷。

# 解决方案

继续保持密钥不落盘、不输出、不进入状态文件或归档正文。后续新增真实 AI 调用时必须继续复用本轮配置层。

# 是否已解决

是。本轮无未解决错误，`errors --current --open --limit 5` 返回空记录。

## v0.1.20

# 错误信息

本轮未发现功能错误。

# 出现阶段

AI 请求构建实现、测试验证和候选提升。

# 原因分析

`ai-request`、单元测试、真实 `advance`、`cycle` 和 `validate` 均通过，未出现需要修复的实现缺陷。

# 解决方案

继续保持请求摘要脱敏。后续新增真实 HTTP 执行时，必须把超时、错误归档和响应脱敏纳入同一流程。

# 是否已解决

是。本轮无未解决错误，`errors --current --open --limit 5` 返回空记录。

## v0.1.21

# 错误信息

本轮未发现功能错误。

# 出现阶段

AI 响应解析实现、测试验证和候选提升。

# 原因分析

响应解析测试、真实 `advance`、`cycle` 和 `validate` 均通过，未出现需要修复的实现缺陷。

# 解决方案

继续保持响应解析的显式错误返回。后续真实 HTTP 执行层必须在解析失败时停止 Agent 流程并记录错误。

# 是否已解决

是。本轮无未解决错误，`errors --current --open --limit 5` 返回空记录。

## v0.1.22

# 错误信息

用户反馈在 PowerShell 中设置 `SELFFORGE_AI_PROVIDER=deepseek` 后运行 `ai-request` 仍提示没有可用 AI 提供商。

# 出现阶段

AI 请求构建前置配置检查。

# 原因分析

PowerShell 当前会话需要使用 `$env:SELFFORGE_AI_PROVIDER="deepseek"` 形式设置环境变量；同时 DeepSeek 请求还必须设置 `DEEPSEEK_API_KEY`。原错误信息没有清楚提示这两点，导致用户难以判断是 provider 未传入子进程，还是 API Key 缺失。

# 解决方案

改进 `MissingProvider` 和 `MissingApiKey` 错误消息，直接提示 PowerShell 当前会话设置方式、对应 API Key 变量和 `cargo run -- ai-config` 检查命令，并补充测试覆盖。

# 是否已解决

是。缺少 DeepSeek Key 的真实命令现在会明确提示设置 `$env:DEEPSEEK_API_KEY="你的密钥"`，设置临时 Key 后 `ai-request` 可生成脱敏请求摘要。

## v0.1.23

# 错误信息

本候选生成阶段暂无错误。

# 出现阶段

候选生成。

# 原因分析

暂无。

# 解决方案

暂无。

# 是否已解决

是。
## v0.1.23 用户反馈错误

# 错误信息

用户反馈：项目根目录 `.env` 中配置 AI 提供商和密钥后，`cargo run -- ai-request "你好"` 仍提示没有可用 AI 提供商。

# 出现阶段

AI 请求构建前的配置读取阶段。

# 原因分析

上一版本只读取当前进程环境变量，没有自动读取项目根目录 `.env`。因此当用户把 `SELFFORGE_AI_PROVIDER` 和 `DEEPSEEK_API_KEY` 写入 `.env`，但未在 PowerShell 当前会话中显式设置环境变量时，AI Provider 注册表无法看到配置。

# 解决方案

在 AI Provider 模块中新增项目环境查找器，合并进程环境变量和项目根目录 `.env`。真实进程环境变量优先，`.env` 作为本地补充配置。`SelfForgeApp::ai_config` 和 `SelfForgeApp::ai_request` 已统一改用该入口，并补充测试。

# 是否已解决

是。`cargo run -- ai-config` 已能从 `.env` 识别 DeepSeek，`cargo run -- ai-request "你好"` 已能生成脱敏请求摘要，`cargo test` 57 个测试全部通过。

## v0.1.24

# 错误信息

本候选生成阶段暂无错误。

# 出现阶段

候选生成。

# 原因分析

暂无。

# 解决方案

暂无。

# 是否已解决

是。
## v0.1.24 用户反馈错误

# 错误信息

用户反馈：`cargo run -- ai-request "提示词"` 只显示请求摘要，没有显示 AI 响应文本。

# 出现阶段

AI 请求命令执行阶段。

# 原因分析

上一版本的 `ai-request` 只负责构建请求规格并输出脱敏摘要，尚未执行真实 HTTP 请求，也没有把响应交给统一文本解析器处理。

# 解决方案

新增受控 HTTP 执行层，让 `ai-request` 默认发起非流式 JSON 请求并显示归一化响应文本。保留 `ai-request --dry-run` 用于查看脱敏请求摘要。HTTP 失败会返回状态码和脱敏响应摘要，解析失败会停止流程并返回明确错误。

# 是否已解决

是。真实 DeepSeek 请求已返回状态码 200，并显示响应文本 `SelfForge 响应测试通过`。`cargo test` 60 个测试全部通过，当前版本无未解决错误。

## v0.1.25

# 错误信息

本候选生成阶段暂无错误。

# 出现阶段

候选生成。

# 原因分析

暂无。

# 解决方案

暂无。

# 是否已解决

是。
## v0.1.25

# 错误信息

本轮未发现功能错误。

# 出现阶段

多 Agent 基础模块实现、CLI 验证、测试验证和候选提升阶段。

# 原因分析

新增的 Agent 注册表、计划生成和 CLI 查询均通过测试与真实命令验证。错误路径已通过空目标、重复 Agent 标识和缺失能力测试覆盖。

# 解决方案

继续保持小步扩展策略。后续接入真实 Agent 执行时，需要先设计持久化执行会话和失败归档，再接入并行调度。

# 是否已解决

是。当前版本无未解决错误，`errors --current --open --limit 5` 返回空记录。

## v0.1.26

# 错误信息

本候选生成阶段暂无错误。

# 出现阶段

候选生成。

# 原因分析

暂无。

# 解决方案

暂无。

# 是否已解决

是。
## v0.1.26 最终错误记录

# 错误信息

本轮未发现功能错误。

# 出现阶段

Agent 会话持久化实现、CLI 验证、测试验证和候选提升阶段。

# 原因分析

新增会话存储、索引查询和加载能力均通过测试与真实命令验证。错误路径已通过空目标、缺失会话和非法会话标识测试覆盖。

# 解决方案

继续保持当前小步扩展策略。后续接入真实 Agent 执行前，需要先补充步骤状态机、执行结果归档和失败恢复规则。

# 是否已解决

是。当前版本无未解决错误，`errors --current --open --limit 5` 返回空记录。

## v0.1.27

# 错误信息

本候选生成阶段暂无错误。

# 出现阶段

候选生成。

# 原因分析

暂无。

# 解决方案

暂无。

# 是否已解决

是。
## v0.1.27 最终错误记录

# 错误信息

本轮未发现功能错误。

# 出现阶段

Agent 自动进化编排、会话状态回写、CLI 验证、测试验证和候选提升阶段。

# 原因分析

新增 `agent_advance` 复用了现有 `preflight` 和 `advance` 流程，并通过测试覆盖了成功准备候选、已有候选推进、开放错误阻断和会话失败标记。

# 解决方案

继续保持 Agent 编排层只调用既有受控流程的原则。后续接入 AI 执行时，仍必须先通过会话状态、错误归档和 Runtime 沙箱验证。

# 是否已解决

是。当前版本无未解决错误，`errors --current --open --limit 5` 返回空记录。

## v0.1.28

# 错误信息

本候选生成阶段暂无错误。

# 出现阶段

候选生成。

# 原因分析

暂无。

# 解决方案

暂无。

# 是否已解决

是。
## v0.1.28 最终错误记录

# 错误信息

本轮未发现功能错误。

# 出现阶段

Agent 单轮完整进化实现、测试验证、真实 `agent-evolve` 提升和最终验证阶段。

# 原因分析

新增 `agent-evolve` 复用了现有候选准备和 `cycle`，并通过测试覆盖了无候选、有候选和开放错误阻断路径。真实执行已成功将 v0.1.28 提升为当前稳定版本。

# 解决方案

继续保持 Agent 只编排受控流程的原则。后续若接入 AI 生成和代码写入，仍必须通过 Runtime 沙箱、测试、错误归档和 cycle 验证。

# 是否已解决

是。当前版本无未解决错误，`errors --current --open --limit 5` 返回空记录。

## v0.1.29

# 错误信息

本候选生成阶段暂无错误。

# 出现阶段

候选生成。

# 原因分析

暂无。

# 解决方案

暂无。

# 是否已解决

是。

## v0.1.30

# 错误信息

本候选生成阶段暂无错误。

# 出现阶段

候选生成。

# 原因分析

暂无。

# 解决方案

暂无。

# 是否已解决

是。

## v0.1.31

# 错误信息

本候选生成阶段暂无错误。

# 出现阶段

候选生成。

# 原因分析

暂无。

# 解决方案

暂无。

# 是否已解决

是。
