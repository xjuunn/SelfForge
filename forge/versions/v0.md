# v0 版本记录

# 记录规则

- 本文件集中记录 v0 大版本下的小版本变化。
- 小版本更新只追加到本文件，禁止创建新的小版本版本文件。

## v0.1.7

# 版本变化

- 从 v0.1.6 生成 v0.1.7 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.7`，并将当前工作区规范化为 `workspaces/v0`。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。
- 旧状态工作区规范化规则。
- 防止小版本独立目录和独立归档文件的测试覆盖。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.40 最终版本记录

# 版本变化

- 从 `v0.1.39` 提升为 `v0.1.40`。
- 继续复用 major 工作区 `workspaces/v0`。
- 小版本记录继续写入 `forge/*/v0.md`。
- 新增冷归档文件 `forge/memory/archive/v0.md`，用于保存久远完整记忆。

# 新增功能

- 新增热记忆压缩与冷归档能力。
- 新增 `memory-compact` 命令。
- `agent-evolve` 在候选提升成功后会自动压缩热记忆。
- 默认记忆读取只访问热记忆文件，避免久远无关内容进入 AI 上下文。

# 修复内容

- 修正记忆文件长期追加导致单个 Markdown 文件过大、上下文读取浪费的问题。
- 将默认热记忆保留窗口收敛为最近 5 条完整记忆。

## v0.1.39 最终版本记录

# 版本变化

- 从 `v0.1.38` 生成并提升 `v0.1.39`。
- 本轮使用 `agent-evolve` 完成候选准备、验证和提升。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.39`。

# 新增功能

- 新增 Agent 会话自动步进能力。
- 新增 `agent-step` 命令。
- 新增应用层 `execute_next_agent_step`。
- 步进器可自动选择下一条待执行步骤中的可运行工具。
- Runtime 步骤可通过显式命令执行，并继续复用沙箱运行记录。

# 修复内容

- 修正新增步进器代码的格式化差异。
- 补充缺少输入时保持步骤待执行的错误测试，避免自动执行伪造成功结果。

## v0.1.38 最终版本记录

# 版本变化

- 从 `v0.1.37` 生成并提升 `v0.1.38`。
- 本轮使用 `agent-evolve` 完成候选准备、验证和提升。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.38`。

# 新增功能

- 新增 Agent Tool 统一调用协议。
- 新增 `agent-tool-run` 命令。
- 新增应用层 `invoke_agent_tool`，集中执行工具调用权限校验、输入校验和分发。
- 支持通过工具调用读取记忆、提取记忆经验、查询会话、执行 Runtime、预览 AI 请求和查询 forge 归档路径。
- Runtime 工具调用可复用 Agent 会话和运行记录。

# 修复内容

- 修正版本提升后下一候选版本断言不准确的问题。
- 修正真实 CLI 验证脚本中会话编号解析不准确的问题。

## v0.1.32 最终版本记录

# 版本变化

- 从 `v0.1.31` 生成并提升 `v0.1.32`。
- 本轮使用 `agent-evolve` 完成候选准备、验证和提升。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.32`。

# 新增功能

- 新增 `agent-verify [goal] -- PROGRAM [ARGS...]` 命令。
- 新增 `SelfForgeApp::agent_verify`。
- 新增 `AgentVerificationReport`。
- Agent 验证会话会通过 Runtime 执行验证命令。
- Agent 验证会话会记录运行标识、运行目录、退出码、超时状态和会话事件。

# 修复内容

- 本轮主要新增验证会话能力，未修复既有缺陷。
- 新增空命令错误测试，确保验证流程失败时会标记会话失败并写入事件。
## v0.1.30 最终版本记录

# 版本变化

- 从 `v0.1.29` 生成并提升 `v0.1.30`。
- 本轮使用 `agent-evolve` 完成单轮完整进化。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.30`。

# 新增功能

- 新增 Agent 会话结构化事件时间线。
- 新增 `AgentSessionEvent` 和 `AgentSessionEventKind`。
- 会话摘要新增事件数量 `event_count`。
- `agent-session` 可显示步骤结果、最终结果、错误信息和事件时间线。
- `agent-sessions` 可显示每条会话的事件数量。

# 修复内容

- 兼容旧会话文件缺少 `events` 字段的情况。
- 兼容旧会话索引缺少 `event_count` 字段的情况。
- 新增测试确保事件记录和兼容性不会破坏既有会话查询。
## v0.1.31 最终版本记录

# 版本变化

- 从 `v0.1.30` 生成并提升 `v0.1.31`。
- 本轮使用 `agent-evolve` 完成单轮完整进化。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.31`。

# 新增功能

- 新增 `agent-run` 命令。
- 新增 `SelfForgeApp::agent_run`。
- 新增 `AgentRunReference`。
- Agent 事件可保存 Runtime 运行编号、报告路径、退出码和超时状态。
- `agent-session` 可显示运行记录引用。

# 修复内容

- 兼容旧事件缺少运行引用的情况。
- 新增测试确保失败 Runtime 运行会标记 Agent 步骤失败并保留运行证据。
## v0.1.29 最终版本记录

# 版本变化

- 从 `v0.1.28` 生成并提升 `v0.1.29`。
- 本轮使用 `agent-evolve` 完成单轮完整进化。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.29`。

# 新增功能

- 新增 `agent-sessions --all [--limit N]` 命令。
- 新增 `SelfForgeApp::agent_sessions_all`。
- 新增 `AgentSessionStore::list_all_major`。
- 会话列表输出新增会话所属版本。
- 同一 major 下可跨小版本查询最近 Agent 会话，并按会话 id 返回最新摘要。

# 修复内容

- 修复版本提升后默认当前版本会话查询看不到刚完成进化会话的审计断点。
- 新增跨版本查询和提升后查询测试，确保自动进化记录可追踪。
- 修复候选归档追加时的路径类型错误。
- 修复格式检查发现的 rustfmt 差异。

## v0.1.8

# 版本变化

- 从 v0.1.7 生成并提升 v0.1.8。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 为 `workspaces/v0` 增加固定内部骨架，避免生成文件堆积在根目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.8`。

# 新增功能

- workspace 固定分区：`source/`、`tests/`、`sandbox/`、`artifacts/`、`logs/`。
- workspace 根目录白名单校验。
- workspace 分区中文 README 与 `.gitignore`。
- workspace 结构错误测试。

# 修复内容

- 降低 major workspace 长期运行后的根目录文件膨胀风险。

## v0.1.9

# 版本变化

- 从 v0.1.8 生成并提升 v0.1.9。
- 新增根目录简洁 `README.md`。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.9`。

# 新增功能

- 根目录 README 必需文件规则。
- Markdown 文档禁止 Emoji 校验。
- README 简洁规则。

# 修复内容

- 防止文档中出现 Emoji。

## v0.1.10

# 版本变化

- 从 v0.1.9 生成并提升 v0.1.10。
- Runtime 受控执行现在会在 `workspaces/v0/sandbox/runs/` 下生成审计记录。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.10`。

# 新增功能

- Runtime 执行记录目录。
- `report.json`、`stdout.txt`、`stderr.txt` 审计文件。
- CLI `run` 输出记录目录。
- 成功执行和超时执行记录测试。

# 修复内容

- 修复 Runtime 记录测试中的路径规范化断言。

## v0.1.11

# 版本变化

- 从 v0.1.10 生成并提升 v0.1.11。
- Runtime 受控执行现在会追加 `workspaces/v0/sandbox/runs/index.jsonl`。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.11`。

# 新增功能

- Runtime 运行索引。
- 成功执行和超时执行索引测试。
- Runtime 执行索引规则。

# 修复内容

- 修复格式检查发现的 rustfmt 差异。

## v0.1.12

# 版本变化

- 从 v0.1.11 生成并提升 v0.1.12。
- 新增 Runtime 运行索引查询能力。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.12`。

# 新增功能

- Runtime `list_runs` 查询接口。
- Supervisor `list_runs` 门面。
- CLI `runs` 命令。
- 运行索引查询测试。

# 修复内容

- 修复格式检查发现的 rustfmt 差异。

## v0.1.13

# 版本变化

- 从 v0.1.12 生成并提升 v0.1.13。
- 新增 Runtime 运行索引失败筛选能力。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.13`。

# 新增功能

- Runtime `RunQuery` 查询结构。
- Runtime `query_runs` 过滤查询接口。
- Supervisor `query_runs` 门面。
- CLI `runs --failed` 与 `runs --timed-out` 参数。
- 失败、超时和零限制查询测试。

# 修复内容

- 修正文档补丁上下文过宽导致的任务记录位置错误。

## v0.1.14

# 版本变化

- 从 v0.1.13 生成并提升 v0.1.14。
- 新增失败运行错误归档能力。
- 修正运行索引查询跨小版本混读问题。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.14`。

# 新增功能

- 应用层 `ErrorArchive` 错误归档模块。
- CLI `record-error` 命令。
- 最近失败运行归档能力。
- 指定运行编号归档能力。
- 重复归档幂等处理。
- 错误归档测试覆盖。

# 修复内容

- Runtime 查询 `index.jsonl` 时按请求版本过滤记录，避免同一 major 工作区内的小版本记录混读。

## v0.1.15

# 版本变化

- 从 v0.1.14 生成并提升 v0.1.15。
- 新增已归档错误解决状态更新能力。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.15`。

# 新增功能

- 应用层 `resolve_run_error` 能力。
- `ErrorResolutionReport`。
- CLI `resolve-error` 命令。
- 已归档错误幂等解决能力。
- 错误解决状态测试覆盖。

# 修复内容

- 将 v0.1.14 的验证用错误记录标记为已解决，并记录验证依据。

## v0.1.16

# 版本变化

- 从 v0.1.15 生成并提升 v0.1.16。
- 新增已归档运行错误列表查询能力。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.16`。

# 新增功能

- `ArchivedErrorEntry`。
- `ErrorListQuery`。
- 应用层 `list_run_errors` 能力。
- CLI `errors` 命令。
- 未解决和已解决错误状态过滤。
- 错误列表查询测试覆盖。

# 修复内容

- 无功能错误修复。

## v0.1.17

# 版本变化

- 从 v0.1.16 生成并提升 v0.1.17。
- 新增 `advance` 未解决错误前置检查。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.17`。

# 新增功能

- `advance` 会在生成候选前查询当前版本未解决错误。
- 存在未解决错误时返回 `OpenErrors`，并保持状态不变。
- 新增未解决错误阻断测试。

# 修复内容

- 修复本轮测试中字段绑定语法错误。

## v0.1.18

# 版本变化

- 从 v0.1.17 生成并提升 v0.1.18。
- 新增显式 `preflight` 进化前置检查入口。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.18`。

# 新增功能

- `PreflightReport` 前置检查报告。
- `SelfForgeApp::preflight` 只读应用用例。
- CLI `preflight` 命令。
- 当前版本布局、候选版本布局和未解决错误汇总。
- 干净状态、候选状态和未解决错误状态测试覆盖。

# 修复内容

- 本轮未发现功能错误。

## v0.1.19

# 版本变化

- 从 v0.1.18 生成并提升 v0.1.19。
- 新增 AI 提供商环境变量配置能力。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.19`。

# 新增功能

- `AiProviderRegistry` 提供商配置注册表。
- `AiConfigReport` 和 `AiProviderStatus` 只读配置报告。
- CLI `ai-config` 命令。
- 支持 `OPENAI_API_KEY`、`DEEPSEEK_API_KEY`、`GEMINI_API_KEY` 和 `GOOGLE_API_KEY`。
- 支持 `SELFFORGE_AI_PROVIDER` 指定 `openai`、`deepseek` 或 `gemini`。
- 支持模型和基础地址环境变量覆盖。
- 新增 `.env.example` 示例，并忽略真实 `.env`。
- AI 配置测试覆盖。

# 修复内容

- 本轮未发现功能错误。

## v0.1.20

# 版本变化

- 从 v0.1.19 生成并提升 v0.1.20。
- 新增 AI 统一请求构建能力。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.20`。

# 新增功能

- `AiRequestSpec` 统一请求规格。
- `AiRequestError` 请求构建错误类型。
- `AiProviderRegistry::build_text_request_env`。
- `AiProviderRegistry::build_text_request_with`。
- `SelfForgeApp::ai_request` 应用入口。
- CLI `ai-request [prompt]` 命令。
- OpenAI Responses 请求体构建。
- DeepSeek Chat Completions 请求体构建。
- Gemini GenerateContent 请求体构建。
- AI 请求构建测试覆盖。

# 修复内容

- 本轮未发现功能错误。

## v0.1.21

# 版本变化

- 从 v0.1.20 生成并提升 v0.1.21。
- 新增 AI 响应文本归一化能力。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.21`。

# 新增功能

- `AiTextResponse` 统一文本响应结构。
- `AiResponseError` 响应解析错误类型。
- `AiProviderRegistry::parse_text_response`。
- OpenAI Responses 文本解析。
- DeepSeek Chat Completions 文本解析。
- Gemini GenerateContent 文本解析。
- 响应解析错误测试覆盖。

# 修复内容

- 本轮未发现功能错误。

## v0.1.22

# 版本变化

- 从 v0.1.21 生成并提升 v0.1.22。
- 改进 AI 环境变量缺失诊断。
- 复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- `state/state.json` 已将 `current_version` 提升为 `v0.1.22`。

# 新增功能

- 缺少可用 AI 提供商时提示 PowerShell 当前会话设置方式。
- 指定 DeepSeek 但缺少密钥时明确提示 `DEEPSEEK_API_KEY`。
- README PowerShell 环境变量示例。
- Agents.md Windows PowerShell 环境变量规则。
- AI 配置错误消息测试覆盖。

# 修复内容

- 修复用户反馈的 AI 环境变量诊断不清晰问题。

## v0.1.23

# 版本变化

- 从 v0.1.22 生成 v0.1.23 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.22，并记录 candidate_version 为 v0.1.23。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。
## v0.1.23 最终版本记录

# 版本变化

- 从 `v0.1.22` 生成并提升 `v0.1.23`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.23`。

# 新增功能

- `ai-config` 自动读取项目根目录 `.env`。
- `ai-request` 自动读取项目根目录 `.env`。
- 真实进程环境变量优先于 `.env`。
- `.env` 解析支持注释、`export`、单双引号和行内注释。
- README 和 `.env.example` 更新为简洁中文说明。

# 修复内容

- 修复 `.env` 中配置 DeepSeek 后仍提示没有可用 AI 提供商的问题。
- 保持 AI Key 脱敏，只输出密钥变量名，不输出密钥值。

## v0.1.24

# 版本变化

- 从 v0.1.23 生成 v0.1.24 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.23，并记录 candidate_version 为 v0.1.24。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。
## v0.1.24 最终版本记录

# 版本变化

- 从 `v0.1.23` 生成并提升 `v0.1.24`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.24`。

# 新增功能

- `ai-request` 默认发起真实 AI HTTP 请求并显示响应文本。
- `ai-request --dry-run` 显示脱敏请求摘要。
- `ai-request --timeout-ms N` 控制请求超时。
- 新增 AI HTTP 执行报告、原始响应结构和错误类型。
- 新增 HTTP 执行成功、失败状态和解析失败测试。

# 修复内容

- 修复 `ai-request` 只显示请求摘要、不显示 AI 响应文本的问题。
- HTTP 失败摘要会对 API Key 做脱敏处理。

## v0.1.25

# 版本变化

- 从 v0.1.24 生成 v0.1.25 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.24，并记录 candidate_version 为 v0.1.25。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。
## v0.1.25 最终版本记录

# 版本变化

- 从 `v0.1.24` 生成并提升 `v0.1.25`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.25`。

# 新增功能

- 新增 `src/app/agent/` 多 Agent 基础模块。
- 新增 Agent 能力模型、Agent 定义、注册表和计划结构。
- 新增标准 Agent 集合：架构、实现、验证、审查、归档。
- 新增 `agents` 命令，用于查看 Agent 目录。
- 新增 `agent-plan [goal]` 命令，用于生成多 Agent 协作计划。

# 修复内容

- 本轮未修复既有缺陷，主要新增多 Agent 扩展基础。
- 新增错误路径测试，覆盖空目标、重复 Agent 标识和缺失能力。

## v0.1.26

# 版本变化

- 从 v0.1.25 生成 v0.1.26 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.25，并记录 candidate_version 为 v0.1.26。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。
## v0.1.26 最终版本记录

# 版本变化

- 从 `v0.1.25` 生成并提升 `v0.1.26`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.26`。

# 新增功能

- 新增 Agent 会话持久化模型。
- 新增 `AgentSessionStore`，统一管理会话文件、索引、列表和加载。
- 新增 CLI `agent-start`、`agent-sessions` 和 `agent-session`。
- 新增会话版本过滤、列表限制、缺失会话和非法会话标识处理。

# 修复内容

- 本轮主要新增多 Agent 会话基础能力，未修复既有缺陷。
- 防止 Agent 会话产物写入 workspace 根目录，统一进入 `artifacts/agents/` 分层。

## v0.1.27

# 版本变化

- 从 v0.1.26 生成 v0.1.27 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.26，并记录 candidate_version 为 v0.1.27。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。
## v0.1.27 最终版本记录

# 版本变化

- 从 `v0.1.26` 生成并提升 `v0.1.27`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.27`。

# 新增功能

- 新增 Agent 自动进化入口 `agent-advance [goal]`。
- Agent 会话支持运行中、已完成和已失败状态。
- Agent 步骤支持运行中、已完成、已失败和结果记录。
- 会话索引支持多次追加更新，并在查询时返回同一会话的最新摘要。
- `agent_advance` 会执行前置检查、调用 `advance`，并将流程结果写回会话。

# 修复内容

- 本轮主要新增自动进化编排能力，未修复既有缺陷。
- 新增未解决错误阻断测试，确保 Agent 自动进化不会绕过错误归档和版本安全规则。

## v0.1.28

# 版本变化

- 从 v0.1.27 生成 v0.1.28 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.27，并记录 candidate_version 为 v0.1.28。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。
## v0.1.28 最终版本记录

# 版本变化

- 从 `v0.1.27` 生成并提升 `v0.1.28`。
- 本轮使用 `agent-evolve` 完成单轮完整进化。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.28`。

# 新增功能

- 新增 `agent-evolve [goal]` 命令。
- 新增 `SelfForgeApp::agent_evolve`。
- 新增 Agent 单轮完整进化报告结构。
- Agent 单轮进化可以在无候选时准备候选并立即执行 `cycle`。
- Agent 单轮进化可以在已有候选时直接验证候选，不额外准备新候选。
- Agent 单轮进化会将候选准备、验证、提升或回滚结果写回会话。

# 修复内容

- 本轮主要新增完整进化编排能力，未修复既有缺陷。
- 新增开放错误阻断测试，确保 `agent-evolve` 不会在未解决错误存在时准备候选。

## v0.1.29

# 版本变化

- 从 v0.1.28 生成 v0.1.29 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.28，并记录 candidate_version 为 v0.1.29。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.30

# 版本变化

- 从 v0.1.29 生成 v0.1.30 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.29，并记录 candidate_version 为 v0.1.30。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.31

# 版本变化

- 从 v0.1.30 生成 v0.1.31 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.30，并记录 candidate_version 为 v0.1.31。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.32

# 版本变化

- 从 v0.1.31 生成 v0.1.32 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.31，并记录 candidate_version 为 v0.1.32。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.33

# 版本变化

- 从 v0.1.32 生成 v0.1.33 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.32，并记录 candidate_version 为 v0.1.33。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.33 最终版本记录

# 版本变化

- 从 `v0.1.32` 生成并提升 `v0.1.33`。
- 本轮使用 `agent-evolve` 完成候选准备、验证和提升。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.33`。

# 新增功能

- 新增 `memory-context` 命令。
- 新增 `SelfForgeApp::memory_context`。
- 新增记忆上下文读取报告和错误模型。
- 最近记忆按语义化版本排序，重复版本优先选择最终记忆。
- Agent 进化和验证会话会在第一阶段记录已读取的历史记忆数量。

# 修复内容

- 本轮主要新增记忆读取能力，未修复既有缺陷。
- 新增缺失记忆归档错误测试，确保记忆读取失败不会静默推进。

## v0.1.34

# 版本变化

- 从 v0.1.33 生成 v0.1.34 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.33，并记录 candidate_version 为 v0.1.34。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.34 最终版本记录

# 版本变化

- 从 `v0.1.33` 生成并提升 `v0.1.34`。
- 本轮使用 `agent-evolve` 完成候选准备、验证和提升。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.34`。

# 新增功能

- 新增 `memory-insights` 命令。
- 新增 `SelfForgeApp::memory_insights`。
- 新增记忆经验提取报告和经验条目结构。
- Agent 进化与验证会话会记录可复用经验和优化建议数量。

# 修复内容

- 修正“未发现功能错误”被误识别为失败风险的问题。
- 新增测试覆盖结构化经验提取和占位内容过滤。

## v0.1.35

# 版本变化

- 从 v0.1.34 生成 v0.1.35 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.34，并记录 candidate_version 为 v0.1.35。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.35 最终版本记录

# 版本变化

- 从 `v0.1.34` 生成并提升 `v0.1.35`。
- 本轮使用 `agent-evolve` 完成候选准备、验证和提升。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.35`。

# 新增功能

- 新增带记忆经验的 Agent 计划报告。
- `agent-plan` 支持按版本和条数读取历史经验。
- `agent-plan` 输出成功经验、失败风险、优化建议和可复用经验摘要。

# 修复内容

- 补齐计划阶段没有真实展示记忆经验的问题。
- 新增测试覆盖带记忆经验计划的正常、边界和错误路径。

## v0.1.36

# 版本变化

- 从 v0.1.35 生成 v0.1.36 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.35，并记录 candidate_version 为 v0.1.36。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.36 最终版本记录

# 版本变化

- 从 `v0.1.35` 生成并提升 `v0.1.36`。
- 本轮使用 `agent-evolve` 完成候选准备、验证和提升。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.36`。

# 新增功能

- 新增 Agent 会话计划上下文快照。
- `agent-start`、`agent-advance`、`agent-evolve` 和 `agent-verify` 会持久化记忆依据。
- `agent-session` 会展示计划依据摘要和来源版本。

# 修复内容

- 补齐 Agent 会话缺少计划依据的问题。
- 新增旧会话文件兼容测试，避免可选字段破坏历史记录读取。

## v0.1.37

# 版本变化

- 从 v0.1.36 生成 v0.1.37 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.36，并记录 candidate_version 为 v0.1.37。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.37 最终版本记录

# 版本变化

- 从 `v0.1.36` 生成并提升 `v0.1.37`。
- 本轮使用 `agent-evolve` 完成候选准备、验证和提升。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 已将 `current_version` 更新为 `v0.1.37`。

# 新增功能

- 新增 Agent Tool 结构化定义和动态配置读取。
- 新增 `agent-tools` 查询与配置初始化命令。
- Agent 可以通过能力匹配和 `agent_bindings` 组合工具。
- `agent-plan` 和 `agent-session` 会展示工具绑定结果。

# 修复内容

- 修正版本替换误改依赖 lockfile 的问题。
- 新增动态工具配置错误测试，避免未知工具静默进入计划。

## v0.1.38

# 版本变化

- 从 v0.1.37 生成 v0.1.38 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.37，并记录 candidate_version 为 v0.1.38。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.39

# 版本变化

- 从 v0.1.38 生成 v0.1.39 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.38，并记录 candidate_version 为 v0.1.39。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.40

# 版本变化

- 从 v0.1.39 生成 v0.1.40 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- state/state.json 保持 current_version 为 v0.1.39，并记录 candidate_version 为 v0.1.40。

# 新增功能

- major 聚合工作区规则。
- major 聚合 forge 文档规则。

# 修复内容

- 减少小版本目录和文件爆炸。

## v0.1.41

# 版本变化

- 从 v0.1.40 生成 v0.1.41 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- `agent-evolve` 已验证候选并将 `state/state.json` 的 `current_version` 提升为 v0.1.41。

# 新增功能

- 新增多 AI 协作任务板，队列持久化到 `workspaces/v0/artifacts/agents/coordination/work-queue.json`。
- 新增协作任务初始化、状态查询、领取、完成和释放能力。
- 新增任务写入范围、依赖关系、验收标准和线程提示词。
- 新增写入范围冲突检测，防止多个线程同时领取重叠任务。
- 新增 CLI：`agent-work-init`、`agent-work-status`、`agent-work-claim`、`agent-work-complete`、`agent-work-release`。

# 修复内容

- 修正领取提示词中“仍可领取任务”包含当前任务的问题。
- 释放任务后恢复候选任务提示词，避免任务板保留旧线程专属提示词。
- 补充多 AI 协作规则到 `Agents.md`，明确所有线程必须先领取任务再修改文件。

## v0.1.42

# 版本变化

- 从 v0.1.41 生成 v0.1.42 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- `agent-evolve` 已验证候选并将 `state/state.json` 的 `current_version` 提升为 v0.1.42。

# 新增功能

- 协作任务领取新增默认租约，并可通过 `agent-work-claim --lease-seconds N` 设置本次领取租约。
- 协作任务状态输出新增租约信息，显示无租约、剩余秒数或已过期。
- 新增 `agent-work-reap`，用于释放租约已过期的任务。
- 过期清理会恢复任务为待领取状态，清空领取者和租约，并写入队列事件。

# 修复内容

- 防止 AI 线程中断后任务永久停留在已领取状态。
- 补充租约规则到 `Agents.md`，明确过期任务必须先清理再重新领取。

## v0.1.43

# 版本变化

- 从 v0.1.42 生成 v0.1.43 候选版本。
- 复用 major 工作区 `workspaces/v0`，不再创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录均追加到 major 聚合文件。
- `agent-evolve` 已验证候选并将 `state/state.json` 的 `current_version` 提升为 v0.1.43。

# 新增功能

- Agent 会话计划上下文新增协作任务板信息。
- `agent-start` 创建会话时自动初始化或复用协作任务板。
- Agent 会话事件新增协作任务板准备记录。
- `agent-start` 和 `agent-session` 输出协作任务板摘要。

# 修复内容

- 复用 major 协作任务板时同步刷新队列版本字段，避免跨 patch 后显示旧版本。
- 补充 Agent 会话必须关联协作任务板的规则。

## v0.1.44

# 版本变化

- 从 v0.1.43 小步提升到 v0.1.44。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.44。

# 新增功能

- Agent 会话步骤新增协作任务编号和工作线程持久化字段。
- `agent-step` 在工具输入完整后自动领取协作任务板任务。
- 步骤执行成功后自动完成协作任务；步骤失败或 Runtime 验证失败后自动释放协作任务。
- `agent-step` 输出和 `agent-session` 详情展示步骤关联协作任务。
- 会话事件新增 `WorkTaskLinked`，记录步骤领取任务的审计事件。

# 修复内容

- 防止缺少 Runtime 命令或 AI 提示词时提前领取任务，避免任务被无效步骤占用。
- 修复任务同步实现中遗漏 `AgentStepStatus::Running` 分支的问题。
- 任务完成摘要新增工具标识，便于审计和测试验证。
- 释放已完成任务时清空旧完成时间，避免待领取任务保留过期完成状态。

## v0.1.45

# 版本变化

- 从 v0.1.44 小步提升到 v0.1.45。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.45。

# 新增功能

- 新增 AI 自我升级入口 `agent-self-upgrade`。
- 新增 `--dry-run` 预览模式，可脱敏展示提供商、模型、协议、记忆来源和提示词规模。
- 新增应用层 AI 自我升级报告结构，统一表达预检、记忆、AI 响应和进化结果。
- 新增 AI 升级目标归一化逻辑，将响应清理为单句中文目标后再交给 `agent-evolve`。
- 新增开放错误阻断，存在未解决错误时禁止触发 AI 自我升级。

# 修复内容

- 将 `tracing` 锁定版本收敛到当前 Cargo 镜像可用的 `0.1.44`，恢复测试可复现性。
- README 更新当前版本和自我升级命令。
- `Agents.md` 新增 AI 自我升级入口规则，防止绕过受控闭环直接修改代码。

## v0.1.46

# 版本变化

- 从 v0.1.45 小步提升到 v0.1.46。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.46。

# 新增功能

- 新增 AI 自我升级结构化审计记录。
- 新增审计存储目录 `workspaces/v0/artifacts/agents/self-upgrades/`。
- 新增审计索引 `workspaces/v0/artifacts/agents/self-upgrades/index.jsonl`。
- 新增 `agent-self-upgrades` 用于查询最近自我升级记录。
- 新增 `agent-self-upgrade-record RECORD_ID` 用于读取单条自我升级记录。
- `agent-self-upgrade` 真实执行成功后输出审计记录编号和文件路径。

# 修复内容

- 防止 AI 自我升级真实执行缺少结构化追溯记录。
- 审计记录只保存摘要、目标和结果，不保存 API Key、完整请求体或完整提示词。
- AI 响应为空或无法归一化为目标时也会写入失败审计记录，便于后续复盘。

## v0.1.47

# 版本变化

- 从 v0.1.46 小步提升到 v0.1.47。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.47。

# 新增功能

- 自我升级审计索引支持按 Agent 会话编号反查记录。
- 应用层新增 `ai_self_upgrade_record_for_session`。
- `agent-session` 详情新增自我升级审计摘要展示。

# 修复内容

- 补齐从 Agent 会话追溯自我升级审计记录的路径，避免只能从审计记录单向定位会话。

## v0.1.48

# 版本变化

- 从 v0.1.47 小步提升到 v0.1.48。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.48。

# 新增功能

- 新增受控多步 Agent 执行报告和停止原因。
- 新增应用层 `execute_agent_steps`，复用单步执行能力连续推进无需外部输入的步骤。
- 新增 CLI 命令 `agent-steps`。
- `agent-steps` 会在会话完成、达到步数上限、缺少输入、无可运行工具或工具失败时停止。

# 修复内容

- 减少人工反复调用 `agent-step` 的操作成本。
- 防止多步执行在缺少 Runtime 命令或 AI 提示词时编造输入继续推进。

## v0.1.49

# 版本变化

- 从 v0.1.48 小步提升到 v0.1.49。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.49。

# 新增功能

- 新增 AI 补丁草案存储目录 `workspaces/v0/artifacts/agents/patch-drafts/`。
- 新增 `agent-patch-draft`，支持真实生成和 `--dry-run` 预览。
- 新增 `agent-patch-drafts` 查询最近草案。
- 新增 `agent-patch-draft-record RECORD_ID` 读取单条草案记录。
- 补丁草案必须包含中文内容、计划章节和测试章节。

# 修复内容

- 防止 AI 直接把补丁生成流程推进到真实源码写入。
- 对缺少测试章节或不符合文档规范的 AI 草案写入失败记录并停止。

## v0.1.50

# 版本变化

- 从 v0.1.49 小步提升到 v0.1.50。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.50。

# 新增功能

- 新增 AI 补丁审计存储目录 `workspaces/v0/artifacts/agents/patch-audits/`。
- 新增 `agent-patch-audit DRAFT_RECORD_ID`，用于审计指定补丁草案。
- 新增 `agent-patch-audits` 查询最近审计记录。
- 新增 `agent-patch-audit-record AUDIT_RECORD_ID` 读取单条审计记录。
- 补丁审计会解析草案中的允许写入范围，并检查受保护路径和协作任务板活跃冲突。

# 修复内容

- 防止 AI 补丁草案未经边界检查就进入后续应用流程。
- 防止多个 AI 线程在已有任务领取时继续推进重叠写入范围。
- 对缺少写入范围、非法路径、受保护路径和活跃冲突写入结构化审计发现。

## v0.1.51

# 版本变化

- 从 v0.1.50 小步提升到 v0.1.51。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.51。

# 新增功能

- 新增 AI 自我升级总结报告存储目录 `workspaces/v0/artifacts/agents/self-upgrade-reports/`。
- 新增 `agent-self-upgrade-report AUDIT_RECORD_ID`，用于为指定自我升级审计生成或读取总结报告。
- 新增 `agent-self-upgrade-reports` 查询最近总结报告。
- 新增 `agent-self-upgrade-report-record REPORT_ID` 读取单条总结报告记录。
- `agent-self-upgrade` 真实成功执行后自动写入中文总结报告。

# 修复内容

- 防止自我升级执行只有结构化审计而缺少面向复盘的中文摘要。
- 总结报告只保存审计摘要、会话摘要和验证信息，不保存 API Key、完整请求体或完整提示词。
- 失败审计也可以生成修复导向报告，便于后续排障。

## v0.1.52

# 版本变化

- 从 v0.1.51 小步提升到 v0.1.52。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.52。

# 新增功能

- 新增 AI 补丁应用预演存储目录 `workspaces/v0/artifacts/agents/patch-previews/`。
- 新增 `agent-patch-preview AUDIT_RECORD_ID`，用于为通过审计的补丁草案生成受控预演。
- 新增 `agent-patch-previews` 查询最近预演记录。
- 新增 `agent-patch-preview-record PREVIEW_RECORD_ID` 读取单条预演记录。
- 预演记录会关联审计记录、草案记录、目标版本、写入范围、代码块数量和 Markdown 预演文件。

# 修复内容

- 防止补丁审计通过后直接跳到真实源码写入。
- 审计失败、草案失败、缺少写入范围或缺少代码块时写入已阻断预演记录。
- 预演只写入可审计产物，不修改真实源码、Runtime、Supervisor 或状态文件。

## v0.1.53

# 版本变化

- 从 v0.1.52 小步提升到 v0.1.53。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.53。

# 新增功能

- 新增 AI 补丁候选应用记录目录 `workspaces/v0/artifacts/agents/patch-applications/`。
- 新增候选应用镜像目录 `workspaces/v0/source/patch-applications/`。
- 新增 `agent-patch-apply PREVIEW_RECORD_ID`，用于将已预演补丁写入候选工作区镜像。
- 新增 `agent-patch-applications` 查询最近候选应用记录。
- 新增 `agent-patch-application-record APPLICATION_ID` 读取单条候选应用记录。
- 新增非法路径阻断测试，确认路径异常会留下审计记录且不会准备候选或写入镜像。

# 修复内容

- 防止补丁预演通过后直接覆盖仓库源码。
- 候选应用会准备或复用候选版本，并在写入后执行候选布局验证。
- 预演未通过、存在未解决错误或路径非法时写入已阻断记录。
- 应用前先完整校验变更清单，再写入候选镜像，避免半写入目录。

## v0.1.54

# 版本变化

- 从 v0.1.53 小步提升到 v0.1.54。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.54。

# 新增功能

- 新增补丁候选应用验证状态和验证执行明细。
- 新增 `agent-patch-verify APPLICATION_RECORD_ID`，用于执行候选应用验证并回写同一条记录。
- 验证命令固定为 `cargo fmt --check`、`cargo test`、`cargo run -- validate` 和 `cargo run -- preflight`。
- `agent-patch-application-record` 输出验证状态和每条验证执行结果。

# 修复内容

- 防止补丁候选应用验证结果只停留在终端输出或进程内存。
- 未应用的候选应用会标记为已跳过，未知验证命令会在执行前失败并写入失败验证明细。
- 任一验证命令失败或超时时，候选应用记录会标记为验证未通过。

## v0.1.55

# 版本变化

- 从 v0.1.54 小步提升到 v0.1.55。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.55。

# 新增功能

- 新增 AI 补丁源码覆盖准备记录目录 `workspaces/v0/artifacts/agents/patch-source-plans/`。
- 新增 `agent-patch-source-plan APPLICATION_RECORD_ID`，用于为已应用且验证通过的候选应用生成源码覆盖准备记录。
- 新增 `agent-patch-source-plans` 查询最近源码覆盖准备记录。
- 新增 `agent-patch-source-plan-record SOURCE_PLAN_ID` 读取单条源码覆盖准备记录。
- 覆盖准备记录会保存目标文件、候选镜像、差异摘要、字节数、回滚备份和回滚动作。

# 修复内容

- 防止补丁候选应用验证通过后直接覆盖仓库源码。
- 未应用、未验证、验证失败或镜像文件缺失时写入已阻断准备记录。
- 源码覆盖准备只写入可审计产物和回滚备份，不修改真实源码、Runtime、Supervisor 或状态文件。

## v0.1.56

# 版本变化

- 从 v0.1.55 小步提升到 v0.1.56。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.56。

# 新增功能

- 新增 AI 补丁源码覆盖执行记录目录 `workspaces/v0/artifacts/agents/patch-source-executions/`。
- 新增 `agent-patch-source-execute SOURCE_PLAN_ID`，用于执行已准备的源码覆盖计划。
- 新增 `agent-patch-source-executions` 查询最近源码覆盖执行记录。
- 新增 `agent-patch-source-execution-record SOURCE_EXECUTION_ID` 读取单条源码覆盖执行记录。
- 源码覆盖执行会在覆盖前写入执行级备份，覆盖后运行固定验证命令，验证失败时自动回滚。

# 修复内容

- 防止源码覆盖计划生成后目标文件变化仍被执行。
- 防止源码覆盖失败后只留下终端输出而没有回滚证据。
- 验证失败或写入失败时，执行记录会保存回滚状态、回滚步骤和错误信息。

## v0.1.57

# 版本变化

- 从 v0.1.56 小步提升到 v0.1.57。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.57。

# 新增功能

- 新增 AI 补丁源码覆盖提升衔接记录目录 `workspaces/v0/artifacts/agents/patch-source-promotions/`。
- 新增 `agent-patch-source-promotion SOURCE_EXECUTION_ID`，用于为成功源码覆盖执行生成版本提升衔接记录。
- 新增 `agent-patch-source-promotions` 查询最近提升衔接记录。
- 新增 `agent-patch-source-promotion-record PROMOTION_ID` 读取单条提升衔接记录。
- 衔接记录会关联源码覆盖执行、覆盖准备、候选应用、验证摘要、建议提交信息和下一候选版本线索。

# 修复内容

- 防止源码覆盖执行后直接跳到下一候选生成而缺少可审计衔接记录。
- 失败、回滚、缺少验证运行或没有覆盖文件的源码覆盖执行会生成已阻断衔接记录。
- 衔接命令只写入产物目录，不修改状态文件、不生成候选版本、不执行真实 Git 提交。

## v0.1.58

# 版本变化

- 从 v0.1.57 小步提升到 v0.1.58。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误、版本记录继续追加到 major 聚合文件。
- `state/state.json` 的 `current_version` 已提升为 v0.1.58。

# 新增功能

- 新增 AI 补丁源码覆盖候选准备记录目录 `workspaces/v0/artifacts/agents/patch-source-candidates/`。
- 新增 `agent-patch-source-candidate PROMOTION_ID`，用于基于已就绪提升衔接记录准备或复用下一候选版本。
- 新增 `agent-patch-source-candidates` 查询最近候选准备记录。
- 新增 `agent-patch-source-candidate-record CANDIDATE_RECORD_ID` 读取单条候选准备记录。
- 候选准备记录会关联提升衔接记录、状态变化、候选工作区、候选验证摘要、阻断原因和后续验证命令。

# 修复内容

- 防止提升衔接记录就绪后直接跳过候选状态机。
- 存在开放错误、衔接记录未就绪、下一候选版本不匹配或已有不同候选时会生成已阻断候选准备记录。
- 目标候选已存在且匹配时只复用并验证布局，避免重复生成候选。

## v0.1.59

# 版本变化

- 从 `v0.1.58` 小步提升到 `v0.1.59`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 的 `current_version` 已提升为 `v0.1.59`。

# 新增功能

- 新增 AI 补丁源码覆盖候选 cycle 记录目录 `workspaces/v0/artifacts/agents/patch-source-cycles/`。
- 新增 `agent-patch-source-cycle CANDIDATE_RECORD_ID`，用于读取候选准备记录、执行预检并调用受控 `cycle`。
- 新增 `agent-patch-source-cycles` 查询最近候选 cycle 记录。
- 新增 `agent-patch-source-cycle-record CYCLE_RECORD_ID` 读取单条候选 cycle 记录。
- cycle 记录会保存候选准备编号、预检摘要、提升或回滚结果、状态变化、失败原因和后续命令。

# 修复内容

- 防止源码覆盖候选准备完成后直接跳过预检或候选验证。
- 候选准备记录未完成、状态文件候选版本不一致或预检失败时，会写入已阻断 cycle 记录并保留状态不变。
- 候选验证失败时，受控 `cycle` 会记录回滚原因和回滚后的状态，为后续修复提供审计依据。

## v0.1.60

# 版本变化

- 从 `v0.1.59` 小步提升到 `v0.1.60`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 的 `current_version` 已提升为 `v0.1.60`。

# 新增功能

- 新增源码覆盖 cycle 后续总结目录 `workspaces/v0/artifacts/agents/patch-source-cycle-summaries/`。
- 新增 `agent-patch-source-cycle-summary CYCLE_RECORD_ID`，用于基于 cycle 记录生成中文后续总结。
- 新增 `agent-patch-source-cycle-summaries` 查询最近后续总结记录。
- 新增 `agent-patch-source-cycle-summary-record SUMMARY_RECORD_ID` 读取单条后续总结记录。
- 后续总结记录会保存 cycle 编号、候选准备编号、提升或回滚后的状态、失败原因、记忆压缩建议、下一任务建议和后续命令。

# 修复内容

- 防止源码覆盖 cycle 完成后只留下状态机结果，而缺少面向下一轮自我升级的中文总结。
- 已提升、已回滚和已阻断的 cycle 会生成不同后续建议，避免把修复路径和继续进化路径混在一起。
- 后续总结命令只写入归档产物，不直接修改状态文件、不生成新候选、不执行真实提交。

## v0.1.61

# 版本变化

- 从 `v0.1.60` 小步提升到 `v0.1.61`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 的 `current_version` 已提升为 `v0.1.61`。

# 新增功能

- 新增源码覆盖下一任务草案目录 `workspaces/v0/artifacts/agents/patch-source-task-drafts/`。
- 新增 `agent-patch-source-task-draft SUMMARY_RECORD_ID`，用于基于 cycle 后续总结生成可审计任务草案。
- 新增 `agent-patch-source-task-drafts` 查询最近任务草案记录。
- 新增 `agent-patch-source-task-draft-record TASK_DRAFT_ID` 读取单条任务草案记录。
- 任务草案记录会保存来源总结、来源 cycle、草案标题、草案描述、建议目标版本、验收检查、审计要求和后续命令。

# 修复内容

- 防止 cycle 后续总结直接变成真实修改任务，新增草案归档作为审计闸门。
- 已提升、已回滚和已阻断的来源总结会生成不同任务标题和描述，区分继续进化与修复路径。
- 任务草案命令只写入归档产物，不修改状态文件、不创建候选版本、不写入真实任务队列。

## v0.1.62

# 版本变化

- 从 `v0.1.61` 小步提升到 `v0.1.62`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 的 `current_version` 已提升为 `v0.1.62`。

# 新增功能

- 新增源码覆盖任务草案审计目录 `workspaces/v0/artifacts/agents/patch-source-task-audits/`。
- 新增 `agent-patch-source-task-audit TASK_DRAFT_ID`，用于基于任务草案生成可审计批准或阻断记录。
- 新增 `agent-patch-source-task-audits` 查询最近任务草案审计记录。
- 新增 `agent-patch-source-task-audit-record TASK_AUDIT_ID` 读取单条任务草案审计记录。
- 审计记录会保存来源草案、审计发现、批准目标、建议目标版本、阻断原因和后续命令。

# 修复内容

- 防止任务草案直接进入补丁生成流程，新增批准或阻断闸门。
- 审计会检查草案状态、审计要求、目标版本、标题描述、来源目标、来源任务、验收检查和后续命令。
- 任务草案审计命令只写入归档产物，不调用 AI、不修改状态文件、不创建候选版本、不写入真实任务队列。

## v0.1.63

# 版本变化

- 从 `v0.1.62` 小步提升到 `v0.1.63`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 的 `current_version` 已提升为 `v0.1.63`。

# 新增功能

- `agent-patch-draft` 新增 `--from-task-audit TASK_AUDIT_ID` 参数。
- 新增应用层 `ai_patch_draft_from_task_audit` 和预览入口，用于从已批准任务草案审计记录读取批准目标。
- 支持 `agent-patch-draft --dry-run --from-task-audit TASK_AUDIT_ID` 预览基于审计记录生成的补丁草案请求。
- 未批准审计、缺失审计或空批准目标会返回明确错误，禁止进入 AI 补丁草案生成。

# 修复内容

- 防止任务草案链路绕过审计后直接传入普通补丁目标。
- 使用 `--from-task-audit` 时禁止同时提供直接目标文本，避免审计目标和用户目标混用。
- 新入口复用现有补丁草案预检、记忆读取、AI 请求、草案存储和合规校验，不引入平行补丁生成流程。

## v0.1.64

# 版本变化

- 从 `v0.1.63` 小步提升到 `v0.1.64`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 的 `current_version` 已提升为 `v0.1.64`。

# 新增功能

- 补丁草案预览、记录和摘要新增可选 `source_task_audit_id`。
- `agent-patch-draft --from-task-audit TASK_AUDIT_ID` 会把已批准任务草案审计编号写入补丁草案记录。
- `agent-patch-drafts` 和 `agent-patch-draft-record` 输出显示来源任务草案审计编号。

# 修复内容

- 防止审计驱动补丁草案生成后丢失来源审计编号，提升任务草案、审计记录和补丁草案之间的可追溯性。
- 旧补丁草案记录通过可选字段继续兼容，普通直接目标补丁草案不强制要求来源审计编号。

## v0.1.65

# 版本变化

- 从 `v0.1.64` 小步提升到 `v0.1.65`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。
- `state/state.json` 的 `current_version` 已提升为 `v0.1.65`。

# 新增功能

- 补丁审计记录和摘要新增可选 `source_task_audit_id`。
- `agent-patch-audit` 生成记录时会继承补丁草案记录中的来源任务草案审计编号。
- `agent-patch-audits` 和 `agent-patch-audit-record` 输出显示来源任务草案审计编号。

# 修复内容

- 防止来源任务草案审计编号在补丁审计阶段丢失。
- 旧补丁审计记录通过可选字段继续兼容，普通直接目标草案生成的审计记录不强制要求来源审计编号。

## v0.1.65 结构拆分补充

# 版本变化

- 当前稳定版本仍为 `v0.1.65`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 结构拆分记录继续追加到 major 聚合文件。

# 结构调整

- `src/lib.rs` 的内联测试模块已迁移到 `src/tests/`。
- 新增 `src/tests/mod.rs` 作为测试父模块和共享辅助函数入口。
- 新增多个主题测试文件，覆盖 Agent、AI、应用闭环、布局版本、记忆、补丁草案、源码覆盖和运行错误归档。

# 修复内容

- 防止 `src/lib.rs` 同时承担公共导出和大量测试实现，降低库入口维护负担。
- 防止测试拆分后形成单个新的超大测试文件，改为按职责分组。

## v0.1.65 补丁范围模块拆分补充

# 版本变化

- 当前稳定版本仍为 `v0.1.65`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 结构拆分记录继续追加到 major 聚合文件。

# 结构调整

- 新增 `src/app/minimal_loop/patch_scope.rs`，集中补丁写入范围解析、路径规范化、受保护路径判断和协作任务冲突审计。
- `src/app/minimal_loop.rs` 通过子模块导入补丁范围审计函数，应用流程调用保持不变。
- `markdown_heading_title` 保持包内可见，供补丁范围审计和补丁预演代码块解析共同复用。

# 修复内容

- 防止补丁范围审计辅助逻辑继续堆叠在应用闭环主文件中。
- 防止迁移后父模块无法复用 Markdown 标题解析工具。

## v0.1.65 协作队列模型拆分补充

# 版本变化

- 当前稳定版本仍为 `v0.1.65`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 结构拆分记录继续追加到 major 聚合文件。

# 结构调整

- 新增 `src/app/agent/coordination/model.rs`，集中协作队列状态、任务、事件、报告、错误和显示实现。
- `src/app/agent/coordination.rs` 重新导出模型类型，保留队列初始化、状态查询、领取、完成、释放、过期清理和冲突检测流程。
- 协作任务板 `coord-002-application` 已记录本轮领取和完成事件。

# 修复内容

- 防止协作队列主文件同时承担模型定义、错误展示和业务流程实现。
- 修复模型迁移后父模块对协调器结构体和默认租约常量的可见性问题。

## v0.1.65 协作 CLI 收口补充

# 版本变化

- 当前稳定版本仍为 `v0.1.65`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 本轮为文档、任务板和归档收口，不变更状态文件版本。

# 结构调整

- README 常用命令补齐协作任务板初始化、状态、领取、完成、释放和租约清理入口。
- 协作任务板 `coord-003-cli`、`coord-004-tests` 和 `coord-005-review-archive` 完成收口记录。
- 复核现有协作队列测试覆盖，保持测试拆分在 `src/tests/agent_core.rs`，避免回退到 `src/lib.rs` 大文件。

# 修复内容

- 防止 README 只展示 `agent-work-status`，导致协作任务板 CLI 使用链路不完整。
- 防止测试任务为了满足旧写入范围而把测试重新堆回 `src/lib.rs`。

## v0.1.65 协作任务板重开能力补充

# 版本变化

- 当前稳定版本仍为 `v0.1.65`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 本轮补充协作任务板重开能力，不变更状态文件版本。

# 新增功能

- `agent-work-init` 新增 `--reset-completed` 参数。
- 应用层新增只在全部任务完成时重开协作队列的路径。
- 未完成队列尝试重开时会返回明确错误，防止覆盖仍在执行的任务板。
- README 和 CLI 帮助文本展示新参数。

# 修复内容

- 修复已完成任务板无法开启下一轮协作目标的问题。
- 防止通过普通初始化隐式覆盖旧任务板，保留显式重开语义。

## v0.1.65 分支协作流程规范补充

# 版本变化

- 当前稳定版本仍为 `v0.1.65`。
- 本轮为流程规范和归档更新，不修改 `state/state.json` 的版本号。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。

# 规则调整

- `Agents.md` 最高原则新增分支隔离要求，禁止直接在 `master` 上写入。
- 开发流程新增创建任务分支、分支提交、合并回 `master`、合并后验证和删除任务分支步骤。
- 多 AI 协作规则要求每个 AI 使用独立 worker、独立任务分支和独立物理工作树。
- 新增“分支与合并规范”章节，规定分支命名、同步、验证、合并、冲突处理和删除条件。

# 修复内容

- 防止多个 AI 在同一个 `master` 工作树中直接修改导致冲突不可追溯。
- 防止任务分支合并后不验证或不删除，造成主干状态不确定和分支遗留。
- 防止后续目标复用旧分支处理不相关任务。

## v0.1.65 分支流程只读检查命令补充

# 版本变化

- 当前稳定版本仍为 `v0.1.65`。
- 本轮为 patch 级功能补充，不修改 `state/state.json` 的版本号。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。

# 新增功能

- 新增 `branch-check` CLI 命令。
- 新增 `SelfForgeApp::branch_check` 应用层只读检查入口。
- 分支检查报告包含当前分支、基础分支、领先落后数量、非任务板改动、任务板状态、worker 与任务匹配关系、阻断项、警告和是否允许继续写入。
- README 常用命令补充 `branch-check` 示例。
- `Agents.md` 记录 `branch-check` 的只读边界和阻断规则。

# 修复内容

- 防止 AI 在未检查分支和任务领取状态时继续写入。
- 防止协作任务板审计改动被误判为阻断项。
- 修复 Git porcelain 状态解析时首行路径首字符丢失的问题。

## v0.1.65 分支命名建议补充

# 版本变化

- 当前稳定版本仍为 `v0.1.65`。
- 本轮为 patch 级功能补充，不修改 `state/state.json` 的版本号。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记忆、任务、错误和版本记录继续写入 `forge/*/v0.md`。

# 新增功能

- `branch-check` 新增 `--suggest` 参数。
- 分支检查报告新增建议分支名和建议来源。
- 建议分支名使用 `codex/` 前缀和小写短横线。
- 分支名建议优先来自显式任务编号，其次来自 worker 已领取任务，最后来自任务板下一条待处理任务。
- README 常用命令示例更新为 `branch-check --suggest`。

# 修复内容

- 防止 AI 凭聊天上下文猜测分支名。
- 防止分支名建议产生副作用，命令仍保持只读。
- 防止建议来源不透明，输出明确展示建议依据。

## v0.1.65 分阶段 PR 流程本地阶段

# 版本变化

- 当前稳定版本仍为 `v0.1.65`。
- 本阶段没有提升 patch、minor 或 major。
- 本阶段没有修改 `state/state.json`。
- 本阶段没有 push、没有创建 Pull Request。
- 阶段性本地提交不携带版本号，符合新的提交规则。

# 新增功能

- `agent-work-compact --keep-events N`：压缩协作任务板已完成任务提示词和旧事件。
- `agent-work-block TASK_ID --reason TEXT`：将过期或不再适用的任务标记为已阻断。
- 任务板压缩和阻断规则写入 `forge/architecture/agent-rules/agent-coordination.md`。

# 状态说明

- 当前本地分支为 `codex/coord-009-batched-pr-flow`。
- 当前任务组仍处于本地阶段。
- 任务板当前待领取 0、已领取 0、已完成 8、已阻断 4。
- 任务组最终收束后，才允许提升下一 patch 版本并创建 Pull Request。

# 验证结果

- `cargo test` 通过，189 个测试全部通过。
- `cargo run -- validate` 通过。
- `cargo run -- preflight` 通过。
- `cargo run -- errors --current --open` 返回无开放错误。

## v0.1.69

# 版本变化

- 当前稳定版本从 `v0.1.68` 提升到 `v0.1.69`。
- 父版本记录为 `v0.1.68`。
- 继续复用 major 工作区 `workspaces/v0`。
- 小版本记录继续写入 `forge/*/v0.md`。

# 收束内容

- `agent-work-status --active-only` 活跃任务过滤纳入本次版本。
- CLI 状态输出逻辑拆分到 `src/cli/agent_work_status.rs`。
- 大文件拆分规则纳入 `Agents.md` 和 `code-architecture.md`。
- 最终版本提交和 PR 标题携带 `v0.1.69`。
- PR 正文关联 `Refs #1` 和 `https://github.com/xjuunn/SelfForge/issues/1`。

# 验证结果

- `cargo fmt --check` 通过。
- `cargo test` 首次发现测试夹具版本期望需要同步，修复后 194 个库测试和 5 个 CLI 单元测试全部通过。
- `git diff --check` 通过。
- `cargo run -- validate` 通过，检查 30 个路径。
- `cargo run -- preflight` 通过，当前版本 `v0.1.69`，未解决错误 0，可进化。
- `cargo run -- errors --current --open` 返回无开放错误。
- `cargo run -- agent-work-finalize-check --current` 通过，任务板无待领取、无已领取、无开放错误，可收束。

## v0.1.69 自我进化循环第一阶段本地阶段

# 版本变化

- 当前稳定版本仍为 `v0.1.69`。
- 本阶段不提升 patch、minor 或 major。
- 本阶段不修改 `state/state.json`。
- 本阶段不 push、不创建 Pull Request。

# 新增内容

- `agent-self-loop` 一键自我进化循环入口。
- 循环记录目录：`workspaces/vMAJOR/artifacts/agents/self-evolution-loops/`。
- 最大轮次、最大连续失败、超时和恢复参数。
- 失败轮次记录和 panic 捕获，达到连续失败上限后受控停止。

# 验证结果

- `cargo fmt --check` 通过。
- `cargo test self_evolution_loop` 通过。
- `cargo run -- agent-self-loop --dry-run --max-cycles 2 --max-failures 2 "继续小步进化"` 通过。
- `cargo test` 通过，195 个库测试和 7 个 CLI 单元测试全部通过。
- `git diff --check` 通过。
- `cargo run -- validate` 通过。
- `cargo run -- preflight` 通过。
- `cargo run -- errors --current --open` 返回无开放错误。

## v0.1.66 无关后续任务分支规则补充

# 版本变化

- 当前稳定版本仍为 `v0.1.66`。
- 本阶段不提升 patch、minor 或 major。
- 本阶段不修改 `state/state.json`。
- 本阶段不 push、不创建 Pull Request。

# 规则内容

- 后续任务若与当前任务组完全无关，不得继续写入当前分支。
- 当前分支已有成果且可收束时，先提升 patch、统一 push 一次并创建 PR。
- PR 合并并同步 `master` 后，再为无关任务创建新的 `codex/` 分支。
- 每个分支只处理一个相关任务组。

# 验证结果

- `git diff --check` 通过。
- `cargo fmt --check` 通过。
- `cargo run -- validate` 通过。
- `cargo run -- preflight` 通过。
- `cargo run -- errors --current --open` 返回无开放错误。

## v0.1.67

# 版本变化

- 当前稳定版本从 `v0.1.66` 提升到 `v0.1.67`。
- 父版本记录为 `v0.1.66`。
- 继续复用 major 工作区 `workspaces/v0`。
- 小版本记录继续写入 `forge/*/v0.md`。

# 收束内容

- 终态任务板重开修复纳入本次版本。
- `agent-work-finalize-check` 只读收束检查命令纳入本次版本。
- 无关后续任务自动收束当前分支并另开分支的规则纳入本次版本。
- 最终版本提交和 PR 标题携带 `v0.1.67`。
- PR 正文关联 `Refs #1` 和 `https://github.com/xjuunn/SelfForge/issues/1`。

# 验证结果

- `cargo fmt --check` 通过。
- `cargo test` 通过，194 个测试全部通过。
- `git diff --check` 通过。
- `cargo run -- validate` 通过。
- `cargo run -- preflight` 通过。
- `cargo run -- errors --current --open` 返回无开放错误。
- `cargo run -- agent-work-finalize-check --current` 通过，任务板无待领取、无已领取、无开放错误，可收束。

## v0.1.67 任务板状态建议分支名本地阶段

# 版本变化

- 当前稳定版本仍为 `v0.1.67`。
- 本阶段先完成实现和验证，最终收束时再提升到下一 patch。
- 本阶段不修改 `state/state.json`。

# 新增内容

- `agent-work-status` 对待领取和已领取任务显示建议分支名。
- 建议分支名来自结构化任务编号，格式为 `codex/<task-id-slug>`。
- 已完成和已阻断任务不显示建议分支名。
- README 和 Git/PR 规则补充只读提示说明。

# 验证结果

- `cargo fmt --check` 通过。
- `cargo test` 通过，194 个库测试和 2 个 CLI 单元测试全部通过。
- `git diff --check` 通过。
- `cargo run -- validate` 通过。
- `cargo run -- preflight` 通过。
- `cargo run -- errors --current --open` 返回无开放错误。

## v0.1.68

# 版本变化

- 当前稳定版本从 `v0.1.67` 提升到 `v0.1.68`。
- 父版本记录为 `v0.1.67`。
- 继续复用 major 工作区 `workspaces/v0`。
- 小版本记录继续写入 `forge/*/v0.md`。

# 收束内容

- `agent-work-status` 活跃任务建议分支名纳入本次版本。
- README 和 Git/PR 规则补充只读分支建议说明。
- 最终版本提交和 PR 标题携带 `v0.1.68`。
- PR 正文关联 `Refs #1` 和 `https://github.com/xjuunn/SelfForge/issues/1`。

# 验证结果

- `cargo fmt --check` 通过。
- `cargo test` 通过，194 个库测试和 2 个 CLI 单元测试全部通过。
- `git diff --check` 通过。
- `cargo run -- validate` 通过，检查 30 个路径。
- `cargo run -- preflight` 通过，当前版本 `v0.1.68`，未解决错误 0，可进化。
- `cargo run -- errors --current --open` 返回无开放错误。
- `cargo run -- agent-work-finalize-check --current` 通过，任务板无待领取、无已领取、无开放错误，可收束。

## v0.1.66 终态任务板重开修复补充

# 版本变化

- 当前稳定版本仍为 `v0.1.66`。
- 本阶段不提升 patch、minor 或 major。
- 继续复用 major 工作区 `workspaces/v0` 和 v0 聚合归档。

# 修复内容

- `agent-work-init --reset-completed` 允许仅包含已完成和已阻断任务的终态队列重开。
- 存在待领取或已领取任务时仍然拒绝重开。
- README 和协作规则补充终态队列重开说明。

# 验证结果

- `cargo test agent_work_reset_completed` 通过，2 个定向测试全部通过。
- `git diff --check` 通过。
- `cargo fmt --check` 通过。
- `cargo test` 通过，191 个测试全部通过。
- `cargo run -- validate` 通过，检查 30 个路径。
- `cargo run -- preflight` 通过，当前版本 `v0.1.66`，未解决错误 0，可进化。
- `cargo run -- errors --current --open` 返回无开放错误。

## v0.1.66 协作任务组收束检查命令补充

# 版本变化

- 当前稳定版本仍为 `v0.1.66`。
- 本阶段不提升 patch、minor 或 major。
- 本阶段不修改 `state/state.json`。
- 本阶段不 push、不创建 Pull Request。

# 新增功能

- `agent-work-finalize-check`：只读检查任务组是否可以进入最终版本提升、统一 push 和 Pull Request。
- 检查内容包含任务总数、待领取、已领取、已完成、已阻断和开放错误数量。
- 存在待领取、已领取任务或开放错误时，判定不可收束。

# 验证结果

- `cargo test agent_work_finalize_check` 通过，3 个定向测试全部通过。
- `cargo test` 通过，194 个测试全部通过。
- `cargo run -- agent-work-finalize-check --current` 在任务完成后报告可收束。
- `cargo run -- validate` 通过。
- `cargo run -- preflight` 通过。
- `cargo run -- errors --current --open` 返回无开放错误。

## v0.1.66

# 版本变化

- 当前稳定版本从 `v0.1.65` 提升到 `v0.1.66`。
- 父版本记录为 `v0.1.65`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记录继续写入 `forge/*/v0.md`。

# 收束内容

- 分阶段 PR 流程规则完成最终收束。
- 任务板压缩命令和任务板阻断命令纳入本次 PR。
- 本地阶段提交保持未携带版本号，最终版本提升提交和 PR 标题携带 `v0.1.66`。
- PR 正文需要关联 `Refs #1` 和 `https://github.com/xjuunn/SelfForge/issues/1`。

# 验证结果

- `git diff --check` 通过。
- `cargo fmt --check` 通过。
- `cargo test` 首次发现测试夹具版本期望需要同步，修复后 189 个测试全部通过。
- `cargo run -- validate` 通过，检查 30 个路径。
- `cargo run -- preflight` 通过，当前版本 `v0.1.66`，未解决错误 0，可进化。
- `cargo run -- errors --current --open` 返回无开放错误。
## v0.1.68 任务板活跃过滤与大文件拆分本地阶段

# 版本变化

- 当前稳定版本仍为 `v0.1.68`。
- 本阶段不提升 patch、minor 或 major。
- 本阶段不修改 `state/state.json`。
- 本阶段不 push、不创建 Pull Request。

# 新增内容

- `agent-work-status --active-only` 只读展示待领取和已领取任务。
- 默认 `agent-work-status` 输出保持完整兼容。
- CLI 状态输出逻辑拆分到 `src/cli/agent_work_status.rs`。
- 新增 `forge/architecture/agent-rules/code-architecture.md`，明确大文件自动拆分规则。

# 验证结果

- `cargo fmt --check` 通过。
- `cargo test` 通过，194 个库测试和 5 个 CLI 单元测试全部通过。
- `git diff --check` 通过。
- `cargo run -- agent-work-status --current --active-only` 通过。
- `cargo run -- agent-work-status --current` 通过。
- `cargo run -- validate` 通过。
- `cargo run -- preflight` 通过。
- `cargo run -- errors --current --open` 返回无开放错误。

## v0.1.70 自我进化循环最终收束

# 版本变化

- 当前稳定版本从 `v0.1.69` 提升到 `v0.1.70`。
- 父版本记录为 `v0.1.69`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记录继续写入 `forge/*/v0.md`。

# 新增内容

- `agent-self-loop` 支持可恢复的一键自我进化循环。
- `agent-self-loop --commit-each-cycle` 支持每轮成功后本地阶段提交。
- `agent-self-loop --finalize-pr --confirm-finalize` 支持最终确认后的统一 push、PR 创建、检查等待、合并和远程分支删除。
- `agent-self-loops` 与 `agent-self-loop-record` 支持循环记录查询。
- 循环记录保存步骤状态、Git/PR 事件、失败摘要和 PR 地址。

# 验证结果

- `cargo fmt --check` 通过。
- `cargo test` 通过，198 个库测试和 10 个 CLI 单元测试全部通过。
- `cargo run -- validate` 通过，检查 30 个路径。
- `cargo run -- preflight` 通过，当前版本 `v0.1.70`，未解决错误 0，可进化。
- `cargo run -- errors --current --open` 返回无开放错误。

# PR 关联

- PR 正文必须包含 `Refs #1`。
- Issue 地址：`https://github.com/xjuunn/SelfForge/issues/1`。

## v0.1.71 AI 技能索引按需加载本地阶段

# 版本变化

- 当前稳定版本仍为 `v0.1.71`。
- 本阶段不提升 patch、minor 或 major。
- 本阶段不修改 `state/state.json`。
- 本阶段不 push、不创建 Pull Request。

# 新增内容

- 新增 Agent 技能索引模型，支持几百个技能只读取元数据。
- 新增目标召回和 token 预算控制，避免全量技能正文进入上下文；预算过滤会扫描全部候选，避免前排大技能挤掉后排轻量技能。
- 新增 `agent-skills` 和 `agent-skill-select` 命令。
- README 补充 AI 技能加载说明。

# 验证结果

- `cargo test agent_skill` 通过，4 个定向测试全部通过。
- `cargo run -- agent-skills --current` 通过。
- `cargo run -- agent-skill-select --current --limit 3 --token-budget 800 "README 命令"` 通过。

## v0.1.71 AI 技能上下文接入本地阶段

# 版本变化

- 当前稳定版本仍为 `v0.1.71`。
- 本阶段不提升 patch、minor 或 major。
- 本阶段不修改 `state/state.json`。
- 本阶段不 push、不创建 Pull Request。

# 新增内容

- 自我升级预览会按用户补充目标召回少量技能。
- 自我升级提示词新增“按需技能上下文”章节。
- `agent-self-upgrade --dry-run` 输出技能索引、候选、选择、正文和 token 统计。
- 新增测试确认技能正文会进入请求体，且 `.env` 密钥不会进入请求体。

# 验证结果

- `cargo fmt --check` 通过。
- `cargo test self_upgrade_preview_includes_selected_skill_context` 通过。
- `cargo test agent_skill` 通过。
- `cargo run -- agent-self-upgrade --dry-run "优化 AI 技能架构"` 通过。

## v0.1.71 AI 补丁草案技能上下文接入本地阶段

# 版本变化

- 当前稳定版本仍为 `v0.1.71`。
- 本阶段不提升 patch、minor 或 major。
- 本阶段不修改 `state/state.json`。
- 本阶段不 push、不创建 Pull Request。

# 新增内容

- 补丁草案预览会按草案目标召回少量技能。
- 补丁草案提示词新增按需技能上下文。
- `agent-patch-draft --dry-run` 输出技能索引、候选、选择、正文和 token 统计。
- 新增测试确认补丁草案请求体包含被召回技能正文，且不包含 `.env` 密钥。
- 本分支本地提交信息已修正为项目规范格式。

# 验证结果

- `cargo fmt --check` 通过。
- `cargo test patch_draft_preview_includes_selected_skill_context` 通过。
- `cargo test agent_skill` 通过。
- `cargo run -- agent-patch-draft --dry-run "优化 AI 技能架构"` 通过。

## v0.1.71 README 命令审计最终收束

# 版本变化

- 当前稳定版本从 `v0.1.70` 提升到 `v0.1.71`。
- 父版本记录为 `v0.1.70`。
- 继续复用 major 工作区 `workspaces/v0`，未创建小版本工作区目录。
- 小版本记录继续写入 `forge/*/v0.md`。

# 新增内容

- README 直接运行命令清单收敛为 16 条。
- README 明确直接运行命令必须可复制执行并以退出码 0 完成。
- README 将真实记录编号、任务板改写、候选版本、真实 AI 请求和最终 PR 收束流程移入上下文说明。
- `ai-request` 巡检示例改为 `--dry-run`。

# 验证结果

- README 中 16 条 `cargo` 命令逐条执行并全部通过。
- `cargo fmt --check` 通过。
- `cargo test` 通过。
- `git diff --check` 通过。
- `cargo run -- validate` 通过。
- `cargo run -- preflight` 通过，当前版本 `v0.1.71`，未解决错误 0，可进化。
- `cargo run -- errors --current --open` 返回无开放错误。

# PR 关联

- PR 正文必须包含 `Refs #1`。
- Issue 地址：`https://github.com/xjuunn/SelfForge/issues/1`。
