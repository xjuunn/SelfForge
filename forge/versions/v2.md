# v2

# 版本变化

- 从 v1 生成 v2 候选版本。
- 新增 `workspaces/v2` 作为候选版本工作区。
- 新增 v2 的 memory、tasks、errors、versions 文档。
- 更新状态文件，记录候选版本与候选工作区。

# 新增功能

- 候选版本生成引擎。
- 结构化状态读写。
- Supervisor 候选生成入口。
- Runtime 按版本验证布局。
- CLI 命令：`evolve`。

# 修复内容

- 修复新增代码格式差异。
