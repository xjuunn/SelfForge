# v1

# 版本变化

- 建立 SelfForge v1 基础架构。
- 建立标准目录：runtime、supervisor、workspaces/v1、forge、state。
- 建立持久化状态文件：state/state.json。

# 新增功能

- Rust CLI：`init`、`validate`、`status`。
- Runtime 验证层：验证基础目录与归档文件完整性。
- Supervisor 编排层：初始化当前版本并调用 Runtime 验证。
- forge 文档归档入口：memory、tasks、errors、versions。
- v1 测试覆盖：初始化、幂等边界、缺失状态错误。

# 修复内容

- 无。
