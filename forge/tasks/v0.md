# v0 任务记录

# 记录规则

- 本文件集中记录 v0 大版本下的小版本任务。
- 每次 patch 或 minor 更新只追加一个任务小节，禁止创建新的小版本任务文件。

## v0.1.7

# 任务来源

SelfForge 受控进化流程。

# 任务描述

从当前稳定版本 v0.1.6 生成下一候选版本 v0.1.7，并按 major 聚合规则追加归档。

# 输入

- 当前稳定版本：v0.1.6
- 用户目标：按大版本聚合小版本目录和归档
- 状态文件：state/state.json

# 输出

- workspaces/v0
- forge/memory/v0.md
- forge/tasks/v0.md
- forge/errors/v0.md
- forge/versions/v0.md
- 更新后的 state/state.json

# 计划（Plan）

1. 读取状态。
2. 验证当前稳定版本。
3. 计算下一 patch 版本。
4. 复用 major 工作区并追加聚合文档。
5. 验证候选版本。
6. 持久化候选状态。

# 执行记录

1. 已读取并保留旧版 `v0.1.x` 历史文件，未在未确认迁移前删除旧资料。
2. 已实现 `v0.1.x -> workspaces/v0` 的运行时映射。
3. 已实现 `v0.1.x -> forge/*/v0.md` 的归档映射。
4. 已生成候选版本 `v0.1.7`，未创建 `workspaces/v0.1.7` 或 `forge/*/v0.1.7*`。
5. 已执行编译、格式、单元测试和 SelfForge 自验证。
6. 已通过 `cycle` 将 `v0.1.7` 提升为当前版本。

## v0.1.8

# 任务来源

用户要求继续优化 `workspaces` 目录结构，避免文件过多导致查找困难。

# 任务描述

在保持大版本聚合规则的前提下，为 `workspaces/v0` 设计稳定、清晰、便于扩展的内部目录结构，并通过校验规则防止文件直接堆积在 workspace 根目录。

# 输入

- 当前稳定版本：v0.1.7
- 用户目标：workspaces 的目录结构需要合理，防止文件过多、找文件麻烦。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 更新后的 workspace 内部布局规则。
- 更新后的 `workspaces/v0` 目录骨架。
- 更新后的测试用例。
- 更新后的中文规则文档与归档记录。

# 计划（Plan）

1. 读取现有记忆、任务和 layout 实现。
2. 设计少量稳定的 workspace 顶层目录。
3. 在 layout 中创建并校验 workspace 内部骨架。
4. 增加防止 workspace 根目录杂乱的错误测试。
5. 更新中文文档和归档。
6. 执行格式、编译、单元测试和 SelfForge 验证。
7. 生成并提升 `v0.1.8`。

# 执行记录

1. 已新增 workspace 固定分区：`source/`、`tests/`、`sandbox/`、`artifacts/`、`logs/`。
2. 已新增根目录白名单校验，防止未规划文件直接出现在 `workspaces/v0`。
3. 已新增 `.gitignore` 和各分区中文 README。
4. 已补充 workspace 结构测试和根目录杂散文件错误测试。
5. 已执行格式、编译、单元测试、初始化、验证和提升流程。

## v0.1.9

# 任务来源

用户要求添加简洁 README，并禁止任何 Emoji。

# 任务描述

为项目根目录添加简洁中文 `README.md`，将禁止 Emoji 写入代理规则，并通过文档校验阻止 Markdown 文档出现 Emoji。

# 输入

- 当前稳定版本：v0.1.8
- 用户目标：添加 README.md，禁止任何 Emoji，README 内容保持简洁。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 根目录 `README.md`
- 更新后的 `Agents.md`
- 更新后的文档校验逻辑和测试
- 更新后的 forge 归档记录

# 计划（Plan）

1. 读取历史记忆和现有文档校验实现。
2. 添加简洁中文根目录 README。
3. 将禁止 Emoji 写入 `Agents.md`。
4. 在 Markdown 文档校验中拒绝 Emoji。
5. 增加错误测试覆盖 Emoji 文档。
6. 执行格式、编译、测试和 SelfForge 验证。
7. 生成并提升 `v0.1.9`。

# 执行记录

1. 已新增根目录简洁 `README.md`。
2. 已将 README 必需文件写入 layout 初始化和验证流程。
3. 已在 `Agents.md` 写入禁止 Emoji 和 README 简洁规则。
4. 已在 Markdown 文档校验中增加 Emoji 拒绝逻辑。
5. 已新增 Emoji 文档错误测试。
6. 已执行格式、编译、测试、验证、候选生成和提升流程。

## v0.1.10

# 任务来源

用户要求继续推进 SelfForge 系统功能。

# 任务描述

为 Runtime 的受控执行增加本地审计记录。每次执行命令后，必须在当前 major workspace 的 `sandbox/runs/` 下生成独立运行目录，并保存标准输出、标准错误和结构化运行报告。

# 输入

- 当前稳定版本：v0.1.9
- 用户目标：继续完善系统功能。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- Runtime 执行审计记录能力。
- 执行记录测试。
- 更新后的 forge 归档记录。

# 计划（Plan）

1. 读取历史记忆和 Runtime 实现。
2. 为 `ExecutionReport` 增加运行记录目录字段。
3. 在命令执行结束后写入 `sandbox/runs/run-*` 记录。
4. 保存 `report.json`、`stdout.txt`、`stderr.txt`。
5. 增加成功执行和超时执行的记录测试。
6. 执行格式、编译、测试和 SelfForge 验证。
7. 生成并提升 `v0.1.10`。

# 执行记录

1. 已为 Runtime 执行增加 `sandbox/runs/run-*` 记录目录。
2. 已写入 `report.json`、`stdout.txt`、`stderr.txt`。
3. 已让 CLI `run` 输出记录目录。
4. 已补充成功执行和超时执行记录测试。
5. 已执行真实 `run` 命令并确认记录落盘。
6. 已执行格式、编译、测试、验证、候选生成和提升流程。

## v0.1.11

# 任务来源

用户要求继续推进 SelfForge 系统功能。

# 任务描述

在现有 `sandbox/runs/run-*` 单次运行记录基础上，增加 `sandbox/runs/index.jsonl` 运行索引。索引用于快速扫描执行历史，避免只能逐个打开运行目录。

# 输入

- 当前稳定版本：v0.1.10
- 用户目标：继续完善系统功能。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- Runtime 运行索引能力。
- 更新后的执行记录测试。
- 更新后的规则与 forge 归档记录。

# 计划（Plan）

1. 读取历史记忆和 Runtime 执行记录实现。
2. 设计 `index.jsonl` 单行摘要字段。
3. 在每次执行记录落盘后追加索引行。
4. 增加成功执行和超时执行的索引测试。
5. 执行格式、编译、测试和 SelfForge 验证。
6. 生成并提升 `v0.1.11`。

# 执行记录

1. 已新增 `sandbox/runs/index.jsonl` 运行索引。
2. 已在 Runtime 每次执行后追加一行 JSON 摘要。
3. 已补充成功执行和超时执行索引测试。
4. 已执行真实 `run` 命令并确认索引可读。
5. 已执行格式、编译、测试、验证、候选生成和提升流程。

## v0.1.12

# 任务来源

用户要求继续推进 SelfForge 系统功能。

# 任务描述

在已有 `index.jsonl` 运行索引基础上，新增只读查询入口，让系统可以直接列出最近运行记录。

# 输入

- 当前稳定版本：v0.1.11
- 用户目标：继续完善系统功能。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- Runtime 运行索引查询接口。
- Supervisor 查询门面。
- CLI `runs` 命令。
- 更新后的测试与文档。

# 计划（Plan）

1. 读取历史记忆和 Runtime 索引实现。
2. 在 Runtime 中实现 `list_runs`。
3. 在 Supervisor 中暴露查询门面。
4. 在 CLI 中新增 `runs` 命令。
5. 增加索引查询测试。
6. 执行真实 `runs` 命令验证输出。
7. 执行格式、编译、测试、验证、候选生成和提升流程。

# 执行记录

1. 已新增 Runtime `list_runs(version, limit)`。
2. 已新增 Supervisor `list_runs`。
3. 已新增 CLI `runs [--current|--candidate|--version VERSION] [--limit N]`。
4. 已更新 README 和 `Agents.md`。
5. 已执行真实 `runs` 命令并确认可读。
6. 已完成 `v0.1.12` 候选生成与提升。

## v0.1.12

# 任务来源

SelfForge 受控进化流程。

# 任务描述

从当前稳定版本 v0.1.11 生成下一候选版本 v0.1.12，并按 major 聚合规则追加归档。

# 输入

- 当前稳定版本：v0.1.11
- 用户目标：查询 Runtime 运行索引
- 状态文件：state/state.json

# 输出

- workspaces/v0
- forge/memory/v0.md
- forge/tasks/v0.md
- forge/errors/v0.md
- forge/versions/v0.md
- 更新后的 state/state.json

# 计划（Plan）

1. 读取状态。
2. 验证当前稳定版本。
3. 计算下一 patch 版本。
4. 复用 major 工作区并追加聚合文档。
5. 验证候选版本。
6. 持久化候选状态。

## v0.1.13

# 任务来源

用户要求继续推进 SelfForge 系统功能。

# 任务描述

在已有 `runs` 查询基础上，增加失败运行筛选能力，使系统可以快速定位非零退出和超时执行记录，为后续自动错误归档打基础。

# 输入

- 当前稳定版本：v0.1.12
- 用户目标：继续完善系统功能。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- Runtime 运行查询过滤条件。
- Supervisor 查询过滤门面。
- CLI `runs --failed` 与 `runs --timed-out` 参数。
- 更新后的测试与文档。

# 计划（Plan）

1. 读取历史记忆和运行索引查询实现。
2. 设计可扩展的运行查询过滤结构。
3. 在 Runtime 中按失败和超时条件过滤索引。
4. 在 Supervisor 和 CLI 中暴露筛选能力。
5. 增加成功、失败、超时和空限制测试。
6. 执行真实失败命令并用 `runs --failed` 验证输出。
7. 执行格式、编译、测试、验证、候选生成和提升流程。

# 执行记录

1. 已新增 `RunQuery` 过滤结构和 `RunIndexEntry::is_failed`。
2. 已新增 Runtime `query_runs(version, query)`。
3. 已新增 Supervisor `query_runs` 门面。
4. 已新增 CLI `runs --failed` 与 `runs --timed-out`。
5. 已补充成功、失败、超时和 `limit = 0` 查询测试。
6. 已执行真实失败 `run` 命令，并用 `runs --failed` 读取失败记录。
7. 已完成 `v0.1.13` 候选生成与提升。

## v0.1.14

# 任务来源

用户要求继续推进 SelfForge 系统功能。

# 任务描述

在失败运行筛选能力基础上，新增将失败运行追加为结构化错误归档的能力，使 Runtime 执行失败可以进入 `forge/errors/v0.md`，并为后续修复、验证和回滚诊断提供可追溯记录。

# 输入

- 当前稳定版本：v0.1.13
- 用户目标：继续完善系统功能。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 应用层错误归档模块。
- CLI `record-error` 命令。
- 失败运行归档测试。
- 更新后的规则与聚合文档。

# 计划（Plan）

1. 读取历史记忆、状态和运行记录实现。
2. 设计错误归档报告与错误类型。
3. 在应用层读取失败运行并追加中文错误小节。
4. 在 CLI 中新增 `record-error` 命令。
5. 增加成功归档、重复归档、无失败运行、非失败运行测试。
6. 执行真实失败运行并归档。
7. 执行格式、编译、测试、验证、候选生成和提升流程。

# 执行记录

1. 已新增应用层 `ErrorArchive` 错误归档模块。
2. 已新增 CLI `record-error` 命令。
3. 已实现最近失败运行和指定运行编号归档。
4. 已实现重复归档不重复追加。
5. 已补充成功归档、重复归档、无失败运行、非失败运行测试。
6. 已修正运行索引查询未按小版本过滤的问题。
7. 已执行真实失败运行并归档到 `forge/errors/v0.md`。
8. 已完成 `v0.1.14` 候选生成与提升。

## v0.1.15

# 任务来源

用户要求继续推进 SelfForge 系统功能。

# 任务描述

在错误归档能力基础上，新增将已归档运行错误标记为已解决的能力，并记录验证依据，使错误记录从“发现”进入“关闭”状态。

# 输入

- 当前稳定版本：v0.1.14
- 用户目标：继续完善系统功能。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 应用层错误解决能力。
- CLI `resolve-error` 命令。
- 错误解决状态更新测试。
- 更新后的规则与聚合文档。

# 计划（Plan）

1. 读取历史记忆、状态和错误归档实现。
2. 设计错误解决报告与错误类型。
3. 在应用层定位指定运行错误小节并更新“是否已解决”。
4. 在 CLI 中新增 `resolve-error` 命令。
5. 增加成功解决、重复解决、缺失错误记录测试。
6. 执行真实错误解决命令并验证文档。
7. 执行格式、编译、测试、验证、候选生成和提升流程。

# 执行记录

1. 已新增应用层错误解决能力。
2. 已新增 `ErrorResolutionReport`。
3. 已新增 CLI `resolve-error` 命令。
4. 已实现指定运行错误的解决状态更新。
5. 已实现重复解决幂等处理。
6. 已补充成功解决、重复解决和缺失错误记录测试。
7. 已执行真实 `resolve-error` 命令，将 v0.1.14 的验证用错误标记为已解决。
8. 已完成 `v0.1.15` 候选生成与提升。

## v0.1.16

# 任务来源

用户要求继续推进 SelfForge 系统功能。

# 任务描述

在错误归档和解决状态能力基础上，新增错误列表查询能力，使系统可以快速查看指定版本的未解决或已解决运行错误。

# 输入

- 当前稳定版本：v0.1.15
- 用户目标：继续完善系统功能。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 应用层错误列表查询能力。
- CLI `errors` 命令。
- 错误列表状态过滤测试。
- 更新后的规则与聚合文档。

# 计划（Plan）

1. 读取历史记忆、状态和错误归档实现。
2. 设计错误列表条目与查询条件。
3. 在应用层解析 `forge/errors/v0.md` 中的运行错误小节。
4. 在 CLI 中新增 `errors` 命令。
5. 增加未解决、已解决、空限制和版本过滤测试。
6. 执行真实错误列表查询命令。
7. 执行格式、编译、测试、验证、候选生成和提升流程。

# 执行记录

1. 已新增 `ArchivedErrorEntry` 错误列表条目。
2. 已新增 `ErrorListQuery` 查询条件。
3. 已新增 `ErrorArchive::list_run_errors(version, query)`。
4. 已新增 CLI `errors` 命令。
5. 已补充未解决、已解决、空限制和版本过滤测试。
6. 已执行真实 `errors --version v0.1.14 --resolved --limit 5` 命令。
7. 已执行真实 `errors --current --open --limit 5` 命令。
8. 已完成 `v0.1.16` 候选生成与提升。

## v0.1.17

# 任务来源

用户要求继续推进 SelfForge 系统功能。

# 任务描述

将未解决错误查询接入 `advance` 前置检查。如果当前稳定版本存在未解决错误，最小进化闭环必须停止，避免带着已知错误继续生成候选版本。

# 输入

- 当前稳定版本：v0.1.16
- 用户目标：继续完善系统功能。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- `advance` 未解决错误阻断能力。
- 最小闭环错误类型。
- 阻断测试与正常推进测试。
- 更新后的规则与聚合文档。

# 计划（Plan）

1. 读取历史记忆、状态和错误列表查询实现。
2. 在 `SelfForgeApp::advance` 开始阶段查询当前版本未解决错误。
3. 存在未解决错误时返回明确错误并保持状态不变。
4. 保持无未解决错误时的原有推进行为。
5. 增加未解决错误阻断测试。
6. 执行真实 `errors --current --open` 和 `advance` 验证。
7. 执行格式、编译、测试、验证、候选生成和提升流程。

# 执行记录

1. 已在 `SelfForgeApp::advance` 中接入未解决错误前置检查。
2. 已新增 `MinimalLoopError::OpenErrors`。
3. 已新增 `MinimalLoopError::ErrorArchive`。
4. 已导出 `MinimalLoopError` 供测试和上层调用判断。
5. 已补充未解决错误阻断测试，验证状态不会写入候选版本。
6. 已执行真实 `errors --current --open --limit 5`，确认当前版本无未解决错误。
7. 已执行真实 `advance` 和 `cycle`，完成 `v0.1.17` 候选生成与提升。
8. 已完成格式、编译、测试和验证流程。

## v0.1.18

# 任务来源

用户要求继续完成 SelfForge 系统功能。

# 任务描述

在已有错误阻断能力基础上，新增显式进化前置检查入口 `preflight`。该入口必须只读取和验证，不修改状态，用于在进入 `advance` 前汇总当前稳定版本、候选版本、布局验证结果和未解决错误数量。

# 输入

- 当前稳定版本：v0.1.17
- 用户目标：继续完善系统功能。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 应用层 `preflight` 前置检查报告。
- CLI `preflight` 命令。
- 干净状态、候选状态和未解决错误阻断状态测试。
- 更新后的规则、README 和聚合归档。

# 计划（Plan）

1. 读取历史记忆、状态、错误查询和最小闭环实现。
2. 在应用层新增只读 `PreflightReport`，汇总当前版本、工作区、候选版本、验证路径和未解决错误。
3. 在 CLI 新增 `preflight` 命令，输出可进化状态摘要。
4. 保持 `advance` 的未解决错误阻断行为不变，并复用同一错误查询来源。
5. 增加干净状态、候选状态和未解决错误状态测试。
6. 执行格式、编译、测试和真实命令验证。
7. 执行 `advance`、`cycle` 和 `validate` 完成候选提升。

# 执行记录

1. 已新增 `PreflightReport`，用于表达进化前置检查结果。
2. 已新增 `SelfForgeApp::preflight`，只读检查当前版本布局、候选版本布局和未解决错误。
3. 已新增 CLI `preflight` 命令。
4. 已将 `PreflightReport` 从 `app` 和 `lib` 导出。
5. 已补充干净状态、候选状态和未解决错误状态测试。
6. 已更新 `README.md` 常用命令和 `Agents.md` 前置检查规则。
7. 已执行 `cargo fmt`、`cargo fmt --check`、`cargo check` 和 `cargo test`。
8. 已执行真实 `preflight`、`advance`、`cycle`、`validate` 和未解决错误查询，完成 `v0.1.18` 候选生成与提升。
9. 已确认未创建 `workspaces/v0.1.18` 或任何 `forge/*/v0.1.18` 小版本归档。

## v0.1.19

# 任务来源

用户要求通过环境变量提供 AI API Key，并自动对接 DeepSeek、OpenAI、Gemini 等主流 AI API，为后续 Agent 开发打基础。

# 任务描述

新增 AI 提供商配置层。系统必须从环境变量读取 API Key、模型和基础地址，自动识别可用提供商，并提供只读检查命令。该能力不得记录或输出真实密钥，避免把敏感信息写入日志、状态或 forge 归档。

# 输入

- 当前稳定版本：v0.1.18
- 用户目标：支持主流 AI API 环境变量配置与自动对接。
- 官方 API 规则：OpenAI、DeepSeek、Gemini 的环境变量和认证方式。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 应用层 AI 提供商配置模块。
- CLI `ai-config` 只读检查命令。
- OpenAI、DeepSeek、Gemini 环境变量识别规则。
- 自动选择、指定提供商、Gemini 双环境变量优先级、未知提供商错误测试。
- 更新后的 README、Agents.md 和聚合归档。

# 计划（Plan）

1. 读取历史记忆、状态和当前应用层结构。
2. 设计 `AiProviderRegistry`，集中管理提供商定义、环境变量、默认基础地址和协议类型。
3. 支持 `SELFFORGE_AI_PROVIDER` 指定提供商；未指定时按已配置密钥自动选择。
4. 支持 `OPENAI_API_KEY`、`DEEPSEEK_API_KEY`、`GEMINI_API_KEY`、`GOOGLE_API_KEY`，并允许模型和基础地址通过环境变量覆盖。
5. 新增 CLI `ai-config`，只输出密钥是否存在和密钥来源变量名，不输出密钥值。
6. 增加单元测试、边界测试和错误测试。
7. 执行格式、编译、测试、前置检查、AI 配置检查、候选生成、提升和验证。

# 执行记录

1. 已新增 `AiProviderRegistry`，集中定义 OpenAI、DeepSeek、Gemini 的环境变量、默认基础地址、默认模型和协议类型。
2. 已新增 `AiConfigReport` 和 `AiProviderStatus`，只保存密钥来源变量名，不保存或输出密钥值。
3. 已新增 `AiConfigError::UnknownProvider`，用于拒绝未知 `SELFFORGE_AI_PROVIDER`。
4. 已新增 `SelfForgeApp::ai_config` 应用入口。
5. 已新增 CLI `ai-config` 命令。
6. 已新增 `.env.example`，并在 `.gitignore` 中忽略真实 `.env`。
7. 已补充自动选择 OpenAI、指定 DeepSeek、Gemini 双密钥变量优先级、未知提供商错误测试。
8. 已执行 `cargo fmt`、`cargo fmt --check`、`cargo check` 和 `cargo test`，39 个测试全部通过。
9. 已执行真实 `preflight`、无密钥 `ai-config`、临时 DeepSeek 配置 `ai-config`、`advance`、`cycle`、`validate` 和未解决错误查询。
10. 已完成 `v0.1.19` 候选生成与提升，并确认未创建小版本工作区或小版本归档文件。

## v0.1.20

# 任务来源

用户要求继续完成系统功能，并延续 AI API 自动对接能力。

# 任务描述

在 AI 提供商配置层之上新增统一请求构建能力。系统应能根据当前选中的 OpenAI、DeepSeek 或 Gemini 提供商生成对应的 HTTP 方法、请求地址、认证头摘要和 JSON 请求体，为后续真实 HTTP 执行和 Agent 调用层打基础。该能力仍然不得输出或持久化真实 API Key。

# 输入

- 当前稳定版本：v0.1.19
- 用户目标：继续完善 AI API 对接能力。
- AI 配置层：`AiProviderRegistry`
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 统一 AI 请求规格结构。
- CLI `ai-request` 请求构建检查命令。
- OpenAI Responses、DeepSeek Chat Completions、Gemini GenerateContent 请求体构建测试。
- 缺少密钥、空提示词错误测试。
- 更新后的 README、Agents.md 和聚合归档。

# 计划（Plan）

1. 读取历史记忆、状态和 AI 配置实现。
2. 新增 `AiRequestSpec`，表达方法、地址、认证头来源、模型、协议和请求体。
3. 基于选中提供商构建不同协议的请求体。
4. 新增 `SelfForgeApp::ai_request` 应用入口。
5. 新增 CLI `ai-request [prompt]`，输出脱敏请求摘要。
6. 增加 OpenAI、DeepSeek、Gemini、缺少密钥、空提示词测试。
7. 执行格式、编译、测试、前置检查、请求构建检查、候选生成、提升和验证。

# 执行记录

1. 已新增 `AiRequestSpec`，用于表达统一 AI 请求规格。
2. 已新增 `AiRequestError`，覆盖配置错误、缺少提供商、缺少 API Key 和空提示词。
3. 已在 `AiProviderRegistry` 中实现 OpenAI Responses 请求体构建。
4. 已在 `AiProviderRegistry` 中实现 DeepSeek Chat Completions 请求体构建。
5. 已在 `AiProviderRegistry` 中实现 Gemini GenerateContent 请求体构建。
6. 已新增 `SelfForgeApp::ai_request` 应用入口。
7. 已新增 CLI `ai-request [prompt]` 命令，输出脱敏请求摘要。
8. 已补充三家提供商请求构建测试、缺少密钥测试和空提示词测试。
9. 已执行 `cargo fmt`、`cargo fmt --check`、`cargo check` 和 `cargo test`，44 个测试全部通过。
10. 已执行真实 `preflight`、`ai-config`、临时 Gemini `ai-request`、临时 DeepSeek `ai-request`、`advance`、`cycle`、`validate` 和未解决错误查询。
11. 已完成 `v0.1.20` 候选生成与提升，并确认未创建小版本工作区或小版本归档文件。

## v0.1.21

# 任务来源

用户要求继续完成系统功能，并延续 AI API 自动对接能力。

# 任务描述

在统一 AI 请求构建层之后，新增 AI 响应文本归一化能力。系统应能从 OpenAI Responses、DeepSeek Chat Completions 和 Gemini GenerateContent 的不同 JSON 响应结构中抽取统一文本结果，为后续真实 HTTP 执行和 Agent 推理结果处理打基础。

# 输入

- 当前稳定版本：v0.1.20
- 用户目标：继续完善 AI API 对接能力。
- AI 请求规格：`AiRequestSpec`
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 统一 AI 文本响应结构。
- AI 响应解析错误类型。
- OpenAI、DeepSeek、Gemini 响应解析测试。
- 无效 JSON、缺失文本和空文本错误测试。
- 更新后的 README、Agents.md 和聚合归档。

# 计划（Plan）

1. 读取历史记忆、状态和 AI 请求构建实现。
2. 新增 `AiTextResponse`，表达归一化后的文本结果。
3. 新增 `AiResponseError`，覆盖无效 JSON、缺失文本和空文本。
4. 基于 `AiRequestSpec.protocol` 解析不同提供商响应结构。
5. 增加 OpenAI `output_text`、OpenAI `output` 数组、DeepSeek `choices`、Gemini `candidates` 测试。
6. 增加无效 JSON、缺失文本和空文本错误测试。
7. 执行格式、编译、测试、前置检查、候选生成、提升和验证。

# 执行记录

1. 已新增 `AiTextResponse`，用于表达统一 AI 文本结果。
2. 已新增 `AiResponseError`，覆盖无效 JSON、缺失文本和空文本。
3. 已新增 `AiProviderRegistry::parse_text_response`。
4. 已实现 OpenAI Responses `output_text` 解析。
5. 已实现 OpenAI Responses `output[].content[].text` 解析。
6. 已实现 DeepSeek Chat Completions `choices[0].message.content` 解析。
7. 已实现 Gemini GenerateContent `candidates[].content.parts[].text` 解析。
8. 已补充 OpenAI、DeepSeek、Gemini、无效 JSON、缺失文本和空文本测试。
9. 已执行 `cargo fmt`、`cargo fmt --check`、`cargo check` 和 `cargo test`，51 个测试全部通过。
10. 已执行真实 `preflight`、`ai-config`、临时 OpenAI `ai-request`、`advance`、`cycle`、`validate` 和未解决错误查询。
11. 已完成 `v0.1.21` 候选生成与提升，并确认未创建小版本工作区或小版本归档文件。

## v0.1.22

# 任务来源

用户反馈已设置 `SELFFORGE_AI_PROVIDER=deepseek`，但执行 `cargo run -- ai-request "你好"` 时仍提示没有可用 AI 提供商。

# 任务描述

改进 AI 环境变量缺失诊断。当前错误信息没有明确区分 PowerShell 当前会话变量不可见、未设置 API Key、只设置 provider 未设置密钥等情况，容易让用户误以为 DeepSeek 对接失败。本轮只增强提示和文档，不引入真实 HTTP 执行。

# 输入

- 当前稳定版本：v0.1.21
- 用户反馈：PowerShell 中 AI provider 设置后 `ai-request` 报错。
- AI 配置与请求构建实现。
- 状态文件：state/state.json
- 历史记忆：forge/memory/v0.md

# 输出

- 更明确的 `MissingProvider` 错误信息。
- 更明确的 `MissingApiKey` 错误信息。
- PowerShell 环境变量设置示例。
- 针对错误消息的测试。
- 更新后的 README、Agents.md 和聚合归档。

# 计划（Plan）

1. 读取当前 AI 错误类型和 CLI 输出逻辑。
2. 改进 `AiRequestError` 的显示文本，明确 PowerShell 当前会话写法。
3. 对缺少 DeepSeek Key 的路径提示 `DEEPSEEK_API_KEY`。
4. 补充错误消息测试。
5. 更新 README 和 Agents.md 的环境变量示例。
6. 执行格式、编译、测试、前置检查、AI 配置检查、请求构建检查、候选生成、提升和验证。

# 执行记录

1. 已改进 `AiRequestError::MissingProvider` 错误提示，说明 PowerShell 当前会话设置方式。
2. 已改进 `AiRequestError::MissingApiKey` 错误提示，按提供商列出需要设置的 API Key 变量。
3. 已新增所有 API Key 环境变量收集辅助函数。
4. 已新增按 provider 查询 API Key 环境变量辅助函数。
5. 已补充缺少 provider 时错误消息测试。
6. 已补充缺少 DeepSeek Key 时错误消息测试。
7. 已更新 README PowerShell 示例。
8. 已更新 Agents.md Windows PowerShell 环境变量规则。
9. 已执行 `cargo fmt`、`cargo fmt --check`、`cargo check` 和 `cargo test`，53 个测试全部通过。
10. 已执行真实 `preflight`、`ai-config`、缺少 DeepSeek Key 的 `ai-request`、临时 DeepSeek Key 的 `ai-request`、`advance`、`cycle`、`validate` 和未解决错误查询。
11. 已完成 `v0.1.22` 候选生成与提升，并确认未创建小版本工作区或小版本归档文件。
## v0.1.23

# 任务来源

用户反馈：`ai-request` 在 `.env` 已配置 AI 提供商和密钥时仍提示没有可用 AI 提供商。

# 任务描述

让 SelfForge 自动读取项目根目录 `.env` 中的 AI 配置，覆盖 `ai-config` 和 `ai-request` 两条路径，同时保持密钥脱敏和不可写入日志、状态、文档。

# 输入

1. 当前稳定版本：`v0.1.22`。
2. 用户反馈命令：`cargo run -- ai-request "你好"`。
3. 期望行为：未显式设置 PowerShell 环境变量时，也能读取项目根目录 `.env`。

# 输出

1. `.env` 自动加载能力。
2. AI 配置与请求构建测试。
3. 中文文档与规则更新。
4. 版本 `v0.1.23` 的记忆、错误和版本记录。

# 计划（Plan）

1. 读取当前状态、历史记忆和 AI Provider 实现。
2. 在应用层通过项目根目录加载 `.env`，并保持进程环境变量优先。
3. 实现安全 `.env` 解析，只读取键值，不输出真实密钥。
4. 让 `SelfForgeApp::ai_config` 和 `SelfForgeApp::ai_request` 统一使用项目环境查找器。
5. 增加 `.env` 正常读取、环境变量覆盖 `.env`、缺失 `.env` 不崩溃和空提示词错误测试。
6. 更新 README 和 Agents.md 中的 `.env` 规则。
7. 执行格式化、测试、预检、验证、候选提升和提交。

# 执行记录

待执行。

## v0.1.34

# 任务来源

用户要求继续向最终目标推进，在最近记忆读取能力之后继续增强自我进化的计划输入。

# 任务描述

新增记忆经验提取能力，将最近记忆中的评估、错误总结、优化建议和可复用经验提炼为结构化结果，供后续 Agent 计划和自动进化使用。

# 输入

1. 当前稳定版本：`v0.1.33`。
2. 已有能力：`memory-context`、Agent 会话记忆读取、major 聚合记忆。
3. 本轮目标：从历史记忆中提取可复用经验与风险信号。

# 输出

1. 新增记忆经验提取报告。
2. 新增 CLI `memory-insights`。
3. Agent 进化与验证会话第一阶段记录经验提取数量。
4. 补充单元测试、边界测试和错误测试。
5. README、Agents.md、记忆、错误和版本归档更新。

# 计划（Plan）

1. 读取当前状态、最近记忆和记忆模块实现。
2. 设计经验提取结构，保持只读和可审计。
3. 实现 `memory-insights` 应用入口与 CLI。
4. 将经验提取接入 Agent 会话第一阶段。
5. 补充测试并执行严格验证。
6. 通过 `agent-evolve` 提升 v0.1.34 并提交。

# 执行记录

待执行。

## v0.1.32 最终执行记录

# 任务来源

用户要求继续朝最终目标前进，在已有 Agent 进化闭环基础上继续完善系统功能。

# 任务描述

新增 `agent-verify` 验证会话命令，使 Agent 能够创建独立验证会话，通过 Runtime 受控执行验证命令，并把运行记录关联到会话事件中。

# 输入

1. 当前稳定版本：`v0.1.31`。
2. 已有能力：`agent-evolve`、`agent-run`、Runtime 运行记录、Agent 会话事件。
3. 本轮目标：让验证行为从零散命令升级为可审计的 Agent 会话。

# 输出

1. 新增 `SelfForgeApp::agent_verify` 应用入口。
2. 新增 CLI `agent-verify [goal] -- PROGRAM [ARGS...]`。
3. 新增 `AgentVerificationReport`。
4. Agent 验证会话会记录 Runtime 运行标识和运行目录。
5. 新增成功验证、失败验证和空命令错误测试。
6. 当前稳定版本已提升为 `v0.1.32`。

# 执行记录

1. 已读取 `state/state.json`、最近记忆、Agent 会话和 Runtime 运行实现。
2. 已实现 `agent_verify`，复用 `agent_run` 生成运行证据。
3. 已扩展 CLI、README 和 Agents.md。
4. 已执行 `cargo fmt --check`、`cargo test`、`agent-evolve`、`agent-verify`、`validate`、`preflight` 和错误查询。
5. 已确认未创建小版本工作区目录或小版本 forge 文档。
## v0.1.31 最终执行记录

# 任务来源

用户要求继续朝最终目标前进，并强调合理、规范。

# 任务描述

新增 `agent-run`，让 Agent 会话步骤可以通过 Runtime 受控执行真实程序，并把运行编号、报告路径、退出码和超时状态写入同一个会话事件时间线。

# 输入

1. 当前稳定版本：`v0.1.30`。
2. 已有能力：Runtime 受控执行、Agent 会话事件时间线、跨版本会话查询。
3. 本轮目标：连接 Agent 计划事件与 Runtime 执行证据。

# 输出

1. `AgentRunReference`。
2. Agent 事件新增运行记录引用。
3. `SelfForgeApp::agent_run`。
4. CLI `agent-run [--session-version VERSION] [--current|--candidate|--version VERSION] [--step N] [--timeout-ms N] SESSION_ID -- PROGRAM [ARGS...]`。
5. `agent-session` 可显示运行引用详情。
6. 当前稳定版本已提升为 `v0.1.31`。

# 执行记录

1. 已扩展 Agent 事件模型，新增可选运行记录引用。
2. 已新增 Agent 步骤关联 Runtime 运行的方法。
3. 已新增应用层 `agent_run`，复用 Supervisor 和 Runtime 受控执行。
4. 已新增 CLI `agent-run` 及参数解析。
5. 已更新 README 和 Agents.md 规则。
6. 已新增成功运行、失败运行和旧事件兼容测试。
7. 已执行 `cargo fmt`、`cargo check`、`cargo test`、`preflight`、`errors`、`agent-evolve`、`validate`、`agent-sessions --all` 和单会话读取验证。
## v0.1.32

# 任务来源

用户要求继续推进系统。

# 任务描述

新增 Agent 验证会话命令，使系统能够在一个受控入口中创建验证会话、执行 Runtime 命令、记录运行证据，并保留完整 Agent 事件时间线。

# 输入

1. 当前稳定版本：`v0.1.31`。
2. 已有能力：`agent-run`、Agent 会话事件、Runtime 运行记录。
3. 本轮目标：把手动会话创建和运行记录关联封装成更直接的验证入口。

# 输出

1. 应用层新增 `agent_verify`。
2. CLI 新增 `agent-verify`。
3. 验证成功时自动完成会话。
4. 验证失败时保留失败会话和运行证据。
5. 覆盖成功、失败和空命令错误测试。
6. README、Agents.md、记忆、错误和版本归档更新。

# 计划（Plan）

1. 读取当前状态、历史记忆和 Agent 运行实现。
2. 记录本轮任务与可验证计划。
3. 实现 `agent_verify`：创建会话、标记计划步骤、调用 Runtime 受控执行、写入运行引用。
4. 新增 CLI `agent-verify` 和参数解析。
5. 补充单元测试、边界测试和错误测试。
6. 执行格式化、编译、测试、预检、Agent 进化、验证和提交。

# 执行记录

待执行。
## v0.1.30 最终执行记录

# 任务来源

用户要求继续完成系统功能，在跨小版本 Agent 会话查询完成后继续提升最小可运行闭环。

# 任务描述

为 Agent 会话新增结构化事件时间线，使自动进化过程中的会话创建、状态变化、步骤更新、完成和失败都可以在同一个会话文件中审计。

# 输入

1. 当前稳定版本：`v0.1.29`。
2. 已有能力：`agent-evolve [goal]`、Agent 会话持久化、跨小版本会话查询。
3. 本轮目标：增强 Agent 自动进化的可解释性和可审计性。

# 输出

1. `AgentSessionEvent` 和 `AgentSessionEventKind`。
2. Agent 会话 JSON 内嵌 `events` 时间线。
3. 会话摘要新增 `event_count`。
4. CLI `agent-session` 展示事件时间线。
5. CLI `agent-sessions` 展示事件数量。
6. 新增真实 Agent 会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777620867868905400-000.json`。
7. 当前稳定版本已提升为 `v0.1.30`。

# 执行记录

1. 已为会话模型新增事件结构和事件类型。
2. 已在会话创建、状态变化、步骤更新、完成和失败时写入事件。
3. 已通过默认值兼容旧会话文件和旧索引行。
4. 已扩展 CLI 输出，支持查看步骤结果和事件时间线。
5. 已更新 README 当前版本和 Agents.md 事件规则。
6. 已执行 `cargo fmt`、`cargo check`、`cargo test`、`preflight`、`errors`、`agent-evolve`、`validate`、`agent-sessions --all` 和单会话读取验证。
## v0.1.31

# 任务来源

用户要求继续朝最终目标前进，并强调合理、规范。

# 任务描述

新增 Agent 会话与 Runtime 运行记录的关联能力，使 Agent 执行步骤可以通过受控命令运行程序，并把运行编号、报告路径、退出码和超时状态写入同一个会话事件时间线。

# 输入

1. 当前稳定版本：`v0.1.30`。
2. 已有能力：Runtime 受控执行、Agent 会话事件时间线、跨版本会话查询。
3. 本轮目标：把计划事件和真实执行证据连接起来。

# 输出

1. Agent 事件支持运行记录引用。
2. 应用层新增 `agent_run` 用例。
3. CLI 新增 `agent-run` 命令。
4. `agent-session` 能显示运行记录引用。
5. 覆盖成功运行、失败运行和旧事件兼容的测试。
6. README、Agents.md、记忆、错误和版本归档更新。

# 计划（Plan）

1. 读取当前状态、历史记忆、Runtime 运行记录结构和 Agent 会话结构。
2. 记录本轮任务与可验证计划。
3. 扩展 Agent 事件结构，新增可选运行记录引用并兼容旧事件。
4. 在应用层新增 `agent_run`，复用 Supervisor 和 Runtime 执行，不绕过沙箱。
5. 扩展 CLI，新增 `agent-run` 参数解析和输出。
6. 补充单元测试、边界测试和错误测试。
7. 执行格式化、编译、测试、预检、Agent 进化、验证和提交。

# 执行记录

待执行。
## v0.1.29 最终执行记录

# 任务来源

用户要求继续完成系统功能，在 `agent-evolve` 已可完成单轮进化后继续增强最小可运行闭环。

# 任务描述

新增 `agent-sessions --all`，让版本提升后仍可在同一 major 工作区内审计最近 Agent 会话，并保持会话索引只写入 `workspaces/v0/artifacts/agents/index.jsonl`。

# 输入

1. 当前稳定版本：`v0.1.28`。
2. 已有能力：`agent-evolve [goal]`、Agent 会话持久化、会话摘要索引。
3. 本轮目标：补齐提升后的跨小版本会话审计入口。

# 输出

1. `AgentSessionStore::list_all_major`。
2. `SelfForgeApp::agent_sessions_all`。
3. CLI `agent-sessions --all [--limit N]`。
4. 新增跨版本查询、最新摘要去重、零 limit 边界、提升后审计查询测试。
5. 新增真实 Agent 会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777620200021938900-000.json`。
6. 当前稳定版本已提升为 `v0.1.29`。

# 执行记录

1. 已把会话索引读取逻辑收敛为 `list_from_index`，避免查询逻辑重复。
2. 已新增同一 major 下跨小版本查询能力，并按会话 id 返回最新摘要。
3. 已新增 CLI `agent-sessions --all`，输出会话版本、状态、步骤、目标和文件路径。
4. 已更新 README 与 Agents.md，写明跨版本审计查询规则。
5. 已执行 `cargo fmt`、`cargo check`、`cargo test`、`preflight`、`errors`、`agent-evolve`、`validate`、`agent-sessions --all` 和单会话读取验证。
## v0.1.30

# 任务来源

用户要求继续完成系统功能，在 v0.1.29 已支持跨小版本 Agent 会话查询后继续推进最小可运行闭环。

# 任务描述

为 Agent 会话新增结构化事件时间线，使会话创建、状态变化、步骤更新、成功完成和失败停止都能持久化到同一个会话文件，并在 CLI 中可审计。

# 输入

1. 当前稳定版本：`v0.1.29`。
2. 已有能力：Agent 会话持久化、跨小版本会话查询、单轮完整 `agent-evolve`。
3. 本轮目标：增强 Agent 自动进化过程的可解释性和可审计性。

# 输出

1. `AgentSessionEvent` 和事件类型。
2. 会话 JSON 内嵌事件时间线。
3. 会话摘要增加事件数量并兼容旧索引。
4. CLI 会话详情显示事件时间线。
5. 覆盖事件记录、旧索引兼容、Agent 进化事件持久化的测试。
6. README、Agents.md、记忆、错误和版本归档更新。

# 计划（Plan）

1. 读取当前状态、历史记忆和 Agent 会话结构。
2. 记录本轮任务与可验证计划。
3. 在会话模型中新增事件结构，保持旧会话和旧索引可读取。
4. 将状态变化和步骤更新写入事件时间线。
5. 扩展 CLI 输出，展示会话事件。
6. 补充单元测试、边界测试和错误测试。
7. 执行格式化、编译、测试、预检、Agent 进化、验证和提交。

# 执行记录

待执行。
## v0.1.23 最终执行记录

# 任务来源

用户反馈：项目根目录 `.env` 中已配置 AI 提供商和密钥，但 `cargo run -- ai-request "你好"` 仍提示没有可用 AI 提供商。

# 任务描述

为 AI 配置增加 `.env` 自动读取能力，并保证 `ai-config` 与 `ai-request` 使用同一套项目配置查找逻辑。

# 输入

1. 当前稳定版本：`v0.1.22`。
2. 用户命令：`cargo run -- ai-request "你好"`。
3. 本地配置来源：项目根目录 `.env`。

# 输出

1. 自动读取 `.env` 的 AI 配置入口。
2. 进程环境变量优先于 `.env` 的覆盖规则。
3. `.env` 读取和密钥脱敏测试。
4. README、`.env.example` 和 Agents.md 中文规则更新。
5. 当前稳定版本提升为 `v0.1.23`。

# 执行记录

1. 已实现 `ProjectEnv` 解析器。
2. 已让 `SelfForgeApp::ai_config` 和 `SelfForgeApp::ai_request` 读取项目根目录 `.env`。
3. 已补充 4 个 `.env` 相关测试。
4. 已执行 `cargo fmt`、`cargo fmt --check`、`cargo check`、`cargo test`、`ai-config`、`ai-request`、`preflight`、`advance`、`cycle`、`validate` 和未解决错误查询。
5. 已确认 `ai-request "你好"` 能从 `.env` 中读取 DeepSeek 配置并生成脱敏请求摘要。
## v0.1.24

# 任务来源

用户反馈：`cargo run -- ai-request "提示词"` 当前只显示请求摘要，应该显示 AI 响应文本。

# 任务描述

将 `ai-request` 从脱敏请求构建推进为受控 HTTP 执行命令。默认发起真实 AI 请求并显示归一化响应文本，同时保留脱敏预览能力用于审计和调试。

# 输入

1. 当前稳定版本：`v0.1.23`。
2. 用户命令：`cargo run -- ai-request "提示词"`。
3. 已有能力：AI 配置读取、请求构建、响应文本归一化。

# 输出

1. 受控 AI HTTP 执行层。
2. `ai-request` 默认显示响应文本。
3. `ai-request --dry-run` 保留脱敏请求摘要。
4. HTTP 成功、失败、密钥脱敏和响应解析测试。
5. 中文文档和规则更新。

# 计划（Plan）

1. 读取当前状态、历史记忆和 AI 请求结构。
2. 新增受控 HTTP 执行结果和错误类型。
3. 实现 API Key 只在执行阶段读取，不进入请求摘要和持久化记录。
4. 用 `reqwest` blocking 客户端执行非流式 JSON 请求，并设置超时。
5. CLI `ai-request` 默认显示响应文本，新增 `--dry-run` 和 `--timeout-ms`。
6. 补充成功、HTTP 状态失败、空提示词和脱敏测试。
7. 执行格式化、编译、测试、预检、真实命令验证、候选提升和提交。

# 执行记录

待执行。
## v0.1.24 最终执行记录

# 任务来源

用户反馈：`ai-request` 应该显示 AI 响应文本，而不是只显示请求摘要。

# 任务描述

将 `ai-request` 接入真实非流式 HTTP 请求执行，并复用统一响应解析能力输出文本结果。

# 输入

1. 当前稳定版本：`v0.1.23`。
2. 用户命令：`cargo run -- ai-request "提示词"`。
3. DeepSeek 配置来源：项目根目录 `.env`。

# 输出

1. 默认显示响应文本的 `ai-request`。
2. 保留脱敏请求摘要的 `ai-request --dry-run`。
3. 可配置超时参数 `--timeout-ms`。
4. HTTP 执行与错误测试。
5. 当前稳定版本提升为 `v0.1.24`。

# 执行记录

1. 已新增 AI HTTP 执行报告、原始响应结构和错误类型。
2. 已新增受控 HTTP 执行函数，使用超时和脱敏错误摘要。
3. 已将 CLI `ai-request` 默认输出切换为响应文本。
4. 已补充 3 个 HTTP 执行相关测试，覆盖成功、失败状态和解析错误。
5. 已执行真实 DeepSeek 请求并获得响应文本。
6. 已执行 `advance` 和 `cycle`，将 `v0.1.24` 提升为当前稳定版本。
## v0.1.25

# 任务来源

用户要求继续完成系统，建立合理封装与多 Agent 开发能力，架构需要便于后续扩展大量 Agent。

# 任务描述

新增多 Agent 架构基础层，提供 Agent 定义、能力模型、注册表和目标到协作计划的确定性生成能力。该层只建立稳定边界，不在本轮实现复杂并发执行。

# 输入

1. 当前稳定版本：`v0.1.24`。
2. 用户目标：支持后续多 Agent 开发与扩展。
3. 现有应用层：AI 提供商、错误归档、最小闭环。

# 输出

1. `src/app/agent/` 模块。
2. Agent 能力、定义、注册表和计划结构。
3. `agents` 与 `agent-plan` CLI 命令。
4. 单元测试、边界测试和错误测试。
5. 中文文档、记忆、版本和错误归档。

# 计划（Plan）

1. 读取当前状态、历史记忆和应用层结构。
2. 设计独立 Agent 模块，避免把多 Agent 逻辑散落到 CLI。
3. 实现 Agent 能力枚举、定义、注册表、唯一性校验和标准 Agent 集合。
4. 实现目标到多 Agent 协作计划的确定性生成。
5. 在 `SelfForgeApp` 暴露 Agent 目录和计划入口。
6. 新增 CLI `agents` 与 `agent-plan [goal]`。
7. 补充标准注册表、空目标、重复 Agent、缺失能力和计划顺序测试。
8. 执行格式化、编译、测试、预检、候选提升、验证和提交。

# 执行记录

待执行。
## v0.1.25 最终执行记录

# 任务来源

用户要求继续完成系统，完成合理封装，支持后续多 Agent 开发，并保持专业、可扩展、易维护的架构。

# 任务描述

新增多 Agent 基础模块，让系统具备 Agent 目录、能力模型和协作计划生成能力。

# 输入

1. 当前稳定版本：`v0.1.24`。
2. 用户目标：多 Agent 开发能力和合理架构封装。
3. 现有应用层边界：AI、错误归档、最小闭环。

# 输出

1. `src/app/agent/` 独立模块。
2. 标准 Agent 注册表。
3. 多 Agent 协作计划结构。
4. CLI `agents` 和 `agent-plan [goal]`。
5. 当前稳定版本提升为 `v0.1.25`。

# 执行记录

1. 已新增 Agent 能力、定义、计划步骤和错误类型。
2. 已新增标准注册表，包含架构、实现、验证、审查和归档 Agent。
3. 已新增确定性计划生成能力。
4. 已新增应用层入口 `SelfForgeApp::agents` 和 `SelfForgeApp::agent_plan`。
5. 已新增 CLI 命令 `agents` 和 `agent-plan`。
6. 已补充 5 个 Agent 相关测试。
7. 已执行 `cargo fmt`、`cargo check`、`cargo test`、`agents`、`agent-plan`、`preflight`、`advance`、`cycle`、`validate` 和未解决错误查询。
## v0.1.26

# 任务来源

用户要求继续完成系统功能，进一步实现合理封装和多 Agent 开发基础，要求架构专业、目录结构清晰、后续扩展简单。

# 任务描述

在现有 Agent 注册表和计划器基础上，新增可持久化的 Agent 会话能力。会话必须记录目标、版本、计划步骤、步骤状态和索引摘要，并统一写入当前 major 工作区的 `artifacts/agents/` 目录，避免污染 workspace 根目录或生成小版本目录。

# 输入

1. 当前稳定版本：`v0.1.25`。
2. 现有能力：`agents` 和 `agent-plan [goal]`。
3. 目标能力：可启动、查询和加载 Agent 会话，为后续多 Agent 执行编排提供基础。

# 输出

1. `src/app/agent/session.rs` 会话存储模块。
2. `SelfForgeApp` 会话启动、列表和加载入口。
3. CLI `agent-start`、`agent-sessions` 和 `agent-session` 命令。
4. 会话持久化、列表限制、空目标和缺失会话测试。
5. README 和 Agents.md 规则更新。
6. 当前稳定版本提升为 `v0.1.26`。

# 计划（Plan）

1. 读取当前状态、历史记忆和 Agent 模块边界。
2. 设计 `AgentSessionStore`，将会话文件写入 `workspaces/vMAJOR/artifacts/agents/sessions/`。
3. 使用 `index.jsonl` 记录会话摘要，支持按版本过滤和限制数量。
4. 在应用层暴露会话启动、查询和加载方法。
5. 在 CLI 增加薄命令入口，保持业务逻辑集中在 `src/app/agent/`。
6. 补充单元测试、边界测试和错误测试。
7. 执行格式化、编译、测试、预检、候选提升、验证和提交。

# 执行记录

待执行。
## v0.1.26 最终执行记录

# 任务来源

用户要求继续完成系统功能，建立更合理的封装，支持后续多 Agent 开发，并保持顶级架构设计、代码规范和可扩展目录结构。

# 任务描述

在 v0.1.25 的 Agent 注册表和计划器基础上，新增 Agent 会话持久化、索引查询和单会话加载能力，为后续接入真实 AI Agent 调度、沙箱执行和归档闭环提供稳定基础。

# 输入

1. 当前稳定版本：`v0.1.25`。
2. 现有 Agent 能力：`agents`、`agent-plan [goal]`。
3. 目标能力：`agent-start`、`agent-sessions`、`agent-session`。

# 输出

1. 新增 `src/app/agent/session.rs`。
2. 新增 `SelfForgeApp::start_agent_session`、`agent_sessions`、`agent_session`。
3. 新增 CLI `agent-start`、`agent-sessions`、`agent-session`。
4. 新增 4 个 Agent 会话测试，覆盖持久化、列表限制、版本过滤、空目标、缺失会话和非法会话标识。
5. README 和 Agents.md 已记录会话目录与命令规则。
6. 当前稳定版本已提升为 `v0.1.26`。

# 执行记录

1. 已读取状态、预检结果、历史记忆和 v0.1.25 Agent 模块。
2. 已实现会话文件写入 `workspaces/vMAJOR/artifacts/agents/sessions/`。
3. 已实现 `workspaces/vMAJOR/artifacts/agents/index.jsonl` 摘要索引。
4. 已实现会话列表限制、版本过滤和单会话加载。
5. 已执行 `cargo fmt`、`cargo check`、`cargo test`、`agents`、`agent-plan`、`agent-sessions`、`preflight`、`advance`、`cycle`、`validate` 和未解决错误查询。
## v0.1.27

# 任务来源

用户要求继续完善 Agent，以进一步实现自动进化流程。

# 任务描述

在 Agent 会话持久化基础上，新增 Agent 驱动的受控进化入口。该入口需要创建会话、执行前置检查、调用现有 `advance` 最小闭环、更新会话步骤状态，并把结果写回当前 major 工作区的 Agent artifacts。

# 输入

1. 当前稳定版本：`v0.1.26`。
2. 已有能力：Agent 注册表、计划生成、会话持久化、`advance` 最小闭环。
3. 本轮目标：让 Agent 能够编排一次受控进化动作，而不是只生成静态计划。

# 输出

1. Agent 会话状态和步骤状态扩展。
2. Agent 会话保存与索引去重查询能力。
3. `SelfForgeApp::agent_advance` 自动进化编排入口。
4. CLI `agent-advance [goal]`。
5. 覆盖成功推进、候选推进、阻断失败和会话状态更新的测试。
6. README、Agents.md、记忆、错误和版本归档更新。

# 计划（Plan）

1. 读取当前状态、历史记忆和 Agent 会话实现。
2. 扩展会话状态模型，支持运行中、已完成、已失败和步骤结果。
3. 新增会话保存方法，更新会话 JSON 并追加最新摘要。
4. 调整会话列表逻辑，按会话标识去重并返回最新摘要。
5. 新增 `agent_advance` 应用层编排：创建会话、预检、调用 `advance`、写回步骤状态。
6. 新增 CLI `agent-advance [goal]`。
7. 补充单元测试、边界测试和错误测试。
8. 执行格式化、编译、测试、预检、候选提升、验证和提交。

# 执行记录

待执行。
## v0.1.27 最终执行记录

# 任务来源

用户要求继续完善 Agent，以进一步实现自动进化流程。

# 任务描述

在 v0.1.26 的 Agent 会话持久化基础上，新增 Agent 驱动的受控进化入口，使 Agent 会话能够编排前置检查、受控 `advance`、步骤状态回写和失败阻断。

# 输入

1. 当前稳定版本：`v0.1.26`。
2. 现有能力：Agent 注册表、计划生成、会话持久化和 `advance` 最小闭环。
3. 目标能力：`agent-advance [goal]`。

# 输出

1. 会话状态扩展为已计划、运行中、已完成、已失败。
2. 步骤状态扩展为待执行、运行中、已完成、已失败。
3. 会话步骤支持记录执行结果。
4. 会话索引支持追加更新并按会话标识返回最新摘要。
5. 新增 `SelfForgeApp::agent_advance`。
6. 新增 CLI `agent-advance [goal]`。
7. 当前稳定版本已提升为 `v0.1.27`。

# 执行记录

1. 已扩展 `AgentSession`、`AgentSessionStep` 和 `AgentSessionSummary`。
2. 已新增 `AgentSessionStore::save`，支持状态回写和索引追加。
3. 已调整会话列表读取逻辑，按最新摘要去重。
4. 已实现 `agent_advance`：创建会话、运行前置检查、调用 `advance`、写回步骤状态。
5. 已实现未解决错误阻断：发现开放错误时会话标记为失败，并停止进化。
6. 已新增 CLI `agent-advance [goal]`。
7. 已执行 `cargo fmt`、`cargo check`、`cargo test`、`agents`、`agent-plan`、`agent-sessions`、`preflight`、`advance`、`cycle`、`validate` 和未解决错误查询。
## v0.1.28

# 任务来源

用户要求继续完善系统功能，在 v0.1.27 的 Agent 自动进化编排基础上继续推进。

# 任务描述

新增 Agent 单轮完整进化入口，使 Agent 能够在一个受控命令中完成前置检查、候选准备、候选验证、提升或回滚，并将整个过程写回 Agent 会话。

# 输入

1. 当前稳定版本：`v0.1.27`。
2. 已有能力：`agent-advance [goal]`、`advance`、`cycle`、Agent 会话状态回写。
3. 本轮目标：闭合单次进化流程，减少人工串联命令。

# 输出

1. `SelfForgeApp::agent_evolve` 单轮完整进化入口。
2. CLI `agent-evolve [goal]`。
3. Agent 会话记录候选版本、提升或回滚结果。
4. 覆盖成功提升、已有候选处理、开放错误阻断和会话状态的测试。
5. README、Agents.md、记忆、错误和版本归档更新。

# 计划（Plan）

1. 读取当前状态、预检和 Agent 自动进化实现。
2. 设计单轮完整进化报告结构，复用 Supervisor 与现有状态机。
3. 实现 `agent_evolve`：无候选时准备候选，有候选时验证已有候选，然后执行 `cycle`。
4. 将准备、验证、提升或回滚结果写入同一个 Agent 会话。
5. 新增 CLI `agent-evolve [goal]`。
6. 补充单元测试、边界测试和错误测试。
7. 执行格式化、编译、测试、预检、Agent 进化、验证和提交。

# 执行记录

待执行。
## v0.1.28 最终执行记录

# 任务来源

用户要求继续完善系统，进一步推进 Agent 自动进化流程。

# 任务描述

新增 `agent-evolve [goal]`，让 Agent 能够在一个受控命令中完成单轮完整进化：前置检查、准备候选、候选验证、提升或回滚，并把结果写回 Agent 会话。

# 输入

1. 当前稳定版本：`v0.1.27`。
2. 已有能力：`agent-advance [goal]`、`advance`、`cycle`、Agent 会话状态回写。
3. 本轮目标：闭合单轮自动进化流程。

# 输出

1. `SelfForgeApp::agent_evolve` 单轮完整进化入口。
2. CLI `agent-evolve [goal]`。
3. 新增 `AgentSingleEvolutionReport`。
4. 新增成功提升、已有候选验证、开放错误阻断测试。
5. 新增真实 Agent 会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777619228657671900-000.json`。
6. 当前稳定版本已提升为 `v0.1.28`。

# 执行记录

1. 已实现无候选时准备下一 patch 候选并立即运行 `cycle`。
2. 已实现已有候选时直接验证候选，避免额外生成候选版本。
3. 已实现开放错误阻断，禁止在未解决错误存在时准备候选。
4. 已将单轮进化过程写回 Agent 会话步骤。
5. 已执行 `cargo fmt`、`cargo check`、`cargo test`、`agent-plan`、`preflight`、`errors`、`agent-evolve`、`validate` 和 Agent 会话查询。
## v0.1.29

# 任务来源

用户要求继续完成系统功能，在已具备 Agent 单轮完整进化入口后继续推进最小可运行闭环。

# 任务描述

新增 Agent 跨小版本会话查询能力，使版本提升后仍可在同一 major 工作区内审计最近的 Agent 进化会话，避免查询入口被 patch 版本变化切断。

# 输入

1. 当前稳定版本：`v0.1.28`。
2. 已有能力：`agent-evolve [goal]`、Agent 会话持久化、同一 major 聚合工作区。
3. 本轮目标：增强自动进化后的审计查询能力。

# 输出

1. `AgentSessionStore` 支持同一 major 下跨版本读取最近会话。
2. `SelfForgeApp::agent_sessions_all` 应用入口。
3. CLI `agent-sessions --all`。
4. 覆盖版本过滤、跨版本查询、去重和版本提升后查询的测试。
5. README、Agents.md、记忆、错误和版本归档更新。

# 计划（Plan）

1. 读取当前状态、预检和 Agent 会话查询实现。
2. 记录本轮任务与可验证计划。
3. 实现同一 major 下跨小版本会话查询，复用既有 `index.jsonl`。
4. 扩展应用层和 CLI，新增 `agent-sessions --all`。
5. 补充单元测试、边界测试和错误测试。
6. 执行格式化、编译、测试、预检、Agent 进化、验证和提交。

# 执行记录

待执行。

## v0.1.33

# 任务来源

用户要求继续推进 SelfForge，优先完成自我进化所需的可追溯能力。

# 任务描述

新增最近记忆上下文读取能力，使 Agent 进化会话能够在开始时读取最近历史记忆，并在会话步骤中留下可审计记录。

# 输入

1. 当前稳定版本：`v0.1.32`。
2. 已有能力：`agent-evolve`、`agent-verify`、major 聚合记忆文件。
3. 本轮目标：让进化计划具备明确的历史记忆输入。

# 输出

1. 新增应用层记忆上下文读取模块。
2. 新增 CLI `memory-context`。
3. `agent-advance`、`agent-evolve`、`agent-verify` 在会话第一阶段记录已读取的记忆数量。
4. 补充单元测试、边界测试和错误测试。
5. README、Agents.md、记忆、错误和版本归档更新。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 会话实现。
2. 设计 `MemoryContextReport` 与最近版本记忆解析规则。
3. 实现 `memory-context` 查询入口。
4. 将记忆读取接入 Agent 进化和验证会话。
5. 补充解析、边界、错误和会话记录测试。
6. 执行格式化、测试、进化提升、验证和提交。

# 执行记录

待执行。

## v0.1.33 最终执行记录

# 任务来源

用户要求继续推进 SelfForge，并优先增强自我进化流程所需的记忆能力。

# 任务描述

新增 `memory-context`，让系统能够读取当前 major 聚合记忆中的最近历史记录，并让 Agent 进化和验证会话在第一阶段记录已读取的记忆数量。

# 输入

1. 当前稳定版本：`v0.1.32`。
2. 已有能力：`agent-evolve`、`agent-verify`、Agent 会话事件、major 聚合记忆。
3. 本轮目标：让进化计划具备可审计历史上下文。

# 输出

1. `MemoryContextReport`、`MemoryContextEntry` 和 `MemoryContextError`。
2. `SelfForgeApp::memory_context`。
3. CLI `memory-context [--current|--candidate|--version VERSION] [--limit N]`。
4. `agent-advance`、`agent-evolve`、`agent-verify` 会话第一阶段记录历史记忆读取数量。
5. 新增真实 Agent 会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777622679341735100-000.json`。
6. 当前稳定版本已提升为 `v0.1.33`。

# 执行记录

1. 已读取当前状态、最近记忆和 Agent 会话实现。
2. 已实现按 major 聚合记忆文件读取最近记录。
3. 已实现重复版本去重，并优先选择带有最终标记的记忆小节。
4. 已将记忆读取接入 Agent 自动进化和验证会话。
5. 已执行 `cargo fmt --check`、`cargo test`、`memory-context`、`agent-evolve`、`validate`、`preflight` 和错误查询。

## v0.1.34 最终执行记录

# 任务来源

用户要求继续向最终目标推进，在最近记忆读取能力之后继续增强自我进化的计划输入。

# 任务描述

新增 `memory-insights`，将最近记忆中的评估、错误总结、优化建议和可复用经验提取为结构化结果，供后续 Agent 计划和自动进化使用。

# 输入

1. 当前稳定版本：`v0.1.33`。
2. 已有能力：`memory-context`、Agent 会话记忆读取、major 聚合记忆。
3. 本轮目标：从历史记忆中提取可复用经验与风险信号。

# 输出

1. `MemoryInsightReport` 和 `MemoryInsight`。
2. `SelfForgeApp::memory_insights`。
3. CLI `memory-insights [--current|--candidate|--version VERSION] [--limit N]`。
4. Agent 进化与验证会话第一阶段记录可复用经验和优化建议数量。
5. 新增真实 Agent 会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777623390716970400-000.json`。
6. 当前稳定版本已提升为 `v0.1.34`。

# 执行记录

1. 已读取当前状态、最近记忆和记忆模块实现。
2. 已实现结构化经验提取和占位内容过滤。
3. 已实现 `memory-insights` 查询入口。
4. 已将经验提取接入 Agent 自动进化和验证会话。
5. 已修正“未发现功能错误”被误识别为失败风险的问题。
6. 已执行 `cargo fmt --check`、`cargo test`、`memory-insights`、`agent-evolve`、`validate`、`preflight` 和错误查询。

## v0.1.35 任务计划

# 任务来源

用户要求继续向最终目标推进，并保持合理、规范、可扩展的实现节奏。

# 任务描述

将最近记忆经验接入 `agent-plan`，让计划生成阶段输出结构化历史经验摘要，为后续多 Agent 协作和自动进化计划提供可审计上下文。

# 输入

1. 当前稳定版本：`v0.1.34`。
2. 已有能力：`memory-insights`、Agent 注册表、Agent 静态计划、major 聚合记忆。
3. 本轮目标：让 Agent 计划阶段真实消费历史记忆经验。

# 输出

1. 带记忆经验的 Agent 计划应用层报告。
2. `agent-plan` 支持按版本和条数读取记忆经验。
3. 相关单元测试、边界测试和错误测试。
4. 当前稳定版本提升为 `v0.1.35`。

# 计划（Plan）

1. 读取当前状态、最近记忆和 `agent-plan` 实现。
2. 编写本轮任务计划。
3. 在应用层新增带记忆经验的 Agent 计划报告。
4. 更新 CLI 输出，让 `agent-plan` 展示历史经验摘要。
5. 补充计划报告的正常、边界和错误测试。
6. 执行格式化、严格测试、进化提升、验证和提交。

# 执行记录

待执行。

## v0.1.35 最终执行记录

# 任务来源

用户要求继续向最终目标推进，并保持合理、规范、可扩展的实现节奏。

# 任务描述

将最近记忆经验接入 `agent-plan`，让计划生成阶段输出结构化历史经验摘要，为后续多 Agent 协作和自动进化计划提供可审计上下文。

# 输入

1. 当前稳定版本：`v0.1.34`。
2. 已有能力：`memory-insights`、Agent 注册表、Agent 静态计划、major 聚合记忆。
3. 本轮目标：让 Agent 计划阶段真实消费历史记忆经验。

# 输出

1. `AgentPlanReport` 和 `AgentPlanReportError`。
2. `SelfForgeApp::agent_plan_with_memory`。
3. CLI `agent-plan [--current|--candidate|--version VERSION] [--limit N] [goal]`。
4. 新增正常、边界和错误测试。
5. 新增真实 Agent 会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777624076611582100-000.json`。
6. 当前稳定版本已提升为 `v0.1.35`。

# 执行记录

1. 已读取当前状态、最近记忆和 `agent-plan` 实现。
2. 已编写 v0.1.35 任务计划。
3. 已实现带记忆经验的 Agent 计划应用层报告。
4. 已更新 `agent-plan` 输出，让计划阶段展示历史经验摘要。
5. 已补充单元测试、边界测试和错误测试。
6. 已执行 `cargo fmt --check`、`cargo test`、`agent-plan`、`agent-evolve`、会话审计、小版本文件检查、`validate`、`preflight` 和开放错误查询。

## v0.1.36 任务计划

# 任务来源

用户要求继续推进 SelfForge，并优先完成更接近最终目标的自我进化能力。

# 任务描述

为 Agent 会话持久化计划上下文快照，让 `agent-start`、`agent-advance`、`agent-evolve` 和 `agent-verify` 创建的会话保存当时读取的记忆来源与经验摘要，提升计划可解释性和后续审计能力。

# 输入

1. 当前稳定版本：`v0.1.35`。
2. 已有能力：`agent-plan`、`memory-insights`、Agent 会话持久化、major 聚合归档。
3. 本轮目标：让会话记录包含计划依据，而不是只保存步骤和执行结果。

# 输出

1. Agent 会话计划上下文快照结构。
2. 会话创建时持久化记忆版本、来源版本和经验摘要数量。
3. `agent-session` 输出计划依据摘要。
4. 兼容旧会话文件的反序列化测试。
5. 当前稳定版本提升为 `v0.1.36`。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 会话结构。
2. 编写本轮任务计划。
3. 新增可选会话计划上下文字段，保持旧会话兼容。
4. 在应用层将 `memory-insights` 转换为会话快照。
5. 更新 `agent-start`、`agent-advance`、`agent-evolve`、`agent-verify` 和 `agent-session`。
6. 补充正常、边界和错误测试。
7. 执行格式化、严格测试、进化提升、验证和提交。

# 执行记录

待执行。

## v0.1.36 最终执行记录

# 任务来源

用户要求继续推进 SelfForge，并优先完成更接近最终目标的自我进化能力。

# 任务描述

为 Agent 会话持久化计划上下文快照，让 `agent-start`、`agent-advance`、`agent-evolve` 和 `agent-verify` 创建的会话保存当时读取的记忆来源与经验摘要，提升计划可解释性和后续审计能力。

# 输入

1. 当前稳定版本：`v0.1.35`。
2. 已有能力：`agent-plan`、`memory-insights`、Agent 会话持久化、major 聚合归档。
3. 本轮目标：让会话记录包含计划依据，而不是只保存步骤和执行结果。

# 输出

1. `AgentSessionPlanContext` 和 `AgentSessionMemoryInsight`。
2. 会话文件中的 `plan_context` 快照。
3. `agent-session` 计划依据摘要输出。
4. 新增真实 Agent 会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777624812019886500-000.json`。
5. 新增真实 Agent 进化会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777624840044233700-000.json`。
6. 当前稳定版本已提升为 `v0.1.36`。

# 执行记录

1. 已读取当前状态、最近记忆和 Agent 会话结构。
2. 已编写 v0.1.36 任务计划。
3. 已实现会话计划上下文快照并保持旧会话兼容。
4. 已将计划上下文接入 `agent-start`、`agent-advance`、`agent-evolve` 和 `agent-verify`。
5. 已更新 `agent-session` 输出计划依据摘要。
6. 已补充正常、边界和错误测试。
7. 已执行 `cargo fmt --check`、`cargo test`、`agent-start`、`agent-session`、`agent-evolve`、小版本文件检查、`validate`、`preflight` 和开放错误查询。

## v0.1.37

# 任务来源

SelfForge 受控进化流程。

# 任务描述

从当前稳定版本 v0.1.36 生成下一候选版本 v0.1.37，并按 major 聚合规则追加归档。

# 输入

- 当前稳定版本：v0.1.36
- 用户目标：新增可插拔 Agent 工具配置
- 状态文件：state/state.json

# 输出

- workspaces/v0
- forge/memory/v0.md
- forge/tasks/v0.md
- forge/errors/v0.md
- forge/versions/v0.md
- 更新后的 state/state.json

# 计划（Plan）

1. 读取状态。
2. 验证当前稳定版本。
3. 计算下一 patch 版本。
4. 复用 major 工作区并追加聚合文档。
5. 验证候选版本。
6. 持久化候选状态。

## v0.1.37 最终执行记录

# 任务来源

用户要求继续推进 SelfForge，并实现 Agent Tool 的可扩展、可插拔和动态配置能力，使 Agent 可以自由组合工具。

# 任务描述

新增 Agent Tool 注册与动态配置系统，支持内置工具、workspace major 级工具配置文件、Agent 工具绑定、计划步骤工具展示和会话步骤工具持久化。

# 输入

1. 当前稳定版本：`v0.1.36`。
2. 已有能力：Agent 注册表、Agent 计划、Agent 会话计划上下文、Runtime 和 AI Provider 基础能力。
3. 本轮目标：让工具成为 Agent 计划和会话中的一等结构化能力。

# 输出

1. `AgentToolDefinition`、`AgentToolConfig`、`AgentToolReport` 和 `AgentToolError`。
2. CLI `agent-tools [--current|--candidate|--version VERSION] [--init]`。
3. 动态配置文件 `workspaces/v0/artifacts/agents/tool-config.json`。
4. `agent-plan` 步骤工具输出。
5. Agent 会话步骤 `tool_ids` 持久化。
6. 新增真实 Agent 会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777625855927865900-000.json`。
7. 当前稳定版本已提升为 `v0.1.37`。

# 执行记录

1. 已读取当前状态、最近记忆与 Agent 计划结构。
2. 已编写 v0.1.37 任务计划。
3. 已实现 Agent Tool 结构、动态配置读取、初始化和验证。
4. 已将工具绑定接入 `agent-plan` 和 Agent 会话步骤。
5. 已补充默认工具、动态配置、错误配置和计划绑定测试。
6. 已执行 `cargo fmt --check`、`cargo test`、`agent-tools`、`agent-plan`、`agent-evolve`、会话审计、小版本文件检查、`validate`、`preflight` 和开放错误查询。

## v0.1.38 任务计划

# 任务来源

用户要求继续完善 SelfForge，重点提升 Agent 代码架构，并继续完成可自我迭代功能。

# 任务描述

在已有 Agent Tool 定义与动态配置基础上，新增统一工具调用协议和应用层工具执行入口，让 Agent 可以通过结构化调用执行已绑定工具，先覆盖记忆读取、记忆经验提取、会话查询、Runtime 受控运行、AI 请求预览和归档状态查询。

# 输入

1. 当前稳定版本：`v0.1.37`。
2. 已有能力：Agent 注册表、工具配置、工具绑定、会话持久化、Runtime 受控执行、AI Provider 请求规格。
3. 本轮目标：将工具从“计划中的绑定信息”推进到“可验证的统一调用协议”。

# 输出

1. Agent Tool 调用输入与调用结果结构。
2. 应用层 `invoke_agent_tool` 执行入口。
3. CLI `agent-tool-run` 命令。
4. 工具调用的权限校验、输入校验和 Runtime 运行关联。
5. 单元、边界和错误测试。
6. 当前稳定版本提升为 `v0.1.38`。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent Tool 模块。
2. 编写本轮任务计划。
3. 新增独立的 Agent Tool 调用协议模块，保持配置和执行分离。
4. 在应用层实现工具调用分发，禁止 CLI 直接解析业务逻辑。
5. 为 `agent-tool-run` 增加最小可用命令入口。
6. 补充工具调用成功、未授权、输入错误和 Runtime 关联测试。
7. 执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

## v0.1.38 最终执行记录

# 任务来源

用户要求继续完善 SelfForge，重点提升 Agent 代码架构，并继续完成可自我迭代功能。

# 任务描述

新增 Agent Tool 统一调用协议和应用层执行入口，使 Agent 可以通过结构化请求调用已绑定工具，并让工具调用成为后续自动执行计划的基础能力。

# 输入

1. 当前稳定版本：`v0.1.37`。
2. 已有能力：Agent 注册表、工具配置、工具绑定、会话持久化、Runtime 受控执行、AI Provider 请求规格。
3. 本轮目标：将工具从“计划中的绑定信息”推进到“可验证的统一调用协议”。

# 输出

1. `AgentToolInvocation`、`AgentToolInvocationInput` 和 `AgentToolInvocationReport`。
2. 应用层 `invoke_agent_tool`。
3. CLI `agent-tool-run TOOL_ID --agent AGENT_ID`。
4. Runtime 工具调用关联 Agent 会话和运行记录。
5. 新增真实 Agent 会话记录：`workspaces/v0/artifacts/agents/sessions/agent-session-1777627078521593200-000.json`。
6. 当前稳定版本已提升为 `v0.1.38`。

# 执行记录

1. 已读取当前状态、最近记忆和 Agent Tool 模块。
2. 已编写 v0.1.38 任务计划。
3. 已新增独立的 Agent Tool 调用协议模块。
4. 已在应用层实现工具调用分发，CLI 保持参数解析和展示职责。
5. 已为 `agent-tool-run` 增加最小可用命令入口。
6. 已补充工具调用成功、未授权、输入错误和 Runtime 关联测试。
7. 已执行 `cargo fmt --check`、`cargo test`、多条 `agent-tool-run` 真实调用、`agent-evolve`、`validate`、`preflight`、开放错误查询和小版本文件检查。

## v0.1.39 任务计划

# 任务来源

用户要求继续推进 SelfForge。

# 任务描述

新增 Agent 会话自动步进能力，让系统可以针对已有会话执行下一条待执行步骤，自动选择已绑定工具，调用后写回步骤状态和审计记录。Runtime 和 AI 这类需要外部输入的工具必须由调用方显式提供命令或提示词。

# 输入

1. 当前稳定版本：`v0.1.38`。
2. 已有能力：Agent 会话、工具绑定、统一工具调用协议、Runtime 运行记录。
3. 本轮目标：把“工具可调用”推进到“计划步骤可自动推进”。

# 输出

1. Agent 步进请求和步进结果结构。
2. 应用层 `execute_next_agent_step`。
3. CLI `agent-step` 命令。
4. 步骤自动选择工具、执行工具、更新会话状态和记录结果。
5. 单元、边界和错误测试。
6. 当前稳定版本提升为 `v0.1.39`。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 会话执行结构。
2. 编写本轮任务计划。
3. 新增 Agent 步进请求、结果和错误结构。
4. 在应用层实现下一待执行步骤选择和工具选择策略。
5. 为 `agent-step` 增加 CLI 入口。
6. 补充正常步进、缺少 Runtime 命令、Runtime 运行关联和无待执行步骤测试。
7. 执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

## v0.1.39 最终执行记录

# 任务来源

用户要求继续推进 SelfForge。

# 任务描述

新增 Agent 会话自动步进能力，让系统可以针对已有会话执行下一条待执行步骤，自动选择已绑定工具，调用后写回步骤状态和审计记录。

# 输入

1. 当前稳定版本：`v0.1.38`。
2. 已有能力：Agent 会话、工具绑定、统一工具调用协议、Runtime 运行记录。
3. 本轮目标：把“工具可调用”推进到“计划步骤可自动推进”。

# 输出

1. `AgentStepExecutionRequest` 和 `AgentStepExecutionReport`。
2. 应用层 `execute_next_agent_step`。
3. CLI `agent-step`。
4. 新增真实 Agent 步进验证会话：`workspaces/v0/artifacts/agents/sessions/agent-session-1777627829933565900-000.json`。
5. 新增真实 Agent 进化会话：`workspaces/v0/artifacts/agents/sessions/agent-session-1777627839639787800-000.json`。
6. 当前稳定版本已提升为 `v0.1.39`。

# 执行记录

1. 已读取当前状态、最近记忆和 Agent 会话执行结构。
2. 已编写 v0.1.39 任务计划。
3. 已新增 Agent 步进请求、结果和错误结构。
4. 已在应用层实现下一待执行步骤选择和工具选择策略。
5. 已为 `agent-step` 增加 CLI 入口。
6. 已补充正常步进、缺少 Runtime 命令、Runtime 运行关联和无待执行步骤测试。
7. 已执行 `cargo fmt --check`、`cargo test`、真实 `agent-step` 连续步进、`agent-evolve`、`validate`、`preflight`、开放错误查询和小版本文件检查。

## v0.1.40 任务计划

# 任务来源

用户要求解决 Markdown 记忆文件长期增长导致 AI 每次读取无关历史内容、浪费上下文的问题，并继续推进 SelfForge 功能。

# 任务描述

新增记忆压缩与冷归档能力：热记忆文件只保留近期完整记忆和压缩索引，久远完整记忆迁移到同一 major 下的冷归档文件，默认记忆读取与 Agent 计划仍只读取热记忆，避免把久远无关内容送入 AI 上下文。

# 输入

1. 当前稳定版本：`v0.1.39`。
2. 已有能力：`memory-context`、`memory-insights`、major 聚合记忆文件、Agent 计划上下文。
3. 本轮目标：实现可测试、可重复执行的 `memory-compact` 能力，并更新规则文档。

# 输出

1. 应用层记忆压缩报告与错误结构。
2. CLI `memory-compact [--current|--candidate|--version VERSION] [--keep N]`。
3. 冷归档文件 `forge/memory/archive/v0.md`。
4. 热记忆文件 `forge/memory/v0.md` 的压缩索引与近期记忆保留策略。
5. 单元、边界和错误测试。
6. 当前稳定版本提升为 `v0.1.40`。

# 计划（Plan）

1. 读取当前状态、最近记忆和记忆模块实现。
2. 编写本轮任务记录。
3. 在应用层新增记忆压缩归档能力，保持默认读取只访问热记忆。
4. 增加 `memory-compact` CLI 入口和参数解析。
5. 补充压缩迁移、幂等执行、错误参数和读取隔离测试。
6. 更新 `Agents.md` 与 `README.md` 的记忆压缩规则。
7. 执行格式化、严格测试、真实命令验证、进化提升、归档记录和提交。

## v0.1.40 最终执行记录

# 任务来源

用户要求解决 Markdown 记忆长期增长导致 AI 读取无关内容、浪费上下文的问题，并继续完善 SelfForge。

# 任务描述

新增热记忆压缩与冷归档能力，默认只让 Agent 和记忆读取命令消费近期完整记忆；久远完整记忆迁移到 major 聚合冷归档文件，保留审计能力但不进入默认上下文。同时让 `agent-evolve` 在候选提升成功后自动压缩热记忆。

# 输入

1. 当前稳定版本：`v0.1.39`。
2. 本轮目标：`v0.1.40`。
3. 现有记忆文件：`forge/memory/v0.md`。

# 输出

1. `MemoryCompactionReport` 与 `MemoryCompactionError`。
2. 应用层 `SelfForgeApp::compact_memory`。
3. CLI `memory-compact [--current|--candidate|--version VERSION] [--keep N]`，默认保留 5 条近期完整记忆。
4. 自动压缩接入 `agent-evolve`。
5. 冷归档文件 `forge/memory/archive/v0.md`。
6. 当前稳定版本已提升为 `v0.1.40`。

# 执行记录

1. 已读取 `preflight`、最近记忆经验、记忆模块、应用用例层和 CLI 结构。
2. 已编写 v0.1.40 任务计划。
3. 已实现热记忆压缩、冷归档渲染、幂等迁移和错误处理。
4. 已新增 `memory-compact` 命令，并将默认保留数设置为 5。
5. 已将 `agent-evolve` 提升成功后的自动记忆压缩纳入会话步骤和报告。
6. 已更新 `Agents.md` 与 `README.md`。
7. 已执行 `cargo fmt --check`、`cargo test`、真实 `memory-compact`、`memory-insights` 和 `agent-evolve` 验证。

## v0.1.41 任务计划

# 任务来源

用户要求适配多个 AI 同步修改代码的能力，支持多个 AI 线程进行任务分配、提示词生成、冲突解决和重复任务防护；单 AI 时可以自行领取任务，多 AI 时优先领取无人处理的任务。

# 任务描述

新增多 AI 协作任务板能力：在 major 工作区中持久化协作队列，生成可并行的任务切片、写入范围、依赖关系、验收标准和线程提示词；提供领取、完成、释放和状态查询命令，防止多个 AI 线程领取同一个任务或同时修改重叠范围。

# 输入

1. 当前稳定版本：`v0.1.40`。
2. 已有能力：Agent 注册表、Agent 会话、工具配置、Runtime 运行记录、热记忆读取。
3. 本轮目标：实现多 AI 协作调度的最小可运行版本，不直接引入真实并发代码执行器。

# 输出

1. `src/app/agent/coordination.rs` 多 AI 协作队列模块。
2. 应用层协作队列入口。
3. CLI `agent-work-init`、`agent-work-status`、`agent-work-claim`、`agent-work-complete`、`agent-work-release`。
4. 结构化提示词、写入范围、依赖关系和冲突检测。
5. 单元、边界和错误测试。
6. 当前稳定版本提升为 `v0.1.41`。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 模块结构。
2. 编写本轮任务记录。
3. 新增协作队列数据结构、路径布局和文件锁保护。
4. 实现初始化、状态查询、领取、完成和释放。
5. 将协作队列接入应用层和 CLI。
6. 更新 `Agents.md` 与 `README.md`。
7. 补充测试并执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

## v0.1.41 最终执行记录

# 任务来源

用户要求适配多个 AI 同步修改代码的能力，需要合理处理多线程任务分配、提示词描述、冲突解决、防止重复任务、单线程自动选择和多线程优先领取无人处理任务。

# 任务描述

实现多 AI 协作任务板的最小可运行版本，让不同 AI 线程在修改代码前先领取持久化任务，任务包含职责 Agent、优先级、依赖关系、写入范围、验收标准和线程提示词。任务板必须能查询状态、领取任务、完成任务和释放任务，并阻止写入范围冲突。

# 输入

1. 当前稳定版本：`v0.1.40`。
2. 本轮目标版本：`v0.1.41`。
3. 现有 Agent 注册表、会话、工具配置、Runtime、记忆和版本状态机。

# 输出

1. `src/app/agent/coordination.rs` 多 AI 协作队列模块。
2. 应用层协作队列入口。
3. CLI `agent-work-init`、`agent-work-status`、`agent-work-claim`、`agent-work-complete`、`agent-work-release`。
4. 持久化队列文件 `workspaces/v0/artifacts/agents/coordination/work-queue.json`。
5. 117 个测试通过。
6. 当前稳定版本已提升为 `v0.1.41`。

# 执行记录

1. 已读取 `preflight`、最近记忆、Agent 模块、应用层和 CLI 结构。
2. 已编写本轮任务计划。
3. 已实现协作队列、文件锁、任务选择、状态流转和冲突检测。
4. 已新增任务提示词，并修正“仍可领取任务”误包含当前任务的问题。
5. 已接入 CLI 命令，并通过真实命令验证初始化、领取、释放和状态查询。
6. 已更新 `Agents.md` 与 `README.md`。
7. 已执行 `cargo fmt --check`、`cargo test`、`agent-evolve` 和协作任务板命令验证。

## v0.1.42 任务计划

# 任务来源

用户要求继续推进 SelfForge，最近记忆建议增强多 AI 协作任务板的超时租约能力，防止某个 AI 线程中断后任务永久停留在已领取状态。

# 任务描述

在多 AI 协作队列中新增任务租约和过期释放能力：领取任务时记录租约过期时间；状态查询展示租约剩余或过期；提供命令释放已过期任务；释放必须写入事件并恢复候选任务提示词。该能力用于提高多 AI 并行修改代码时的可恢复性，暂不引入复杂调度器。

# 输入

1. 当前稳定版本：`v0.1.41`。
2. 已有协作任务板：`workspaces/v0/artifacts/agents/coordination/work-queue.json`。
3. 已有命令：`agent-work-init`、`agent-work-status`、`agent-work-claim`、`agent-work-complete`、`agent-work-release`。

# 输出

1. 任务领取租约字段和默认租约时长。
2. `agent-work-reap` 释放过期任务命令。
3. 状态输出展示租约信息。
4. 单元、边界和错误测试。
5. 当前稳定版本提升为 `v0.1.42`。

# 计划（Plan）

1. 读取当前状态、最近记忆和协作队列实现。
2. 编写本轮任务记录。
3. 在协作任务结构中增加租约字段和过期判断。
4. 实现过期任务释放能力，并写入队列事件。
5. 接入应用层与 CLI。
6. 更新 `Agents.md` 和 `README.md`。
7. 补充测试并执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

## v0.1.42 最终执行记录

# 任务来源

用户要求继续推进 SelfForge；最近记忆建议增强多 AI 协作任务板的超时租约能力，防止 AI 线程中断后任务长期占用。

# 任务描述

在多 AI 协作队列中新增任务租约与过期释放能力。领取任务时记录租约到期时间，状态查询展示租约信息，过期清理命令释放已过期任务并写入事件，确保其他线程可以继续领取。

# 输入

1. 当前稳定版本：`v0.1.41`。
2. 本轮目标版本：`v0.1.42`。
3. 已有多 AI 协作任务板和任务领取、完成、释放命令。

# 输出

1. 协作任务租约字段和默认租约时长。
2. `agent-work-claim --lease-seconds N`。
3. `agent-work-reap [--reason TEXT]`。
4. 状态查询租约展示。
5. 121 个测试通过。
6. 当前稳定版本已提升为 `v0.1.42`。

# 执行记录

1. 已读取 `preflight`、最近记忆和协作队列实现。
2. 已编写本轮任务计划。
3. 已实现租约记录、过期判断、过期清理和队列事件。
4. 已接入应用层与 CLI，并更新帮助文本。
5. 已更新 `Agents.md` 与 `README.md`。
6. 已补充租约记录、非法租约、过期释放和活跃租约保留测试。
7. 已执行 `cargo fmt --check`、`cargo test`、`agent-evolve` 和真实租约过期清理验证。

## v0.1.43 任务计划

# 任务来源

用户要求继续推进 SelfForge；最近记忆建议把协作任务板和 Agent 会话步进器连接起来，让会话开始时自动初始化任务板，并把任务板作为计划上下文的一部分。

# 任务描述

将多 AI 协作任务板接入 Agent 会话创建流程：`agent-start` 创建会话时自动初始化或复用当前 major 的协作任务板，把任务板路径、任务数量、线程数量和租约配置写入会话计划上下文，并在会话事件中记录任务板准备结果。该改动先连接计划与任务板，不直接自动领取或执行任务。

# 输入

1. 当前稳定版本：`v0.1.42`。
2. 已有 Agent 会话、计划上下文和多 AI 协作任务板。
3. 已有任务板租约和过期清理能力。

# 输出

1. Agent 会话计划上下文中的协作任务板信息。
2. 会话事件 `WorkQueuePrepared`。
3. `agent-start` 和 `agent-session` 输出展示任务板信息。
4. 单元测试覆盖新建队列、复用队列和旧会话兼容。
5. 当前稳定版本提升为 `v0.1.43`。

# 计划（Plan）

1. 读取当前状态、最近记忆和会话/任务板结构。
2. 编写本轮任务记录。
3. 扩展 Agent 会话计划上下文，增加任务板上下文。
4. 在 `agent-start` 成功读取记忆后初始化或复用协作任务板。
5. 更新 CLI 输出、`Agents.md` 和 `README.md`。
6. 补充测试并执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

## v0.1.43 最终执行记录

# 任务来源

用户要求继续推进 SelfForge；最近记忆建议将协作任务板和 Agent 会话步进器连接起来，先让会话创建时自动准备任务板上下文。

# 任务描述

将多 AI 协作任务板接入 Agent 会话创建流程。`agent-start` 创建会话时自动初始化或复用当前 major 的任务板，将队列路径、任务数量、线程数量、租约配置和创建或复用状态写入会话计划上下文，并记录会话事件。

# 输入

1. 当前稳定版本：`v0.1.42`。
2. 本轮目标版本：`v0.1.43`。
3. 已有 Agent 会话、计划上下文、协作任务板和租约清理能力。

# 输出

1. `AgentSessionWorkQueueContext`。
2. 会话事件 `WorkQueuePrepared`。
3. `agent-start` 和 `agent-session` 的协作任务板摘要输出。
4. 复用 major 队列时刷新队列版本字段。
5. 123 个测试通过。
6. 当前稳定版本已提升为 `v0.1.43`。

# 执行记录

1. 已读取 `preflight`、最近记忆、会话存储和协作任务板实现。
2. 已编写本轮任务计划。
3. 已扩展会话计划上下文和事件类型。
4. 已将协作任务板准备接入 `agent-start`。
5. 已更新 CLI 输出、`Agents.md` 和 `README.md`。
6. 已补充新建任务板、复用任务板、旧队列刷新版本和旧会话兼容测试。
7. 已执行 `cargo fmt --check`、`cargo test`、`agent-evolve`、`agent-start` 和 `agent-session` 真实验证。

## v0.1.44 任务计划

# 任务来源

用户要求继续推进 SelfForge；最近记忆建议把 `agent-step` 和协作任务板真正连接起来，让步骤执行前领取任务，成功后完成任务，失败时释放任务。

# 任务描述

实现 Agent 步进执行和多 AI 协作任务板的最小同步闭环。

# 输入

1. 当前稳定版本：`v0.1.43`。
2. 本轮目标版本：`v0.1.44`。
3. 已有 Agent 会话、协作任务板、租约、工具调用和 Runtime 运行记录能力。

# 输出

1. Agent 步骤持久化协作任务编号和工作线程。
2. `agent-step` 执行前自动领取任务。
3. 步骤成功后同步完成任务，失败后同步释放任务。
4. CLI 和会话详情展示步骤关联任务。
5. 124 个测试通过。
6. 当前稳定版本提升为 `v0.1.44`。

# 计划（Plan）

1. 读取最近记忆、当前状态和 Agent 步进实现。
2. 扩展会话步骤结构，保存任务编号和工作线程。
3. 在步进器中接入任务领取、完成和释放。
4. 更新 CLI 输出和 `Agents.md` 规则。
5. 补充单元、边界和错误测试。
6. 执行格式化、测试、真实命令验证、文档归档、版本提升和提交。

## v0.1.44 最终执行记录

# 任务来源

用户要求继续推进 SelfForge；本轮承接 v0.1.43 的优化建议，将会话步骤和协作任务板状态同步。

# 任务描述

让 `agent-step` 在工具输入完整后领取协作任务，并根据步骤执行结果完成或释放任务，形成可审计的步骤级任务所有权。

# 输入

1. 当前稳定版本：`v0.1.43`。
2. 本轮目标版本：`v0.1.44`。
3. 热记忆来源：`v0.1.43`、`v0.1.42`、`v0.1.41`、`v0.1.40`、`v0.1.39`。

# 输出

1. `AgentSessionStep.work_task_id` 和 `AgentSessionStep.work_worker_id`。
2. 会话事件 `WorkTaskLinked`。
3. `AgentStepExecutionReport` 返回协作任务信息。
4. `agent-step` 和 `agent-session` 展示步骤关联任务。
5. `Agents.md` 新增步进任务同步规则。
6. `state/state.json`、`Cargo.toml`、`Cargo.lock` 和 `CURRENT_VERSION` 提升到 `v0.1.44`。

# 执行记录

1. 已通过 `preflight` 确认 v0.1.43 可继续进化，开放错误为 0。
2. 已扩展 Agent 会话步骤结构和事件类型。
3. 已在 `execute_next_agent_step` 中接入任务领取、完成和释放。
4. 已补充测试，覆盖成功完成任务、缺少输入不领取任务、Runtime 步骤完成任务。
5. 已执行真实 `agent-start` 和 `agent-step` 验证，确认 CLI 输出协作任务并写入会话事件。
6. 已用 `agent-work-release` 恢复真实验证占用的任务板任务，保留事件记录。
7. 已执行版本线索提升和归档。

## v0.1.45 任务计划

# 任务来源

用户要求继续完成系统功能，并明确要求可以让 AI 自动进行自我升级。

# 任务描述

实现 AI 自我升级最小入口：由系统读取预检状态和近期记忆，让 AI 生成下一轮升级目标，再复用现有 `agent-evolve` 执行受控进化。

# 输入

1. 当前稳定版本：`v0.1.44`。
2. 本轮目标版本：`v0.1.45`。
3. 已有 AI Provider、`.env` 配置读取、`agent-evolve`、记忆提取、候选验证和回滚机制。

# 输出

1. 新增 `agent-self-upgrade [--dry-run] [--timeout-ms N] [hint]`。
2. 应用层新增 AI 自我升级预览和执行报告。
3. AI 响应归一化为单句中文升级目标。
4. 存在未解决错误时阻断自我升级。
5. README 和 `Agents.md` 补充 AI 自我升级规则。
6. 测试扩展到 128 个用例。
7. 当前稳定版本提升为 `v0.1.45`。

# 计划（Plan）

1. 读取最近记忆、当前状态、AI Provider 和 `agent-evolve` 实现。
2. 在应用层新增 AI 自我升级预览与执行用例。
3. 在 CLI 新增 `agent-self-upgrade` 命令，支持预览和真实执行。
4. 补充单元、边界和错误测试。
5. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
6. 执行格式化、测试、验证、预检、开放错误查询和自我升级预览。
7. 提交本轮小版本。

## v0.1.45 最终执行记录

# 任务来源

用户要求 SelfForge 能够让 AI 自动进行自我升级，本轮以最小可运行闭环优先。

# 任务描述

新增受控 AI 自我升级入口，允许系统读取历史经验，由 AI 选择下一轮升级目标，并通过现有进化状态机执行候选生成、验证、提升或回滚。

# 输入

1. 当前稳定版本：`v0.1.44`。
2. 本轮目标版本：`v0.1.45`。
3. 近期热记忆来源：`v0.1.44`、`v0.1.43`、`v0.1.42`、`v0.1.41`、`v0.1.40`。

# 输出

1. `AiSelfUpgradePreview`、`AiSelfUpgradeReport` 和 `AiSelfUpgradeError`。
2. `SelfForgeApp::ai_self_upgrade_preview` 与 `SelfForgeApp::ai_self_upgrade`。
3. `normalize_ai_self_upgrade_goal` 用于清理 AI 返回目标。
4. CLI 命令 `agent-self-upgrade`。
5. README 和 `Agents.md` 新增自我升级使用方式与约束。
6. 锁文件中 `tracing` 收敛到当前镜像可用版本，保证测试可复现。

# 执行记录

1. 已通过 `preflight` 确认 v0.1.44 可继续进化，开放错误为 0。
2. 已在应用层实现 AI 自我升级预检、记忆读取、提示词生成、请求预览、响应目标归一化和 `agent-evolve` 调用。
3. 已在 CLI 新增 `agent-self-upgrade`，支持 `--dry-run` 和 `--timeout-ms`。
4. 已补充测试，覆盖提示词生成、目标归一化、空目标拒绝和开放错误阻断。
5. 首次 `cargo test` 因 Cargo 镜像缺少锁定的 `tracing 0.1.45` 失败，已将锁文件收敛为镜像可用的 `tracing 0.1.44` 并复验。
6. 已执行 `cargo test`，128 个测试全部通过。
7. 已刷新协作任务板到 `v0.1.45`。
8. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight` 和 `cargo run -- errors --current --open --limit 5`。
9. 已执行 `cargo run -- agent-self-upgrade --dry-run "优先完善自动自我升级能力"`，确认预览输出脱敏摘要且不触发真实升级。
10. 已执行 `cargo run -- memory-compact --current --keep 5`，完成热记忆压缩。

## v0.1.46 任务计划

# 任务来源

用户要求继续推进 SelfForge；最近记忆建议增加自我升级执行记录的结构化归档，方便后续分析 AI 目标质量。

# 任务描述

为 AI 自我升级真实执行增加结构化审计记录，并提供查询命令。

# 输入

1. 当前稳定版本：`v0.1.45`。
2. 本轮目标版本：`v0.1.46`。
3. 已有 `agent-self-upgrade`、AI Provider、`agent-evolve` 和 workspace 产物目录。

# 输出

1. `workspaces/v0/artifacts/agents/self-upgrades/` 审计记录目录。
2. `index.jsonl` 审计摘要索引。
3. `agent-self-upgrades` 查询最近记录。
4. `agent-self-upgrade-record RECORD_ID` 读取单条记录。
5. 成功和失败自我升级均可写入结构化审计记录。
6. 测试扩展到 130 个用例。
7. 当前稳定版本提升为 `v0.1.46`。

# 计划（Plan）

1. 读取当前状态、最近记忆和 AI 自我升级实现。
2. 新增自我升级审计存储模块。
3. 将真实 `agent-self-upgrade` 成功和失败结果写入审计记录。
4. 新增审计记录查询 CLI。
5. 补充成功、失败、脱敏和查询相关测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检和提交。

## v0.1.46 最终执行记录

# 任务来源

用户要求继续；本轮承接 v0.1.45 的优化建议，为 AI 自我升级加入可追溯审计产物。

# 任务描述

让真实 AI 自我升级不只输出终端摘要，还能把提供商、模型、记忆来源、AI 响应摘要、归一化目标、Agent 会话、候选版本和结果写入结构化记录。

# 输入

1. 当前稳定版本：`v0.1.45`。
2. 本轮目标版本：`v0.1.46`。
3. 近期热记忆来源：`v0.1.45`、`v0.1.44`、`v0.1.43`、`v0.1.42`、`v0.1.41`。

# 输出

1. 新增 `src/app/agent/self_upgrade_audit.rs`。
2. `AiSelfUpgradeReport` 新增审计记录字段。
3. `SelfForgeApp` 新增审计记录写入、列表和读取能力。
4. CLI 新增 `agent-self-upgrades` 和 `agent-self-upgrade-record`。
5. `agent-self-upgrade` 成功输出审计记录编号和路径。
6. README 和 `Agents.md` 新增审计记录规则。

# 执行记录

1. 已通过 `preflight` 确认 v0.1.45 可继续进化，开放错误为 0。
2. 已读取最近 5 条热记忆并确认本轮目标。
3. 已新增 major 级自我升级审计目录和索引文件设计。
4. 已让真实自我升级成功后写入成功审计记录。
5. 已让 AI 请求失败、空目标和进化失败场景写入失败审计记录。
6. 已新增查询最近审计记录和读取单条审计记录的 CLI。
7. 已补充测试，确认成功审计、失败审计、脱敏和提示词不落盘。
8. 已执行 `cargo fmt` 和 `cargo test`，130 个测试全部通过。
9. 已将后续任务队列写入本文档，并确定下一轮优先关联自我升级审计记录与 Agent 会话。
10. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-self-upgrades --current --limit 5` 和自我升级预览。

## v0 后续任务队列

# 任务来源

用户要求继续思考接下来的任务，并写入文档后依次解决。任务排序依据当前系统目标、最近 5 个版本记忆和最小可运行闭环缺口。

# 排序原则

1. 优先补齐自我升级闭环中的可审计证据。
2. 再提升自动执行能力。
3. 再扩展 AI 写代码能力。
4. 每次只解决一个小版本任务，完成验证和提交后再进入下一项。

# 任务列表

1. 已完成：为 AI 自我升级真实执行写入结构化审计记录，并提供查询命令。对应版本：`v0.1.46`。
2. 已完成：将自我升级审计记录与 Agent 会话详情互相关联，便于从会话追溯自我升级来源，也能从审计记录定位会话执行过程。对应版本：`v0.1.47`。
3. 已完成：实现受控多步运行命令，让系统连续推进无需外部输入的 Agent 步骤，并在遇到输入缺失、Runtime 失败、AI 响应异常或任务板阻断时暂停。对应版本：`v0.1.48`。
4. 已完成：实现 AI 生成补丁的沙箱化草案流程，只允许写入候选 workspace 的受控范围，并必须先生成计划和测试。对应版本：`v0.1.49`。
5. 已完成：实现候选补丁差异审计和冲突检查，防止多个 AI 线程覆盖同一写入范围。对应版本：`v0.1.50`。
6. 已完成：实现自我升级执行后的中文总结报告，汇总目标、计划、代码变更、测试、错误、审计记录和下一步建议。对应版本：`v0.1.51`。
7. 已完成：实现补丁草案审计通过后的受控应用预演流程，只允许在候选工作区或沙箱中生成可审计变更，并继续保持测试和回滚约束。对应版本：`v0.1.52`。
8. 已完成：实现预演通过后的候选工作区受控应用流程，应用后必须自动执行格式化、测试、验证和回滚准备。对应版本：`v0.1.53`。
9. 已完成：实现候选应用后的验证执行记录，把格式化、测试、验证和预检命令的执行结果关联到候选应用记录。对应版本：`v0.1.54`。
10. 已完成：实现补丁验证通过后的受控源码覆盖准备记录，生成可人工复核的覆盖计划、差异摘要和回滚清单。对应版本：`v0.1.55`。
11. 已完成：实现源码覆盖准备通过后的受控源码覆盖执行记录，在真正覆盖源码前后生成备份、执行覆盖、重新验证和回滚记录。对应版本：`v0.1.56`。
12. 已完成：实现源码覆盖执行成功后的版本提升衔接记录，把覆盖执行记录、验证结果、建议提交信息和下一候选版本线索关联到同一条自我升级链路。对应版本：`v0.1.57`。
13. 已完成：实现基于源码覆盖提升衔接记录的候选生成准备流程，让系统在衔接记录就绪后可以受控生成或复用下一候选版本并继续验证。对应版本：`v0.1.58`。
14. 已完成：实现基于源码覆盖候选准备记录的自动候选验证与 cycle 衔接，把候选准备、预检、cycle 结果和回滚原因写入同一条后续审计链路。对应版本：`v0.1.59`。
15. 已完成：实现基于源码覆盖 cycle 记录的自动归档和后续自我升级总结，将提升或回滚后的状态、记忆压缩和下一任务生成连接起来。对应版本：`v0.1.60`。
16. 已完成：实现基于 cycle 后续总结记录的下一任务自动生成草案，把总结中的下一目标转换为受控任务草案并等待审计。对应版本：`v0.1.61`。
17. 已完成：实现基于下一任务草案的审计闸门，把草案验证为可执行计划后再允许进入 `agent-patch-draft` 或后续源码覆盖流程。对应版本：`v0.1.62`。
18. 已完成：让 `agent-patch-draft` 显式接收已批准的任务草案审计记录，自动使用批准目标生成补丁草案，并拒绝未审计草案链路。对应版本：`v0.1.63`。
19. 已完成：让补丁草案记录显式保存来源任务草案审计编号，便于从补丁草案反查任务草案、审计记录和后续源码覆盖链路。对应版本：`v0.1.64`。
20. 已完成：让补丁审计记录继承补丁草案中的来源任务草案审计编号，便于从审计结果继续反查任务草案审计链路。对应版本：`v0.1.65`。
21. 待执行：把来源任务草案审计编号继续贯通到补丁预演记录，形成任务草案审计、补丁草案、补丁审计和补丁预演的连续追踪链路。

# 下一步执行目标

本轮执行任务 20：让补丁审计记录继承补丁草案中的来源任务草案审计编号。下一轮优先解决任务 21：把来源任务草案审计编号继续贯通到补丁预演记录。

## v0.1.54 任务计划

# 任务来源

用户要求继续推进；后续任务队列的下一项是实现候选应用后的验证执行记录。

# 任务描述

新增 AI 补丁候选应用验证能力，读取已应用的补丁候选应用记录，按固定命令执行格式化、测试、系统验证和预检，并把每条命令的退出码、超时状态、输出摘要和最终验证状态回写到同一条候选应用记录。

# 输入

1. 当前稳定版本：`v0.1.53`。
2. 本轮目标版本：`v0.1.54`。
3. 最近 5 个版本记忆、补丁候选应用记录、预检结果和任务队列。

# 输出

1. 补丁候选应用记录新增验证状态和验证执行明细。
2. 新增 `agent-patch-verify APPLICATION_ID`，用于执行并记录候选应用验证。
3. `agent-patch-application-record` 展示验证状态和每条验证执行结果。
4. 记录仍写入 `workspaces/v0/artifacts/agents/patch-applications/`，不创建小版本目录。

# 计划（Plan）

1. 读取近期记忆和当前状态。
2. 编写本轮任务文档。
3. 扩展补丁候选应用记录结构，兼容旧记录。
4. 新增应用层验证执行流程，禁止隐式 shell 包装。
5. 新增 CLI 命令和参数解析。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

## v0.1.54 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 9。

# 任务描述

新增补丁候选应用后的验证执行记录，把格式化、测试、系统验证和预检命令的执行结果关联到同一条候选应用记录。

# 输入

1. 当前稳定版本：`v0.1.53`。
2. 本轮目标版本：`v0.1.54`。
3. 最近 5 个版本记忆、补丁候选应用记录、预检结果和任务队列。

# 输出

1. 补丁候选应用记录新增验证状态、验证时间和命令执行明细。
2. 新增 `agent-patch-verify`。
3. `agent-patch-application-record` 展示验证状态和验证执行结果。
4. 验证结果继续写入 `workspaces/v0/artifacts/agents/patch-applications/` 下的同一条候选应用记录。

# 执行记录

1. 已通过 `cargo run -- preflight` 确认 v0.1.53 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认下一步优先实现候选应用后的验证执行记录。
3. 已扩展候选应用记录结构，兼容旧记录的默认验证状态。
4. 已新增 `agent-patch-verify`，按白名单命令执行验证并回写同一条应用记录。
5. 已禁止验证命令隐式 shell 包装，未知命令会在执行前失败并写入失败验证明细。
6. 已补充测试，确认验证成功、验证失败、阻断应用跳过和未知命令错误路径。
7. 已执行 `cargo test ai_patch_verify`，4 个测试全部通过。
8. 首次执行 `cargo test ai_patch_verify` 发现新增验证字段初始化遗漏，已修复。
9. 已执行 `cargo test`，150 个测试全部通过。
10. 已执行 `cargo run -- validate`，检查 30 个路径。
11. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 43 条。
12. 已执行 `cargo run -- agent-work-init --current --threads 3 "补丁候选验证后续协作"`，协作任务板版本为 `v0.1.54`。
13. 记忆压缩后已再次执行 `cargo run -- validate` 和 `cargo run -- preflight`，均通过。
14. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-applications --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.55 任务计划

# 任务来源

用户要求继续推进；后续任务队列的下一项是实现补丁验证通过后的受控源码覆盖准备记录。

# 任务描述

新增 AI 补丁源码覆盖准备能力，读取已应用且验证通过的候选应用记录，生成覆盖计划、差异摘要和回滚清单，并保存目标文件的回滚备份。该流程只写入可审计记录，不直接覆盖仓库源码。

# 输入

1. 当前稳定版本：`v0.1.54`。
2. 本轮目标版本：`v0.1.55`。
3. 最近 5 个版本记忆、补丁候选应用记录、候选验证结果、预检结果和任务队列。

# 输出

1. `workspaces/v0/artifacts/agents/patch-source-plans/` 源码覆盖准备记录目录。
2. `agent-patch-source-plan APPLICATION_RECORD_ID` 生成源码覆盖准备记录。
3. `agent-patch-source-plans` 查询最近源码覆盖准备记录。
4. `agent-patch-source-plan-record SOURCE_PLAN_ID` 读取单条源码覆盖准备记录。
5. 覆盖准备记录必须包含目标文件、镜像文件、差异摘要、回滚备份和阻断原因。

# 计划（Plan）

1. 读取近期记忆和当前状态。
2. 编写本轮任务文档。
3. 新增源码覆盖准备记录存储模块，所有记录写入 major workspace 产物目录。
4. 新增应用层准备流程，校验候选应用状态和验证状态。
5. 生成目标文件差异摘要和回滚清单，不直接覆盖仓库源码。
6. 新增 CLI 命令和参数解析。
7. 补充单元、边界和错误测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

## v0.1.55 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 10。

# 任务描述

新增补丁验证通过后的受控源码覆盖准备记录，生成可人工复核的覆盖计划、差异摘要和回滚清单，并在准备阶段保留目标文件回滚备份。

# 输入

1. 当前稳定版本：`v0.1.54`。
2. 本轮目标版本：`v0.1.55`。
3. 最近 5 个版本记忆、补丁候选应用记录、候选验证结果、预检结果和任务队列。

# 输出

1. 新增 `src/app/agent/patch_source_plan.rs`。
2. 新增 `AiPatchSourcePlanStore`、`AiPatchSourcePlanRecord`、`AiPatchSourcePlanFile` 和覆盖准备状态。
3. 新增 `agent-patch-source-plan`、`agent-patch-source-plans` 和 `agent-patch-source-plan-record`。
4. 覆盖准备记录写入 `workspaces/v0/artifacts/agents/patch-source-plans/`。
5. 目标文件回滚备份写入单条准备记录目录下的 `rollback/` 分层。
6. 新增 4 个测试，覆盖成功准备、未验证阻断、验证失败阻断和镜像文件缺失阻断。

# 执行记录

1. 已通过 `cargo run -- preflight` 确认 v0.1.54 可继续进化，开放错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认下一步优先实现源码覆盖准备记录。
3. 已新增 major 级源码覆盖准备记录存储和索引。
4. 已新增已应用和验证通过两道前置闸门。
5. 已生成目标路径、镜像路径、目标存在状态、字节数、差异摘要和回滚动作。
6. 已对已有目标文件写入回滚备份，对新增目标文件记录删除回滚动作。
7. 已新增 CLI 命令并补充帮助信息。
8. 已补充测试，确认准备流程不会覆盖真实源码，未验证、验证失败和镜像缺失都会写入阻断记录。
9. 已执行 `cargo test ai_patch_source_plan`，4 个测试全部通过。
10. 已执行 `cargo test`，154 个测试全部通过。
11. 已执行 `cargo fmt --check`，格式检查通过。
12. 已执行 `cargo run -- validate`，检查 30 个路径。
13. 已执行 `cargo run -- preflight`，当前版本 `v0.1.55`，未解决错误 0，可进化。
14. 已执行 `cargo run -- errors --current --open --limit 5`，无开放错误记录。
15. 已执行 `cargo run -- agent-patch-source-plans --current --limit 5`，当前版本暂无真实源码覆盖准备记录。
16. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 44 条。
17. 已执行 `cargo run -- agent-work-init --current --threads 3 "源码覆盖准备后续协作"`，协作任务板版本为 `v0.1.55`。
18. 记忆压缩后已再次执行 `cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-source-plans --current --limit 5`，均通过。
19. 最终复验 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight` 和 `git diff --check`，均通过。

## v0.1.56 任务计划

# 任务来源

用户要求继续推进；后续任务队列的下一项是实现源码覆盖准备通过后的受控源码覆盖执行记录。

# 任务描述

新增 AI 补丁源码覆盖执行能力，读取已准备的源码覆盖计划，在执行前重新校验目标文件未变化，执行覆盖前写入执行级备份，执行覆盖后运行固定验证命令。若验证失败或写入失败，必须执行回滚并写入执行记录。

# 输入

1. 当前稳定版本：`v0.1.55`。
2. 本轮目标版本：`v0.1.56`。
3. 最近 5 个版本记忆、源码覆盖准备记录、候选应用验证结果、预检结果和任务队列。

# 输出

1. `workspaces/v0/artifacts/agents/patch-source-executions/` 源码覆盖执行记录目录。
2. `agent-patch-source-execute SOURCE_PLAN_ID` 执行指定源码覆盖准备记录。
3. `agent-patch-source-executions` 查询最近源码覆盖执行记录。
4. `agent-patch-source-execution-record EXECUTION_ID` 读取单条源码覆盖执行记录。
5. 执行记录必须包含覆盖文件、执行级备份、验证命令、验证结果、回滚状态和错误信息。

# 计划（Plan）

1. 读取近期记忆、当前状态和源码覆盖准备实现。
2. 新增源码覆盖执行记录存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层执行流程，校验准备记录状态、目标路径、镜像文件和目标文件新鲜度。
4. 执行覆盖前写入执行级备份，覆盖后运行固定验证命令。
5. 验证失败或写入失败时按执行级备份回滚，并记录回滚结果。
6. 新增 CLI 命令和参数解析。
7. 补充单元、边界和错误测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

## v0.1.56 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 11。

# 任务描述

新增源码覆盖准备通过后的受控源码覆盖执行记录，在真实覆盖源码前重新校验计划新鲜度，覆盖后运行验证命令，验证失败或写入失败时执行回滚并保留记录。

# 输入

1. 当前稳定版本：`v0.1.55`。
2. 本轮目标版本：`v0.1.56`。
3. 最近 5 个版本记忆、源码覆盖准备记录、候选应用验证结果、预检结果和任务队列。

# 输出

1. 新增 `src/app/agent/patch_source_execution.rs`。
2. 新增 `AiPatchSourceExecutionStore`、`AiPatchSourceExecutionRecord`、`AiPatchSourceExecutionFile` 和源码覆盖执行状态。
3. 新增 `agent-patch-source-execute`、`agent-patch-source-executions` 和 `agent-patch-source-execution-record`。
4. 执行记录写入 `workspaces/v0/artifacts/agents/patch-source-executions/`。
5. 执行级回滚备份写入单条执行记录目录下的 `rollback/` 分层。
6. 新增 4 个测试，覆盖成功覆盖、未准备计划阻断、验证失败回滚和陈旧目标阻断。

# 执行记录

1. 已通过 `cargo run -- preflight` 确认 v0.1.55 可继续进化，开放错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认下一步优先实现源码覆盖执行记录。
3. 已新增 major 级源码覆盖执行记录存储和索引。
4. 已新增源码覆盖执行前的新鲜度校验，检查准备状态、镜像文件、目标存在状态、字节数和准备级备份。
5. 已新增执行级备份写入、源码覆盖写入、验证命令执行和验证失败回滚逻辑。
6. 已新增 CLI 命令并补充帮助信息。
7. 已补充测试，确认执行成功会覆盖源码并记录验证，未准备计划会阻断，验证失败会回滚，目标文件变化会阻断。
8. 已执行 `cargo test ai_patch_source_execute`，首次发现陈旧目标断言文字过窄，已修正断言后 4 个测试全部通过。
9. 已执行 `cargo test`，158 个测试全部通过。
10. 已执行 `cargo fmt --check`，格式检查通过。
11. 已执行 `cargo run -- validate`，检查 30 个路径。
12. 已执行 `cargo run -- preflight`，当前版本 `v0.1.56`，未解决错误 0，可进化。
13. 已执行 `cargo run -- errors --current --open --limit 5`，无开放错误记录。
14. 已执行 `cargo run -- agent-patch-source-executions --current --limit 5`，当前版本暂无真实源码覆盖执行记录。
15. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 45 条。
16. 已执行 `cargo run -- agent-work-init --current --threads 3 "源码覆盖执行后续协作"`，协作任务板版本为 `v0.1.56`。
17. 记忆压缩后已再次执行 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-executions --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.47 任务计划

# 任务来源

用户要求按后续任务队列依次解决；队列任务 2 要求将自我升级审计记录与 Agent 会话详情互相关联。

# 任务描述

让审计记录可以按 Agent 会话反查，并让 `agent-session` 输出对应的自我升级审计记录。

# 输入

1. 当前稳定版本：`v0.1.46`。
2. 本轮目标版本：`v0.1.47`。
3. 已有自我升级审计记录、审计索引、Agent 会话详情命令。

# 输出

1. 审计存储支持按会话编号查找审计摘要。
2. 应用层提供 `ai_self_upgrade_record_for_session`。
3. `agent-session` 展示关联的自我升级审计记录。
4. 测试覆盖自我升级成功后通过会话反查审计记录。
5. 当前稳定版本提升为 `v0.1.47`。

# 计划（Plan）

1. 读取最近记忆、当前状态、审计存储和 `agent-session` 输出实现。
2. 在审计存储中新增按会话编号反查能力。
3. 在应用层暴露查询接口。
4. 更新 `agent-session` 输出。
5. 补充测试。
6. 更新归档和版本线索。
7. 执行格式化、测试、验证、预检和提交。

## v0.1.47 最终执行记录

# 任务来源

用户要求继续按文档队列依次解决任务；本轮执行队列任务 2。

# 任务描述

将 AI 自我升级审计记录与 Agent 会话详情互相关联。

# 输入

1. 当前稳定版本：`v0.1.46`。
2. 本轮目标版本：`v0.1.47`。
3. 近期热记忆来源：`v0.1.46`、`v0.1.45`、`v0.1.44`、`v0.1.43`、`v0.1.42`。

# 输出

1. `AiSelfUpgradeAuditStore::find_by_session`。
2. `SelfForgeApp::ai_self_upgrade_record_for_session`。
3. `agent-session` 输出自我升级审计记录摘要。
4. 自我升级成功测试新增会话反查断言。

# 执行记录

1. 已通过 `preflight` 确认 v0.1.46 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认本轮目标与队列任务一致。
3. 已实现审计索引按会话编号反查。
4. 已在应用层提供会话到审计记录的查询接口。
5. 已更新 `agent-session`，若存在关联审计记录则展示记录编号、状态、目标、候选版本、稳定版本和文件路径。
6. 已补充测试，验证自我升级成功后可通过会话编号找到审计记录。
7. 已刷新协作任务板到 `v0.1.47`。
8. 已执行 `cargo fmt`、`cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-self-upgrades --current --limit 5`。

## v0.1.48 任务计划

# 任务来源

用户要求继续思考接下来的任务，并写入文档后依次解决；后续任务队列中的下一项是受控多步运行命令。

# 任务描述

实现 `agent-steps`，让系统可以连续执行无需外部输入的 Agent 步骤，并在遇到 Runtime 命令、AI 提示词、无可运行工具、工具失败或步数上限时停止。

# 输入

1. 当前稳定版本：`v0.1.47`。
2. 本轮目标版本：`v0.1.48`。
3. 现有 `agent-step`、Agent 会话、Agent Tool 调用协议和协作任务板。

# 输出

1. 应用层 `execute_agent_steps`。
2. CLI 命令 `agent-steps`。
3. 多步运行停止原因结构。
4. 单元测试覆盖步数上限、缺少外部输入和错误边界。
5. README 和 `Agents.md` 补充命令与约束。

# 计划（Plan）

1. 读取最近记忆、当前状态和 `agent-step` 实现。
2. 在应用层实现受控多步运行报告与停止原因。
3. 新增 CLI 命令和参数解析。
4. 补充单元、边界和错误测试。
5. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
6. 执行格式化、测试、验证、预检和开放错误查询。
7. 提交本轮小版本。

## v0.1.48 最终执行记录

# 任务来源

用户要求继续思考接下来的任务，并写入文档后依次解决；本轮执行队列任务 3。

# 任务描述

新增受控多步运行命令，让 Agent 会话可以连续执行无需外部输入的步骤，并在遇到需要人工或外部程序输入的步骤时停止。

# 输入

1. 当前稳定版本：`v0.1.47`。
2. 本轮目标版本：`v0.1.48`。
3. 近期热记忆来源：`v0.1.47`、`v0.1.46`、`v0.1.45`、`v0.1.44`、`v0.1.43`。

# 输出

1. `AgentStepRunReport`、`AgentStepRunStop` 和 `AgentStepRunError`。
2. `SelfForgeApp::execute_agent_steps`。
3. CLI 命令 `agent-steps`。
4. README 和 `Agents.md` 新增受控多步运行规则。
5. 新增 3 个测试，覆盖步数上限、缺少外部输入和零步数错误。

# 执行记录

1. 已通过 `preflight` 确认 v0.1.47 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认下一步优先实现受控多步运行。
3. 已在应用层复用 `execute_next_agent_step` 实现多步推进。
4. 已让多步执行在会话完成、达到步数上限、缺少输入、无可运行工具或工具失败时停止。
5. 已新增 `agent-steps` CLI，并禁止在多步模式中直接传入 `--tool`、`--prompt` 或 `-- PROGRAM`。
6. 已补充测试，验证连续执行前两个记忆步骤、第三步缺少 Runtime 命令时停止，以及 `--max-steps 0` 错误路径。
7. 已执行 `cargo test`，133 个测试全部通过。
8. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight` 和 `cargo run -- errors --current --open --limit 5`。
9. 已执行 `cargo run -- memory-compact --current --keep 5`，保持热记忆只保留近期 5 条。
10. 已执行 `cargo run -- agent-work-init --current --threads 3 "受控多步运行后续协作"`，协作任务板版本为 `v0.1.48`。

## v0.1.49 任务计划

# 任务来源

用户要求继续推进；后续任务队列的下一项是实现 AI 生成补丁的沙箱化草案流程。

# 任务描述

新增受控 AI 补丁草案入口，让 AI 基于当前状态和近期记忆生成中文补丁计划、代码草案和测试草案，并只把草案写入当前 major workspace 的受控产物目录。

# 输入

1. 当前稳定版本：`v0.1.48`。
2. 本轮目标版本：`v0.1.49`。
3. 近期热记忆、AI Provider、Agent 产物目录和预检结果。

# 输出

1. `workspaces/v0/artifacts/agents/patch-drafts/` 补丁草案目录。
2. `agent-patch-draft [--dry-run] [goal]` 生成或预览草案。
3. `agent-patch-drafts` 查询最近草案。
4. `agent-patch-draft-record RECORD_ID` 读取草案记录。
5. 草案必须包含计划和测试章节，缺失时记录失败并停止。

# 计划（Plan）

1. 读取近期记忆、当前状态、AI 请求和 Agent 审计存储实现。
2. 新增补丁草案存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层预览、执行、校验、查询能力。
4. 新增 CLI 命令和参数解析。
5. 补充成功、错误和脱敏测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检和提交。

## v0.1.49 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 4。

# 任务描述

新增 AI 补丁草案流程，让 AI 在受控产物目录中生成中文 Markdown 补丁草案，并要求草案先包含计划和测试章节。

# 输入

1. 当前稳定版本：`v0.1.48`。
2. 本轮目标版本：`v0.1.49`。
3. 近期热记忆来源：`v0.1.48`、`v0.1.47`、`v0.1.46`、`v0.1.45`、`v0.1.44`。

# 输出

1. 新增 `src/app/agent/patch_draft.rs`。
2. 新增 `AiPatchDraftPreview`、`AiPatchDraftReport` 和 `AiPatchDraftError`。
3. 新增 `agent-patch-draft`、`agent-patch-drafts` 和 `agent-patch-draft-record`。
4. 草案写入 `workspaces/v0/artifacts/agents/patch-drafts/`。
5. 新增 3 个测试，覆盖预览、成功落盘和缺少测试章节的失败记录。

# 执行记录

1. 已通过 `preflight` 确认 v0.1.48 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认下一步优先实现 AI 补丁草案。
3. 已新增 major 级补丁草案存储和索引。
4. 已新增应用层预览、执行、校验、列表和读取能力。
5. 已新增 CLI 命令并补充帮助信息。
6. 已要求 AI 草案必须包含中文内容、计划章节和测试章节，且不得包含 Emoji。
7. 已补充测试，确认草案不保存 API Key 和完整提示词。
8. 已执行 `cargo test ai_patch_draft`，3 个测试全部通过。
9. 已执行 `cargo test`，136 个测试全部通过。
10. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-drafts --current --limit 5`。
11. 已执行 `cargo run -- memory-compact --current --keep 5`，保持热记忆只保留近期 5 条。
12. 已执行 `cargo run -- agent-work-init --current --threads 3 "AI 补丁草案后续协作"`，协作任务板版本为 `v0.1.49`。
13. 已执行 `cargo run -- agent-patch-draft --dry-run "为候选补丁生成受控草案"`，确认补丁草案预览不会写入真实草案文件。

## v0.1.50 任务计划

# 任务来源

用户要求继续推进；后续任务队列的下一项是实现候选补丁差异审计和冲突检查。

# 任务描述

新增 AI 补丁草案审计能力，读取已生成的补丁草案记录，解析草案中的目标写入范围，检查受保护目录、非法路径和协作任务板中的活跃写入范围冲突，并把审计结果写入 major 工作区产物目录。

# 输入

1. 当前稳定版本：`v0.1.49`。
2. 本轮目标版本：`v0.1.50`。
3. 近期热记忆、补丁草案记录、协作任务板和预检结果。

# 输出

1. `workspaces/v0/artifacts/agents/patch-audits/` 补丁审计目录。
2. `agent-patch-audit RECORD_ID` 审计指定补丁草案。
3. `agent-patch-audits` 查询最近审计。
4. `agent-patch-audit-record AUDIT_ID` 读取单条审计记录。
5. 审计必须覆盖通过、受保护路径和活跃写入范围冲突。

# 计划（Plan）

1. 读取近期记忆、当前状态、补丁草案存储和协作任务板实现。
2. 新增补丁审计存储与记录结构，所有记录写入 major workspace 产物目录。
3. 新增草案写入范围解析、路径边界检查和活跃任务冲突检查。
4. 在应用层提供审计、查询和读取能力。
5. 新增 CLI 命令和参数解析。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

## v0.1.50 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 5。

# 任务描述

新增 AI 补丁草案审计能力，解析补丁草案的目标写入范围，并检查受保护路径和协作任务板活跃写入范围冲突。

# 输入

1. 当前稳定版本：`v0.1.49`。
2. 本轮目标版本：`v0.1.50`。
3. 近期热记忆来源：`v0.1.49`、`v0.1.48`、`v0.1.47`、`v0.1.46`、`v0.1.45`。

# 输出

1. 新增 `src/app/agent/patch_audit.rs`。
2. 新增 `AiPatchAuditRecord`、`AiPatchAuditFinding`、`AiPatchAuditStore` 和审计状态。
3. 新增 `agent-patch-audit`、`agent-patch-audits` 和 `agent-patch-audit-record`。
4. 审计写入 `workspaces/v0/artifacts/agents/patch-audits/`。
5. 新增 3 个测试，覆盖通过、受保护路径和活跃写入范围冲突。

# 执行记录

1. 已通过 `preflight` 确认 v0.1.49 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认下一步优先实现补丁差异审计和冲突检查。
3. 已新增 major 级补丁审计存储和索引。
4. 已新增草案 `允许写入范围` 解析、路径规范化和受保护目录检查。
5. 已新增协作任务板活跃领取任务的写入范围冲突检查。
6. 已新增应用层审计、列表和读取能力。
7. 已新增 CLI 命令并补充帮助信息。
8. 已补充测试，确认审计记录可查询，受保护路径会失败，活跃冲突会失败。
9. 已执行 `cargo test ai_patch_audit`，3 个测试全部通过。
10. 已执行 `cargo test`，139 个测试全部通过。
11. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-audits --current --limit 5`。
12. 已执行 `cargo run -- memory-compact --current --keep 5`，保持热记忆只保留近期 5 条。
13. 已执行 `cargo run -- agent-work-init --current --threads 3 "补丁审计后续协作"`，协作任务板版本为 `v0.1.50`。

## v0.1.51 任务计划

# 任务来源

用户要求继续推进；后续任务队列的下一项是实现自我升级执行后的中文总结报告。

# 任务描述

新增 AI 自我升级总结报告能力，在真实自我升级执行完成并写入审计记录后，自动生成中文 Markdown 总结，汇总目标、计划、代码变更、测试、错误、审计记录和下一步建议，并提供查询命令。

# 输入

1. 当前稳定版本：`v0.1.50`。
2. 本轮目标版本：`v0.1.51`。
3. 近期热记忆、自我升级审计记录、Agent 会话和预检结果。

# 输出

1. `workspaces/v0/artifacts/agents/self-upgrade-reports/` 自我升级报告目录。
2. `agent-self-upgrade-report AUDIT_RECORD_ID` 为指定审计记录生成或读取报告。
3. `agent-self-upgrade-reports` 查询最近报告。
4. `agent-self-upgrade-report-record REPORT_ID` 读取单条报告记录。
5. `agent-self-upgrade` 真实成功执行后自动写入总结报告。

# 计划（Plan）

1. 读取近期记忆、当前状态、自我升级审计和会话结构。
2. 新增自我升级总结报告存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层报告生成、查询和读取能力。
4. 在 `agent-self-upgrade` 成功路径自动生成报告。
5. 新增 CLI 命令和参数解析。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

## v0.1.51 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 6。

# 任务描述

新增 AI 自我升级总结报告能力，在真实自我升级执行完成并写入审计记录后，自动生成中文 Markdown 总结，并提供报告生成、查询和读取命令。

# 输入

1. 当前稳定版本：`v0.1.50`。
2. 本轮目标版本：`v0.1.51`。
3. 近期热记忆、自我升级审计记录、Agent 会话和预检结果。

# 输出

1. 新增 `src/app/agent/self_upgrade_summary.rs`。
2. 新增 `AiSelfUpgradeSummaryStore`、`AiSelfUpgradeSummaryRecord` 和总结报告状态。
3. 新增 `agent-self-upgrade-report`、`agent-self-upgrade-reports` 和 `agent-self-upgrade-report-record`。
4. `agent-self-upgrade` 真实成功执行后自动写入总结报告。
5. 总结报告写入 `workspaces/v0/artifacts/agents/self-upgrade-reports/`。

# 执行记录

1. 已通过 `cargo run -- preflight` 确认 v0.1.50 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认下一步优先实现自我升级执行后的中文总结报告。
3. 已新增 major 级自我升级总结报告存储和索引。
4. 已新增报告生成逻辑，汇总目标、计划、代码变更、测试、错误、审计记录和下一步建议。
5. 已新增应用层报告生成、列表和读取能力。
6. 已将 `agent-self-upgrade` 成功路径接入自动报告生成。
7. 已新增 CLI 命令并补充帮助信息。
8. 已补充测试，确认成功和失败审计都能生成报告，且报告不保存 API Key 或完整提示词。
9. 已执行 `cargo test ai_self_upgrade`，6 个测试全部通过。
10. 已执行 `cargo test`，139 个测试全部通过。
11. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-self-upgrade-reports --current --limit 5`。
12. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 40 条。
13. 已执行 `cargo run -- agent-work-init --current --threads 3 "自我升级总结报告后续协作"`，协作任务板版本为 `v0.1.51`。
14. 记忆压缩后已再次执行 `cargo run -- validate` 和 `cargo run -- preflight`，均通过。
15. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-self-upgrade-reports --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.52 任务计划

# 任务来源

用户要求继续推进；后续任务队列的下一项是实现补丁草案审计通过后的受控应用预演流程。

# 任务描述

新增 AI 补丁应用预演能力，读取已通过的补丁审计记录和对应草案，只在 major 工作区产物目录中生成可审计预演记录和 Markdown 变更摘要，不直接修改源码、Runtime、Supervisor、状态文件或真实候选代码。

# 输入

1. 当前稳定版本：`v0.1.51`。
2. 本轮目标版本：`v0.1.52`。
3. 近期热记忆、补丁草案记录、补丁审计记录和预检结果。

# 输出

1. `workspaces/v0/artifacts/agents/patch-previews/` 补丁应用预演目录。
2. `agent-patch-preview AUDIT_RECORD_ID` 为通过审计的补丁生成预演记录。
3. `agent-patch-previews` 查询最近预演。
4. `agent-patch-preview-record PREVIEW_ID` 读取单条预演记录。
5. 预演记录必须关联审计记录、草案记录、目标版本、写入范围、代码块数量和 Markdown 预演文件。

# 计划（Plan）

1. 读取近期记忆、当前状态、补丁草案和补丁审计实现。
2. 新增补丁应用预演存储模块，所有记录写入 major workspace 产物目录。
3. 新增草案代码块提取和预演 Markdown 生成逻辑。
4. 新增应用层预演、查询和读取能力。
5. 新增 CLI 命令和参数解析。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

## v0.1.52 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 7。

# 任务描述

新增补丁草案审计通过后的受控应用预演流程，只在 major 工作区产物目录中生成可审计预演记录和中文 Markdown 预演文件，不直接修改真实源码。

# 输入

1. 当前稳定版本：`v0.1.51`。
2. 本轮目标版本：`v0.1.52`。
3. 近期热记忆、补丁草案记录、补丁审计记录和预检结果。

# 输出

1. 新增 `src/app/agent/patch_preview.rs`。
2. 新增 `AiPatchPreviewStore`、`AiPatchPreviewRecord`、`AiPatchPreviewChange` 和预演状态。
3. 新增 `agent-patch-preview`、`agent-patch-previews` 和 `agent-patch-preview-record`。
4. 预演写入 `workspaces/v0/artifacts/agents/patch-previews/`。
5. 新增 3 个测试，覆盖成功预演、审计失败阻断和缺少代码块阻断。

# 执行记录

1. 已通过 `cargo run -- preflight` 确认 v0.1.51 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认下一步优先实现补丁应用预演。
3. 已新增 major 级补丁预演存储和索引。
4. 已新增草案 `代码草案` 章节代码块提取。
5. 已新增应用层预演、列表和读取能力。
6. 已新增 CLI 命令并补充帮助信息。
7. 已补充测试，确认预演不会修改真实源码，审计失败会写入阻断记录，缺少代码块会写入阻断记录。
8. 已执行 `cargo test ai_patch_preview`，3 个测试全部通过。
9. 首次执行 `cargo test` 发现 14 个版本生命周期断言滞后一位，已修复。
10. 再次执行 `cargo test`，142 个测试全部通过。
11. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-previews --current --limit 5`。
12. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 41 条。
13. 已执行 `cargo run -- agent-work-init --current --threads 3 "补丁应用预演后续协作"`，协作任务板版本为 `v0.1.52`。
14. 记忆压缩后已再次执行 `cargo run -- validate` 和 `cargo run -- preflight`，均通过。
15. 最终复验 `cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `git diff --check`，均通过。

## v0.1.53 任务计划

# 任务来源

用户要求继续推进；后续任务队列的下一项是实现预演通过后的候选工作区受控应用流程。

# 任务描述

新增 AI 补丁候选应用能力，读取已通过的补丁预演记录，准备或复用候选版本，将预演变更写入候选工作区的 `source/patch-applications/` 镜像目录，并生成结构化应用记录。该流程只生成候选应用镜像，不直接覆盖仓库源码。

# 输入

1. 当前稳定版本：`v0.1.52`。
2. 本轮目标版本：`v0.1.53`。
3. 近期热记忆、补丁预演记录、补丁审计记录、补丁草案记录和预检结果。

# 输出

1. `workspaces/v0/artifacts/agents/patch-applications/` 补丁候选应用记录目录。
2. `workspaces/v0/source/patch-applications/` 补丁候选应用镜像目录。
3. `agent-patch-apply PREVIEW_RECORD_ID` 应用指定预演到候选工作区镜像。
4. `agent-patch-applications` 查询最近应用记录。
5. `agent-patch-application-record APPLICATION_ID` 读取单条应用记录。

# 计划（Plan）

1. 读取近期记忆、当前状态、补丁预演和候选版本流程实现。
2. 新增补丁候选应用存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层候选准备、预演状态校验、路径边界检查和镜像写入能力。
4. 新增应用后候选布局验证和回滚提示记录。
5. 新增 CLI 命令和参数解析。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

## v0.1.53 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 8。

# 任务描述

新增预演通过后的候选工作区受控应用流程，准备或复用候选版本，将预演变更写入候选工作区镜像目录，并生成结构化候选应用记录。

# 输入

1. 当前稳定版本：`v0.1.52`。
2. 本轮目标版本：`v0.1.53`。
3. 近期热记忆、补丁预演记录、补丁审计记录、补丁草案记录和预检结果。

# 输出

1. 新增 `src/app/agent/patch_application.rs`。
2. 新增 `AiPatchApplicationStore`、`AiPatchApplicationRecord`、`AiPatchApplicationFile` 和候选应用状态。
3. 新增 `agent-patch-apply`、`agent-patch-applications` 和 `agent-patch-application-record`。
4. 候选应用记录写入 `workspaces/v0/artifacts/agents/patch-applications/`。
5. 候选应用镜像写入 `workspaces/v0/source/patch-applications/`。
6. 新增 4 个测试，覆盖成功应用、失败预演阻断、非法路径阻断和复用已有候选。

# 执行记录

1. 已通过 `cargo run -- preflight` 确认 v0.1.52 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认下一步优先实现候选工作区受控应用。
3. 已新增 major 级补丁候选应用存储和索引。
4. 已新增候选版本准备或复用逻辑。
5. 已新增预演状态校验、代码块读取、路径边界检查和镜像写入。
6. 已新增应用后候选布局验证、验证命令记录和回滚提示。
7. 已新增 CLI 命令并补充帮助信息。
8. 已补充测试，确认应用会写入镜像目录且不覆盖仓库源码，失败预演会阻断，非法路径会写入阻断记录，已有候选会复用。
9. 已执行 `cargo test ai_patch_apply`，4 个测试全部通过。
10. 首次执行 `cargo test` 发现 14 个版本生命周期断言滞后一位，已修复；新增非法路径阻断时曾出现变量遮蔽编译错误，已修复。
11. 再次执行 `cargo test`，146 个测试全部通过。
12. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-applications --current --limit 5`。
13. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 42 条。
14. 已执行 `cargo run -- agent-work-init --current --threads 3 "补丁候选应用后续协作"`，协作任务板版本为 `v0.1.53`。
15. 记忆压缩后已再次执行 `cargo run -- validate` 和 `cargo run -- preflight`，均通过。
16. 最终复验 `cargo test`、`cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-applications --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.57 任务计划

# 任务来源

用户要求继续推进；近期记忆建议下一步实现源码覆盖执行成功后的版本提升衔接记录。

# 任务描述

新增 AI 补丁源码覆盖提升衔接能力，读取已成功执行且验证通过的源码覆盖执行记录，生成一条可审计的提升衔接记录，把源码覆盖执行、验证结果、建议提交信息和下一候选版本线索串成同一条自我升级链路。该能力只写入 major 工作区产物目录，不直接生成候选版本、不修改状态文件、不执行真实提交。

# 输入

1. 当前稳定版本：`v0.1.56`。
2. 本轮目标版本：`v0.1.57`。
3. 近期热记忆、源码覆盖执行记录、验证结果、状态文件和预检结果。

# 输出

1. `workspaces/v0/artifacts/agents/patch-source-promotions/` 源码覆盖提升衔接记录目录。
2. `agent-patch-source-promotion SOURCE_EXECUTION_ID` 为成功源码覆盖执行生成提升衔接记录。
3. `agent-patch-source-promotions` 查询最近提升衔接记录。
4. `agent-patch-source-promotion-record PROMOTION_ID` 读取单条提升衔接记录。
5. 衔接记录必须包含源码覆盖执行编号、覆盖计划链路、验证摘要、建议提交信息、下一候选版本和阻断原因。

# 计划（Plan）

1. 读取近期记忆、当前状态、源码覆盖执行记录实现和版本状态机实现。
2. 新增源码覆盖提升衔接存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层提升衔接用例，成功执行生成就绪记录，失败或缺少验证证据生成阻断记录。
4. 新增 CLI 命令和输出摘要。
5. 补充单元、边界和错误测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检和提交。

## v0.1.57 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 12。

# 任务描述

新增源码覆盖执行成功后的版本提升衔接记录，让系统可以在源码覆盖验证通过后生成独立审计记录，关联源码覆盖执行、验证摘要、建议提交信息和下一候选版本线索。

# 输入

1. 当前稳定版本：`v0.1.56`。
2. 本轮目标版本：`v0.1.57`。
3. 近期热记忆、源码覆盖执行记录、验证结果、状态文件和预检结果。

# 输出

1. 新增 `src/app/agent/patch_source_promotion.rs`。
2. 新增 `AiPatchSourcePromotionStore`、`AiPatchSourcePromotionRecord`、`AiPatchSourcePromotionSummary` 和提升衔接状态。
3. 新增 `agent-patch-source-promotion`、`agent-patch-source-promotions` 和 `agent-patch-source-promotion-record`。
4. 提升衔接记录写入 `workspaces/v0/artifacts/agents/patch-source-promotions/`。
5. 新增 3 个测试，覆盖成功衔接、回滚执行阻断和缺少验证运行阻断。

# 执行记录

1. 已通过 `cargo run -- preflight` 确认 v0.1.56 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认下一步优先实现源码覆盖执行后的版本提升衔接记录。
3. 已新增 major 级源码覆盖提升衔接存储和索引。
4. 已新增应用层衔接用例，成功执行生成就绪记录，失败或证据不足生成阻断记录。
5. 已新增建议提交信息、下一候选版本和下一候选目标字段。
6. 已新增 CLI 命令并补充帮助信息。
7. 已补充测试，确认成功衔接会保留验证证据和提交建议，回滚执行会阻断，缺少验证运行会阻断。
8. 首次执行 `cargo test ai_patch_source_promotion` 发现 1 个测试断言问题，已修复为检查衔接命令前后状态不变。
9. 修复后 `cargo test ai_patch_source_promotion` 通过，3 个测试全部通过。
10. 已执行 `cargo test`，161 个测试全部通过。
11. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-source-promotions --current --limit 5`。
12. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 46 条。
13. 已执行 `cargo run -- agent-work-init --current --threads 3 "源码覆盖提升衔接后续协作"`，协作任务板版本为 `v0.1.57`。
14. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-promotions --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.58 任务计划

# 任务来源

用户要求继续推进；近期记忆建议下一步实现基于源码覆盖提升衔接记录的候选生成准备流程。

# 任务描述

新增 AI 补丁源码覆盖候选准备能力，读取已就绪的源码覆盖提升衔接记录，检查当前稳定版本、开放错误、下一候选版本和候选状态；若没有候选版本则调用受控版本状态机生成候选，若目标候选已存在则复用并验证布局。该能力必须生成独立审计记录，把提升衔接记录、状态变更、候选验证结果和下一步动作串起来。

# 输入

1. 当前稳定版本：`v0.1.57`。
2. 本轮目标版本：`v0.1.58`。
3. 近期热记忆、源码覆盖提升衔接记录、状态文件、开放错误查询和版本状态机。

# 输出

1. `workspaces/v0/artifacts/agents/patch-source-candidates/` 源码覆盖候选准备记录目录。
2. `agent-patch-source-candidate PROMOTION_ID` 基于已就绪提升衔接记录准备或复用下一候选版本。
3. `agent-patch-source-candidates` 查询最近候选准备记录。
4. `agent-patch-source-candidate-record CANDIDATE_RECORD_ID` 读取单条候选准备记录。
5. 候选准备记录必须包含提升衔接编号、下一候选版本、状态变更、候选验证摘要、阻断原因和后续验证命令。

# 计划（Plan）

1. 读取近期记忆、当前状态、提升衔接记录实现、错误归档和版本状态机实现。
2. 新增源码覆盖候选准备存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层候选准备用例：就绪记录生成候选或复用候选，阻断记录、开放错误或版本不一致生成阻断记录。
4. 新增 CLI 命令和输出摘要。
5. 补充单元、边界和错误测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检和提交。

## v0.1.58 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 13。

# 任务描述

新增基于源码覆盖提升衔接记录的候选生成准备流程，让系统可以在衔接记录就绪后受控生成或复用下一候选版本，并写入独立审计记录。

# 输入

1. 当前稳定版本：`v0.1.57`。
2. 本轮目标版本：`v0.1.58`。
3. 近期热记忆、源码覆盖提升衔接记录、状态文件、开放错误查询和版本状态机。

# 输出

1. 新增 `src/app/agent/patch_source_candidate.rs`。
2. 新增 `AiPatchSourceCandidateStore`、`AiPatchSourceCandidateRecord`、`AiPatchSourceCandidateSummary` 和候选准备状态。
3. 新增 `agent-patch-source-candidate`、`agent-patch-source-candidates` 和 `agent-patch-source-candidate-record`。
4. 候选准备记录写入 `workspaces/v0/artifacts/agents/patch-source-candidates/`。
5. 新增 4 个测试，覆盖成功准备、复用已有候选、未就绪衔接阻断和开放错误阻断。

# 执行记录

1. 已通过 `cargo run -- preflight` 确认 v0.1.57 可继续进化，开放错误为 0。
2. 已读取近期记忆，确认下一步优先实现源码覆盖提升衔接后的候选准备流程。
3. 已新增 major 级源码覆盖候选准备存储和索引。
4. 已新增应用层候选准备用例，读取提升衔接记录并检查稳定版本、下一候选版本、开放错误和候选状态。
5. 已接入受控版本状态机：无候选时生成下一候选，目标候选已存在且匹配时复用并验证布局。
6. 已新增阻断记录逻辑：提升衔接未就绪、存在开放错误、版本不一致或已有不同候选时写入已阻断记录。
7. 已新增 CLI 命令并补充帮助信息。
8. 已补充测试，确认候选准备会更新状态文件，复用候选不会改动状态，阻断路径会保留原因。
9. 已执行 `cargo check`，通过。
10. 已执行 `cargo test ai_patch_source_candidate`，4 个测试全部通过。
11. 已执行 `cargo test`，165 个测试全部通过。
12. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-source-candidates --current --limit 5`。
13. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 47 条。
14. 已执行 `cargo run -- agent-work-init --current --threads 3 "源码覆盖候选准备后续协作"`，协作任务板版本为 `v0.1.58`。
15. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-candidates --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.59 任务计划

# 任务来源

用户要求继续推进；近期记忆建议下一步实现基于源码覆盖候选准备记录的自动候选验证与 cycle 衔接。

# 任务描述

新增 AI 补丁源码覆盖候选 cycle 能力，读取已准备或已复用的源码覆盖候选准备记录，执行前置检查和预检，确认候选版本与状态文件一致后调用受控 `cycle` 流程，并把候选准备记录、预检摘要、cycle 结果、提升或回滚后的状态和阻断原因写入同一条审计记录。

# 输入

1. 当前稳定版本：`v0.1.58`。
2. 本轮目标版本：`v0.1.59`。
3. 近期热记忆、源码覆盖候选准备记录、状态文件、预检结果和版本 cycle 状态机。

# 输出

1. `workspaces/v0/artifacts/agents/patch-source-cycles/` 源码覆盖候选 cycle 记录目录。
2. `agent-patch-source-cycle CANDIDATE_RECORD_ID` 基于候选准备记录执行预检和受控 cycle。
3. `agent-patch-source-cycles` 查询最近 cycle 记录。
4. `agent-patch-source-cycle-record CYCLE_RECORD_ID` 读取单条 cycle 记录。
5. cycle 记录必须包含候选准备编号、候选版本、预检摘要、cycle 结果、状态变化、失败原因和后续命令。

# 计划（Plan）

1. 读取近期记忆、当前状态、候选准备记录实现、预检和 cycle 状态机实现。
2. 新增源码覆盖候选 cycle 存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层 cycle 衔接用例：候选准备就绪且状态一致时执行预检和 cycle，失败或不一致时生成阻断记录。
4. 新增 CLI 命令和输出摘要。
5. 补充单元、边界和错误测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检和提交。

## v0.1.59 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 14。

# 任务描述

实现基于源码覆盖候选准备记录的自动候选验证与 cycle 衔接，把候选准备、预检、cycle 结果、提升或回滚状态和阻断原因写入同一条后续审计链路。

# 输入

1. 当前稳定版本：`v0.1.58`。
2. 本轮目标版本：`v0.1.59`。
3. 近期热记忆、源码覆盖候选准备记录、状态文件、预检结果和版本 cycle 状态机。

# 输出

1. 新增 `src/app/agent/patch_source_cycle.rs`。
2. 新增 `AiPatchSourceCycleStore`、`AiPatchSourceCycleRecord`、`AiPatchSourceCycleSummary`、`AiPatchSourceCycleStatus` 和 `AiPatchSourceCycleResult`。
3. 新增 `agent-patch-source-cycle CANDIDATE_RECORD_ID`。
4. 新增 `agent-patch-source-cycles`。
5. 新增 `agent-patch-source-cycle-record CYCLE_RECORD_ID`。
6. 更新中文规则、README、版本、错误和记忆归档。

# 执行结果

1. 已读取近期记忆、当前状态和开放错误，确认本轮目标为源码覆盖候选 cycle 衔接。
2. 已新增 major 级源码覆盖候选 cycle 存储和索引。
3. 已新增应用层 cycle 衔接用例，执行候选准备状态检查、状态文件一致性检查和 `preflight`。
4. 已接入受控 `run_candidate_cycle`：候选通过时记录提升，候选失败时记录回滚原因，前置不满足时写入已阻断记录。
5. 已新增 CLI 命令并补充帮助信息。
6. 已补充测试，确认正常候选会提升，未准备候选会阻断，状态候选版本不一致会阻断。
7. 已执行 `cargo check`，通过。
8. 已执行 `cargo test ai_patch_source_cycle`，3 个测试全部通过。
9. 已执行 `cargo test`，168 个测试全部通过。
10. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-source-cycles --current --limit 5`。
11. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 48 条。
12. 已执行 `cargo run -- agent-work-init --current --threads 3 "源码覆盖候选 cycle 后续协作"`，协作任务板版本为 `v0.1.59`。
13. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-cycles --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.60 任务计划

# 任务来源

用户要求继续推进；近期记忆建议下一步基于源码覆盖 cycle 记录自动生成后续归档和自我升级总结。

# 任务描述

新增 AI 补丁源码覆盖 cycle 后续总结能力，读取已生成的源码覆盖候选 cycle 记录，汇总提升或回滚后的状态、预检摘要、失败原因、记忆压缩建议和下一任务建议，并写入独立中文审计记录。该能力只生成归档和后续动作建议，不直接修改状态文件、不执行新的候选生成、不自动提交代码。

# 输入

1. 当前稳定版本：`v0.1.59`。
2. 本轮目标版本：`v0.1.60`。
3. 近期热记忆、源码覆盖 cycle 记录、状态文件和记忆压缩策略。

# 输出

1. `workspaces/v0/artifacts/agents/patch-source-cycle-summaries/` 源码覆盖 cycle 后续总结目录。
2. `agent-patch-source-cycle-summary CYCLE_RECORD_ID` 基于 cycle 记录生成后续总结。
3. `agent-patch-source-cycle-summaries` 查询最近后续总结记录。
4. `agent-patch-source-cycle-summary-record SUMMARY_RECORD_ID` 读取单条后续总结记录。
5. 总结记录必须包含 cycle 编号、cycle 状态、提升或回滚后的状态、记忆压缩建议、下一任务建议和后续命令。

# 计划（Plan）

1. 读取近期记忆、当前状态、cycle 记录实现和已有总结记录模式。
2. 新增源码覆盖 cycle 后续总结存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层总结用例：根据 cycle 状态生成成功、回滚或阻断的下一步建议。
4. 新增 CLI 命令和输出摘要。
5. 补充单元、边界和错误测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检和提交。

## v0.1.60 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 15。

# 任务描述

实现基于源码覆盖 cycle 记录的自动归档和后续自我升级总结，将提升或回滚后的状态、记忆压缩建议和下一任务建议连接起来。

# 输入

1. 当前稳定版本：`v0.1.59`。
2. 本轮目标版本：`v0.1.60`。
3. 近期热记忆、源码覆盖 cycle 记录、状态文件和记忆压缩策略。

# 输出

1. 新增 `src/app/agent/patch_source_cycle_summary.rs`。
2. 新增 `AiPatchSourceCycleFollowUpStore`、`AiPatchSourceCycleFollowUpRecord`、`AiPatchSourceCycleFollowUpSummary` 和后续总结状态。
3. 新增 `agent-patch-source-cycle-summary CYCLE_RECORD_ID`。
4. 新增 `agent-patch-source-cycle-summaries`。
5. 新增 `agent-patch-source-cycle-summary-record SUMMARY_RECORD_ID`。
6. 更新中文规则、README、版本、错误和记忆归档。

# 执行结果

1. 已读取近期记忆、当前状态和开放错误，确认本轮目标为源码覆盖 cycle 后续总结。
2. 已新增 major 级源码覆盖 cycle 后续总结存储和索引。
3. 已新增应用层后续总结用例，按 cycle 状态生成提升、回滚或阻断对应的下一步建议。
4. 已新增 CLI 命令并补充帮助信息。
5. 已补充测试，确认提升后总结建议记忆压缩，阻断后总结建议修复，缺失 cycle 记录会返回明确错误。
6. 已执行 `cargo check`，通过。
7. 已执行 `cargo test ai_patch_source_cycle_summary`，3 个测试全部通过。
8. 已执行 `cargo test`，171 个测试全部通过。
9. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-source-cycle-summaries --current --limit 5`。
10. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 49 条。
11. 已执行 `cargo run -- agent-work-init --current --threads 3 "源码覆盖 cycle 总结后续协作"`，协作任务板版本为 `v0.1.60`。
12. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-cycle-summaries --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.61 任务计划

# 任务来源

用户要求优化；近期记忆建议基于 cycle 后续总结自动生成下一任务草案，并把草案纳入审计流程，避免 AI 直接把建议变成未验证的真实修改。

# 任务描述

新增 AI 补丁源码覆盖下一任务草案能力，读取已生成的源码覆盖 cycle 后续总结，将其中的下一目标和下一任务转换为独立、可审计、可查询的任务草案。该能力只生成草案归档和后续命令建议，不直接修改状态文件、不创建候选版本、不写入真实任务队列、不自动提交代码。

# 输入

1. 当前稳定版本：`v0.1.60`。
2. 本轮目标版本：`v0.1.61`。
3. 近期热记忆、源码覆盖 cycle 后续总结记录、版本规则和现有 Agent 归档结构。

# 输出

1. `workspaces/v0/artifacts/agents/patch-source-task-drafts/` 源码覆盖下一任务草案目录。
2. `agent-patch-source-task-draft SUMMARY_RECORD_ID` 基于后续总结生成任务草案。
3. `agent-patch-source-task-drafts` 查询最近任务草案记录。
4. `agent-patch-source-task-draft-record TASK_DRAFT_ID` 读取单条任务草案记录。
5. 草案记录必须包含来源总结、建议目标、建议任务、目标版本、验收检查、审计要求和后续命令。

# 计划（Plan）

1. 读取近期记忆、当前状态、cycle 后续总结实现和 CLI 命令模式。
2. 新增源码覆盖下一任务草案存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层任务草案用例：根据总结状态生成普通下一轮任务或修复任务草案。
4. 新增 CLI 命令和输出摘要。
5. 补充单元、边界和错误测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检和提交。

## v0.1.61 最终执行记录

# 任务来源

用户要求优化；本轮执行队列任务 16。

# 任务描述

实现基于源码覆盖 cycle 后续总结的下一任务草案归档，把总结中的下一目标和下一任务转换为可审计草案，并保持状态文件、候选版本和真实任务队列不变。

# 输入

1. 当前稳定版本：`v0.1.60`。
2. 本轮目标版本：`v0.1.61`。
3. 近期热记忆、源码覆盖 cycle 后续总结记录、版本规则和现有 Agent 归档结构。

# 输出

1. 新增 `src/app/agent/patch_source_task_draft.rs`。
2. 新增 `AiPatchSourceTaskDraftStore`、`AiPatchSourceTaskDraftRecord`、`AiPatchSourceTaskDraftSummary` 和任务草案状态。
3. 新增 `agent-patch-source-task-draft SUMMARY_RECORD_ID`。
4. 新增 `agent-patch-source-task-drafts`。
5. 新增 `agent-patch-source-task-draft-record TASK_DRAFT_ID`。
6. 更新中文规则、README、版本、错误和记忆归档。

# 执行结果

1. 已读取近期记忆、当前状态和开放错误，确认本轮目标为源码覆盖下一任务草案。
2. 已新增 major 级源码覆盖下一任务草案存储和索引。
3. 已新增应用层任务草案用例，按来源总结状态生成继续进化或修复任务草案。
4. 已新增 CLI 命令并补充帮助信息。
5. 已补充测试，确认提升总结会生成下一轮 patch 草案，阻断总结会生成修复草案，缺失总结记录会返回明确错误。
6. 已执行 `cargo check`，通过。
7. 已执行 `cargo test ai_patch_source_task_draft`，3 个测试全部通过。
8. 已执行 `cargo test`，174 个测试全部通过。
9. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-source-task-drafts --current --limit 5`。
10. 验证中发现并修复 `state/state.json` UTF-8 BOM 解析问题，修复后 `preflight`、任务草案查询和开放错误查询均通过。
11. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 50 条。
12. 已执行 `cargo run -- agent-work-init --current --threads 3 "源码覆盖任务草案后续协作"`，协作任务板版本为 `v0.1.61`。
13. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-task-drafts --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.62 任务计划

# 任务来源

用户要求继续推进；近期记忆建议实现基于任务草案的审计闸门，把任务草案验证为可执行计划后，再允许进入 `agent-patch-draft` 或后续源码覆盖流程。

# 任务描述

新增 AI 补丁源码覆盖任务草案审计能力，读取已生成的下一任务草案，检查草案状态、审计要求、目标版本、标题描述、验收检查和后续命令是否满足进入补丁草案流程的最低条件。审计结果写入独立中文归档，只提供批准或阻断结论和建议命令，不直接调用 AI、不生成候选版本、不修改状态文件、不写入真实任务队列。

# 输入

1. 当前稳定版本：`v0.1.61`。
2. 本轮目标版本：`v0.1.62`。
3. 近期热记忆、源码覆盖下一任务草案记录、版本规则和现有 Agent 归档结构。

# 输出

1. `workspaces/v0/artifacts/agents/patch-source-task-audits/` 源码覆盖任务草案审计目录。
2. `agent-patch-source-task-audit TASK_DRAFT_ID` 基于任务草案生成审计记录。
3. `agent-patch-source-task-audits` 查询最近任务草案审计记录。
4. `agent-patch-source-task-audit-record TASK_AUDIT_ID` 读取单条任务草案审计记录。
5. 审计记录必须包含来源草案、审计状态、发现项、建议目标版本、批准后的后续命令和阻断原因。

# 计划（Plan）

1. 读取近期记忆、当前状态、任务草案实现和 CLI 命令模式。
2. 新增源码覆盖任务草案审计存储模块，所有记录写入 major workspace 产物目录。
3. 新增应用层审计用例：检查草案完整性、状态、审计要求、验收检查和后续命令。
4. 新增 CLI 命令和输出摘要。
5. 补充单元、边界和错误测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检和提交。

## v0.1.62 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 17。

# 任务描述

实现基于源码覆盖下一任务草案的审计闸门，读取任务草案并生成批准或阻断记录，让后续补丁草案流程只能基于已审计的任务意图继续推进。

# 输入

1. 当前稳定版本：`v0.1.61`。
2. 本轮目标版本：`v0.1.62`。
3. 近期热记忆、源码覆盖下一任务草案记录、版本规则和现有 Agent 归档结构。

# 输出

1. 新增 `src/app/agent/patch_source_task_audit.rs`。
2. 新增 `AiPatchSourceTaskAuditStore`、`AiPatchSourceTaskAuditRecord`、`AiPatchSourceTaskAuditSummary`、`AiPatchSourceTaskAuditFinding` 和任务草案审计状态。
3. 新增 `agent-patch-source-task-audit TASK_DRAFT_ID`。
4. 新增 `agent-patch-source-task-audits`。
5. 新增 `agent-patch-source-task-audit-record TASK_AUDIT_ID`。
6. 更新中文规则、README、版本、错误和记忆归档。

# 执行结果

1. 已读取近期记忆、当前状态和开放错误，确认本轮目标为源码覆盖任务草案审计闸门。
2. 已新增 major 级源码覆盖任务草案审计存储和索引。
3. 已新增应用层任务草案审计用例，检查草案状态、审计要求、目标版本、标题描述、来源目标、来源任务、验收检查和后续命令。
4. 已新增 CLI 命令并补充帮助信息。
5. 已补充测试，确认完整草案会批准，异常草案会阻断，缺失任务草案记录会返回明确错误。
6. 已执行 `cargo check`，通过。
7. 已执行 `cargo test ai_patch_source_task_audit`，3 个测试全部通过。
8. 已执行 `cargo test`，177 个测试全部通过。
9. 已执行 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-source-task-audits --current --limit 5`。
10. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 51 条。
11. 已执行 `cargo run -- agent-work-init --current --threads 3 "任务草案审计后续协作"`，协作任务板版本为 `v0.1.62`。
12. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-task-audits --current --limit 5`、`cargo run -- memory-insights --current --limit 5` 和 `git diff --check`，均通过。

## v0.1.63 任务计划

# 任务来源

用户要求继续推进；近期记忆建议让 `agent-patch-draft` 能显式接收已批准的任务草案审计记录，自动使用批准目标生成补丁草案，并在缺少审计时拒绝来自任务草案链路的继续执行。

# 任务描述

新增基于任务草案审计记录的补丁草案入口，读取已批准的源码覆盖任务草案审计记录，使用其中的批准目标作为补丁草案目标，并复用现有 `ai_patch_draft` 流程生成草案。若审计记录缺失、未批准或目标为空，必须停止并返回明确错误。该能力只扩展受控入口，不绕过现有预检、记忆读取、AI 请求、草案存储和后续审计流程。

# 输入

1. 当前稳定版本：`v0.1.62`。
2. 本轮目标版本：`v0.1.63`。
3. 近期热记忆、源码覆盖任务草案审计记录、现有 `agent-patch-draft` 流程和 AI 补丁草案存储结构。

# 输出

1. `agent-patch-draft --from-task-audit TASK_AUDIT_ID` 支持从已批准任务草案审计生成补丁草案。
2. 应用层新增读取审计记录并调用现有补丁草案流程的受控入口。
3. 未批准审计、缺失审计或空批准目标必须返回明确错误。
4. README、`Agents.md`、任务、错误、版本和记忆归档同步更新。

# 计划（Plan）

1. 读取近期记忆、当前状态、任务草案审计实现和补丁草案流程。
2. 新增应用层 `ai_patch_draft_from_task_audit`，只接受已批准审计记录。
3. 更新 CLI 参数解析，让 `agent-patch-draft --from-task-audit TASK_AUDIT_ID` 使用批准目标。
4. 补充单元、边界和错误测试。
5. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
6. 执行格式化、测试、验证、预检和提交。

## v0.1.63 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 18。

# 任务描述

让 `agent-patch-draft` 显式接收已批准的任务草案审计记录，自动使用审计记录中的批准目标生成补丁草案，并拒绝缺失、未批准或空批准目标的审计记录。

# 输入

1. 当前稳定版本：`v0.1.62`。
2. 本轮目标版本：`v0.1.63`。
3. 近期热记忆、源码覆盖任务草案审计记录、现有 `agent-patch-draft` 流程和 AI 补丁草案存储结构。

# 输出

1. `agent-patch-draft --from-task-audit TASK_AUDIT_ID` 支持从已批准任务草案审计生成补丁草案。
2. 应用层新增 `ai_patch_draft_preview_from_task_audit` 和 `ai_patch_draft_from_task_audit`。
3. 未批准审计、缺失审计或空批准目标会返回明确错误。
4. 更新中文规则、README、版本、错误和记忆归档。

# 执行结果

1. 已读取近期记忆、当前状态和开放错误，确认本轮目标为审计驱动补丁草案入口。
2. 已新增应用层从任务草案审计读取批准目标并复用现有补丁草案流程。
3. 已新增 CLI 参数 `--from-task-audit TASK_AUDIT_ID`，并禁止该参数与直接目标文本同时使用。
4. 已补充测试，确认已批准审计会构建补丁草案预览，阻断审计会拒绝，缺失审计会返回明确错误。
5. 已执行 `cargo check`，通过。
6. 已执行 `cargo test ai_patch_draft_preview_from_task_audit`，3 个测试全部通过。
7. 已执行缺失任务草案审计编号的 CLI 负向验证，确认命令会按预期返回明确错误并停止。
8. 已执行 `cargo fmt --check`，通过。
9. 已执行 `cargo test`，180 个测试全部通过。
10. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
11. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.63`，未解决错误 0，可进化。
12. 已执行 `cargo run -- errors --current --open --limit 5`，返回无开放错误。
13. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 52 条。
14. 已执行 `cargo run -- agent-work-init --current --threads 3 "审计驱动补丁草案后续协作"`，协作任务板复用到 `v0.1.63`。
15. 已执行 `cargo run -- memory-insights --current --limit 5`，通过。
16. 已执行 `git diff --check`，通过。

## v0.1.64 任务计划

# 任务来源

用户要求检查未完成任务、完成并收尾；近期任务队列中唯一明确未完成项是让补丁草案记录显式保存来源任务草案审计编号。

# 任务描述

补丁草案从 `--from-task-audit` 链路生成时，必须把来源任务草案审计编号写入补丁草案预览、成功记录、失败记录和列表摘要，并在 CLI 查询输出中显示该编号。普通直接目标生成的补丁草案保持向后兼容，不强制包含来源编号。

# 输入

1. 当前稳定版本：`v0.1.63`。
2. 本轮目标版本：`v0.1.64`。
3. 现有任务草案审计入口、补丁草案记录结构、补丁草案列表和单条记录查询输出。

# 输出

1. 补丁草案记录新增可选来源任务草案审计编号。
2. 补丁草案摘要新增可选来源任务草案审计编号。
3. `agent-patch-draft --from-task-audit` 生成的预览和记录都能反查来源审计编号。
4. CLI 预览、完成、列表和单条记录输出显示来源审计编号。
5. 中文规则、任务、错误、版本和记忆归档同步更新。

# 计划（Plan）

1. 巡检当前状态、开放错误、任务队列和近期记忆。
2. 扩展补丁草案预览、记录和摘要结构，新增可选来源任务草案审计编号。
3. 在 `--from-task-audit` 链路中写入来源审计编号，普通目标链路保持为空。
4. 更新 CLI 输出，显式显示来源审计编号。
5. 补充记录、摘要和读取接口的来源编号持久化测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检、记忆压缩和提交。

## v0.1.64 最终执行记录

# 任务来源

用户要求检查未完成任务、完成并收尾；本轮执行队列任务 19。

# 任务描述

让补丁草案记录显式保存来源任务草案审计编号，便于从补丁草案反查任务草案、审计记录和后续源码覆盖链路。

# 输入

1. 当前稳定版本：`v0.1.63`。
2. 本轮目标版本：`v0.1.64`。
3. 近期热记忆、补丁草案记录结构、任务草案审计记录和 CLI 查询输出。

# 输出

1. `AiPatchDraftPreview`、`AiPatchDraftRecord` 和 `AiPatchDraftSummary` 新增可选来源任务草案审计编号。
2. `agent-patch-draft --from-task-audit TASK_AUDIT_ID` 会把来源审计编号写入草案记录。
3. `agent-patch-drafts` 和 `agent-patch-draft-record` 会显示来源审计编号。
4. 普通直接目标补丁草案记录继续兼容旧数据。

# 执行结果

1. 已巡检 `git status --short`，工作区起始状态干净。
2. 已执行 `cargo run -- preflight`，当前版本 `v0.1.63` 可进化，未解决错误 0。
3. 已执行 `cargo run -- errors --current --open --limit 10`，无开放错误。
4. 已执行 `cargo run -- memory-insights --current --limit 5`，确认任务 19 为当前最明确未完成任务。
5. 已扩展补丁草案预览、记录和摘要结构，新增 `source_task_audit_id`。
6. 已让 `--from-task-audit` 链路把已批准审计编号写入预览和最终记录。
7. 已更新 CLI 预览、完成、列表和单条记录输出，显示来源审计编号。
8. 已补充 `ai_patch_draft_from_task_audit_persists_source_audit_id` 测试，验证记录、摘要和读取接口均保留来源编号。
9. 已执行 `cargo check`，通过。
10. 已执行 `cargo test ai_patch_draft_from_task_audit_persists_source_audit_id`，通过。
11. 已执行 `cargo test ai_patch_draft_preview_from_task_audit`，3 个测试全部通过。
12. 已执行 `cargo fmt --check`，通过。
13. 已执行 `cargo test`，181 个测试全部通过。
14. 已执行 `git diff --check`，通过。
15. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
16. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.64`，未解决错误 0，可进化。
17. 已执行 `cargo run -- errors --current --open --limit 10`，返回无开放错误。
18. 已执行 `cargo run -- agent-patch-drafts --current --limit 5`，通过，当前版本暂无真实补丁草案记录。
19. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 53 条。
20. 已执行 `cargo run -- agent-work-init --current --threads 3 "补丁草案来源追踪收尾协作"`，协作任务板复用到 `v0.1.64`。

## v0.1.65 任务计划

# 任务来源

用户要求继续推进；近期记忆建议把来源任务草案审计编号继续贯通到补丁审计、预演、应用和源码覆盖记录。

# 任务描述

在补丁审计阶段继承补丁草案记录中的来源任务草案审计编号。若补丁草案记录包含 `source_task_audit_id`，则补丁审计记录、摘要和 CLI 查询输出都必须保留并显示该编号；普通直接目标或旧记录继续兼容。

# 输入

1. 当前稳定版本：`v0.1.64`。
2. 本轮目标版本：`v0.1.65`。
3. 补丁草案记录、补丁审计记录、补丁审计列表和单条查询输出。

# 输出

1. `AiPatchAuditRecord` 和 `AiPatchAuditSummary` 新增可选来源任务草案审计编号。
2. `agent-patch-audit` 生成记录时继承补丁草案中的来源任务草案审计编号。
3. `agent-patch-audits` 和 `agent-patch-audit-record` 显示来源任务草案审计编号。
4. 中文规则、任务、错误、版本和记忆归档同步更新。

# 计划（Plan）

1. 巡检当前状态、开放错误和近期记忆。
2. 扩展补丁审计记录和摘要结构，新增可选来源任务草案审计编号。
3. 在应用层审计流程中从补丁草案记录继承来源编号。
4. 更新 CLI 输出，显示来源审计编号。
5. 补充补丁审计来源编号持久化测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检、记忆压缩和提交。

## v0.1.65 最终执行记录

# 任务来源

用户要求继续推进；本轮执行队列任务 20。

# 任务描述

让补丁审计记录继承补丁草案中的来源任务草案审计编号，便于从审计结果继续反查任务草案审计链路。

# 输入

1. 当前稳定版本：`v0.1.64`。
2. 本轮目标版本：`v0.1.65`。
3. 近期热记忆、补丁草案来源追踪字段、补丁审计记录和 CLI 查询输出。

# 输出

1. `AiPatchAuditRecord` 和 `AiPatchAuditSummary` 新增可选 `source_task_audit_id`。
2. `agent-patch-audit` 会从补丁草案记录继承来源任务草案审计编号。
3. `agent-patch-audits` 和 `agent-patch-audit-record` 会显示来源任务草案审计编号。
4. 普通旧审计记录继续兼容。

# 执行结果

1. 已巡检 `git status --short`，工作区起始状态干净。
2. 已执行 `cargo run -- preflight`，当前版本 `v0.1.64` 可进化，未解决错误 0。
3. 已执行 `cargo run -- errors --current --open --limit 10`，无开放错误。
4. 已执行 `cargo run -- memory-insights --current --limit 5`，确认本轮目标为继续贯通来源追踪。
5. 已扩展补丁审计记录和摘要结构，新增 `source_task_audit_id`。
6. 已让补丁审计流程从补丁草案记录继承来源审计编号。
7. 已更新补丁审计 CLI 完成、列表和单条记录输出，显示来源审计编号。
8. 已补充 `ai_patch_audit_persists_source_task_audit_id` 测试，验证记录、摘要和读取接口均保留来源编号。
9. 已执行 `cargo check`，通过。
10. 已执行 `cargo test ai_patch_audit_persists_source_task_audit_id`，通过。
11. 已执行 `cargo test ai_patch_audit`，4 个测试全部通过。
12. 已执行 `cargo fmt --check`，通过。
13. 已执行 `cargo test`，182 个测试全部通过。
14. 已执行 `git diff --check`，通过。
15. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
16. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
17. 已执行 `cargo run -- errors --current --open --limit 10`，返回无开放错误。
18. 已执行 `cargo run -- agent-patch-audits --current --limit 5`，通过，当前版本暂无真实补丁审计记录。
19. 已执行 `cargo run -- memory-compact --current --keep 5`，热记忆保留 5 条，冷归档共 54 条。
20. 已执行 `cargo run -- agent-work-init --current --threads 3 "补丁审计来源追踪后续协作"`，协作任务板复用到 `v0.1.65`。

## v0.1.65 结构拆分执行记录

# 任务来源

用户要求继续拆分文件，避免单个文件职责过重。

# 任务描述

先处理当前最明显的结构负担：`src/lib.rs` 同时承载公共导出和大量测试，导致库入口职责不清。将内联测试迁移到独立测试模块，并按主题拆分测试文件，降低单文件体量和后续维护成本。

# 输入

1. 当前稳定版本：`v0.1.65`。
2. 最近 5 条热记忆和结构体检结果。
3. 当前测试集中在 `src/lib.rs` 的现状。

# 输出

1. `src/lib.rs` 保留模块声明、公共导出和测试模块入口。
2. 新增 `src/tests/mod.rs` 保存共享测试辅助函数和测试子模块声明。
3. 新增 `src/tests/agent_core.rs`、`src/tests/ai_provider.rs`、`src/tests/app_loop.rs`、`src/tests/layout_version.rs`、`src/tests/memory.rs`、`src/tests/patch_draft_flow.rs`、`src/tests/patch_source_flow.rs` 和 `src/tests/runtime_error.rs`。

# 计划（Plan）

1. 读取近期记忆并确认拆分目标。
2. 将 `src/lib.rs` 的内联测试模块迁出。
3. 按职责继续拆分测试模块，避免新增单个超大测试文件。
4. 运行格式化、完整测试、布局验证、前置检查和开放错误查询。
5. 写入任务、记忆、错误和版本归档。

# 执行结果

1. 已将 `src/lib.rs` 从 6000 多行缩减为 83 行。
2. 已把测试按职责拆分到 `src/tests/` 下的多个文件。
3. 已将共享测试辅助函数保留在 `src/tests/mod.rs`，供各测试子模块复用。
4. 已执行 `cargo fmt`，通过。
5. 已执行 `cargo test`，182 个测试全部通过。
6. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
7. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
8. 已执行 `cargo run -- errors --current --open`，返回无开放错误。

## v0.1.65 补丁范围模块拆分执行记录

# 任务来源

用户要求继续推进文件拆分，避免单个文件职责过重；近期记忆建议优先拆分 `src/app/minimal_loop.rs`。

# 任务描述

将 `src/app/minimal_loop.rs` 中的补丁写入范围解析、受保护路径判断和协作任务冲突审计逻辑迁移到独立子模块，降低应用闭环文件体量，保持补丁审计行为不变。

# 输入

1. 当前稳定版本：`v0.1.65`。
2. 最近 5 条热记忆和经验摘要。
3. `src/app/minimal_loop.rs` 中的补丁范围审计辅助逻辑。

# 输出

1. 新增 `src/app/minimal_loop/patch_scope.rs`。
2. `src/app/minimal_loop.rs` 通过子模块导入补丁范围审计函数。
3. 补丁预演代码块解析继续复用标题解析函数。

# 计划（Plan）

1. 读取近期记忆、经验摘要和前置检查结果。
2. 定位补丁范围审计辅助函数和调用点。
3. 新建补丁范围子模块并迁移低耦合逻辑。
4. 运行格式化和完整测试，修复迁移产生的可见性问题。
5. 执行布局验证、前置检查、开放错误查询和差异检查。
6. 写入任务、记忆、错误和版本归档。

# 执行结果

1. 已执行 `cargo run -- memory-context --current --limit 5`，读取 5 条近期记忆。
2. 已执行 `cargo run -- memory-insights --current --limit 5`，确认下一步继续拆分 `src/app/minimal_loop.rs`。
3. 已执行 `cargo run -- preflight`，当前版本 `v0.1.65` 可进化，未解决错误 0。
4. 已新增 `src/app/minimal_loop/patch_scope.rs`，迁入补丁范围解析、路径规范化、受保护路径判断和协作冲突审计逻辑。
5. 已更新 `src/app/minimal_loop.rs`，保留应用流程调用并导入子模块函数。
6. 首次 `cargo test` 发现标题解析函数仍被补丁预演代码块解析复用，已改为子模块内 `pub(super)` 函数并复验通过。
7. 已执行 `cargo test`，182 个测试全部通过。
8. 已执行 `cargo fmt --check`，通过。
9. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
10. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
11. 已执行 `cargo run -- errors --current --open`，返回无开放错误。
12. 已执行 `git diff --check`，通过。

## v0.1.65 协作队列模型拆分执行记录

# 任务来源

用户要求继续推进文件拆分，避免单个文件职责过重；本轮在协作任务板中领取 `coord-002-application` 应用层任务。

# 任务描述

拆分 `src/app/agent/coordination.rs`，把协作队列的数据模型、报告类型、错误类型和显示实现迁移到独立模型子模块，让主文件更专注于队列初始化、领取、完成、释放和冲突检测流程。

# 输入

1. 当前稳定版本：`v0.1.65`。
2. 最近 5 条热记忆和经验摘要。
3. 协作任务板 `coord-002-application` 的写入范围。
4. `src/app/agent/coordination.rs` 当前 1034 行的结构负担。

# 输出

1. 新增 `src/app/agent/coordination/model.rs`。
2. `src/app/agent/coordination.rs` 重新导出模型类型并保留队列行为实现。
3. 协作任务板记录本轮领取和完成事件。

# 计划（Plan）

1. 读取近期记忆、经验摘要、开放错误和前置检查结果。
2. 查询协作任务板并领取应用层写入范围。
3. 将协作队列模型、报告和错误类型迁入 `model` 子模块。
4. 修复迁移后的可见性和导出问题。
5. 执行完整测试、格式检查、布局验证、前置检查、开放错误查询和差异检查。
6. 写入任务、记忆、错误和版本归档。

# 执行结果

1. 已执行 `cargo run -- memory-context --current --limit 5`，读取 5 条近期记忆。
2. 已执行 `cargo run -- memory-insights --current --limit 5`，确认继续选择低耦合职责块拆分。
3. 已执行 `cargo run -- preflight`，当前版本 `v0.1.65` 可进化，未解决错误 0。
4. 已执行 `cargo run -- errors --current --open`，返回无开放错误。
5. 已执行 `cargo run -- agent-work-status --current`，确认当前应用层任务可领取。
6. 已执行 `cargo run -- agent-work-claim --current --worker ai-1 --agent builder --lease-seconds 7200`，领取 `coord-002-application`。
7. 已新增 `src/app/agent/coordination/model.rs`，迁入协作队列状态、记录、报告、错误和显示实现。
8. 已更新 `src/app/agent/coordination.rs`，通过 `model` 子模块重新导出类型，主文件保留队列操作流程。
9. 首次 `cargo test` 发现 `AgentWorkCoordinator` 和默认租约常量可见性不足，已使用包内可见字段和常量修复。
10. 已执行 `cargo test`，182 个测试全部通过。
11. 已执行 `cargo run -- agent-work-complete coord-002-application --current --worker ai-1 --summary ...`，任务板记录完成。
12. 已执行 `cargo fmt --check`，通过。
13. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
14. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
15. 已执行 `cargo run -- errors --current --open`，返回无开放错误。
16. 已执行 `cargo run -- memory-context --current --limit 1`，确认最新热记忆为本轮最终记忆。
17. 已执行 `git diff --check`，通过。

## v0.1.65 协作 CLI 收口执行记录

# 任务来源

用户要求继续完成项目；协作任务板中剩余 `coord-003-cli`、`coord-004-tests` 和 `coord-005-review-archive` 三个收口任务。

# 任务描述

完成协作命令入口的使用面收口，补齐 README 中协作任务板 CLI 常用命令；复核现有协作队列测试覆盖，并把本轮结果写入 major 聚合归档。

# 输入

1. 当前稳定版本：`v0.1.65`。
2. 最近 5 条热记忆和经验摘要。
3. 协作任务板 `coord-003-cli`、`coord-004-tests` 和 `coord-005-review-archive`。
4. `src/main.rs` 已有协作命令入口和帮助文本。
5. `src/tests/agent_core.rs` 已有协作队列测试。

# 输出

1. `README.md` 常用命令补齐 `agent-work-init`、`agent-work-claim`、`agent-work-complete`、`agent-work-release` 和 `agent-work-reap`。
2. 协作任务板记录 CLI、测试和归档任务的领取与完成。
3. `forge/tasks/v0.md`、`forge/memory/v0.md`、`forge/errors/v0.md` 和 `forge/versions/v0.md` 追加中文记录。

# 计划（Plan）

1. 读取近期记忆、经验摘要、前置检查和开放错误。
2. 领取并完成 `coord-003-cli`，补齐 README 协作命令。
3. 领取并完成 `coord-004-tests`，复核协作队列测试覆盖并执行测试。
4. 领取并完成 `coord-005-review-archive`，追加中文归档。
5. 执行完整验证、前置检查、开放错误查询和差异检查。
6. 使用规范提交信息提交本轮收口变更。

# 执行结果

1. 已执行 `cargo run -- memory-context --current --limit 5`，读取 5 条近期记忆。
2. 已执行 `cargo run -- memory-insights --current --limit 5`，确认继续小步收口。
3. 已执行 `cargo run -- preflight`，当前版本 `v0.1.65` 可进化，未解决错误 0。
4. 已执行 `cargo run -- errors --current --open`，返回无开放错误。
5. 已领取 `coord-003-cli`，补齐 README 协作任务板命令清单。
6. 已执行 `cargo run -- validate` 和 `cargo run -- agent-work-status --current`，通过并确认任务板状态可读。
7. 已完成 `coord-003-cli`。
8. 已领取 `coord-004-tests`，复核现有协作队列测试覆盖。
9. 已执行 `cargo test agent_work`，12 个协作队列测试全部通过。
10. 已执行 `cargo test`，182 个测试全部通过。
11. 已完成 `coord-004-tests`。
12. 已领取 `coord-005-review-archive`，追加本轮中文归档。
13. 已完成 `coord-005-review-archive`，任务板 5 个协作任务全部完成。
14. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
15. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
16. 已执行 `cargo run -- errors --current --open`，返回无开放错误。
17. 已执行 `cargo run -- memory-context --current --limit 1`，确认最新热记忆为本轮最终记忆。
18. 已执行 `cargo run -- agent-work-status --current`，确认待领取 0、已领取 0、已完成 5、已阻断 0。
19. 已执行 `git diff --check`，通过。

## v0.1.65 协作任务板重开能力执行记录

# 任务来源

用户要求继续推进；近期记忆建议初始化新的结构拆分任务板，但现有 `agent-work-init` 在旧任务板全部完成后仍只会复用旧队列，无法开启新的可领取任务。

# 任务描述

为协作任务板补充显式重开能力：`agent-work-init --reset-completed` 只允许在现有队列所有任务都已完成时重建当前 major 的协作任务板，未完成、待领取或已领取队列必须拒绝重开。

# 输入

1. 当前稳定版本：`v0.1.65`。
2. 已完成的当前 major 协作任务板。
3. `src/app/agent/coordination.rs`、`src/app/agent/coordination/model.rs`、`src/app/minimal_loop.rs`、`src/main.rs` 和 `README.md`。

# 输出

1. 应用层新增已完成队列重开路径。
2. CLI 新增 `agent-work-init --reset-completed` 参数。
3. README 和帮助文本同步展示新参数。
4. 未完成队列重开会返回明确错误。

# 计划（Plan）

1. 确认旧任务板全部完成且普通领取已无可领取任务。
2. 重新领取应用层任务，补充完成队列重开能力。
3. 重新领取 CLI 任务，接入 `--reset-completed` 参数和文档。
4. 验证未完成队列无法重开，运行协作队列定向测试和布局验证。
5. 重新领取归档任务，写入中文任务、记忆、错误和版本记录。
6. 执行完整测试、前置检查、开放错误查询和差异检查。

# 执行结果

1. 已确认旧任务板 5 个任务全部完成，`agent-work-claim` 返回无可领取任务。
2. 已确认未带新参数的 `agent-work-init` 只复用旧任务板。
3. 已重新领取 `coord-002-application`，新增 `initialize_with_reset_completed` 应用层入口。
4. 已新增 `AgentWorkError::QueueNotCompleted`，用于拒绝未完成队列重开。
5. 已重新领取 `coord-003-cli`，新增 `--reset-completed` 参数解析、帮助文本和 README 示例。
6. 已执行 `cargo run -- agent-work-init --current --threads 3 --reset-completed "拆分 src/main.rs CLI 结构"`，在队列尚未全部完成时返回明确拒绝错误。
7. 已执行 `cargo test agent_work`，12 个协作队列测试全部通过。
8. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
9. 已重新领取 `coord-005-review-archive`，追加本轮中文归档。
10. 已执行 `cargo fmt --check`，通过。
11. 已执行 `cargo test`，182 个测试全部通过。
12. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
13. 已执行 `cargo run -- errors --current --open`，返回无开放错误。
14. 已执行 `git diff --check`，通过。

## v0.1.65 分支协作流程规范执行记录

# 任务来源

用户要求从现在开始每次操作都创建新分支，在分支内完成修改和提交，再合并更新到 `master`，合并完成后删除任务分支，以支持多个 AI 同步修改并降低冲突风险。

# 任务描述

补充 `Agents.md` 的企业级分支协作规则：明确禁止直接在 `master` 上写入，要求独立任务分支、合并前后验证、合并后删除分支，并把多 AI 并行场景约束为独立分支、独立工作树和协作任务板共同管理。

# 输入

1. 当前稳定版本：`v0.1.65`。
2. 当前分支：`codex/branch-workflow-rules`。
3. 最近 5 条热记忆和经验摘要。
4. 协作任务板目标：规范分支协作流程。
5. 用户对 `master` 合并和分支删除的明确要求。

# 输出

1. `Agents.md` 增加分支隔离最高原则。
2. 开发流程新增创建分支、分支提交、合并回 `master`、`master` 再验证和删除分支步骤。
3. 多 AI 协作规则新增独立任务分支和独立物理工作树要求。
4. 新增“分支与合并规范”章节，定义标准命令、验证要求、冲突处理和分支删除条件。
5. `forge/tasks/v0.md`、`forge/memory/v0.md`、`forge/errors/v0.md` 和 `forge/versions/v0.md` 追加本轮中文归档。

# 计划（Plan）

1. 在 `master` 干净状态下创建 `codex/branch-workflow-rules` 任务分支。
2. 读取近期记忆、经验摘要、开放错误和前置检查结果。
3. 重开协作任务板并领取架构任务。
4. 更新 `Agents.md` 的开发流程、协作任务和分支合并规范。
5. 执行格式、测试、布局验证、前置检查和开放错误查询。
6. 领取归档任务并追加 major 聚合归档。
7. 在任务分支提交，通过验证后合并回 `master` 并删除任务分支。

# 执行结果

1. 已执行 `git status --short --branch`，确认 `master` 干净。
2. 已执行 `cargo run -- preflight`，当前版本 `v0.1.65` 可进化，未解决错误 0。
3. 已执行 `cargo run -- errors --current --open`，返回无开放错误。
4. 已执行 `cargo run -- memory-context --current --limit 5`，读取 5 条近期记忆。
5. 已执行 `cargo run -- memory-insights --current --limit 5`，提取成功经验、失败风险、优化建议和可复用经验。
6. 已创建任务分支 `codex/branch-workflow-rules`。
7. 已执行 `cargo run -- agent-work-init --current --threads 3 --reset-completed "规范分支协作流程"`，重开任务板。
8. 已领取并完成 `coord-001-architecture`，更新 `Agents.md`。
9. 已明确 `coord-002-application` 和 `coord-003-cli` 本轮无需源码或 CLI 变更。
10. 已领取并完成 `coord-004-tests`，执行完整验证。
11. 已执行 `cargo fmt --check`，通过。
12. 已执行 `git diff --check`，通过。
13. 已执行 `cargo test`，182 个测试全部通过。
14. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
15. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
16. 已执行 `cargo run -- errors --current --open`，返回无开放错误。

# 后续提交

计划提交信息：`docs(agents): v0.1.65 规范分支协作流程`。

## v0.1.65 分支流程只读检查命令执行记录

# 任务来源

延续分支协作流程规范，近期记忆建议增加只读检查命令，用于提示当前分支、任务编号和协作任务板状态是否一致，减少 AI 执行分支流程时的顺序错误。

# 任务描述

新增 `branch-check` 命令。该命令只读取 Git 状态、基础分支、当前分支、协作任务板、worker 和任务领取关系，输出阻断项、警告和是否允许继续写入，不修改文件、不切换分支、不领取任务、不合并分支。

# 输入

1. 当前稳定版本：`v0.1.65`。
2. 当前任务分支：`codex/branch-check-command`。
3. 最近 5 条热记忆和经验摘要。
4. 协作任务板目标：实现分支流程只读检查命令。
5. 已有分支协作规范和协作任务板能力。

# 输出

1. `Agents.md` 补充 `branch-check` 命令规则。
2. `SelfForgeApp::branch_check` 应用层只读检查入口。
3. CLI 新增 `branch-check [--current|--candidate|--version VERSION] [--worker ID] [--task TASK_ID] [--base BRANCH]`。
4. README 常用命令补充 `branch-check` 示例。
5. 新增分支检查定向测试。

# 计划（Plan）

1. 从最新 `master` 创建任务分支。
2. 读取近期记忆、经验摘要、前置检查和开放错误。
3. 重开协作任务板并领取架构任务。
4. 更新 `Agents.md` 的命令规则。
5. 领取应用层任务，新增只读检查报告结构和应用入口。
6. 领取 CLI 任务，接入命令解析和中文输出。
7. 领取测试任务，补充分支检查定向测试。
8. 执行格式检查、完整测试、布局验证、前置检查和开放错误查询。
9. 领取归档任务，写入中文任务、记忆、错误和版本归档。
10. 提交、合并回 `master`、删除分支并推送。

# 执行结果

1. 已执行 `git pull --ff-only`，确认 `master` 已是最新。
2. 已执行 `cargo run -- preflight`，当前版本 `v0.1.65` 可进化，未解决错误 0。
3. 已执行 `cargo run -- errors --current --open`，返回无开放错误。
4. 已执行 `cargo run -- memory-context --current --limit 5`，读取 5 条近期记忆。
5. 已执行 `cargo run -- memory-insights --current --limit 5`，提取 15 条可复用经验。
6. 已创建任务分支 `codex/branch-check-command`。
7. 已执行 `cargo run -- agent-work-init --current --threads 3 --reset-completed "实现分支流程只读检查命令"`，重开任务板。
8. 已领取并完成 `coord-001-architecture`，更新 `Agents.md`。
9. 已领取并完成 `coord-002-application`，新增应用层分支检查能力。
10. 已领取并完成 `coord-003-cli`，新增 CLI 命令和 README 示例。
11. 已领取并完成 `coord-004-tests`，新增 2 个分支检查测试。
12. 已执行 `cargo test branch_check`，2 个定向测试通过。
13. 已执行 `cargo fmt --check`，通过。
14. 已执行 `git diff --check`，通过。
15. 已执行 `cargo test`，184 个测试全部通过。
16. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
17. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
18. 已执行 `cargo run -- errors --current --open`，返回无开放错误。

# 后续提交

计划提交信息：`feat(cli): v0.1.65 增加分支流程检查命令`。

## v0.1.65 分支命名建议执行记录

# 任务来源

近期记忆建议继续完善分支检查入口：通过 `branch-check --suggest` 把任务编号、目标和 worker 信息转换成稳定分支名建议，减少 AI 分支命名不一致。

# 任务描述

为 `branch-check` 增加 `--suggest` 参数。该参数只读输出建议分支名和来源，优先使用显式任务编号，其次使用当前 worker 已领取任务，最后使用任务板中下一条待处理任务。命令禁止创建分支或修改任务板。

# 输入

1. 当前稳定版本：`v0.1.65`。
2. 当前任务分支：`codex/branch-check-suggest`。
3. 最近 5 条热记忆和经验摘要。
4. 协作任务板目标：增加分支命名建议。
5. 已有 `branch-check` 只读检查入口。

# 输出

1. `Agents.md` 补充 `branch-check --suggest` 规则。
2. `BranchCheckReport` 新增建议分支和来源字段。
3. `SelfForgeApp::branch_check` 支持只读生成建议分支。
4. CLI 新增 `--suggest` 参数并输出“建议分支”。
5. README 示例更新为 `branch-check --suggest`。
6. 新增分支建议定向测试。

# 计划（Plan）

1. 从最新 `master` 创建任务分支。
2. 读取近期记忆、经验摘要、前置检查和开放错误。
3. 重开协作任务板并领取架构任务。
4. 更新 `Agents.md` 的 `--suggest` 只读规则。
5. 领取应用层任务，新增分支建议字段和生成逻辑。
6. 领取 CLI 任务，接入 `--suggest` 参数和中文输出。
7. 领取测试任务，补充分支建议测试。
8. 执行定向测试、完整测试、布局验证、前置检查和开放错误查询。
9. 领取归档任务，写入中文任务、记忆、错误和版本归档。
10. 提交、合并回 `master`、删除分支并推送。

# 执行结果

1. 已执行 `git pull --ff-only`，确认 `master` 已是最新。
2. 已执行 `cargo run -- preflight`，当前版本 `v0.1.65` 可进化，未解决错误 0。
3. 已执行 `cargo run -- errors --current --open`，返回无开放错误。
4. 已执行 `cargo run -- memory-context --current --limit 5`，读取 5 条近期记忆。
5. 已执行 `cargo run -- memory-insights --current --limit 5`，提取 15 条可复用经验。
6. 已创建任务分支 `codex/branch-check-suggest`。
7. 已重开协作任务板，目标为“增加分支命名建议”。
8. 已领取并完成 `coord-001-architecture`，更新 `Agents.md`。
9. 已领取并完成 `coord-002-application`，新增应用层分支建议逻辑。
10. 已领取并完成 `coord-003-cli`，新增 CLI 参数和 README 示例。
11. 已领取并完成 `coord-004-tests`，新增 1 个分支建议测试。
12. 已执行 `cargo test branch_check`，3 个定向测试全部通过。
13. 已执行 `cargo run -- branch-check --suggest --worker ai-1 --task coord-002-application`，输出建议分支 `codex/coord-002-application`。
14. 已执行 `cargo fmt --check`，通过。
15. 已执行 `git diff --check`，通过。
16. 已执行 `cargo test`，185 个测试全部通过。
17. 已执行 `cargo run -- validate`，通过，检查 30 个路径。
18. 已执行 `cargo run -- preflight`，通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
19. 已执行 `cargo run -- errors --current --open`，返回无开放错误。

# 后续提交

计划提交信息：`feat(cli): v0.1.65 增加分支命名建议`。

## v0.1.65 分阶段 PR 流程本地阶段执行记录

# 任务来源

用户要求将开发流程调整为企业级分阶段本地提交模式：同一相关任务组在一个分支内多轮微调和多次本地提交，只有任务组完全收束后才提升小版本、统一推送一次并创建 Pull Request。

# 任务描述

本阶段在本地分支 `codex/coord-009-batched-pr-flow` 中完善 AI 任务流程规则，并补充任务板治理能力，避免每次对话都触发 GitHub Actions，避免旧协作任务误导后续 AI。

# 输入

1. 当前稳定版本：`v0.1.65`。
2. 当前本地分支：`codex/coord-009-batched-pr-flow`。
3. 关联 Issue：`https://github.com/xjuunn/SelfForge/issues/1`。
4. 任务板文件：`workspaces/v0/artifacts/agents/coordination/work-queue.json`。
5. 用户确认的新约束：未提升版本时本地提交不携带版本号。

# 输出

1. `Agents.md` 明确多轮对话和多次本地提交留在同一任务组分支。
2. `forge/architecture/agent-rules/git-pr.md` 明确最终 PR 才对应版本提升。
3. `forge/architecture/agent-rules/testing-output.md` 明确阶段性回复必须说明未 push、未创建 PR。
4. 新增 `agent-work-compact --keep-events N`，用于压缩任务板长提示词和旧事件。
5. 新增 `agent-work-block TASK_ID --reason TEXT`，用于阻断过期旧任务。
6. 任务板旧待办 `coord-002-application`、`coord-003-cli`、`coord-004-tests`、`coord-005-review-archive` 已阻断。

# 计划（Plan）

1. 沿用当前本地任务分支，不推送远端。
2. 修正文档规则，区分阶段性本地提交和最终版本提升提交。
3. 为任务板新增受控压缩能力，减少上下文噪声。
4. 为任务板新增受控阻断能力，避免直接删除旧任务。
5. 使用阻断命令处理旧待办。
6. 压缩任务板并保留审计摘要。
7. 执行完整本地验证。
8. 本地提交阶段性成果，不提升版本。

# 执行结果

1. 已创建并沿用分支 `codex/coord-009-batched-pr-flow`。
2. 已本地提交 `docs(agents): 澄清分阶段 PR 与提交规则`。
3. 已本地提交 `feat(agent): 增加任务板压缩命令`。
4. 已本地提交 `feat(agent): 增加任务板阻断命令`。
5. 已执行 `agent-work-compact --keep-events 20`，任务板事件保留 21 条，已完成任务长提示词已压缩。
6. 已执行 `agent-work-block` 阻断 4 个旧默认待办任务。
7. 当前任务板状态为待领取 0、已领取 0、已完成 8、已阻断 4。
8. 本阶段未 push、未创建 PR、未提升版本。

# 验证结果

1. `cargo test agent_work_compact` 通过。
2. `cargo test agent_work_block` 通过。
3. `git diff --check` 通过。
4. `cargo fmt --check` 通过。
5. `cargo test` 通过，189 个测试全部通过。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，未解决错误 0，可进化。
8. `cargo run -- errors --current --open` 返回无开放错误。

# 后续任务

等待用户确认任务组是否完全收束。确认后再提升小版本、统一 push 一次并创建 Pull Request，PR 正文关联 `Refs #1`。

## v0.1.66 分阶段 PR 流程最终收束执行记录

# 任务来源

用户确认继续完成当前任务组，并要求相关任务全部完成后统一 push、创建 Pull Request、关联 Issue #1，合并后自动删除远程分支。

# 任务描述

将本地分阶段 PR 流程成果从 `v0.1.65` 提升到 `v0.1.66`，完成最终归档、验证、提交、统一 push、创建 PR、等待检查、合并和远程分支清理。

# 输入

1. 当前本地分支：`codex/coord-009-batched-pr-flow`。
2. 当前稳定版本：`v0.1.65`。
3. 目标版本：`v0.1.66`。
4. 关联 Issue：`https://github.com/xjuunn/SelfForge/issues/1`。
5. 协作任务：`coord-014-finalize-pr`。

# 输出

1. 版本线索提升到 `v0.1.66`。
2. v0 major 聚合归档补充最终收束记录。
3. 最终提交信息携带 `v0.1.66`。
4. 仅在最终收束阶段统一 push 一次并创建 Pull Request。
5. PR 正文关联 `Refs #1` 和完整 Issue 链接。

# 计划（Plan）

1. 追加并领取最终 PR 收束任务。
2. 提升 patch 版本并更新状态文件。
3. 补充任务、记忆、错误和版本聚合归档。
4. 执行完整本地验证。
5. 提交最终版本、统一 push、创建 PR、等待检查、合并并删除远程分支。

# 执行记录

1. 已追加并领取 `coord-014-finalize-pr`。
2. 已将 `Cargo.toml` 和 `Cargo.lock` 从 `0.1.65` 更新到 `0.1.66`。
3. 已将 `README.md`、`src/lib.rs` 和 `state/state.json` 从 `v0.1.65` 更新到 `v0.1.66`。
4. 计划最终提交信息：`chore(release): v0.1.66 收束分阶段 PR 流程`。

# 验证结果

1. `git diff --check` 通过。
2. `cargo fmt --check` 通过。
3. `cargo test` 首次发现测试夹具仍期望旧版本；同步更新 `src/tests/` 中的版本期望后，189 个测试全部通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.66`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open` 返回无开放错误。

## v0.1.66 终态任务板重开修复执行记录

# 任务来源

新一轮协作开始时，`agent-work-init --reset-completed` 因旧队列包含已阻断任务而拒绝重开。旧队列没有待领取或已领取任务，属于已完成和已阻断混合的终态队列，应允许安全重开下一轮协作。

# 任务描述

修复协作任务板重开判断，使仅包含已完成和已阻断任务的终态队列可以通过 `--reset-completed` 重开，同时继续拒绝存在待领取或已领取任务的活跃队列。

# 输入

1. 当前分支：`codex/coord-015-terminal-queue-reset`。
2. 当前版本：`v0.1.66`。
3. 协作任务：`coord-015-terminal-queue-reset`。
4. 复现命令：`cargo run -- agent-work-init --current --threads 3 --reset-completed "允许终态任务板重开下一轮协作"`。

# 输出

1. `AgentWorkCoordinator` 的重开判断接受 `Completed` 和 `Blocked` 两种终态。
2. 新增测试覆盖终态队列可重开、活跃已领取队列不可重开。
3. README 和协作规则补充任务板重开说明。
4. 本阶段不提升版本、不 push、不创建 PR。

# 计划（Plan）

1. 领取终态任务板重开修复任务。
2. 将队列重开判断从“全部已完成”调整为“全部终态”。
3. 增加终态重开和活跃任务拒绝测试。
4. 补充中文文档和 v0 聚合归档。
5. 执行本地验证并创建阶段提交。

# 验证结果

1. `cargo test agent_work_reset_completed` 通过，2 个定向测试全部通过。
2. `git diff --check` 通过。
3. `cargo fmt --check` 首次提示新增测试需要格式化；已执行 `cargo fmt` 后复验通过。
4. `cargo test` 通过，191 个测试全部通过。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.66`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open` 返回无开放错误。
