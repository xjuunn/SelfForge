# 任务来源

语义化版本规则迁移。

# 任务描述

创建 SelfForge 的 `v0.1.0` 稳定基线，用于替代旧 `v1` 作为当前运行版本标识。

# 输入

- 历史版本：v1、v2
- 用户要求：使用 `v0.1.1` 形式的版本规则

# 输出

- workspaces/v0.1.0
- forge/memory/v0.1.0.md
- forge/tasks/v0.1.0.md
- forge/errors/v0.1.0
- forge/versions/v0.1.0.md

# 计划（Plan）

1. 创建 `v0.1.0` 基线目录。
2. 保留旧 `v1/v2` 历史归档。
3. 由 `v0.1.1` 继续生成候选版本。
