# v0 历史记忆冷归档

# 记录规则

- 本文件保存从热记忆文件迁出的完整历史记忆，按 major 聚合。
- 默认计划、Agent 和 `memory-context` 不读取本文件，避免无关历史占用上下文。
- 只有审计、追溯、问题复盘或人工指定时才读取本文件。

## 冷归档索引

- 完整归档记忆：48 条。
- 最新归档记忆：v0.1.54。
- 最早归档记忆：v0.1.7。

## v0.1.54

# 版本信息
- 版本号：v0.1.54
- 时间：unix:1777639360
- 父版本：v0.1.53

# 目标

实现补丁候选应用后的验证执行记录，把格式化、测试、系统验证和预检命令的执行结果关联到同一条候选应用记录。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 扩展候选应用记录结构，保存验证状态和命令执行明细。
4. 新增候选应用验证执行流程。
5. 新增 CLI 命令。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.53 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已为补丁候选应用记录新增验证状态、验证时间和命令执行明细。
4. 已新增 `agent-patch-verify`，按白名单命令执行验证并回写同一条应用记录。
5. 已保证验证命令不使用隐式 shell 包装，未知命令会在执行前失败并写入失败验证明细。
6. 已补充成功、失败、阻断跳过和未知命令四条测试路径。
7. 已将当前版本线索提升为 `v0.1.54`。

# 代码变更

1. `src/app/agent/patch_application.rs` 扩展验证状态、验证执行记录和记录更新能力。
2. `src/app/minimal_loop.rs` 新增补丁候选应用验证用例、命令白名单、执行器和 Markdown 回写逻辑。
3. `src/main.rs` 新增 `agent-patch-verify` 命令、参数解析和验证结果输出。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出验证相关类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.54`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test ai_patch_verify` 通过，4 个测试全部成功。
3. `cargo test` 通过，150 个测试全部成功。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 43 条。
6. `cargo run -- agent-work-init --current --threads 3 "补丁候选验证后续协作"` 通过，协作任务板版本为 `v0.1.54`。
7. 记忆压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。
8. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-applications --current --limit 5` 和 `git diff --check` 均通过。

# 错误总结

本轮暂无未解决错误。开发中曾出现一次新增字段初始化遗漏，已补齐默认验证状态后通过定向测试。

# 评估

v0.1.54 把候选应用从“写入镜像”推进到“验证可审计”。系统现在能把每条验证命令的结果回写到候选应用记录中，为后续真实源码覆盖和版本提升提供机器可读证据。

# 优化建议

下一步应实现补丁验证通过后的受控源码覆盖准备记录，先生成覆盖计划、差异摘要和回滚清单，再允许进入真实源码覆盖。

# 可复用经验

验证执行必须与应用记录绑定，避免只在终端或聊天中保存结果。真实写入前的每一道闸门都应有结构化状态，且失败命令要保留退出码、超时和输出摘要。

## v0.1.53

# 版本信息
- 版本号：v0.1.53
- 时间：unix:1777646400
- 父版本：v0.1.52

# 目标

实现预演通过后的候选工作区受控应用流程，让 AI 补丁先写入候选工作区镜像目录，并保留结构化记录、验证命令和回滚提示。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增补丁候选应用存储模块。
4. 新增候选准备、预演状态校验、路径边界检查和镜像写入能力。
5. 新增应用后候选布局验证和回滚提示记录。
6. 新增 CLI 命令。
7. 补充测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.52 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiPatchApplicationStore`、`AiPatchApplicationRecord`、`AiPatchApplicationFile` 和候选应用状态。
4. 已新增 `agent-patch-apply`、`agent-patch-applications` 和 `agent-patch-application-record`。
5. 已实现候选版本准备或复用、预演状态校验、代码块镜像写入、候选布局验证和回滚提示记录。
6. 已保证候选应用只写入 `workspaces/v0/source/patch-applications/` 镜像目录，不直接覆盖仓库源码。
7. 已补充成功应用、阻断失败预演、阻断非法路径和复用已有候选四个测试。
8. 已将当前版本线索提升为 `v0.1.53`。

# 代码变更

1. `src/app/agent/patch_application.rs` 新增候选应用存储、索引、记录、摘要和错误类型。
2. `src/app/minimal_loop.rs` 新增补丁候选应用用例、镜像写入和验证记录逻辑。
3. `src/main.rs` 新增补丁候选应用 CLI。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出补丁候选应用类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.53`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test ai_patch_apply` 通过，4 个测试全部成功。
3. 首次 `cargo test` 发现 14 个版本生命周期断言仍停留在上一轮版本线索，已修复；新增非法路径阻断时曾出现变量遮蔽编译错误，已修复。
4. 再次 `cargo test` 通过，146 个测试全部成功。
5. `cargo fmt --check` 通过。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，当前版本 `v0.1.53`，未解决错误 0，可进化。
8. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
9. `cargo run -- agent-patch-applications --current --limit 5` 通过，当前版本暂无真实补丁候选应用记录。
10. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 42 条。
11. `cargo run -- agent-work-init --current --threads 3 "补丁候选应用后续协作"` 通过，协作任务板版本为 `v0.1.53`。
12. 记忆压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。
13. 最终复验 `cargo test`、`cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-applications --current --limit 5` 和 `git diff --check` 均通过。

# 错误总结

本轮出现两个已解决错误：版本生命周期测试断言滞后一位，以及非法路径阻断修正中的变量遮蔽编译错误。修复后无未解决错误。

# 评估

v0.1.53 让 AI 补丁进入候选工作区应用阶段，但仍然不覆盖仓库源码。系统现在具备草案、审计、预演、候选镜像应用四级闸门，权限扩大是可追踪、可阻断、可回滚的。

# 优化建议

下一步应实现候选应用后的验证执行记录，把格式化、测试、验证和预检命令的执行结果关联到候选应用记录。

# 可复用经验

候选应用应优先写入镜像目录，并保留与预演、审计、草案的链路。真正覆盖源码前，必须先把验证结果和回滚方案结构化。

## v0.1.52

# 版本信息
- 版本号：v0.1.52
- 时间：unix:1777642800
- 父版本：v0.1.51

# 目标

实现补丁草案审计通过后的受控应用预演流程，让 AI 补丁在真实写入源码前先生成可审计的中文预演报告和结构化记录。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增补丁应用预演存储模块。
4. 新增草案代码块提取和预演 Markdown 生成逻辑。
5. 新增应用层预演、查询和读取能力。
6. 新增 CLI 命令。
7. 补充测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.51 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiPatchPreviewStore`、`AiPatchPreviewRecord`、`AiPatchPreviewChange` 和预演状态。
4. 已新增 `agent-patch-preview`、`agent-patch-previews` 和 `agent-patch-preview-record`。
5. 已实现从补丁草案 `代码草案` 章节提取代码块，并按审计通过的写入范围生成预演变更摘要。
6. 已保证预演只写入 major 工作区产物目录，不直接修改源码。
7. 已补充预演成功、审计失败阻断和缺少代码块阻断测试。
8. 已将当前版本线索提升为 `v0.1.52`。

# 代码变更

1. `src/app/agent/patch_preview.rs` 新增补丁预演存储、索引、记录、摘要和错误类型。
2. `src/app/minimal_loop.rs` 新增补丁预演应用用例、代码块提取和 Markdown 生成逻辑。
3. `src/main.rs` 新增补丁预演 CLI。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出补丁预演类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.52`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test ai_patch_preview` 通过，3 个测试全部成功。
3. 首次 `cargo test` 发现 14 个版本生命周期断言仍停留在上一轮版本线索，已修复。
4. 再次 `cargo test` 通过，142 个测试全部成功。
5. `cargo fmt --check` 通过。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，当前版本 `v0.1.52`，未解决错误 0，可进化。
8. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
9. `cargo run -- agent-patch-previews --current --limit 5` 通过，当前版本暂无真实补丁预演记录。
10. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 41 条。
11. `cargo run -- agent-work-init --current --threads 3 "补丁应用预演后续协作"` 通过，协作任务板版本为 `v0.1.52`。
12. 记忆压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。
13. 最终复验 `cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `git diff --check`，均通过。

# 错误总结

本轮出现一个已解决测试错误：版本生命周期测试断言滞后一位。修复后无未解决错误。

# 评估

v0.1.52 将 AI 补丁从“草案”和“审计”推进到“可应用预演”，但仍保持严格克制：预演只生成中文报告和 JSON 记录，不直接修改源码。系统因此多了一道真实写入前的可审计缓冲层。

# 优化建议

下一步应实现预演通过后的候选工作区受控应用流程，应用前继续检查写入范围，应用后立即执行格式化、测试、验证和回滚准备。

# 可复用经验

扩大 AI 写代码权限时，应把权限拆成草案、审计、预演、候选应用、验证提升几个小闸门。每个闸门都要有独立记录，并且失败时也要留下阻断原因。

## v0.1.51

# 版本信息
- 版本号：v0.1.51
- 时间：unix:1777639200
- 父版本：v0.1.50

# 目标

实现 AI 自我升级执行后的中文总结报告，让真实自我升级审计记录可以进一步沉淀为便于复盘、查询和后续规划的中文 Markdown 报告。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增自我升级总结报告存储模块。
4. 新增应用层报告生成、查询和读取能力。
5. 在 `agent-self-upgrade` 成功路径自动生成报告。
6. 新增 CLI 命令。
7. 补充测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.50 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiSelfUpgradeSummaryStore`、`AiSelfUpgradeSummaryRecord` 和总结报告状态。
4. 已新增报告 Markdown 生成逻辑，覆盖目标、计划、代码变更、测试结果、错误信息、审计记录和下一步建议。
5. 已新增 `agent-self-upgrade-report`、`agent-self-upgrade-reports` 和 `agent-self-upgrade-report-record`。
6. 已将 `agent-self-upgrade` 真实成功路径接入自动报告生成，并在成功输出中显示报告编号和 Markdown 路径。
7. 已补充成功审计和失败审计的报告测试。
8. 已将当前版本线索提升为 `v0.1.51`。

# 代码变更

1. `src/app/agent/self_upgrade_summary.rs` 新增 major 级报告存储、记录、摘要和错误类型。
2. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出总结报告类型。
3. `src/app/minimal_loop.rs` 新增报告生成、查询和读取流程，并在自我升级成功后自动创建报告。
4. `src/main.rs` 新增报告命令，并在真实自我升级输出中展示报告记录。
5. `src/lib.rs` 扩展自我升级测试，并将版本期望前移到 `v0.1.51`。
6. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.51`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test ai_self_upgrade` 通过，6 个测试全部成功。
3. 首次 `cargo test` 发现 14 个版本生命周期断言仍停留在上一轮版本线索，已修复。
4. 再次 `cargo test` 通过，139 个测试全部成功。
5. `cargo fmt --check` 通过。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，当前版本 `v0.1.51`，未解决错误 0，可进化。
8. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
9. `cargo run -- agent-self-upgrade-reports --current --limit 5` 通过，当前版本暂无真实自我升级总结报告。
10. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 40 条。
11. `cargo run -- agent-work-init --current --threads 3 "自我升级总结报告后续协作"` 通过，协作任务板版本为 `v0.1.51`。
12. 记忆压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。
13. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-self-upgrade-reports --current --limit 5` 和 `git diff --check`，均通过。

# 错误总结

本轮出现一个已解决测试错误：版本生命周期测试断言滞后一位。修复后无未解决错误。

# 评估

v0.1.51 补齐了自我升级审计后的中文复盘层。系统现在不仅能记录结构化审计，还能生成面向人和后续 Agent 的总结报告，便于快速理解本次自我升级做了什么、如何验证、是否有风险以及下一步应该做什么。

# 优化建议

下一步应实现补丁草案审计通过后的受控应用预演流程，让 AI 生成的补丁先在候选工作区或沙箱中形成可审计变更，再进入测试和验证。

# 可复用经验

结构化审计适合机器查询，中文总结适合人类复盘和 Agent 规划。两者应通过记录编号互相关联，但不要重复保存敏感提示词或密钥。

## v0.1.50

# 版本信息
- 版本号：v0.1.50
- 时间：unix:1777635903
- 父版本：v0.1.49

# 目标

实现 AI 补丁草案的差异审计和冲突检查能力，在补丁应用前解析目标写入范围，阻断受保护路径和多 AI 活跃写入范围冲突。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增补丁审计存储模块。
4. 新增草案写入范围解析、路径边界检查和协作冲突检查。
5. 新增应用层审计、查询和读取能力。
6. 新增 CLI 命令。
7. 补充测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.49 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标是补丁审计和冲突检查。
3. 已新增 `AiPatchAuditStore`、`AiPatchAuditRecord`、`AiPatchAuditFinding` 和审计状态。
4. 已新增 `SelfForgeApp::ai_patch_audit`、审计记录列表和单条读取能力。
5. 已新增 `agent-patch-audit`、`agent-patch-audits` 和 `agent-patch-audit-record`。
6. 已实现草案 `允许写入范围` 解析、仓库相对路径检查、受保护路径检查和活跃协作任务冲突检查。
7. 已将当前版本线索提升为 `v0.1.50`。

# 代码变更

1. `src/app/agent/patch_audit.rs` 新增补丁审计存储、索引、记录和发现结构。
2. `src/app/minimal_loop.rs` 新增补丁审计应用用例、写入范围解析和冲突检查。
3. `src/main.rs` 新增补丁审计 CLI。
4. `src/app/mod.rs` 和 `src/lib.rs` 导出补丁审计类型。
5. `README.md` 和 `Agents.md` 补充命令和约束。
6. `Cargo.toml`、`Cargo.lock` 和 `state/state.json` 已更新到 `v0.1.50`。

# 测试结果

1. `cargo test ai_patch_audit` 通过，3 个针对性测试全部成功。
2. 首次 `cargo test` 失败，原因是 14 个版本生命周期测试仍断言上一轮版本关系；已修正为 `v0.1.50 -> v0.1.51`。
3. 再次 `cargo test` 通过，139 个测试全部成功。
4. `cargo fmt --check` 通过。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.50`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
8. `cargo run -- agent-patch-audits --current --limit 5` 通过，当前版本暂无真实补丁审计记录。
9. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 39 条。
10. `cargo run -- agent-work-init --current --threads 3 "补丁审计后续协作"` 通过，协作任务板版本为 `v0.1.50`。

# 错误总结

本轮暂无未解决错误。已解决测试断言版本偏移问题，新增错误测试覆盖受保护路径和活跃写入范围冲突。

# 评估

v0.1.50 给 AI 补丁草案增加了第一道应用前闸门：草案即使生成成功，也必须先通过写入范围审计，才能进入后续应用和验证流程。

# 优化建议

下一步应实现自我升级执行后的中文总结报告，把目标、计划、代码变更、测试、错误、审计记录和下一步建议聚合成一次可读的闭环结果。

# 可复用经验

在允许 AI 应用补丁前，必须先把写入范围、受保护边界和并行协作冲突变成结构化审计记录；审计失败应保留证据并阻断继续推进，而不是依赖人工在聊天记录中判断。

## v0.1.49

# 版本信息
- 版本号：v0.1.49
- 时间：unix:1777663772
- 父版本：v0.1.48

# 目标

实现 AI 生成补丁的沙箱化草案流程，只允许把中文补丁计划、代码草案和测试草案写入受控产物目录。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增补丁草案存储模块。
4. 新增应用层预览、执行、校验和查询能力。
5. 新增 CLI 命令。
6. 补充测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.48 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标是 AI 补丁草案。
3. 已新增 major 级补丁草案目录和索引设计。
4. 已新增 `AiPatchDraftStore`、`AiPatchDraftRecord`、`AiPatchDraftSummary` 和草案状态。
5. 已新增 `SelfForgeApp::ai_patch_draft_preview`、`SelfForgeApp::ai_patch_draft`、草案查询和读取能力。
6. 已新增 `agent-patch-draft`、`agent-patch-drafts` 和 `agent-patch-draft-record`。
7. 已要求 AI 草案必须包含中文内容、计划章节和测试章节，缺失时写入失败记录并停止。
8. 已将当前版本线索提升为 `v0.1.49`。

# 代码变更

1. `src/app/agent/patch_draft.rs` 新增补丁草案存储、索引和记录读取能力。
2. `src/app/minimal_loop.rs` 新增 AI 补丁草案应用用例、提示词构造和草案校验。
3. `src/main.rs` 新增补丁草案 CLI。
4. `src/app/mod.rs` 和 `src/lib.rs` 导出补丁草案类型。
5. `README.md` 和 `Agents.md` 补充命令和约束。
6. `Cargo.toml`、`Cargo.lock` 和 `state/state.json` 已更新到 `v0.1.49`。

# 测试结果

1. `cargo test ai_patch_draft` 通过，3 个针对性测试全部成功。
2. `cargo test` 通过，136 个测试全部成功。
3. `cargo fmt --check` 通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.49`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
7. `cargo run -- agent-patch-drafts --current --limit 5` 通过，当前版本暂无真实补丁草案记录。
8. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 38 条。
9. `cargo run -- agent-work-init --current --threads 3 "AI 补丁草案后续协作"` 通过，协作任务板版本为 `v0.1.49`。
10. `cargo run -- agent-patch-draft --dry-run "为候选补丁生成受控草案"` 通过，确认 CLI 预览可生成受控提示词且不创建真实草案文件。

# 错误总结

本轮暂无未解决错误。新增错误测试覆盖缺少测试章节时写入失败记录并停止的路径。

# 评估

v0.1.49 让 AI 写代码能力迈出受控第一步：AI 可以生成补丁草案，但不会直接修改源码，所有输出都被限制在 major workspace 的 `patch-drafts` 产物目录，并且必须先给出计划和测试。

# 优化建议

下一步应实现候选补丁差异审计和冲突检查，解析草案中的目标写入范围，防止多个 AI 线程覆盖同一文件或触碰受保护边界。

# 可复用经验

扩大 AI 写代码权限前，应先把 AI 输出降级为可审计草案，并用结构化记录保存目标、提供商、模型、允许写入范围、必要章节、响应摘要和失败原因。

## v0.1.48

# 版本信息
- 版本号：v0.1.48
- 时间：unix:1777662977
- 父版本：v0.1.47

# 目标

实现受控多步运行命令，让 Agent 会话可以自动连续推进无需外部输入的步骤，并在遇到阻断条件时停止。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 在应用层新增多步运行报告、停止原因和错误类型。
4. 复用 `execute_next_agent_step` 实现受控连续执行。
5. 在 CLI 新增 `agent-steps` 命令。
6. 补充测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.47 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标是受控多步运行。
3. 已新增 `AgentStepRunReport`、`AgentStepRunStop` 和 `AgentStepRunError`。
4. 已新增 `SelfForgeApp::execute_agent_steps`，按步骤顺序连续执行无需外部输入的工具。
5. 已让多步执行在会话完成、达到步数上限、缺少输入、无可运行工具或工具失败时停止。
6. 已新增 `agent-steps` CLI，并将其加入帮助和 README。
7. 已将受控多步执行约束写入 `Agents.md`。
8. 已将当前版本线索提升为 `v0.1.48`。

# 代码变更

1. `src/app/minimal_loop.rs` 新增受控多步执行应用用例和停止原因。
2. `src/app/mod.rs` 和 `src/lib.rs` 导出多步执行类型。
3. `src/main.rs` 新增 `agent-steps` 命令、参数解析和输出。
4. `src/lib.rs` 新增多步执行相关测试，并将版本期望前移到 `v0.1.48`。
5. `Cargo.toml`、`Cargo.lock`、`README.md`、`Agents.md` 和 `state/state.json` 已更新。

# 测试结果

1. `cargo test agent_steps` 通过，3 个针对性测试全部成功。
2. `cargo fmt` 通过。
3. `cargo test` 通过，133 个测试全部成功。
4. `cargo fmt --check` 通过。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.48`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
8. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 37 条。
9. `cargo run -- agent-work-init --current --threads 3 "受控多步运行后续协作"` 通过，协作任务板版本为 `v0.1.48`。
10. 压缩后再次执行 `cargo run -- validate`、`cargo run -- preflight` 和开放错误查询，均通过。

# 错误总结

本轮暂无未解决错误。新增错误测试覆盖零步数上限和缺少 Runtime 命令时的受控停止路径。

# 评估

v0.1.48 让 Agent 会话具备最小连续推进能力。系统现在可以自动执行前置的记忆读取、经验提取等无外部输入步骤，并在真正需要程序命令或 AI 提示词时停止等待明确输入。

# 优化建议

下一步应实现 AI 生成补丁的沙箱化草案流程，让 AI 可以先生成受控补丁计划和候选变更，再进入测试与审计。

# 可复用经验

多步自动化应复用单步原语和既有状态写回逻辑；自动化范围扩大时，最重要的是把停止原因结构化，而不是让流程猜测缺失输入。

## v0.1.47

# 版本信息
- 版本号：v0.1.47
- 时间：unix:1777662277
- 父版本：v0.1.46

# 目标

将 AI 自我升级审计记录与 Agent 会话详情互相关联，让审计记录和会话可以互相追溯。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 在审计存储中新增按会话编号反查记录。
4. 在应用层暴露会话关联审计查询接口。
5. 更新 `agent-session` 输出。
6. 补充测试。
7. 更新任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.46 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiSelfUpgradeAuditStore::find_by_session`，按 `session_id` 从审计索引反查记录。
4. 已新增 `SelfForgeApp::ai_self_upgrade_record_for_session`。
5. 已更新 `agent-session`，展示关联自我升级审计记录摘要。
6. 已补充测试，验证自我升级成功后可通过会话编号找到审计记录。
7. 已将当前版本线索提升为 `v0.1.47`。

# 代码变更

1. `src/app/agent/self_upgrade_audit.rs` 新增按会话编号反查审计摘要。
2. `src/app/minimal_loop.rs` 新增应用层查询方法。
3. `src/main.rs` 更新 `agent-session` 输出。
4. `src/lib.rs` 扩展自我升级审计测试，并将版本期望前移到 `v0.1.47`。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.47`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test` 通过，130 个测试全部成功。
3. `cargo fmt --check` 通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.47`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
7. `cargo run -- agent-self-upgrades --current --limit 5` 通过，当前版本暂无真实自我升级审计记录。
8. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 36 条。

# 错误总结

本轮暂无未解决错误。

# 评估

v0.1.47 让自我升级审计记录和 Agent 会话形成双向追溯：审计记录中保存会话编号，会话详情中也能显示关联审计记录。

# 优化建议

下一步应实现受控多步运行命令，让系统连续推进无需外部输入的 Agent 步骤，并在阻断时生成清晰待办。

# 可复用经验

追溯关系可以先通过索引反查实现，避免为了展示反向关联而修改已存在会话 JSON 结构，减少迁移风险。

## v0.1.46

# 版本信息
- 版本号：v0.1.46
- 时间：unix:1777661868
- 父版本：v0.1.45

# 目标

为 AI 自我升级增加结构化审计记录，让真实自我升级过程具备可查询、可复盘、可验证的持久化证据。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增自我升级审计存储模块。
4. 将真实自我升级成功和失败结果写入审计记录。
5. 新增审计记录列表和单条读取命令。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误和版本归档。
8. 执行格式化、测试、验证、预检、开放错误查询、审计查询和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.45 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮优先实现自我升级审计归档。
3. 已新增 `AiSelfUpgradeAuditStore`、`AiSelfUpgradeAuditRecord`、`AiSelfUpgradeAuditSummary` 和审计状态。
4. 已让 `ai_self_upgrade` 在 AI 请求失败、空目标、进化失败和成功执行后写入审计记录。
5. 已新增 `agent-self-upgrades` 和 `agent-self-upgrade-record`。
6. 已让 `agent-self-upgrade` 成功输出审计记录编号和文件路径。
7. 已补充测试，验证成功记录、失败记录、查询能力、API Key 不落盘和完整提示词不落盘。
8. 已将当前版本线索提升为 `v0.1.46`。

# 代码变更

1. `src/app/agent/self_upgrade_audit.rs` 新增 major 级审计存储、记录、摘要和错误类型。
2. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出审计类型。
3. `src/app/minimal_loop.rs` 将 AI 自我升级执行结果写入审计记录，并新增记录查询接口。
4. `src/main.rs` 新增审计查询命令，并在真实自我升级输出中显示审计记录。
5. `src/lib.rs` 新增 2 个审计测试，并将版本期望前移到 `v0.1.46`。
6. `README.md` 和 `Agents.md` 补充审计记录命令和约束。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test` 通过，130 个测试全部成功。
3. `cargo fmt --check` 通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.46`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
7. `cargo run -- agent-self-upgrades --current --limit 5` 通过，当前版本暂无真实自我升级审计记录。
8. `cargo run -- agent-self-upgrade --dry-run "优先关联自我升级审计记录和 Agent 会话"` 通过，输出脱敏预览。
9. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 35 条。
10. 压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。

# 错误总结

本轮没有新增未解决错误。

# 评估

v0.1.46 补齐了 AI 自我升级闭环中的重要审计缺口。系统现在不仅能让 AI 选择下一轮目标，也能在真实执行后留下结构化证据，且不会保存 API Key、完整请求体或完整提示词。

# 优化建议

下一步应把审计记录与 Agent 会话详情进一步关联展示，例如在 `agent-session` 中提示对应的自我升级记录，或在自我升级记录中补充测试命令和验证摘要。

# 可复用经验

任何 AI 自动化流程都应先把“输入摘要、决策结果、执行链路、验证结果、失败原因”落为结构化记录，再扩大自动写代码的权限。

## v0.1.45

# 版本信息
- 版本号：v0.1.45
- 时间：unix:1777661208
- 父版本：v0.1.44

# 目标

让 SelfForge 具备 AI 自动自我升级入口：系统可以读取预检状态和近期记忆，让 AI 生成下一轮小版本升级目标，并复用受控进化闭环执行候选验证、提升或回滚。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 在应用层新增 AI 自我升级预览与执行用例。
4. 在 CLI 新增 `agent-self-upgrade` 命令。
5. 补充目标归一化、开放错误阻断和请求预览测试。
6. 更新 README、`Agents.md`、任务、错误和版本归档。
7. 执行格式化、测试、验证、预检、开放错误查询和自我升级预览。
8. 提升版本线索并提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.44 可继续进化，未解决错误为 0。
2. 已读取最近热记忆，确认下一步应继续推进自动自我升级能力。
3. 已新增 `AiSelfUpgradePreview`、`AiSelfUpgradeReport` 和 `AiSelfUpgradeError`。
4. 已实现 `ai_self_upgrade_preview`，负责预检、读取记忆、生成受控提示词和构造脱敏请求规格。
5. 已实现 `ai_self_upgrade`，负责真实 AI 请求、目标归一化和复用 `agent-evolve` 执行受控进化。
6. 已新增 CLI 命令 `agent-self-upgrade`，支持 `--dry-run`、`--timeout-ms` 和可选目标提示。
7. 已补充测试，覆盖预览提示词、目标清理、空目标拒绝和开放错误阻断。
8. 已将当前版本线索提升为 `v0.1.45`。
9. 已刷新协作任务板到 `v0.1.45`。

# 代码变更

1. `src/app/minimal_loop.rs` 新增 AI 自我升级应用用例、提示词生成、目标归一化和错误类型。
2. `src/main.rs` 新增 `agent-self-upgrade` CLI 入口和参数解析。
3. `src/app/mod.rs` 与 `src/lib.rs` 导出新的 AI 自我升级类型和归一化函数。
4. `src/lib.rs` 新增 4 个自我升级相关测试，并将版本期望前移到 `v0.1.45`。
5. `Cargo.toml`、`Cargo.lock` 和 `state/state.json` 提升到 `v0.1.45`。
6. `README.md` 和 `Agents.md` 补充 AI 自我升级命令与规则。

# 测试结果

1. `cargo fmt` 通过。
2. 首次 `cargo test` 被 Cargo 镜像缺少 `tracing 0.1.45` 阻断。
3. `cargo update -p tracing --precise 0.1.44` 通过。
4. `cargo test` 通过，128 个测试全部成功。
5. `cargo fmt --check` 通过。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，当前版本 `v0.1.45`，未解决错误 0，可进化。
8. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
9. `cargo run -- agent-self-upgrade --dry-run "优先完善自动自我升级能力"` 通过，输出提供商、模型、协议、记忆来源和提示词字节，不输出 API Key。
10. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 34 条。

# 错误总结

本轮发现一个已解决的环境可复现性问题：Cargo 镜像缺少锁定的 `tracing 0.1.45`，导致首次 `cargo test` 在依赖选择阶段失败。已将锁文件收敛到镜像可用的 `tracing 0.1.44`，并通过完整测试复验。

# 评估

v0.1.45 让 SelfForge 首次具备“AI 决策下一轮目标并触发受控进化”的入口。实现仍保持克制：AI 只产出一个目标句子，真正的候选准备、验证、提升和回滚仍由现有 `agent-evolve`、Supervisor、Runtime 和状态文件负责。

# 优化建议

下一步应增加自我升级执行记录的结构化归档，把 AI 生成目标、归一化目标、预检摘要和进化结果写入同一个可审计会话或任务产物中，方便后续分析 AI 目标质量。

# 可复用经验

AI 自动能力应先被限制为“生成目标和调用既有闭环”，再逐步扩展到补丁生成。这样可以在不破坏架构边界的前提下，持续提高自动化程度。

## v0.1.44

# 版本信息
- 版本号：v0.1.44
- 时间：unix:1777660177
- 父版本：v0.1.43

# 目标

将 Agent 步进执行和多 AI 协作任务板连接起来，让每个可执行步骤在工具调用前领取任务，在成功完成后同步完成任务，在失败时释放任务，形成可审计的步骤级任务所有权。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 扩展 Agent 会话步骤结构，保存协作任务编号和工作线程。
4. 在 `execute_next_agent_step` 中接入任务领取、完成和释放。
5. 更新 CLI 输出和 `Agents.md` 规则。
6. 补充单元、边界和错误测试。
7. 执行格式化、严格测试、真实命令验证、版本提升、归档和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.43 可继续进化，未解决错误为 0。
2. 已新增 `AgentSessionStep.work_task_id` 和 `AgentSessionStep.work_worker_id`，并新增会话事件 `WorkTaskLinked`。
3. 已让 `agent-step` 在工具输入完整后领取任务板任务，并把任务编号写回步骤。
4. 已让步骤成功时调用 `complete_agent_work`，步骤失败或 Runtime 验证失败时调用 `release_agent_work`。
5. 已让 `agent-step` 输出协作任务信息，并让 `agent-session` 展示步骤关联任务。
6. 已补充测试覆盖成功同步、缺少输入不领取任务和 Runtime 步骤同步。
7. 已通过真实会话 `agent-session-1777631215682135400-000` 验证 `agent-step` 会领取并完成 `coord-001-architecture`，随后用释放命令恢复任务板。
8. 已将当前版本线索提升为 `v0.1.44`，并刷新协作任务板内部版本为 `v0.1.44`。

# 代码变更

1. `src/app/agent/session.rs` 新增步骤任务关联字段、`WorkTaskLinked` 事件和 `attach_work_claim`。
2. `src/app/agent/tool_invocation.rs` 扩展 `AgentStepExecutionReport`，返回协作任务编号和工作线程。
3. `src/app/minimal_loop.rs` 在步进执行中接入任务领取、完成、释放和错误传播。
4. `src/main.rs` 在 `agent-step` 和 `agent-session` 输出中展示协作任务信息。
5. `src/lib.rs` 新增和调整测试，并将版本期望前移到 `v0.1.44`。
6. `Agents.md` 新增 Agent 步进和协作任务同步规则。
7. `src/app/agent/coordination.rs` 修正释放已完成任务时的完成时间清理逻辑，避免待领取任务保留旧完成时间。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，124 个测试全部成功。
3. `cargo run -- agent-start --current "验证 agent-step 同步协作任务"` 通过，创建会话 `agent-session-1777631215682135400-000`。
4. `cargo run -- agent-step --current agent-session-1777631215682135400-000 --limit 2` 通过，输出协作任务 `coord-001-architecture`。
5. `cargo run -- agent-session --current agent-session-1777631215682135400-000` 通过，事件时间线包含 `WorkTaskLinked`。
6. `cargo run -- agent-work-release --current coord-001-architecture --worker agent-session-1777631215682135400-000-step-1 --reason "真实验证结束，恢复任务板以便后续进化继续领取。"` 通过。
7. `cargo run -- agent-work-init --current --threads 3 "多 AI 协作修改代码"` 通过，任务板版本刷新为 `v0.1.44`。
8. `cargo run -- validate` 通过，检查 30 个路径。
9. `cargo run -- preflight` 通过，当前版本 `v0.1.44`，未解决错误 0。
10. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
11. `cargo run -- agent-work-status --current` 通过，任务板 5 个任务均为待领取。

# 错误总结

本轮没有新增未解决错误。开发阶段发现三个已解决问题：一是新增状态同步时遗漏 `AgentStepStatus::Running` 分支导致编译失败；二是任务完成摘要未包含工具标识导致测试断言失败；三是完成后释放任务会残留旧完成时间。三者均已修复并复验通过。

# 评估

v0.1.44 让 Agent 会话执行开始具备任务所有权：步骤不再只是更新自己的会话状态，也会同步影响多 AI 协作任务板。系统仍保持克制，缺少外部输入时不会领取任务，避免任务被无效步骤占用。

# 优化建议

下一步应实现受控多步运行命令，让系统可以连续推进不需要外部输入的步骤，并在遇到 Runtime 命令、AI 提示词、测试失败或任务板阻断时暂停并生成清晰待办。

# 可复用经验

任务领取必须发生在工具输入校验之后，否则缺少输入的步骤会错误占用任务。步骤结果和任务板结果要同时包含步骤号、工具标识和摘要，方便后续审计与冲突排查。

## v0.1.43

# 版本信息
- 版本号：v0.1.43
- 时间：unix:1777630618
- 父版本：v0.1.42

# 目标

将协作任务板接入 Agent 会话上下文，让 `agent-start` 创建的会话自动初始化或复用当前 major 的协作任务板，并把任务板路径、任务数量、线程数量和租约配置写入计划上下文。

# 计划（Plan）

1. 读取当前状态、最近记忆和会话/任务板结构。
2. 编写本轮任务记录。
3. 扩展 Agent 会话计划上下文，增加任务板上下文。
4. 在 `agent-start` 成功读取记忆后初始化或复用协作任务板。
5. 更新 CLI 输出、`Agents.md` 和 `README.md`。
6. 补充测试并执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.42 可继续进化，开放错误为 0。
2. 已新增 `AgentSessionWorkQueueContext`，并挂到 `AgentSessionPlanContext.work_queue`。
3. 已新增会话事件类型 `WorkQueuePrepared`，记录协作任务板创建或复用结果。
4. 已让 `agent-start` 在记忆上下文附加成功后初始化或复用协作任务板，并把队列信息写入会话。
5. 已让 `agent-start` 和 `agent-session` 输出协作任务板摘要。
6. 已补充任务板复用时同步刷新队列版本字段，避免 major 队列跨 patch 后显示旧版本。
7. 已通过 `agent-evolve "将协作任务板接入 Agent 会话上下文"` 将当前稳定版本提升为 v0.1.43。

# 代码变更

1. `src/app/agent/session.rs` 新增任务板上下文结构和任务板准备事件。
2. `src/app/minimal_loop.rs` 新增 `attach_work_queue_context`，在会话创建时初始化或复用协作任务板。
3. `src/app/agent/coordination.rs` 在复用 major 队列时同步刷新队列版本并记录 `retarget` 事件。
4. `src/main.rs` 在 `agent-start` 和 `agent-session` 输出中展示任务板摘要。
5. `src/lib.rs` 新增会话任务板上下文、任务板复用和队列版本刷新测试。
6. `Agents.md` 和 `README.md` 补充会话自动关联任务板规则和命令说明。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，123 个测试全部成功。
3. `cargo run -- preflight` 通过，当前稳定版本 v0.1.42，未解决错误 0，可进化为是。
4. `cargo run -- errors --current --open --limit 5` 返回无记录。
5. `cargo run -- agent-evolve "将协作任务板接入 Agent 会话上下文"` 通过，当前稳定版本提升为 `v0.1.43`。
6. `cargo run -- agent-start --current "验证 Agent 会话自动关联协作任务板"` 通过，输出协作任务板摘要。
7. `cargo run -- agent-session --current agent-session-1777630670861410300-000` 通过，会话事件包含协作任务板准备记录。

# 错误总结

本轮没有新增未解决错误。开发阶段发现 major 任务板复用到新 patch 时队列内部版本字段可能停留在旧版本，已通过复用时刷新版本字段和 `retarget` 事件解决，并补充测试。

# 评估

v0.1.43 把“Agent 计划会话”和“多 AI 任务板”连成同一个可审计上下文。系统仍保持克制：本轮只在会话创建时准备任务板，不自动领取或执行任务，为下一步受控步进执行留下清晰边界。

# 优化建议

下一步应在 `agent-step` 执行前按当前步骤 Agent 自动领取任务板任务，并在工具执行成功、失败或阻断时分别完成、释放或保留任务租约，将步骤状态和任务状态绑定起来。

# 可复用经验

长期复用的 major 级协作文件需要在版本提升后刷新版本线索，但不能覆盖任务、事件和租约状态。会话上下文应保存任务板路径，避免后续执行阶段重新推断队列位置。

## v0.1.42

# 版本信息
- 版本号：v0.1.42
- 时间：unix:1777630201
- 父版本：v0.1.41

# 目标

新增多 AI 协作任务租约清理能力，防止 AI 线程中断后任务永久停留在已领取状态，并让后续线程可以审计、释放和重新领取过期任务。

# 计划（Plan）

1. 读取当前状态、最近记忆和协作队列实现。
2. 编写本轮任务记录。
3. 在协作任务结构中增加租约字段和过期判断。
4. 实现过期任务释放能力，并写入队列事件。
5. 接入应用层与 CLI。
6. 更新 `Agents.md` 和 `README.md`。
7. 补充测试并执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.41 可继续进化，开放错误为 0。
2. 已为协作队列新增默认租约时长、任务租约到期时间和领取时可选 `--lease-seconds`。
3. 已新增 `agent-work-reap`，用于释放租约已过期的已领取任务。
4. 已让状态输出展示每个任务的租约信息。
5. 已让释放和过期清理恢复候选任务提示词，并写入队列事件。
6. 已通过 `agent-evolve "新增多 AI 协作任务租约清理能力"` 将当前稳定版本提升为 v0.1.42。
7. 已重新生成当前协作任务板，并用 1 秒租约完成真实过期清理验证。

# 代码变更

1. `src/app/agent/coordination.rs` 新增任务租约字段、默认租约、领取租约、过期清理报告和 `reap_expired`。
2. `src/app/minimal_loop.rs` 新增应用层租约领取和过期清理入口。
3. `src/main.rs` 新增 `agent-work-claim --lease-seconds`、`agent-work-reap` 和状态租约输出。
4. `src/app/agent/mod.rs`、`src/app/mod.rs`、`src/lib.rs` 导出租约清理报告类型。
5. `src/lib.rs` 新增租约记录、非法租约、过期释放和活跃租约保留测试。
6. `Agents.md` 与 `README.md` 补充租约命令和协作规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，121 个测试全部成功。
3. `cargo run -- preflight` 通过，当前稳定版本 v0.1.41，未解决错误 0，可进化为是。
4. `cargo run -- errors --current --open --limit 5` 返回无记录。
5. `cargo run -- agent-evolve "新增多 AI 协作任务租约清理能力"` 通过，当前稳定版本提升为 `v0.1.42`。
6. `cargo run -- agent-work-init --current --threads 3 "多 AI 协作修改代码"` 通过，队列版本为 `v0.1.42`。
7. `cargo run -- agent-work-claim --current --worker ai-lease --agent builder --lease-seconds 1` 通过，提示词包含租约到期时间。
8. `cargo run -- agent-work-reap --current --reason "真实租约过期验证"` 通过，释放 1 个过期任务。
9. `cargo run -- agent-work-status --current` 通过，任务板 5 个任务均为待领取且无活跃租约。

# 错误总结

本轮没有新增未解决错误。测试和真实命令验证阶段未发现需要归档为开放错误的问题。

# 评估

v0.1.42 让多 AI 协作任务板从“能领取任务”推进到“能从中断领取中恢复”。租约机制保持轻量，只处理任务所有权过期和恢复，不引入复杂调度器，符合当前阶段稳定优先的策略。

# 优化建议

下一步应把协作任务板和 Agent 会话步进器连接起来：会话开始时自动初始化任务板，执行前领取任务，步骤成功后完成任务，失败或阻断时释放任务，并将 Runtime 验证结果写回同一协作事件链。

# 可复用经验

多 AI 协作必须同时管理“任务是否有人做”和“领取是否仍有效”。租约过期不应直接删除任务，而应恢复为待领取、保留事件和原因，确保后续线程可以基于同一事实来源继续工作。

## v0.1.41

# 版本信息
- 版本号：v0.1.41
- 时间：unix:1777629472
- 父版本：v0.1.40

# 目标

新增多 AI 协作任务板能力，使多个 AI 线程可以基于同一个持久化任务队列领取、执行、释放和完成任务，避免重复实现和写入范围冲突。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 模块结构。
2. 编写本轮任务记录。
3. 新增协作队列数据结构、路径布局和文件锁保护。
4. 实现初始化、状态查询、领取、完成和释放。
5. 将协作队列接入应用层和 CLI。
6. 更新 `Agents.md` 与 `README.md`。
7. 补充单元、边界和错误测试。
8. 执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.40 可继续进化，开放错误为 0。
2. 已新增 `AgentWorkCoordinator` 和协作队列 JSON 结构，队列写入 `workspaces/v0/artifacts/agents/coordination/work-queue.json`。
3. 已实现任务初始化、状态查询、任务领取、任务完成、任务释放、写入范围冲突检测和简单文件锁。
4. 已新增按线程生成的中文提示词，提示词明确只处理已领取任务、只修改写入范围、冲突时释放任务。
5. 已将命令接入 CLI：`agent-work-init`、`agent-work-status`、`agent-work-claim`、`agent-work-complete`、`agent-work-release`。
6. 已修正领取提示词中“仍可领取任务”误包含当前任务的问题，并让释放后的任务恢复为候选任务提示词。
7. 已通过 `agent-evolve "新增多 AI 协作任务板能力"` 将当前稳定版本提升为 v0.1.41。

# 代码变更

1. `src/app/agent/coordination.rs` 新增多 AI 协作队列、锁文件、任务选择、冲突检测和提示词生成。
2. `src/app/agent/mod.rs`、`src/app/mod.rs`、`src/lib.rs` 导出协作队列相关类型。
3. `src/app/minimal_loop.rs` 新增应用层协作队列入口。
4. `src/main.rs` 新增协作任务板命令和参数解析。
5. `src/lib.rs` 新增初始化、重复领取、写入范围冲突、错误完成、任务释放和非法线程数量测试。
6. `Agents.md` 新增多 AI 同步修改代码规则。
7. `README.md` 增加协作任务板常用命令。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，117 个测试全部成功。
3. `cargo run -- preflight` 通过，当前稳定版本 v0.1.40，未解决错误 0，可进化为是。
4. `cargo run -- agent-evolve "新增多 AI 协作任务板能力"` 通过，当前稳定版本提升为 `v0.1.41`。
5. `cargo run -- agent-work-init --current --threads 3 "多 AI 协作修改代码"` 通过，生成协作队列文件。
6. `cargo run -- agent-work-claim --current --worker ai-1 --agent builder` 通过，领取 `coord-002-application`，且剩余可领取任务不再包含当前任务。
7. `cargo run -- agent-work-release --current coord-002-application --worker ai-1 --reason "真实领取验证完成，释放任务保持队列可复用。"` 通过。
8. `cargo run -- agent-work-status --current` 通过，任务板 5 个任务均回到待领取状态。

# 错误总结

测试阶段发现一个已解决问题：领取提示词生成时仍可领取任务列表包含当前已领取任务，可能诱导多线程重复处理；已在提示词生成阶段排除当前任务，并让释放后的任务恢复为候选任务提示词，新增测试验证。本轮没有未解决错误。

# 评估

v0.1.41 建立了多 AI 同步修改代码的最小协调层。它不直接启动真实并发 AI 执行器，而是先把并发协作中最关键的事实来源、写入范围、依赖关系、领取状态和提示词边界持久化，符合受控自进化的安全顺序。

# 优化建议

下一步应让 Agent 会话或自动进化流程在开始执行前自动创建协作队列，并把队列任务与 Agent 步进执行、Runtime 验证和最终归档串联起来。后续可增加超时租约、任务阻断状态、冲突人工审查和任务动态拆分。

# 可复用经验

多 AI 并行写代码前，必须先持久化任务所有权和写入范围。提示词不能只描述能力，还必须描述边界、禁止事项、完成命令和释放命令；否则多个线程很容易重复做同一个任务或覆盖彼此改动。

## v0.1.40 最终记忆

# 版本信息
- 版本号：v0.1.40
- 时间：unix:1777628573
- 父版本：v0.1.39

# 目标

解决长期 Markdown 记忆无限增长的问题，新增热记忆压缩、冷归档和进化后的自动压缩能力。

# 计划（Plan）

1. 读取最近记忆和当前状态。
2. 编写任务文档。
3. 在记忆模块实现压缩归档用例。
4. 在 CLI 增加 `memory-compact`。
5. 让 `agent-evolve` 在提升成功后自动压缩热记忆。
6. 补充单元、边界和错误测试。
7. 执行真实命令验证、候选提升、文档归档和提交。

# 执行过程

先实现可重复执行的压缩函数，将 `forge/memory/v0.md` 中超出保留窗口的完整小节迁移到 `forge/memory/archive/v0.md`。随后将命令接入 CLI，并把 `agent-evolve` 的成功提升流程扩展为自动执行热记忆压缩。

# 代码变更

1. `src/app/memory.rs` 新增记忆压缩报告、错误类型、热记忆渲染和冷归档渲染。
2. `src/app/minimal_loop.rs` 新增应用层压缩入口，并在 `agent-evolve` 成功提升后自动压缩热记忆。
3. `src/main.rs` 新增 `memory-compact` 命令和输出。
4. `src/lib.rs` 新增压缩迁移、幂等执行、错误参数和自动压缩相关测试。
5. `Agents.md` 与 `README.md` 补充记忆压缩规则和命令。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，111 个测试全部成功。
3. `cargo run -- memory-compact --current --keep 8` 通过，首次将 25 条旧记忆迁入冷归档。
4. `cargo run -- memory-insights --current --limit 5` 通过，只读取热记忆文件中的近期来源。
5. `cargo run -- agent-evolve "新增记忆压缩冷归档能力"` 通过，当前稳定版本提升为 `v0.1.40`。

# 错误总结

本轮没有未解决错误。并行执行两个 `cargo run` 时出现 Cargo 文件锁等待提示，命令最终成功，不构成系统错误。

# 评估

v0.1.40 将记忆系统从“单文件持续追加”推进到“热记忆加冷归档”的结构，既保留审计追溯能力，又降低默认 AI 上下文读取成本。自动压缩接入 `agent-evolve` 后，后续进化不会继续让热记忆无限增长。

# 优化建议

下一步应继续完善 Agent 多步自动执行，让系统在受控工具范围内自动推进多个计划步骤，并在遇到需要外部输入或验证失败时暂停、归档并给出清晰原因。

# 可复用经验

记忆读取和记忆压缩要分离：默认读取只访问热记忆，冷归档只服务审计和复盘。进化流程中的维护动作应放在应用用例层，由 `agent-evolve` 统一编排，避免 CLI 或 Agent 业务逻辑各自实现一套规则。

## v0.1.39 最终记忆

# 版本信息
- 版本号：v0.1.39
- 时间：unix:1777627839
- 父版本：v0.1.38

# 目标

新增 Agent 会话自动步进能力，让已有会话可以按计划推进下一条待执行步骤，并把工具调用结果写回会话记录。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 会话执行结构。
2. 编写本轮任务计划。
3. 新增 Agent 步进请求、结果和错误结构。
4. 在应用层实现下一待执行步骤选择和工具选择策略。
5. 为 `agent-step` 增加 CLI 入口。
6. 补充正常步进、缺少 Runtime 命令、Runtime 运行关联和无待执行步骤测试。
7. 执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.38 可继续进化，开放错误为 0。
2. 已新增 `AgentStepExecutionRequest` 和 `AgentStepExecutionReport`。
3. 已新增应用层 `execute_next_agent_step`，可选择下一条待执行步骤并按已绑定工具执行。
4. 已新增 `agent-step` 命令，支持安全工具自动输入、Runtime 命令输入和 AI 提示词输入。
5. 已通过真实会话 `agent-session-1777627829933565900-000` 验证连续步进，步骤 3 生成运行记录 `run-1777627831264923400-000`。
6. 已通过 `agent-evolve "新增 Agent 会话自动步进能力"` 将候选提升为 v0.1.39。

# 代码变更

1. `src/app/agent/tool_invocation.rs` 新增 Agent 步进请求和结果结构。
2. `src/app/minimal_loop.rs` 新增 `execute_next_agent_step`、步骤工具选择策略和 `AgentStepExecutionError`。
3. `src/main.rs` 新增 `agent-step` 命令与参数解析。
4. `src/app/mod.rs`、`src/app/agent/mod.rs` 和 `src/lib.rs` 导出新的步进类型。
5. `src/lib.rs` 新增 4 个步进测试，覆盖成功步进、Runtime 输入缺失、Runtime 运行关联和无待执行步骤。
6. `README.md` 和 `Agents.md` 补充自动步进命令与规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，108 个测试全部成功。
3. `cargo run -- agent-start "验证 Agent 自动步进"` 通过，创建会话 `agent-session-1777627829933565900-000`。
4. `cargo run -- agent-step agent-session-1777627829933565900-000 --limit 2` 连续两次通过，完成步骤 1 和步骤 2。
5. `cargo run -- agent-step agent-session-1777627829933565900-000 -- target/debug/selfforge.exe --help` 通过，完成步骤 3 并记录 Runtime 运行。
6. `cargo run -- agent-evolve "新增 Agent 会话自动步进能力"` 通过，候选 v0.1.39 已提升。
7. `cargo run -- validate` 通过，检查 30 个路径。
8. `cargo run -- preflight` 通过，当前版本 v0.1.39，未解决错误 0，可进化为是。
9. `cargo run -- errors --current --open --limit 5` 返回无记录。
10. 已确认没有创建 `workspaces/v0.1.39` 或 `forge/*/v0.1.39.md`。

# 错误总结

本轮没有未解决错误。开发阶段 `cargo fmt --check` 提示 rustfmt 排版调整，已运行 `cargo fmt` 并复验通过。

# 评估

v0.1.39 让 Agent 会话从“记录计划和工具”推进到“可以执行下一步计划”，自我迭代闭环更进一步。系统仍保持克制：需要外部输入的 Runtime 和 AI 工具不会自动猜测输入，而是明确阻断或要求调用方提供。

# 优化建议

下一步应实现受控的多步执行命令，让系统可以在安全工具范围内自动推进多个步骤，并在遇到 Runtime、AI 或错误阻断时暂停并生成可审计的待办。

# 可复用经验

自动执行必须以会话中的待执行步骤为唯一来源，不能重新推断计划；缺少必要输入时保持步骤待执行，比伪造成功结果更安全。

## v0.1.38 最终记忆

# 版本信息
- 版本号：v0.1.38
- 时间：unix:1777627078
- 父版本：v0.1.37

# 目标

新增 Agent Tool 统一调用协议，优化 Agent 代码架构，让工具从计划绑定推进到可校验、可分发、可审计的应用层调用能力。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent Tool 模块。
2. 编写本轮任务计划。
3. 新增独立的 Agent Tool 调用协议模块，保持配置和执行分离。
4. 在应用层实现工具调用分发，禁止 CLI 直接解析业务逻辑。
5. 为 `agent-tool-run` 增加最小可用命令入口。
6. 补充工具调用成功、未授权、输入错误和 Runtime 关联测试。
7. 执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.37 可继续进化，开放错误为 0。
2. 已新增 `AgentToolInvocation`、`AgentToolInvocationInput` 和 `AgentToolInvocationReport`，将工具调用协议从工具配置中拆分出来。
3. 已新增应用层 `invoke_agent_tool`，统一校验 Agent 工具绑定并按工具类型分发执行。
4. 已新增 `agent-tool-run` 命令，CLI 只负责参数解析和结果展示。
5. 已覆盖 `memory.context`、`memory.insights`、`agent.session`、`runtime.run`、`ai.request` 请求预览和 `forge.archive` 状态查询。
6. 已通过 `agent-evolve "新增 Agent Tool 统一调用协议"` 将候选提升为 v0.1.38。

# 代码变更

1. `src/app/agent/tool_invocation.rs` 新增工具调用输入和结果结构。
2. `src/app/minimal_loop.rs` 新增 `invoke_agent_tool` 和 `AgentToolInvocationError`，集中处理工具权限校验、输入校验和执行分发。
3. `src/main.rs` 新增 `agent-tool-run` 命令和参数解析。
4. `src/app/mod.rs`、`src/app/agent/mod.rs` 和 `src/lib.rs` 导出新的工具调用类型。
5. `src/lib.rs` 新增工具调用协议测试，覆盖成功调用、未绑定拒绝、Runtime 运行关联和自定义工具缺失执行器。
6. `README.md` 和 `Agents.md` 补充工具调用命令与架构规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，104 个测试全部成功。
3. `cargo run -- agent-tool-run memory.insights --agent architect --limit 2` 通过。
4. `cargo run -- agent-tool-run forge.archive --agent archivist` 通过。
5. `cargo run -- agent-tool-run runtime.run --agent builder --session agent-session-1777627070452815300-000 --step 3 -- target/debug/selfforge.exe --help` 通过，生成运行记录 `run-1777627071037892500-000`。
6. `cargo run -- agent-tool-run agent.session --agent archivist --all --limit 3` 通过。
7. `cargo run -- agent-evolve "新增 Agent Tool 统一调用协议"` 通过，候选 v0.1.38 已提升。
8. `cargo run -- validate` 通过，检查 30 个路径。
9. `cargo run -- preflight` 通过，当前版本 v0.1.38，未解决错误 0，可进化为是。
10. `cargo run -- errors --current --open --limit 5` 返回无记录。
11. 已确认没有创建 `workspaces/v0.1.38` 或 `forge/*/v0.1.38.md`。

# 错误总结

测试阶段发现两个已解决问题：版本提升后 3 个测试断言仍停留在旧的下一个期望版本，已修正为 `v0.1.40` 或 `v0.1.41`；真实 CLI Runtime 工具验证时，首次 PowerShell 正则未能解析会话编号，已改用直接匹配会话标识并复验通过。本轮没有未解决错误。

# 评估

v0.1.38 将 Agent Tool 从“可配置、可绑定”推进到“可调用、可校验、可审计”，代码边界更清晰：工具定义在 Agent 模块，工具执行在应用用例层，CLI 不承载业务逻辑。这让后续多 Agent 自动执行计划具备了稳定入口。

# 优化建议

下一步应实现 Agent 执行计划的自动步进器，让会话可以按计划步骤自动选择工具、执行工具、记录结果，并在失败时自动转入错误归档和回滚流程。

# 可复用经验

工具执行必须先校验 Agent 绑定，再校验输入类型；自定义工具没有执行器时要显式失败。版本提升后，涉及“下一个候选版本”的测试断言需要按语义重新计算，不能只做机械替换。

## v0.1.37 最终记忆

# 版本信息
- 版本号：v0.1.37
- 时间：unix:1777625855
- 父版本：v0.1.36

# 目标

新增可插拔 Agent Tool 配置能力，让 Agent 可以通过内置工具、动态配置和显式绑定自由组合工具，并让计划和会话显示工具绑定结果。

# 计划（Plan）

1. 读取当前状态、最近记忆与 Agent 计划结构。
2. 编写本轮任务计划。
3. 新增 Agent Tool 结构、动态配置读取和初始化能力。
4. 将工具绑定接入 `agent-plan` 和 Agent 会话步骤。
5. 补充工具默认分配、动态配置、错误配置和计划绑定测试。
6. 执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.36 可继续进化，开放错误为 0。
2. 已新增 `AgentToolDefinition`、`AgentToolConfig`、`AgentToolReport` 和相关错误类型。
3. 已新增 `agent-tools` 命令，可查询内置工具、动态配置和 Agent 工具分配，也可初始化 `tool-config.json`。
4. 已让 `agent-plan` 输出每个步骤的工具列表。
5. 已让 Agent 会话步骤持久化工具绑定，并通过 `agent-session` 展示。
6. 已通过 `agent-evolve "新增可插拔 Agent 工具配置"` 将候选提升为 v0.1.37。

# 代码变更

1. `src/app/agent/tools.rs` 新增可插拔工具定义、配置读取、默认工具、绑定验证和配置初始化。
2. `src/app/agent/types.rs` 和 `src/app/agent/session.rs` 为计划步骤和会话步骤新增 `tool_ids`。
3. `src/app/minimal_loop.rs` 将工具报告接入计划生成与会话创建。
4. `src/main.rs` 新增 `agent-tools` 命令，并更新 `agent-plan`、`agent-session` 的工具输出。
5. `src/lib.rs` 新增工具系统测试。
6. `README.md` 和 `Agents.md` 补充工具系统命令与规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，99 个测试全部成功。
3. `cargo run -- agent-tools --init` 通过，已创建 `workspaces/v0/artifacts/agents/tool-config.json`。
4. `cargo run -- agent-tools` 通过，显示 6 个内置工具和 5 个 Agent 分配。
5. `cargo run -- agent-plan --limit 3 "验证工具配置进入计划"` 通过，计划步骤显示工具绑定。
6. `cargo run -- agent-evolve "新增可插拔 Agent 工具配置"` 通过，候选 v0.1.37 已提升。
7. `cargo run -- agent-session --version v0.1.36 agent-session-1777625855927865900-000` 通过，会话步骤显示工具绑定。
8. 已确认没有创建 `workspaces/v0.1.37` 或 `forge/*/v0.1.37.md`。
9. `cargo run -- validate` 通过，检查 30 个路径。
10. `cargo run -- preflight` 通过，当前版本 v0.1.37，未解决错误 0，可进化为是。
11. `cargo run -- errors --current --open --limit 5` 返回无记录。
12. `cargo run -- agent-plan --limit 2 "验证工具配置进入计划"` 通过，计划继续显示工具绑定和记忆经验。

# 错误总结

测试阶段发现机械版本替换误改了 `Cargo.lock` 中依赖 `tracing-core` 的版本号，已恢复依赖版本并复验通过。动态配置测试初版未创建父目录，已补齐夹具目录创建。本轮没有未解决错误。

# 评估

v0.1.37 建立了 Agent Tool 的可插拔配置基础，Agent 计划从“按能力分工”进一步扩展为“按能力和工具组合执行”，为后续真实工具执行、AI 工具接入和多 Agent 编排打下了可维护边界。

# 优化建议

下一步应实现工具执行协议，让工具定义不仅能进入计划，还能由 Agent 步骤通过统一接口调用 Runtime、AI 请求、记忆读取和归档能力。

# 可复用经验

动态配置必须先做结构化验证，再进入计划；工具绑定结果应写入计划和会话，避免执行阶段重新推断导致审计不一致。

## v0.1.36 最终记忆

# 版本信息
- 版本号：v0.1.36
- 时间：unix:1777624840
- 父版本：v0.1.35

# 目标

为 Agent 会话持久化计划上下文快照，让会话记录包含记忆来源与经验摘要，提升计划可解释性和后续审计能力。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 会话结构。
2. 编写本轮任务计划。
3. 新增可选会话计划上下文字段，保持旧会话兼容。
4. 在应用层将 `memory-insights` 转换为会话快照。
5. 更新 `agent-start`、`agent-advance`、`agent-evolve`、`agent-verify` 和 `agent-session`。
6. 补充正常、边界和错误测试。
7. 执行格式化、严格测试、进化提升、验证和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.35 可继续进化，开放错误为 0。
2. 已新增 `AgentSessionPlanContext` 和 `AgentSessionMemoryInsight`。
3. 已让应用层在会话创建后附加计划上下文快照。
4. 已让 `agent-session` 输出计划依据摘要和来源版本。
5. 已通过 `agent-evolve "持久化 Agent 会话计划上下文"` 将候选提升为 v0.1.36。

# 代码变更

1. `src/app/agent/session.rs` 新增会话计划上下文快照，并保持旧会话文件兼容。
2. `src/app/minimal_loop.rs` 将 `memory-insights` 转换为会话快照，并接入 `agent-start`、`agent-advance`、`agent-evolve` 和 `agent-verify`。
3. `src/main.rs` 在 `agent-start` 和 `agent-session` 输出中展示计划依据。
4. `src/lib.rs` 新增计划上下文持久化、缺失记忆错误和旧会话兼容测试。
5. `README.md`、`Agents.md`、`Cargo.toml` 和 `Cargo.lock` 更新到 v0.1.36 相关规则与版本。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，95 个测试全部成功。
3. `cargo run -- agent-start "验证计划上下文持久化"` 通过，输出计划依据摘要。
4. `cargo run -- agent-session agent-session-1777624812019886500-000` 通过，展示来源版本 v0.1.35 到 v0.1.31。
5. `cargo run -- agent-evolve "持久化 Agent 会话计划上下文"` 通过，候选 v0.1.36 已提升。
6. `cargo run -- agent-session --version v0.1.35 agent-session-1777624840044233700-000` 通过，进化会话包含计划依据。
7. 已确认没有创建 `workspaces/v0.1.36` 或 `forge/*/v0.1.36.md`。
8. `cargo run -- validate` 通过，检查 30 个路径。
9. `cargo run -- preflight` 通过，当前版本 v0.1.36，未解决错误为 0，可进化为是。
10. `cargo run -- errors --current --open --limit 5` 返回无记录。

# 错误总结

测试阶段发现初始化记忆归档可能为空白可读但没有可提取的小节，已通过显式测试夹具修正断言。本轮没有未解决错误。

# 评估

v0.1.36 让 Agent 会话不仅保存计划和执行结果，还保存计划依据，计划、记忆、执行和审计之间的连接更完整。

# 优化建议

下一步应让自动进化流程在候选验证前自动执行一组受控验证命令，并把 Runtime 运行证据绑定到同一 Agent 会话中。

# 可复用经验

会话文件中的历史依据应保存为摘要和引用，避免复制完整记忆正文；新增字段必须使用可选结构并提供旧文件兼容测试。

## v0.1.35 最终记忆

# 版本信息
- 版本号：v0.1.35
- 时间：unix:1777624076
- 父版本：v0.1.34

# 目标

将最近记忆经验接入 `agent-plan`，让计划生成阶段输出结构化历史经验摘要，为后续多 Agent 协作和自动进化计划提供可审计上下文。

# 计划（Plan）

1. 读取当前状态、最近记忆和 `agent-plan` 实现。
2. 编写本轮任务计划。
3. 在应用层新增带记忆经验的 Agent 计划报告。
4. 更新 CLI 输出，让 `agent-plan` 展示历史经验摘要。
5. 补充计划报告的正常、边界和错误测试。
6. 执行格式化、严格测试、进化提升、验证和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.34 可继续进化，开放错误为 0。
2. 已新增 `AgentPlanReport` 和 `AgentPlanReportError`。
3. 已新增 `SelfForgeApp::agent_plan_with_memory`，复用统一 `memory-insights` 结果。
4. 已更新 `agent-plan`，支持 `--current`、`--candidate`、`--version` 和 `--limit`，并输出成功经验、失败风险、优化建议和可复用经验。
5. 已通过 `agent-evolve "新增带记忆经验的 Agent 计划"` 将候选提升为 v0.1.35。

# 代码变更

1. `src/app/minimal_loop.rs` 新增带记忆经验的 Agent 计划应用层报告。
2. `src/main.rs` 为 `agent-plan` 增加版本和数量参数解析，并展示历史经验摘要。
3. `src/lib.rs` 新增正常、边界和错误测试，覆盖带记忆经验的计划生成。
4. `src/app/mod.rs` 和顶层导出补充新报告类型。
5. `README.md`、`Agents.md`、`Cargo.toml` 和 `Cargo.lock` 更新到 v0.1.35 相关规则与版本。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，93 个测试全部成功。
3. `cargo run -- agent-plan --limit 3 "用记忆经验生成计划"` 通过，输出 3 条成功经验、0 条失败风险、3 条优化建议和 3 条可复用经验。
4. `cargo run -- agent-evolve "新增带记忆经验的 Agent 计划"` 通过，候选 v0.1.35 已提升。
5. `cargo run -- agent-session --version v0.1.34 agent-session-1777624076611582100-000` 通过，第一步骤记录已读取 5 条历史记忆并提取 5 条可复用经验和 5 条优化建议。
6. 已确认没有创建 `workspaces/v0.1.35` 或 `forge/*/v0.1.35.md`。
7. `cargo run -- validate` 通过，检查 30 个路径。
8. `cargo run -- preflight` 通过，当前版本 v0.1.35，未解决错误为 0，可进化为是。
9. `cargo run -- errors --current --open --limit 5` 返回无记录。

# 错误总结

本轮未发现未解决错误。验证过程中未出现需要归档的新失败运行。

# 评估

v0.1.35 让 Agent 计划阶段真实消费历史记忆经验，计划输出从静态步骤扩展为带上下文证据的协作计划，进一步接近自动进化流程的可解释闭环。

# 优化建议

下一步应让 Agent 会话在执行计划时引用 `agent-plan` 的结构化经验摘要，并逐步把 AI 提供商能力接入规划 Agent，而不是直接写入 CLI。

# 可复用经验

计划上下文应通过应用层统一报告传递；CLI 只做参数解析和展示，可以避免未来真实多 Agent 执行时出现第二套记忆读取逻辑。

## v0.1.34 最终记忆

# 版本信息
- 版本号：v0.1.34
- 时间：unix:1777623390
- 父版本：v0.1.33

# 目标

新增记忆经验提取能力，将最近记忆中的成功经验、失败风险、优化建议和可复用经验提炼为结构化结果。

# 计划（Plan）

1. 读取当前状态、最近记忆和记忆模块实现。
2. 设计只读的经验提取结构。
3. 实现 `memory-insights` 应用入口与 CLI。
4. 将经验提取接入 Agent 会话第一阶段。
5. 补充单元测试、边界测试和错误测试。
6. 执行格式化、测试、受控进化、验证和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.33 可继续进化，开放错误为 0。
2. 已实现 `MemoryInsightReport` 和 `MemoryInsight`。
3. 已新增 `memory-insights` 命令，默认基于最近 5 条 major 聚合记忆提取经验。
4. 已修正经验过滤规则，避免将“未发现功能错误”归入失败风险。
5. 已通过 `agent-evolve "新增记忆经验提取"` 将候选提升为 v0.1.34。

# 代码变更

1. `src/app/memory.rs` 新增经验提取、标题小节解析和占位内容过滤。
2. `src/app/minimal_loop.rs` 让 Agent 会话第一阶段记录可复用经验和优化建议数量。
3. `src/main.rs` 新增 `memory-insights` 命令。
4. `src/lib.rs` 新增记忆经验提取测试，并更新版本滚动测试。
5. `README.md` 和 `Agents.md` 补充经验提取命令与规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，90 个测试全部成功。
3. `cargo run -- memory-insights --current --limit 5` 通过，提取 5 条成功经验、0 条失败风险、5 条优化建议和 5 条可复用经验。
4. `cargo run -- agent-evolve "新增记忆经验提取"` 通过，候选 v0.1.34 已提升。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，未解决错误为 0，可进化为是。
7. `cargo run -- errors --current --open --limit 5` 返回无记录。
8. `cargo run -- memory-insights --current --limit 3` 通过，提取 3 条成功经验、0 条失败风险、3 条优化建议和 3 条可复用经验。

# 错误总结

真实命令验证时发现“未发现功能错误”会被误归入失败风险，已通过过滤规则和测试修正。本轮没有未解决错误。

# 评估

v0.1.34 将记忆系统从读取推进到结构化提取，后续 Agent 计划可以直接消费历史经验，向自动生成计划更近一步。

# 优化建议

下一步可将 `memory-insights` 的结果注入 `agent-plan` 输出，让计划命令显式展示历史经验如何影响步骤设计。

# 可复用经验

历史经验提取应保持只读、可重复、可测试；先生成结构化视图，再接入计划和执行流程，可以避免直接改写记忆造成审计混乱。

## v0.1.33 最终记忆

# 版本信息
- 版本号：v0.1.33
- 时间：unix:1777622679
- 父版本：v0.1.32

# 目标

新增最近记忆上下文读取能力，并让 Agent 会话在第一阶段记录已读取的历史记忆数量。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 会话实现。
2. 设计 `MemoryContextReport` 与最近版本记忆解析规则。
3. 实现 `memory-context` 查询入口。
4. 将记忆读取接入 `agent-advance`、`agent-evolve` 和 `agent-verify`。
5. 补充单元测试、边界测试和错误测试。
6. 执行格式化、测试、受控进化、验证和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.32 可继续进化，开放错误为 0。
2. 已实现最近记忆读取模块，按语义化版本选择最近记录并优先使用最终记忆。
3. 已新增 `memory-context` 命令，默认读取最近 5 条 major 聚合记忆。
4. 已将记忆读取接入 Agent 进化与验证会话，真实会话 `agent-session-1777622679341735100-000` 第一阶段记录已读取 5 条历史记忆。
5. 已通过 `agent-evolve "新增最近记忆上下文读取"` 将候选提升为 v0.1.33。

# 代码变更

1. `src/app/memory.rs` 新增记忆上下文读取、去重、排序和错误模型。
2. `src/app/minimal_loop.rs` 新增 `SelfForgeApp::memory_context`，并在 Agent 会话启动阶段记录记忆读取数量。
3. `src/main.rs` 新增 `memory-context` 命令和参数解析。
4. `src/lib.rs` 新增记忆上下文测试，并更新版本滚动测试。
5. `README.md` 和 `Agents.md` 补充记忆读取命令与规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，88 个测试全部成功。
3. `cargo run -- memory-context --current --limit 5` 通过，读取最近 5 条记忆。
4. `cargo run -- agent-evolve "新增最近记忆上下文读取"` 通过，候选 v0.1.33 已提升。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，未解决错误为 0，可进化为是。
7. `cargo run -- errors --current --open --limit 5` 返回无记录。

# 错误总结

本轮没有新增未解决错误。并行执行多个 `cargo run` 时出现 Cargo 构建锁等待提示，但命令最终成功。

# 评估

v0.1.33 让进化流程显式读取历史记忆，提升了计划可解释性和审计能力，符合受控自进化系统的核心约束。

# 优化建议

下一步可将记忆上下文中的成功经验和失败经验提取为结构化计划输入，进一步减少手写计划依赖。

# 可复用经验

记忆读取应放在应用用例层并复用 major 聚合文件；Agent 会话只记录读取结果和数量，避免复制完整历史造成会话文件膨胀。

## v0.1.32 最终记忆

# 版本信息
- 版本号：v0.1.32
- 时间：unix:1777622117
- 父版本：v0.1.31

# 目标

新增 Agent 验证会话命令，让验证命令具备会话、计划、运行证据和最终状态。

# 计划（Plan）

1. 读取历史记忆和当前状态。
2. 记录 v0.1.32 任务计划。
3. 实现应用层 `agent_verify`，复用 Runtime 与 Agent 会话存储。
4. 扩展 CLI、README 和 Agents.md。
5. 补充单元测试、边界测试和错误测试。
6. 执行格式化、测试、进化提升和运行验证。
7. 写入聚合归档并提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.31 可继续进化，开放错误为 0。
2. 已实现 `agent-verify [goal] -- PROGRAM [ARGS...]`。
3. 已通过 `agent-evolve "新增 Agent 验证会话命令"` 将候选提升为 v0.1.32。
4. 已执行 `agent-verify "验证 Agent 验证命令可执行"`，生成会话 `agent-session-1777622117733095900-000` 和运行记录 `run-1777622117812228300-000`。
5. 已确认没有生成 `workspaces/v0.1.32`、`forge/memory/v0.1.32.md` 或 `forge/versions/v0.1.32.md`。

# 代码变更

1. `src/app/minimal_loop.rs` 新增 `AgentVerificationReport` 和 `SelfForgeApp::agent_verify`。
2. `src/main.rs` 新增 `agent-verify` 命令、参数解析和输出。
3. `src/lib.rs` 新增 Agent 验证命令相关测试。
4. `src/app/mod.rs` 导出验证报告类型。
5. `README.md` 和 `Agents.md` 补充命令与流程规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，85 个测试全部成功。
3. `cargo run -- validate` 通过，检查 30 个路径。
4. `cargo run -- preflight` 通过，未解决错误为 0，可进化为是。
5. `cargo run -- errors --current --open --limit 5` 返回无记录。
6. `cargo run -- agent-verify "验证 Agent 验证命令可执行" --current --timeout-ms 5000 -- target/debug/selfforge --help` 等价验证通过，退出码为 0，未超时。

# 错误总结

本轮没有新增未解决错误。并行运行多个 `cargo run` 时出现文件锁等待提示，但命令最终均成功，不构成系统错误。

# 评估

v0.1.32 将验证动作沉淀为独立 Agent 会话，进一步缩短从计划到可审计证据的距离，符合最小可运行闭环目标。

# 优化建议

下一步应继续让 Agent 自动生成更结构化的验证目标，并把多 Agent 分工结果纳入同一会话报告。

# 可复用经验

复杂流程应优先封装在 `src/app/` 用例层，CLI 只负责解析和输出；运行证据应优先复用 Runtime 记录，避免并行创建另一套审计体系。

## v0.1.31 最终记忆

# 版本信息
- 版本号：v0.1.31
- 时间：2026-05-01
- 父版本：v0.1.30

# 目标

新增 Agent 运行记录关联能力，把 Agent 会话步骤和 Runtime 受控执行证据连接起来。

# 计划（Plan）

1. 读取当前状态、历史记忆、Runtime 运行记录结构和 Agent 会话结构。
2. 记录 v0.1.31 任务文档。
3. 扩展 Agent 事件结构，新增可选运行记录引用并兼容旧事件。
4. 在应用层新增 `agent_run`，复用 Supervisor 和 Runtime。
5. 扩展 CLI，新增 `agent-run`。
6. 扩展会话详情输出，显示运行编号、报告路径、退出码和超时状态。
7. 补充成功运行、失败运行和旧事件兼容测试。
8. 执行格式化、编译、测试、预检、真实 Agent 进化、验证和提交。

# 执行过程

已在 v0.1.30 基础上实现 Agent 运行记录关联能力，并通过 `cargo run -- agent-evolve "新增 Agent 运行记录关联"` 完成候选准备、验证和提升。真实会话 `agent-session-1777621553820530000-000` 从 v0.1.30 发起，候选 v0.1.31 验证成功后提升为当前稳定版本。

# 代码变更

1. 新增 `AgentRunReference`。
2. `AgentSessionEvent` 新增可选 `run` 字段。
3. `AgentSessionEventKind` 新增 `RuntimeRun`。
4. `AgentSession` 新增 `update_step_with_run`。
5. `SelfForgeApp` 新增 `agent_run`。
6. CLI 新增 `agent-run`。
7. `agent-session` 输出新增运行引用展示。
8. README 和 Agents.md 新增 `agent-run` 规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，82 个测试全部成功。
4. `cargo run -- preflight`：通过。
5. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。
6. `cargo run -- agent-evolve "新增 Agent 运行记录关联"`：通过，已提升 v0.1.31。
7. `cargo run -- validate`：通过，检查 30 个路径。
8. `cargo run -- agent-sessions --all --limit 5`：通过，可查询本轮会话。
9. `cargo run -- agent-session --version v0.1.30 agent-session-1777621553820530000-000`：通过。

# 错误总结

本轮未发现功能错误。新增测试覆盖成功 Runtime 运行、失败 Runtime 运行和旧事件缺少运行引用时的兼容读取。

# 评估

v0.1.31 将 Agent 计划步骤与 Runtime 执行证据连接起来。`agent-run` 不直接执行程序，而是复用 Supervisor 与 Runtime，并将运行证据写回会话事件，符合安全隔离、可审计和可解释要求。

# 优化建议

下一步可以实现 Agent 级验证编排，让 `agent-evolve` 在候选提升前自动创建或复用会话步骤运行测试命令，并将运行记录引用写入同一会话。

# 可复用经验

执行证据应以引用形式挂到 Agent 事件上，既保留 Runtime 原始记录，又避免复制日志内容到会话文件中造成膨胀。

## v0.1.30 最终记忆

# 版本信息
- 版本号：v0.1.30
- 时间：2026-05-01
- 父版本：v0.1.29

# 目标

新增 Agent 会话事件时间线，使自动进化过程具备更强的结构化审计能力。

# 计划（Plan）

1. 读取当前状态、历史记忆和 Agent 会话结构。
2. 记录 v0.1.30 任务文档。
3. 在会话模型中新增事件类型和事件记录结构。
4. 将会话创建、状态变化、步骤更新、成功完成和失败停止写入事件时间线。
5. 保持旧会话和旧索引兼容。
6. 扩展 CLI 详情输出，展示步骤结果和事件时间线。
7. 补充事件记录、旧索引兼容和 Agent 进化事件持久化测试。
8. 执行格式化、编译、测试、预检、真实 Agent 进化、验证和提交。

# 执行过程

已在 v0.1.29 基础上实现 Agent 会话事件时间线，并通过 `cargo run -- agent-evolve "新增 Agent 会话事件时间线"` 完成候选准备、验证和提升。真实会话 `agent-session-1777620867868905400-000` 从 v0.1.29 发起，候选 v0.1.30 验证成功后提升为当前稳定版本，且会话文件记录 9 条事件。

# 代码变更

1. 新增 `AgentSessionEvent` 和 `AgentSessionEventKind`。
2. `AgentSession` 新增 `events` 字段，并通过默认值兼容旧会话。
3. `AgentSessionSummary` 新增 `event_count` 字段，并通过默认值兼容旧索引。
4. 会话创建、运行、完成、失败和步骤更新会自动记录事件。
5. CLI `agent-session` 显示步骤结果、最终结果、错误信息和事件时间线。
6. CLI `agent-sessions` 显示事件数量。
7. Agents.md 新增会话事件时间线规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，79 个测试全部成功。
4. `cargo run -- preflight`：通过，当前稳定版本 v0.1.30 可进化。
5. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。
6. `cargo run -- agent-evolve "新增 Agent 会话事件时间线"`：通过，已提升 v0.1.30。
7. `cargo run -- validate`：通过，检查 30 个路径。
8. `cargo run -- agent-sessions --all --limit 5`：通过，本轮会话显示 9 条事件。
9. `cargo run -- agent-session --version v0.1.29 agent-session-1777620867868905400-000`：通过，可读取完整事件时间线。

# 错误总结

本轮未发现功能错误。新增测试覆盖事件记录、旧索引兼容和 Agent 进化事件持久化。

# 评估

v0.1.30 将 Agent 会话从“最终状态记录”推进为“可解释事件时间线”。事件保存在同一个会话 JSON 中，摘要索引只记录事件数量，没有引入新的目录或平行索引，符合可维护和可追溯原则。

# 优化建议

下一步可以把 Runtime 运行记录编号、错误归档编号和 Agent 事件建立引用关系，让一次进化的计划、执行、测试、错误和提升形成更完整的审计链。

# 可复用经验

审计能力应优先内嵌到已有聚合对象中，先形成稳定的数据模型，再扩展查询视图。这样可以提升解释性，同时避免目录和索引失控。

## v0.1.29 最终记忆

# 版本信息
- 版本号：v0.1.29
- 时间：2026-05-01
- 父版本：v0.1.28

# 目标

新增 Agent 跨小版本会话查询能力，使版本提升后仍可审计最近的 Agent 自动进化会话。

# 计划（Plan）

1. 读取当前状态、预检结果和 Agent 会话查询实现。
2. 记录 v0.1.29 任务文档。
3. 在 Agent 会话存储层实现同一 major 下的跨小版本查询。
4. 扩展应用层和 CLI，新增 `agent-sessions --all`。
5. 补充版本过滤、跨版本查询、去重、零 limit 和提升后查询测试。
6. 执行格式化、编译、测试、预检、真实 Agent 进化、验证和提交。

# 执行过程

已在 v0.1.28 基础上实现跨版本会话查询，并通过 `cargo run -- agent-evolve "新增 Agent 跨版本会话查询"` 完成候选准备、验证和提升。真实会话 `agent-session-1777620200021938900-000` 从 v0.1.28 发起，候选 v0.1.29 验证成功后提升为当前稳定版本。

# 代码变更

1. `AgentSessionStore` 新增 `list_all_major`。
2. `AgentSessionStore` 抽出 `list_from_index`，统一处理倒序读取和按会话 id 去重。
3. `SelfForgeApp` 新增 `agent_sessions_all`。
4. CLI `agent-sessions` 新增 `--all` 参数。
5. CLI 会话列表新增会话所属版本显示。
6. README 新增 `agent-sessions --all` 常用命令。
7. Agents.md 新增跨小版本 Agent 会话审计规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，78 个测试全部成功。
4. `cargo run -- preflight`：通过，当前稳定版本 v0.1.29 可进化。
5. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。
6. `cargo run -- agent-evolve "新增 Agent 跨版本会话查询"`：通过，已提升 v0.1.29。
7. `cargo run -- validate`：通过，检查 30 个路径。
8. `cargo run -- agent-sessions --all --limit 5`：通过，可查询 v0.1.28 与 v0.1.27 发起的最近会话。
9. `cargo run -- agent-session --version v0.1.28 agent-session-1777620200021938900-000`：通过，可读取本轮真实会话。

# 错误总结

本轮未发现功能错误。新增测试覆盖跨版本查询边界和版本提升后的审计查询断点。

# 评估

v0.1.29 修复了自动进化审计上的一个实际断点：Agent 会话属于发起进化时的稳定版本，而提升后默认查询当前版本可能看不到刚完成的会话。`agent-sessions --all` 复用 major 工作区索引，不新增文件体系，符合聚合归档和克制扩展规则。

# 优化建议

下一步可以将 Agent 会话与运行记录、错误归档建立更明确的引用关系，使单次进化从计划、执行、测试到错误处理形成可机器读取的审计链。

# 可复用经验

跨版本审计应优先复用已有 major 聚合索引，通过读取视图解决查询问题，而不是新增小版本目录或平行索引体系。

## v0.1.28 最终记录

# 版本信息
- 版本号：v0.1.28
- 时间：2026-05-01
- 父版本：v0.1.27

# 目标

新增 Agent 单轮完整自动进化入口，使系统可以用一个 Agent 命令完成候选准备、验证、提升或回滚，并保留完整会话记录。

# 计划（Plan）
1. 读取当前状态、预检结果和 Agent 自动进化实现。
2. 设计单轮完整进化报告结构。
3. 实现无候选时准备候选并运行 `cycle`。
4. 实现已有候选时直接运行 `cycle`，避免生成额外候选。
5. 将候选版本、cycle 结果和最终稳定版本写回 Agent 会话。
6. 新增 CLI `agent-evolve [goal]`。
7. 补充成功、边界和错误测试。
8. 使用 `agent-evolve` 完成本轮真实版本提升。

# 执行过程

已新增 `agent_evolve` 应用入口和 `agent-evolve` CLI。真实执行中，命令 `cargo run -- agent-evolve "新增 Agent 单轮完整自动进化"` 创建 Agent 会话 `agent-session-1777619228657671900-000`，准备候选版本 `v0.1.28`，随后运行 `cycle` 并成功提升为当前稳定版本。

# 代码变更

1. 新增 `AgentSingleEvolutionReport`。
2. `SelfForgeApp` 新增 `agent_evolve`。
3. CLI 新增 `agent-evolve [goal]`。
4. README 和 Agents.md 新增单轮完整进化规则。
5. 测试新增 `agent_evolve_prepares_candidate_runs_cycle_and_completes_session`。
6. 测试新增 `agent_evolve_cycles_existing_candidate_without_preparing_another`。
7. 测试新增 `agent_evolve_stops_before_candidate_when_open_errors_exist`。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，76 个测试全部成功。
4. `cargo run -- agent-plan "Agent 单轮完整自动进化"`：通过。
5. `cargo run -- preflight`：通过。
6. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。
7. `cargo run -- agent-evolve "新增 Agent 单轮完整自动进化"`：通过，已将 `v0.1.28` 提升为当前稳定版本。
8. `cargo run -- validate`：通过，检查 30 个路径。
9. `cargo run -- agent-sessions --version v0.1.27 --limit 5`：通过，可查询本轮 Agent 会话。

# 错误总结

本轮未发现功能错误。新增错误测试覆盖开放错误存在时必须在候选准备前停止，并将 Agent 会话标记为失败。

# 评估

v0.1.28 将 Agent 自动进化从“调用 advance 的编排入口”推进为“单轮完整进化入口”。该入口仍然复用 Supervisor、Runtime、状态文件、错误归档和 `cycle`，没有绕过任何受控边界。

# 优化建议

下一步可以为 `agent-evolve` 增加结构化审计事件，或接入 AI 生成更细粒度的任务计划，但仍必须保持单轮受控、可回滚和测试通过。

# 可复用经验

Agent 自动化应优先把已有手工流程可靠串联起来，再逐步引入智能生成。完整进化命令必须以 `preflight` 开始，以 `cycle` 的提升或回滚结束。

## v0.1.27 最终记录

# 版本信息
- 版本号：v0.1.27
- 时间：2026-05-01
- 父版本：v0.1.26

# 目标

完善 Agent 自动进化能力，让 Agent 会话能够驱动一次受控 `advance` 流程，并记录每个关键步骤的状态和结果。

# 计划（Plan）
1. 读取当前状态、历史记忆和 Agent 会话实现。
2. 扩展会话状态、步骤状态和步骤结果字段。
3. 新增会话保存能力，并让索引返回最新摘要。
4. 在应用层新增 `agent_advance`，串联会话、前置检查和 `advance`。
5. 在 CLI 新增 `agent-advance [goal]`。
6. 覆盖成功推进、候选推进、错误阻断和索引更新测试。
7. 执行格式化、编译、测试、预检、候选提升、验证和归档。

# 执行过程

已将 Agent 会话从静态计划记录扩展为可更新的进化过程记录。`agent_advance` 会先创建会话并写入协作计划，再执行 `preflight`，通过后调用现有 `advance` 最小闭环。若发现未解决错误，则停止进化并把会话标记为失败。成功时会把 6 个步骤全部标记为已完成，并将稳定版本、候选版本和结果写入会话。

# 代码变更

1. `AgentSessionStatus` 新增运行中、已完成、已失败。
2. `AgentStepStatus` 新增运行中、已完成、已失败。
3. `AgentSession` 新增更新时间、结果和错误字段。
4. `AgentSessionStep` 新增步骤结果字段。
5. `AgentSessionStore` 新增 `save`，并让 `list` 按最新摘要去重。
6. `SelfForgeApp` 新增 `agent_advance` 自动进化编排入口。
7. CLI 新增 `agent-advance [goal]`。
8. README 和 Agents.md 已补充 Agent 自动进化规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，73 个测试全部成功。
4. `cargo run -- agents`：通过，输出 5 个标准 Agent。
5. `cargo run -- agent-plan "Agent 自动进化流程"`：通过，输出 6 步协作计划。
6. `cargo run -- agent-sessions --current --limit 5`：通过，当前版本无会话记录。
7. `cargo run -- preflight`：通过。
8. `cargo run -- advance "新增 Agent 自动进化编排"`：通过，生成 v0.1.27 候选版本。
9. `cargo run -- cycle`：通过，v0.1.27 已提升为当前稳定版本。
10. `cargo run -- validate`：通过，检查 30 个路径。
11. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。

# 错误总结

本轮未发现功能错误。新增错误测试覆盖存在未解决错误时 `agent_advance` 必须停止，并将会话标记为失败。

# 评估

v0.1.27 让 Agent 首次具备编排受控进化的能力。它仍然复用现有 Supervisor、Runtime、状态文件、错误归档和版本规则，没有绕过任何安全边界；这为后续接入 AI 生成计划、真实步骤执行和沙箱验证打下了更稳的基础。

# 优化建议

下一步可以把 `agent_advance` 的步骤结果进一步细化为结构化事件，并让真实 Agent 输出逐步写入会话，之后再接入 AI 请求和 Runtime 沙箱执行。

# 可复用经验

自动进化入口必须先复用现有受控流程，再逐步增加智能化能力。Agent 可以成为编排层，但不能绕过 `preflight`、`advance`、Supervisor、Runtime、错误归档和状态文件。

## v0.1.26 最终记录

# 版本信息
- 版本号：v0.1.26
- 时间：2026-04-30
- 父版本：v0.1.25

# 目标

新增 Agent 会话持久化能力，让多 Agent 计划可以转化为可记录、可查询、可加载的会话基础。

# 计划（Plan）
1. 读取当前状态、最近记忆和 Agent 模块边界。
2. 将会话模型放入 `src/app/agent/`，避免 CLI 承载业务逻辑。
3. 将会话文件写入当前 major 工作区的 `artifacts/agents/sessions/`。
4. 使用 `index.jsonl` 追加会话摘要，支持版本过滤和数量限制。
5. 在应用层暴露启动、列表和加载入口。
6. 增加 CLI 命令和严格测试。
7. 执行测试、验证、候选提升、归档和提交。

# 执行过程

已完成 `AgentSessionStore`，它根据语义版本解析 major 工作区，只在 `workspaces/v0/artifacts/agents/` 下创建会话文件和索引。CLI 新增的三个命令均调用应用层入口，避免散落业务逻辑。随后通过 `advance` 生成 v0.1.26 候选版本，并通过 `cycle` 提升为当前稳定版本。

# 代码变更

1. `AgentCapability`、`AgentDefinition`、`AgentPlan` 和 `AgentPlanStep` 支持序列化。
2. 新增 `AgentSession`、`AgentSessionStep`、`AgentSessionSummary`、`AgentSessionStore` 和相关错误类型。
3. 新增会话唯一标识生成、JSON 会话文件写入、JSONL 索引追加、列表查询和单会话加载。
4. `SelfForgeApp` 新增 Agent 会话应用入口。
5. CLI 新增 `agent-start`、`agent-sessions` 和 `agent-session`。
6. README 和 Agents.md 补充 Agent 会话规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，69 个测试全部成功。
4. `cargo run -- agents`：通过，输出 5 个标准 Agent。
5. `cargo run -- agent-plan "持久化 Agent 会话"`：通过，输出 6 步协作计划。
6. `cargo run -- agent-sessions --current --limit 5`：通过，当前版本无会话记录。
7. `cargo run -- preflight`：通过，v0.1.26 可进化。
8. `cargo run -- advance "新增 Agent 会话持久化"`：通过，生成 v0.1.26 候选版本。
9. `cargo run -- cycle`：通过，v0.1.26 已提升为当前稳定版本。
10. `cargo run -- validate`：通过，检查 30 个路径。
11. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。

# 错误总结

本轮未发现功能错误。新增错误测试覆盖空目标、缺失会话和非法会话标识。

# 评估

v0.1.26 将多 Agent 基础从静态计划推进到可持久化会话。当前仍然保持小步扩展，没有接入复杂并发执行，但已经具备后续接入 AI 请求、Runtime 沙箱和审核归档的稳定状态载体。

# 优化建议

下一步可以为会话步骤增加执行结果字段，并将单步 Agent 输出写入会话文件；在此之前应先设计步骤状态机和失败归档规则。

# 可复用经验

多 Agent 能力应先固化注册表、计划、会话和索引，再逐步接入真实执行。运行产物必须进入 workspace 的 `artifacts/` 分层，避免污染根目录。

## v0.1.25 最终记录

# 版本信息
- 版本号：v0.1.25
- 时间：2026-04-30
- 父版本：v0.1.24

# 目标

建立可扩展的多 Agent 架构基础，让后续新增 Agent、生成协作计划和接入执行流程变得简单、清晰、可维护。

# 计划（Plan）

1. 读取当前状态、历史记忆和应用层结构。
2. 将多 Agent 能力放入独立 `src/app/agent/` 模块。
3. 实现 Agent 能力、定义、注册表和计划结构。
4. 提供标准 Agent 集合：架构、实现、验证、审查、归档。
5. 实现目标到协作计划的确定性生成。
6. 在 `SelfForgeApp` 暴露 Agent 目录和计划入口。
7. 新增 CLI `agents` 和 `agent-plan [goal]`。
8. 补充单元测试、边界测试和错误测试。
9. 执行格式化、编译、测试、预检、候选提升、验证和归档。

# 执行过程

已新增独立 Agent 模块，并通过注册表集中管理 Agent 定义与能力。`agent-plan` 现在可以把目标转成固定顺序的协作计划：读取记忆与拆解目标、确认架构边界、实现最小修改、执行测试、审查风险、写入归档。该设计先建立稳定接口，后续可以在计划步骤上接入真实 AI 调用、沙箱执行和并行验证。

# 代码变更

1. 新增 `src/app/agent/mod.rs`。
2. 新增 `src/app/agent/types.rs`，定义 `AgentCapability`、`AgentDefinition`、`AgentPlan`、`AgentPlanStep` 和 `AgentError`。
3. 新增 `src/app/agent/registry.rs`，实现 `AgentRegistry`、标准 Agent 集合、唯一性校验和协作计划生成。
4. `SelfForgeApp` 新增 `agents` 和 `agent_plan` 应用入口。
5. CLI 新增 `agents` 和 `agent-plan [goal]` 命令。
6. README 和 Agents.md 已补充多 Agent 使用与扩展规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，65 个测试全部成功。
4. `cargo run -- agents`：通过，输出 5 个标准 Agent。
5. `cargo run -- agent-plan "实现多 Agent 协作"`：通过，输出 6 步协作计划。
6. `cargo run -- preflight`：通过。
7. `cargo run -- advance "新增多 Agent 架构基础"`：通过，生成候选版本 `v0.1.25`。
8. `cargo run -- cycle`：通过，已将 `v0.1.25` 提升为当前稳定版本。
9. `cargo run -- validate`：通过，检查 30 个路径。
10. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。

# 错误总结

本轮未发现功能错误。新增测试覆盖了空目标、重复 Agent 标识和缺失能力等错误路径。

# 评估

版本 `v0.1.25` 完成多 Agent 开发基础封装。系统现在有清晰的 Agent 扩展点，后续可以围绕注册表和计划结构逐步接入 AI 规划、任务分派、沙箱执行和结果归档。

# 优化建议

下一步可以新增 Agent 执行会话模型，将 `AgentPlanStep` 转成可持久化的执行记录，并接入 AI 请求执行与 runtime 沙箱。

# 可复用经验

多 Agent 系统应先稳定定义、能力、计划和归档边界，再做并发执行。新增 Agent 应通过注册表扩展，CLI 只负责展示和参数解析。

## v0.1.24 最终记录

# 版本信息
- 版本号：v0.1.24
- 时间：2026-04-30
- 父版本：v0.1.23

# 目标

让 `cargo run -- ai-request "提示词"` 默认发起真实 AI 请求并显示响应文本，而不是只显示脱敏请求摘要。

# 计划（Plan）

1. 读取当前状态、历史记忆和 AI 请求结构。
2. 新增受控 HTTP 执行报告和错误类型。
3. 在执行阶段读取 API Key，避免进入请求摘要、状态和日志。
4. 使用非流式 JSON 请求调用 AI 提供商，并设置超时。
5. 复用现有响应文本归一化解析。
6. 保留 `ai-request --dry-run` 作为脱敏请求预览模式。
7. 补充 HTTP 成功、失败状态、解析失败和密钥脱敏测试。
8. 执行格式化、编译、测试、真实请求验证、候选提升和归档。

# 执行过程

已将 `ai-request` 默认路径改为受控 HTTP 执行，命令会输出提供商、模型、协议、状态码、响应字节数和归一化响应文本。`ai-request --dry-run` 继续输出脱敏请求摘要。已使用 DeepSeek 配置执行真实请求，返回状态码 200，并显示响应文本 `SelfForge 响应测试通过`。

# 代码变更

1. 新增 `AiExecutionReport`、`AiRawHttpResponse` 和 `AiExecutionError`。
2. 新增 `AiProviderRegistry::execute_text_request_project` 和可测试的 `execute_text_request_project_with`。
3. 新增受控 HTTP 发送函数，使用 `reqwest` blocking 客户端、JSON 请求和超时控制。
4. HTTP 失败响应摘要会脱敏 API Key。
5. `SelfForgeApp` 新增真实 AI 请求执行入口，并保留 `ai_request_preview`。
6. CLI `ai-request` 默认显示响应文本，新增 `--dry-run` 与 `--timeout-ms` 参数。
7. README 和 Agents.md 已更新真实请求与脱敏预览规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，60 个测试全部成功。
5. `cargo run -- ai-request --dry-run "提示词"`：通过，显示脱敏请求摘要。
6. `cargo run -- ai-request --timeout-ms 60000 "请只回复：SelfForge 响应测试通过"`：通过，状态码 200，并显示响应文本。
7. `cargo run -- preflight`：通过，显示 `v0.1.24` 可进化。
8. `cargo run -- advance "显示 AI 响应文本"`：通过，生成候选版本 `v0.1.24`。
9. `cargo run -- cycle`：通过，已将 `v0.1.24` 提升为当前稳定版本。
10. `cargo run -- validate`：通过，检查 30 个路径。
11. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。

# 错误总结

用户反馈的问题已解决：旧版 `ai-request` 只做请求构建，不执行 HTTP 请求，因此无法显示响应文本。本轮已补上执行层并保留预览模式。

# 评估

版本 `v0.1.24` 完成 AI API 最小真实调用闭环。配置读取、请求构建、HTTP 执行、响应解析和 CLI 输出已串起来，为后续 Agent 自动规划与执行提供基础能力。

# 优化建议

下一步可以将 AI 请求执行接入 Agent 计划生成流程，但必须先加入请求记录脱敏摘要、错误归档和可控重试策略。

# 可复用经验

真实 HTTP 执行应放在应用统一能力层，不应散落在 CLI。API Key 只允许在内存中用于认证头，所有用户可见输出和归档只记录变量名、状态码和脱敏摘要。

## v0.1.23 最终记录

# 版本信息
- 版本号：v0.1.23
- 时间：2026-04-30
- 父版本：v0.1.22

# 目标

让 `ai-config` 和 `ai-request` 自动读取项目根目录 `.env`，解决用户在 `.env` 中配置 DeepSeek 后仍提示没有可用 AI 提供商的问题。

# 计划（Plan）

1. 读取当前状态、历史记忆和 AI Provider 实现。
2. 在 AI Provider 模块中实现项目 `.env` 读取和解析。
3. 保持真实进程环境变量优先于 `.env`。
4. 让 `SelfForgeApp::ai_config` 和 `SelfForgeApp::ai_request` 统一使用项目根目录配置。
5. 增加 `.env` 读取、覆盖优先级、缺失 `.env` 容错和密钥脱敏测试。
6. 更新 README、`.env.example` 和 Agents.md。
7. 执行格式化、编译、测试、预检、验证、候选生成和提升。

# 执行过程

已新增项目环境查找器，读取当前进程环境变量和项目根目录 `.env`。真实环境变量优先，`.env` 作为本地补充配置。已验证在未显式设置 PowerShell 环境变量时，`cargo run -- ai-config` 能从 `.env` 识别 DeepSeek，`cargo run -- ai-request "你好"` 能生成脱敏请求摘要。

# 代码变更

1. `AiProviderRegistry` 新增 `inspect_project`、`inspect_project_with`、`build_text_request_project` 和 `build_text_request_project_with`。
2. 新增内部 `ProjectEnv` 解析器，支持空行、注释、`export KEY=VALUE`、单引号、双引号和未加引号行内注释。
3. `SelfForgeApp::ai_config` 与 `SelfForgeApp::ai_request` 改为读取项目根目录 `.env`。
4. README 重写为简洁中文说明，并补充 `.env` 自动读取规则。
5. `.env.example` 改为中文示例，默认展示 DeepSeek 配置。
6. Agents.md 补充 `.env` 规则、环境变量优先级和密钥脱敏约束。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，57 个测试全部成功。
5. `cargo run -- ai-config`：通过，已从 `.env` 识别 DeepSeek，未输出密钥值。
6. `cargo run -- ai-request "你好"`：通过，生成脱敏请求摘要。
7. `cargo run -- preflight`：通过，显示 `v0.1.23` 可进化。
8. `cargo run -- advance "自动读取 .env AI 配置"`：通过，生成候选版本 `v0.1.23`。
9. `cargo run -- cycle`：通过，已将 `v0.1.23` 提升为当前稳定版本。
10. `cargo run -- validate`：通过，检查 30 个路径。
11. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。

# 错误总结

用户反馈的问题已解决：之前 AI 配置只读取进程环境变量，未读取项目根目录 `.env`，导致 `.env` 中的 DeepSeek 配置不可见。本轮已改为应用层统一读取项目配置。

# 评估

版本 `v0.1.23` 完成最小闭环提升。AI 配置入口更符合本地开发习惯，同时仍保持密钥不进入日志、状态、文档和请求摘要。

# 优化建议

下一步可以在当前请求构建能力之上增加受控 HTTP 执行层，但必须继续保持超时、脱敏、错误归档和可测试边界。

# 可复用经验

本地配置应由应用层统一合并，真实进程环境变量优先，`.env` 只作为本地补充。密钥值只能用于请求执行，不得进入可审计文本输出。

## v0.1.22

# 版本信息
- 版本号：v0.1.22
- 时间：unix:1777562602
- 父版本：v0.1.21

# 目标

改进 AI 环境变量诊断

# 计划（Plan）

1. 读取用户反馈、最近版本记忆和 AI 请求错误实现。
2. 改进缺少可用提供商时的错误提示，明确 PowerShell 当前会话写法。
3. 改进指定提供商但缺少 API Key 时的错误提示，明确对应密钥变量名。
4. 补充错误消息测试。
5. 更新 README 和 Agents.md 的 PowerShell 环境变量示例。
6. 执行格式、编译、测试、前置检查、AI 配置检查、请求构建检查、候选生成、提升和验证。

# 执行过程

已根据用户反馈改进 AI 环境变量诊断。现在只设置 provider 但未设置 `DEEPSEEK_API_KEY` 时，`ai-request` 会明确提示 PowerShell 当前会话中应执行的 `$env:DEEPSEEK_API_KEY="你的密钥"` 写法，并建议先运行 `cargo run -- ai-config` 检查。本轮真实闭环已将 `v0.1.22` 提升为当前稳定版本。

# 代码变更

1. `AiRequestError::MissingProvider` 文本现在包含 PowerShell 当前会话示例、`SELFFORGE_AI_PROVIDER` 和支持的密钥变量。
2. `AiRequestError::MissingApiKey` 文本现在按提供商列出应设置的 API Key 环境变量。
3. 新增 provider 到 API Key 环境变量的内部辅助函数。
4. README 新增 PowerShell 当前会话配置示例。
5. Agents.md 新增 Windows PowerShell 环境变量规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，53 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.21` 可进化。
6. `cargo run -- ai-config`：通过，无密钥时显示未就绪且不输出密钥。
7. 使用 `SELFFORGE_AI_PROVIDER=deepseek` 但不设置 `DEEPSEEK_API_KEY` 执行 `ai-request`：按预期失败，并明确提示设置 `DEEPSEEK_API_KEY`。
8. 使用临时 `DEEPSEEK_API_KEY` 执行 `ai-request "你好"`：通过，输出脱敏请求摘要。
9. `cargo run -- advance "改进 AI 环境变量诊断"`：通过，生成候选版本 `v0.1.22`。
10. `cargo run -- cycle`：通过，已将 `v0.1.22` 提升为当前版本。
11. `cargo run -- preflight`：通过，提升后显示 `v0.1.22` 可进化。
12. `cargo run -- validate`：通过，检查 30 个路径。
13. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

用户反馈的体验问题已修复：原错误提示无法指导 PowerShell 当前会话环境变量写法，也没有清楚指出缺少 `DEEPSEEK_API_KEY`。本轮已通过错误消息和测试固化修复。

# 评估

版本 `v0.1.22` 已完成最小闭环提升。AI 配置失败时的诊断更清晰，能直接指导用户检查当前进程可见环境变量。

# 优化建议

后续可增加 `ai-config --explain` 或类似结构化诊断输出，但当前先保持 CLI 简洁。

# 可复用经验

Windows PowerShell 中，环境变量必须使用 `$env:变量名="值"` 才会传递给当前会话启动的 `cargo run` 子进程；只写类 bash 形式不会生效。AI 配置错误必须直接指出当前进程看到了什么、缺少什么。

## v0.1.21

# 版本信息
- 版本号：v0.1.21
- 时间：unix:1777561661
- 父版本：v0.1.20

# 目标

新增 AI 响应文本归一化

# 计划（Plan）

1. 读取最近版本记忆、任务记录和 AI 请求构建实现。
2. 新增统一 AI 文本响应结构。
3. 新增 AI 响应解析错误类型。
4. 基于 OpenAI、DeepSeek、Gemini 的不同协议解析响应文本。
5. 增加 OpenAI `output_text`、OpenAI `output` 数组、DeepSeek `choices`、Gemini `candidates` 测试。
6. 增加无效 JSON、缺失文本和空文本错误测试。
7. 执行格式、编译、测试、前置检查、候选生成、提升和验证。

# 执行过程

已在 AI 请求构建层之后新增响应文本归一化能力。系统现在可以从 OpenAI Responses、DeepSeek Chat Completions 和 Gemini GenerateContent 的不同 JSON 响应结构中抽取统一文本结果。本轮真实闭环已将 `v0.1.21` 提升为当前稳定版本。

# 代码变更

1. 新增 `AiTextResponse`，包含提供商、模型、协议、文本和原始响应字节数。
2. 新增 `AiResponseError`，覆盖无效 JSON、缺失文本和空文本。
3. `AiProviderRegistry` 新增 `parse_text_response`。
4. OpenAI Responses 支持解析 `output_text` 和 `output[].content[].text`。
5. DeepSeek 支持解析 `choices[0].message.content`。
6. Gemini 支持解析 `candidates[].content.parts[].text`。
7. `Agents.md` 补充 AI 响应解析集中化规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，51 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.20` 可进化。
6. `cargo run -- ai-config`：通过，无密钥时显示未就绪且不输出密钥。
7. 使用临时 OpenAI 配置执行 `cargo run -- ai-request "响应解析前置检查"`：通过，输出脱敏请求摘要。
8. `cargo run -- advance "新增 AI 响应文本归一化"`：通过，生成候选版本 `v0.1.21`。
9. `cargo run -- cycle`：通过，已将 `v0.1.21` 提升为当前版本。
10. `cargo run -- preflight`：通过，提升后显示 `v0.1.21` 可进化。
11. `cargo run -- validate`：通过，检查 30 个路径。
12. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

本轮未发现功能错误。测试和真实闭环均通过。

# 评估

版本 `v0.1.21` 已完成最小闭环提升。系统已经具备从 AI 配置、请求构建到响应文本归一化的基础链路。

# 优化建议

后续可新增真实 AI HTTP 执行层，执行完成后直接复用 `parse_text_response` 得到统一文本结果。

# 可复用经验

响应解析必须显式处理缺失文本和空文本，不能静默返回空结果继续推进 Agent 流程。厂商响应差异应继续集中在 AI 提供商模块内。

## v0.1.20

# 版本信息
- 版本号：v0.1.20
- 时间：unix:1777561348
- 父版本：v0.1.19

# 目标

新增 AI 统一请求构建

# 计划（Plan）

1. 读取最近版本记忆、任务记录和 AI 配置实现。
2. 新增统一 AI 请求规格结构。
3. 基于 OpenAI、DeepSeek、Gemini 的协议差异构建请求地址、认证头摘要和 JSON 请求体。
4. 新增 `SelfForgeApp::ai_request` 应用入口。
5. 新增 CLI `ai-request [prompt]`，输出脱敏请求摘要。
6. 增加 OpenAI、DeepSeek、Gemini、缺少密钥、空提示词测试。
7. 执行格式、编译、测试、前置检查、请求构建检查、候选生成、提升和验证。

# 执行过程

已在 AI 配置层之上新增统一请求构建能力。系统现在可以根据当前选中提供商构建 OpenAI Responses、DeepSeek Chat Completions 和 Gemini GenerateContent 请求规格，并通过 `ai-request` 输出脱敏摘要。本轮真实闭环已将 `v0.1.20` 提升为当前稳定版本。

# 代码变更

1. 新增 `AiRequestSpec`，包含提供商、模型、协议、方法、地址、认证头名、密钥变量名、内容类型和 JSON 请求体。
2. 新增 `AiRequestError`，覆盖配置错误、缺少提供商、缺少 API Key 和空提示词。
3. `AiProviderRegistry` 新增 `build_text_request_env` 和 `build_text_request_with`。
4. `SelfForgeApp` 新增 `ai_request` 应用入口。
5. CLI 新增 `ai-request [prompt]` 命令。
6. `README.md` 与 `Agents.md` 补充 AI 请求构建规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，44 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.19` 可进化。
6. `cargo run -- ai-config`：通过，无密钥时显示未就绪且不输出密钥。
7. 使用临时 Gemini 配置执行 `cargo run -- ai-request "生成一个最小测试计划"`：通过，输出脱敏请求摘要。
8. 使用临时 DeepSeek 配置执行 `cargo run -- ai-request "分析一个错误"`：通过，输出脱敏请求摘要。
9. `cargo run -- advance "新增 AI 统一请求构建"`：通过，生成候选版本 `v0.1.20`。
10. `cargo run -- cycle`：通过，已将 `v0.1.20` 提升为当前版本。
11. `cargo run -- preflight`：通过，提升后显示 `v0.1.20` 可进化。
12. `cargo run -- validate`：通过，检查 30 个路径。
13. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

本轮未发现功能错误。测试和真实闭环均通过。

# 评估

版本 `v0.1.20` 已完成最小闭环提升。系统现在具备从环境变量配置到统一请求规格生成的基础链路，下一步可以在此基础上实现受控 HTTP 执行。

# 优化建议

后续可新增真实 AI HTTP 执行层，但必须保持超时控制、错误归档、密钥脱敏和测试隔离。

# 可复用经验

请求构建层应先于网络执行层落地。这样可以独立测试不同厂商的协议差异，并防止真实 API Key、提示词和响应内容过早进入日志或状态文件。

## v0.1.19

# 版本信息
- 版本号：v0.1.19
- 时间：unix:1777560286
- 父版本：v0.1.18

# 目标

新增 AI 提供商环境变量配置

# 计划（Plan）

1. 读取最近版本记忆、任务记录和持久化状态。
2. 设计 AI 提供商配置注册表，集中管理 OpenAI、DeepSeek、Gemini。
3. 从环境变量读取 API Key、模型和基础地址，并只记录密钥来源变量名。
4. 支持 `SELFFORGE_AI_PROVIDER` 指定提供商；未指定时按已配置密钥自动选择。
5. 新增 CLI `ai-config` 只读检查命令。
6. 增加自动选择、指定提供商、Gemini 双变量优先级、未知提供商错误测试。
7. 执行格式、编译、测试、前置检查、AI 配置检查、候选生成、提升和验证。

# 执行过程

已新增 AI 提供商配置层，支持通过环境变量识别 OpenAI、DeepSeek 和 Gemini。`ai-config` 可以显示提供商是否就绪、选中来源、模型、基础地址、协议和请求路径，但不会输出真实 API Key。本轮真实闭环已将 `v0.1.19` 提升为当前稳定版本。

# 代码变更

1. 新增 `src/app/ai_provider.rs`，包含 `AiProviderRegistry`、`AiConfigReport`、`AiProviderStatus` 和 `AiConfigError`。
2. `SelfForgeApp` 新增 `ai_config` 应用入口。
3. CLI 新增 `ai-config` 命令。
4. 支持 `OPENAI_API_KEY`、`DEEPSEEK_API_KEY`、`GEMINI_API_KEY` 和 `GOOGLE_API_KEY`。
5. 支持 `SELFFORGE_AI_PROVIDER` 指定 `openai`、`deepseek` 或 `gemini`。
6. 支持提供商模型和基础地址环境变量覆盖。
7. 新增 `.env.example`，并在 `.gitignore` 中忽略真实 `.env`。
8. `README.md` 与 `Agents.md` 补充 AI 配置规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，39 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.18` 可进化。
6. `cargo run -- ai-config`：通过，无密钥时显示未就绪且不输出密钥。
7. 使用临时 `SELFFORGE_AI_PROVIDER=deepseek` 与 `DEEPSEEK_API_KEY` 执行 `cargo run -- ai-config`：通过，显示选中 DeepSeek 且不输出密钥值。
8. `cargo run -- advance "新增 AI 提供商环境变量配置"`：通过，生成候选版本 `v0.1.19`。
9. `cargo run -- cycle`：通过，已将 `v0.1.19` 提升为当前版本。
10. `cargo run -- preflight`：通过，提升后显示 `v0.1.19` 可进化。
11. `cargo run -- validate`：通过，检查 30 个路径。
12. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

本轮未发现功能错误。测试和真实闭环均通过。

# 评估

版本 `v0.1.19` 已完成最小闭环提升。系统已经具备主流 AI API 的安全配置入口，为后续 Agent 调用、模型路由和执行计划生成打下基础。

# 优化建议

后续可在配置层之上增加统一 AI 调用接口，先支持非流式文本生成，再逐步扩展工具调用、结构化输出和流式响应。

# 可复用经验

密钥处理必须与日志、状态和文档隔离；可审计输出只记录密钥来源变量名和是否存在。AI 提供商差异应集中在注册表中，避免后续 Agent 功能重复编码不同厂商规则。

## v0.1.18

# 版本信息
- 版本号：v0.1.18
- 时间：unix:1777559770
- 父版本：v0.1.17

# 目标

新增进化前置检查命令

# 计划（Plan）

1. 读取最近版本记忆、任务记录和持久化状态。
2. 在应用层设计只读 `PreflightReport`。
3. 新增 `SelfForgeApp::preflight`，汇总状态、布局验证、候选版本和未解决错误。
4. 新增 CLI `preflight` 命令，输出进化前风险摘要。
5. 增加干净状态、候选状态和未解决错误状态测试。
6. 执行格式、编译、测试和真实命令验证。
7. 通过 `advance` 和 `cycle` 完成候选生成与提升。

# 执行过程

已新增显式 `preflight` 前置检查入口。该入口只读取状态并执行验证，不修改 `state/state.json`，可在进化前显示当前版本、候选版本、验证路径数量、未解决错误数量和是否可继续进化。本轮真实闭环已将 `v0.1.18` 提升为当前稳定版本。

# 代码变更

1. `SelfForgeApp` 新增 `preflight` 方法。
2. 新增 `PreflightReport`，包含当前版本、当前工作区、状态、候选版本、验证路径、未解决错误和可进化标记。
3. `MinimalLoopError` 新增 `Forge` 分支，用于承载布局验证错误。
4. CLI 新增 `preflight` 命令。
5. `app` 与 `lib` 导出 `PreflightReport`。
6. `README.md` 与 `Agents.md` 补充 `preflight` 使用规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，35 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.17` 可进化。
6. `cargo run -- advance "新增进化前置检查命令"`：通过，生成候选版本 `v0.1.18`。
7. `cargo run -- cycle`：通过，已将 `v0.1.18` 提升为当前版本。
8. `cargo run -- preflight`：通过，提升后显示 `v0.1.18` 可进化。
9. `cargo run -- validate`：通过，检查 30 个路径。
10. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

本轮未发现功能错误。测试和真实闭环均通过。

# 评估

版本 `v0.1.18` 已完成最小闭环提升。系统现在具备显式进化前置检查能力，`advance` 前的风险状态更容易被人和后续自动流程读取。

# 优化建议

后续可将 `preflight` 报告扩展为结构化输出格式，用于自动代理在进入 `advance` 前进行机器判定。

# 可复用经验

只读检查入口应放在应用用例层，并保证不修改状态；CLI 只负责展示结果。前置检查应优先复用已有的验证和错误查询能力，避免产生平行规则。

## v0.1.17

# 版本信息
- 版本号：v0.1.17
- 时间：unix:1777558865
- 父版本：v0.1.16

# 目标

阻断未解决错误进化

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在 `advance` 开始阶段查询当前版本未解决错误。
4. 存在未解决错误时返回明确错误并保持状态不变。
5. 保持无未解决错误时的原有推进行为。
6. 增加未解决错误阻断测试。
7. 执行真实 `errors --current --open` 和 `advance` 验证。
8. 生成并提升候选版本。

# 执行过程

已将未解决错误前置检查接入 `SelfForgeApp::advance`。当前版本无未解决错误时，真实 `advance` 和 `cycle` 已将候选版本提升为当前稳定版本。

# 代码变更

1. `SelfForgeApp::advance` 新增未解决错误检查。
2. `MinimalLoopError` 新增 `ErrorArchive` 和 `OpenErrors` 错误类型。
3. `app` 与 `lib` 导出 `MinimalLoopError`。
4. `Agents.md` 补充 `advance` 前置检查规则。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，32 个测试全部成功。
4. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。
5. `cargo run -- advance "阻断未解决错误进化"`：通过，生成候选版本 `v0.1.17`。
6. `cargo run -- cycle`：通过，已将 `v0.1.17` 提升为当前版本。
7. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮测试阶段发现一处 Rust 模式匹配字段绑定写法错误，已修正后重新运行测试通过。

# 评估

版本 `v0.1.17` 已完成最小闭环提升。系统现在会在进化前检查未解决错误，降低带病生成候选版本的风险。

# 优化建议

后续可增加显式的人工豁免机制，但默认仍应保持阻断策略。

# 可复用经验

安全前置检查应进入应用用例层，而不是分散在 CLI；测试中需要同时验证阻断结果和状态未被修改。

## v0.1.16

# 版本信息
- 版本号：v0.1.16
- 时间：unix:1777558440
- 父版本：v0.1.15

# 目标

查询错误归档列表

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在应用层实现错误列表查询。
4. 新增 CLI `errors` 命令。
5. 增加未解决、已解决、空限制和版本过滤测试。
6. 执行真实错误列表查询命令。
7. 生成并提升候选版本。

# 执行过程

已新增 `errors` 只读查询入口，可按版本、解决状态和数量限制查询已归档运行错误。候选版本已通过 `cycle` 提升为当前稳定版本，并通过真实命令验证已解决和未解决查询。

# 代码变更

1. 新增 `ArchivedErrorEntry` 错误列表条目。
2. 新增 `ErrorListQuery` 查询条件。
3. `ErrorArchive` 新增 `list_run_errors(version, query)`。
4. CLI 新增 `errors [--current|--candidate|--version VERSION] [--limit N] [--open] [--resolved]`。
5. README 和 `Agents.md` 补充错误查询命令和规则。

# 测试结果

1. `cargo fmt`：已执行并统一格式。
2. `cargo check`：通过。
3. `cargo test`：通过，31 个测试全部成功。
4. `cargo run -- advance "查询错误归档列表"`：通过，生成候选版本 `v0.1.16`。
5. `cargo run -- cycle`：通过，已将 `v0.1.16` 提升为当前版本。
6. `cargo run -- errors --version v0.1.14 --resolved --limit 5`：通过，可读取已解决错误。
7. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。
8. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮未出现功能错误。

# 评估

版本 `v0.1.16` 已完成最小闭环提升。错误记录现在支持归档、解决和查询，基本错误生命周期更完整。

# 优化建议

后续可将错误查询结果用于 `advance` 的前置检查：存在未解决错误时要求先修复或显式记录继续原因。

# 可复用经验

只读查询能力应从聚合文档解析，不应为了方便查询创建额外索引；状态过滤要保持版本边界。

## v0.1.15

# 版本信息
- 版本号：v0.1.15
- 时间：unix:1777557750
- 父版本：v0.1.14

# 目标

标记错误归档已解决

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在应用层实现错误解决状态更新。
4. 新增 CLI `resolve-error` 命令。
5. 增加成功解决、重复解决和缺失错误记录测试。
6. 执行真实错误解决命令并验证幂等。
7. 生成并提升候选版本。

# 执行过程

已新增 `resolve-error` 入口，可定位已归档运行错误并更新“是否已解决”为已解决，同时记录验证依据。候选版本已通过 `cycle` 提升为当前稳定版本，并用真实错误记录验证解决和幂等行为。

# 代码变更

1. `ErrorArchive` 新增 `resolve_run_error(version, run_id, verification)`。
2. 新增 `ErrorResolutionReport`。
3. `ErrorArchiveError` 新增已归档错误缺失和结构异常错误类型。
4. CLI 新增 `resolve-error [--current|--candidate|--version VERSION] --run-id RUN_ID [--verification TEXT]`。
5. README 和 `Agents.md` 补充错误解决命令和规则。

# 测试结果

1. `cargo fmt`：已执行并统一格式。
2. `cargo check`：通过。
3. `cargo test`：通过，30 个测试全部成功。
4. `cargo run -- advance "标记错误归档已解决"`：通过，生成候选版本 `v0.1.15`。
5. `cargo run -- cycle`：通过，已将 `v0.1.15` 提升为当前版本。
6. `cargo run -- resolve-error --version v0.1.14 --run-id run-1777557380568972400-000 --verification "v0.1.15 已通过 cargo test 与 cargo run -- validate，错误归档能力验证完成"`：通过，已标记错误为已解决。
7. 再次执行同一 `resolve-error`：通过，确认不会重复修改。
8. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮未出现功能错误。上一轮用于验证错误归档的运行错误已通过 `resolve-error` 标记为已解决。

# 评估

版本 `v0.1.15` 已完成最小闭环提升。错误记录现在支持从发现、归档到解决关闭的基本生命周期。

# 优化建议

后续可增加错误列表查询能力，用于查看未解决错误和已解决错误，避免手工扫描整个聚合文档。

# 可复用经验

对 Markdown 聚合文档做状态更新时，必须用稳定标题定位目标小节，并让重复执行保持幂等。

## v0.1.14

# 版本信息
- 版本号：v0.1.14
- 时间：unix:1777557374
- 父版本：v0.1.13

# 目标

归档失败 Runtime 运行记录

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在应用层实现失败运行错误归档。
4. 新增 CLI `record-error` 命令。
5. 增加错误归档成功、重复、边界和错误测试。
6. 修正同一 major 工作区运行索引跨小版本混读问题。
7. 执行真实失败运行并归档。
8. 生成并提升候选版本。

# 执行过程

已新增 `record-error` 归档入口，可将最近失败运行或指定运行编号追加到 `forge/errors/v0.md`。候选版本已通过 `cycle` 提升为当前稳定版本，并通过真实失败运行完成归档验证。

# 代码变更

1. 新增应用层 `ErrorArchive`、`ErrorArchiveReport` 和 `ErrorArchiveError`。
2. CLI 新增 `record-error [--current|--candidate|--version VERSION] [--run-id RUN_ID] [--stage TEXT] [--solution TEXT]`。
3. 错误归档写入当前 major 的 `forge/errors/v0.md`，不会创建小版本错误文件。
4. Runtime 运行索引查询新增版本过滤，避免同一 major 工作区内混入其他小版本记录。
5. README 和 `Agents.md` 补充错误归档命令和规则。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，28 个测试全部成功。
4. `cargo run -- advance "归档失败 Runtime 运行记录"`：通过，生成候选版本 `v0.1.14`。
5. `cargo run -- cycle`：通过，已将 `v0.1.14` 提升为当前版本。
6. `cargo run -- run --current --timeout-ms 5000 -- <selfforge> unknown-command-for-error-archive`：通过，生成失败运行记录。
7. `cargo run -- record-error --current --stage "Runtime 受控执行验证" --solution "这是功能验证用失败运行，归档能力已验证，后续无需修改程序行为。"`：通过，已追加错误归档。
8. `cargo run -- runs --current --failed --limit 5`：通过，仅显示当前版本失败运行。
9. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮发现运行索引查询会读取同一 major 工作区内其他小版本记录，已在 Runtime 查询中按 `entry.version` 过滤并补充测试。真实验证用失败运行已通过 `record-error` 追加到错误归档。

# 评估

版本 `v0.1.14` 已完成最小闭环提升。SelfForge 现在能从失败运行进入错误归档，执行审计和错误追踪之间形成了更完整的连接。

# 优化建议

后续可增加错误归档状态更新能力，例如将已修复的错误小节标记为已解决，并记录对应验证命令。

# 可复用经验

共享 major 工作区时，索引文件可以共享，但查询结果必须保留小版本边界；应用层负责认知归档，Runtime 只负责执行和记录。

## v0.1.13

# 版本信息
- 版本号：v0.1.13
- 时间：unix:1777556860
- 父版本：v0.1.12

# 目标

筛选失败 Runtime 运行记录

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 设计可扩展的运行查询过滤结构。
4. 在 Runtime 和 Supervisor 中实现失败与超时过滤。
5. 在 CLI 中新增 `runs --failed` 与 `runs --timed-out`。
6. 增加成功、失败、超时和边界测试。
7. 执行真实失败命令并验证筛选输出。
8. 生成并提升候选版本。

# 执行过程

已新增运行索引过滤能力，并通过真实失败命令确认 `runs --failed` 可以读取非零退出记录。候选版本已通过 `cycle` 提升为当前稳定版本。

# 代码变更

1. 新增 `RunQuery` 查询结构，支持最近记录、失败记录和超时记录。
2. `RunIndexEntry` 新增 `is_failed` 判断，失败包含非零退出和超时。
3. Runtime 新增 `query_runs(version, query)`，并保留 `list_runs(version, limit)` 兼容入口。
4. Supervisor 新增 `query_runs` 门面。
5. CLI `runs` 新增 `--failed` 与 `--timed-out` 参数。
6. README 和 `Agents.md` 补充失败筛选命令和规则。

# 测试结果

1. `cargo fmt`：已执行并统一格式。
2. `cargo check`：通过。
3. `cargo test`：通过，25 个测试全部成功。
4. `cargo run -- run --current --timeout-ms 5000 -- <selfforge> unknown-command-for-runtime-filter`：通过，生成非零退出运行记录。
5. `cargo run -- runs --current --failed --limit 5`：通过，可读取失败记录。
6. `cargo run -- advance "筛选失败 Runtime 运行记录"`：通过，生成候选版本 `v0.1.13`。
7. `cargo run -- cycle`：通过，已将 `v0.1.13` 提升为当前版本。
8. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮未出现功能错误。文档记录过程中曾发现任务段落插入到旧版本记录中间，已移动到正确位置。

# 评估

版本 `v0.1.13` 已完成最小闭环提升。Runtime 运行历史现在不仅可查看，也能快速筛出失败执行，为后续自动错误归档提供基础。

# 优化建议

后续可将 `runs --failed` 的结果转换为结构化错误草稿，追加到 `forge/errors/v0.md`，但需要保持人工可审阅。

# 可复用经验

查询能力应先沉入 Runtime/Supervisor，再由 CLI 暴露；文档更新要使用足够精确的上下文，避免把新版本记录插入旧版本段落。

## v0.1.12

# 版本信息
- 版本号：v0.1.12
- 时间：unix:1777556437
- 父版本：v0.1.11

# 目标

查询 Runtime 运行索引

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在 Runtime/Supervisor 层实现运行索引查询。
4. 新增 CLI `runs` 命令。
5. 增加运行索引查询测试。
6. 执行真实 `runs` 命令验证输出。
7. 生成并提升候选版本。

# 执行过程

已新增 `runs` 只读查询入口，可从 `sandbox/runs/index.jsonl` 列出最近运行记录。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. 新增 `RunIndexEntry` 结构。
2. Runtime 新增 `list_runs(version, limit)`。
3. Supervisor 新增 `list_runs` 门面。
4. CLI 新增 `runs [--current|--candidate|--version VERSION] [--limit N]`。
5. README 和 `Agents.md` 补充 `runs` 查询入口。
6. 测试验证运行索引可查询。

# 测试结果

1. `cargo fmt --check`：首次发现格式差异，执行 `cargo fmt` 后通过。
2. `cargo check`：通过。
3. `cargo test`：通过，24 个测试全部成功。
4. `cargo run -- validate`：通过，检查 30 个路径。
5. `cargo run -- runs --current --limit 3`：通过，可读取最近运行记录。
6. `cargo run -- advance "查询 Runtime 运行索引"`：通过，生成候选版本 `v0.1.12`。
7. `cargo run -- cycle`：通过，已将 `v0.1.12` 提升为当前版本。

# 错误总结

本轮未出现功能错误。格式检查发现新代码换行差异，已通过 `cargo fmt` 修复。

# 评估

版本 `v0.1.12` 已完成最小闭环提升。Runtime 执行历史现在既能落盘，也能通过 CLI 快速查看。

# 优化建议

后续可继续增加失败筛选参数，例如 `runs --failed`，用于快速定位异常执行。

# 可复用经验

索引读取应留在 Runtime/Supervisor 层，CLI 只负责参数解析和展示，避免业务逻辑散落在入口。

## v0.1.11

# 版本信息
- 版本号：v0.1.11
- 时间：unix:1777556093
- 父版本：v0.1.10

# 目标

增加 Runtime 运行索引

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 设计 `index.jsonl` 单行摘要。
4. 在每次执行记录落盘后追加索引行。
5. 增加成功执行和超时执行的索引测试。
6. 执行真实 `run` 命令验证索引落盘。
7. 生成并提升候选版本。

# 执行过程

已在 `sandbox/runs/index.jsonl` 中追加 Runtime 运行摘要索引，并通过真实 `run` 命令验证索引可读。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. Runtime 每次执行后追加 `sandbox/runs/index.jsonl`。
2. 索引行包含 `run_id`、版本、程序、参数、退出码、是否超时、输出大小和报告文件路径。
3. 测试新增成功执行索引断言。
4. 测试新增超时执行索引断言。
5. `Agents.md` 补充运行索引规则。

# 测试结果

1. `cargo fmt --check`：首次发现格式差异，执行 `cargo fmt` 后通过。
2. `cargo check`：通过。
3. `cargo test`：通过，24 个测试全部成功。
4. `cargo run -- validate`：通过，检查 30 个路径。
5. `cargo run -- run --current --timeout-ms 5000 -- <selfforge> help`：通过，`index.jsonl` 已追加运行摘要。
6. `cargo run -- advance "增加 Runtime 运行索引"`：通过，生成候选版本 `v0.1.11`。
7. `cargo run -- cycle`：通过，已将 `v0.1.11` 提升为当前版本。

# 错误总结

本轮未出现功能错误。格式检查发现测试代码换行差异，已通过 `cargo fmt` 修复。

# 评估

版本 `v0.1.11` 已完成最小闭环提升。Runtime 执行记录现在既有单次详情目录，也有可快速扫描的索引文件。

# 优化建议

后续可基于索引实现失败查询命令，或把失败运行自动汇总到 `forge/errors/v0.md`。

# 可复用经验

单次记录和索引是互补结构：目录适合深查，JSON Lines 适合增量追加和快速扫描。

## v0.1.10

# 版本信息
- 版本号：v0.1.10
- 时间：unix:1777555810
- 父版本：v0.1.9

# 目标

记录 Runtime 执行审计

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 为 Runtime 执行结果设计审计记录目录。
4. 在命令执行结束后写入结构化记录。
5. 增加成功执行和超时执行的记录测试。
6. 执行真实 `run` 命令验证记录落盘。
7. 生成并提升候选版本。

# 执行过程

已为 Runtime 执行增加 `sandbox/runs/run-*` 审计记录，并通过真实 `run` 命令验证记录落盘。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. `ExecutionReport` 新增 `run_dir` 字段。
2. Runtime 执行结束后写入 `report.json`、`stdout.txt`、`stderr.txt`。
3. CLI `run` 输出新增执行记录目录。
4. 测试新增成功执行记录和超时执行记录断言。
5. `Agents.md` 补充 Runtime 执行记录规则。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，24 个测试全部成功。
4. `cargo run -- validate`：通过，检查 30 个路径。
5. `cargo run -- run --current --timeout-ms 5000 -- <selfforge> help`：通过，已写入 `sandbox/runs/run-*`。
6. `cargo run -- advance "记录 Runtime 执行审计"`：通过，生成候选版本 `v0.1.10`。
7. `cargo run -- cycle`：通过，已将 `v0.1.10` 提升为当前版本。

# 错误总结

首次测试中发现路径断言未考虑 canonical workspace 路径，已调整为校验运行记录位于返回的 workspace 下。修复后测试通过。

# 评估

版本 `v0.1.10` 已完成最小闭环提升。Runtime 受控执行现在具备本地审计记录，后续错误分析不再只依赖进程返回值。

# 优化建议

后续可增加运行记录摘要索引，并把关键失败摘要自动追加到 `forge/errors/v0.md`。

# 可复用经验

执行类能力必须同时返回结果和落盘记录；测试应以 Runtime 返回的规范化路径为准。

## v0.1.9

# 版本信息
- 版本号：v0.1.9
- 时间：unix:1777480393
- 父版本：v0.1.8

# 目标

添加简洁 README 并禁止 Emoji

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 添加简洁根目录 README。
4. 将禁止 Emoji 写入代理规则。
5. 在 Markdown 文档校验中拒绝 Emoji。
6. 增加 Emoji 文档错误测试。
7. 生成并提升候选版本。

# 执行过程

已添加简洁根目录 README，并将 Markdown Emoji 校验纳入 SelfForge 验证流程。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. 新增根目录 `README.md`，内容保持简洁。
2. `layout` 将根目录 `README.md` 纳入必需文件和初始化模板。
3. `documentation` 新增 Emoji 检测，Markdown 文档包含 Emoji 时验证失败。
4. `Agents.md` 新增禁止 Emoji 和 README 简洁规则。
5. 测试新增 Emoji 文档错误用例。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，24 个测试全部成功。
4. `cargo run -- validate`：通过，检查 30 个路径。
5. `cargo run -- advance "添加简洁 README 并禁止 Emoji"`：通过，生成候选版本 `v0.1.9`。
6. `cargo run -- cycle`：通过，已将 `v0.1.9` 提升为当前版本。

# 错误总结

本轮未出现新增实现错误。新增错误测试验证 Markdown 文档包含 Emoji 时会被拒绝。

# 评估

版本 `v0.1.9` 已完成最小闭环提升。README 简洁可读，Emoji 禁止规则已经从文档约定变为可测试校验。

# 优化建议

后续可把禁止 Emoji 的范围从 Markdown 扩展到更多用户可见文本，例如 CLI 输出模板和提交信息检查。

# 可复用经验

规则只写在提示词里不够，能自动验证的规则应该进入测试和校验流程。

## v0.1.8

# 版本信息
- 版本号：v0.1.8
- 时间：unix:1777479846
- 父版本：v0.1.7

# 目标

设计并约束 workspace 内部目录结构

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 设计 workspace 内部固定分区。
4. 在 layout 中创建并验证 workspace 骨架。
5. 增加防止 workspace 根目录杂乱的测试。
6. 更新中文规则和工作区 README。
7. 生成并提升候选版本。

# 执行过程

已为 `workspaces/v0` 创建固定内部骨架，并通过 layout 验证禁止未登记文件直接出现在 workspace 根目录。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. `layout` 新增 workspace 固定目录：`source/`、`tests/`、`sandbox/`、`artifacts/`、`logs/`。
2. `layout` 新增 workspace 根目录白名单校验，拒绝未规划的根目录文件或目录。
3. `workspace` 新增 `.gitignore` 和各分区中文 README。
4. 测试新增 workspace 骨架断言和根目录杂散文件错误测试。
5. `Agents.md` 与架构文档补充 workspace 内部分区规则。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，23 个测试全部成功。
4. `cargo run -- init`：通过，创建 workspace 内部骨架。
5. `cargo run -- advance "设计并约束 workspace 内部目录结构"`：通过，生成候选版本 `v0.1.8`。
6. `cargo run -- cycle`：通过，已将 `v0.1.8` 提升为当前版本。
7. `cargo run -- validate`：通过，检查 29 个路径。

# 错误总结

本轮未出现新增功能错误。新增错误测试验证了 workspace 根目录杂散文件会被拒绝。

# 评估

版本 `v0.1.8` 已完成最小闭环提升。workspace 根目录现在只有固定入口，后续文件必须进入明确职责目录继续分层，能降低大版本聚合后文件过多的风险。

# 优化建议

后续可继续为 `sandbox` 增加按 run id 创建执行目录的运行时 API，并把资源限制记录写入 `forge/errors` 或 `forge/memory`。

# 可复用经验

major workspace 必须配合内部职责分区，否则虽然减少了小版本目录，却会把复杂度转移到单个 workspace 根目录。

## v0.1.7

# 版本信息
- 版本号：v0.1.7
- 时间：unix:1777479352
- 父版本：v0.1.6

# 目标

按大版本聚合小版本目录和归档

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 生成或复用 major 工作区与聚合归档文件。
4. 追加候选版本记忆、任务、错误和版本记录。
5. 验证候选版本布局和中文文档规范。
6. 将 state/state.json 更新为 candidate_prepared。
7. 候选验证成功后执行 cycle，将版本提升为 active。

# 执行过程

已按 major 聚合规则生成候选记录，未创建新的小版本工作区目录，并已通过 cycle 提升为当前版本。

# 代码变更

1. 将版本布局从小版本目录切换为 major 聚合布局：`workspaces/v0` 和 `forge/*/v0.md`。
2. 让 Runtime 执行 `v0.1.x` 时映射到 `workspaces/v0`。
3. 让 Evolution 生成候选版本时只追加 major 聚合文档，不创建 `v0.1.7` 级目录或文件。
4. 新增旧状态规范化逻辑，将旧的 `workspaces/v0.1.x` 状态迁移为 `workspaces/v0`。
5. 更新中文规则文档，明确小版本只追加记录。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，22 个测试全部成功。
4. `cargo run -- init`：通过，创建 major 聚合结构。
5. `cargo run -- advance "按大版本聚合小版本目录和归档"`：通过，生成候选版本 `v0.1.7`。
6. `cargo run -- validate`：通过，检查 18 个路径。
7. `cargo run -- cycle`：通过，已将 `v0.1.7` 从候选版本提升为当前版本。

# 错误总结

本轮发现 1 个编译阶段错误和 1 个格式检查问题，均已解决并记录在 `forge/errors/v0.md`。

# 评估

版本 `v0.1.7` 已生成、验证并提升为当前版本。新规则满足“不要每个小版本都生成一个文件夹”的要求，未来小版本会复用 major 工作区和 major 归档文件。

# 优化建议

后续可在确认迁移策略后清理旧版小版本目录和文件；清理前应先生成迁移记录，避免丢失历史审计链路。

# 可复用经验

小版本记录应追加到 major 聚合文件，避免目录数量随 patch 增长。旧状态字段也需要在进化流程中被主动规范化，避免新旧目录规则混用。

