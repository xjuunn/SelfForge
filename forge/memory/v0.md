# v0 记忆记录

# 记录规则

- 本文件集中记录 v0 大版本下的小版本记忆。
- 每次 patch 或 minor 更新只追加一个版本小节，禁止创建新的小版本记忆文件。

## v0.1.7

# 版本信息
- 版本号：v0.1.7
- 时间：unix:1777479352
- 父版本：v0.1.6

# 目标

按大版本聚合小版本目录和归档

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 生成或复用 major 工作区与聚合归档文件。
4. 追加候选版本记忆、任务、错误和版本记录。
5. 验证候选版本布局和中文文档规范。
6. 将 state/state.json 更新为 candidate_prepared。
7. 候选验证成功后执行 cycle，将版本提升为 active。

# 执行过程

已按 major 聚合规则生成候选记录，未创建新的小版本工作区目录，并已通过 cycle 提升为当前版本。

# 代码变更

1. 将版本布局从小版本目录切换为 major 聚合布局：`workspaces/v0` 和 `forge/*/v0.md`。
2. 让 Runtime 执行 `v0.1.x` 时映射到 `workspaces/v0`。
3. 让 Evolution 生成候选版本时只追加 major 聚合文档，不创建 `v0.1.7` 级目录或文件。
4. 新增旧状态规范化逻辑，将旧的 `workspaces/v0.1.x` 状态迁移为 `workspaces/v0`。
5. 更新中文规则文档，明确小版本只追加记录。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，22 个测试全部成功。
4. `cargo run -- init`：通过，创建 major 聚合结构。
5. `cargo run -- advance "按大版本聚合小版本目录和归档"`：通过，生成候选版本 `v0.1.7`。
6. `cargo run -- validate`：通过，检查 18 个路径。
7. `cargo run -- cycle`：通过，已将 `v0.1.7` 从候选版本提升为当前版本。

# 错误总结

本轮发现 1 个编译阶段错误和 1 个格式检查问题，均已解决并记录在 `forge/errors/v0.md`。

# 评估

版本 `v0.1.7` 已生成、验证并提升为当前版本。新规则满足“不要每个小版本都生成一个文件夹”的要求，未来小版本会复用 major 工作区和 major 归档文件。

# 优化建议

后续可在确认迁移策略后清理旧版小版本目录和文件；清理前应先生成迁移记录，避免丢失历史审计链路。

# 可复用经验

小版本记录应追加到 major 聚合文件，避免目录数量随 patch 增长。旧状态字段也需要在进化流程中被主动规范化，避免新旧目录规则混用。

## v0.1.8

# 版本信息
- 版本号：v0.1.8
- 时间：unix:1777479846
- 父版本：v0.1.7

# 目标

设计并约束 workspace 内部目录结构

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 设计 workspace 内部固定分区。
4. 在 layout 中创建并验证 workspace 骨架。
5. 增加防止 workspace 根目录杂乱的测试。
6. 更新中文规则和工作区 README。
7. 生成并提升候选版本。

# 执行过程

已为 `workspaces/v0` 创建固定内部骨架，并通过 layout 验证禁止未登记文件直接出现在 workspace 根目录。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. `layout` 新增 workspace 固定目录：`source/`、`tests/`、`sandbox/`、`artifacts/`、`logs/`。
2. `layout` 新增 workspace 根目录白名单校验，拒绝未规划的根目录文件或目录。
3. `workspace` 新增 `.gitignore` 和各分区中文 README。
4. 测试新增 workspace 骨架断言和根目录杂散文件错误测试。
5. `Agents.md` 与架构文档补充 workspace 内部分区规则。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，23 个测试全部成功。
4. `cargo run -- init`：通过，创建 workspace 内部骨架。
5. `cargo run -- advance "设计并约束 workspace 内部目录结构"`：通过，生成候选版本 `v0.1.8`。
6. `cargo run -- cycle`：通过，已将 `v0.1.8` 提升为当前版本。
7. `cargo run -- validate`：通过，检查 29 个路径。

# 错误总结

本轮未出现新增功能错误。新增错误测试验证了 workspace 根目录杂散文件会被拒绝。

# 评估

版本 `v0.1.8` 已完成最小闭环提升。workspace 根目录现在只有固定入口，后续文件必须进入明确职责目录继续分层，能降低大版本聚合后文件过多的风险。

# 优化建议

后续可继续为 `sandbox` 增加按 run id 创建执行目录的运行时 API，并把资源限制记录写入 `forge/errors` 或 `forge/memory`。

# 可复用经验

major workspace 必须配合内部职责分区，否则虽然减少了小版本目录，却会把复杂度转移到单个 workspace 根目录。

## v0.1.9

# 版本信息
- 版本号：v0.1.9
- 时间：unix:1777480393
- 父版本：v0.1.8

# 目标

添加简洁 README 并禁止 Emoji

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 添加简洁根目录 README。
4. 将禁止 Emoji 写入代理规则。
5. 在 Markdown 文档校验中拒绝 Emoji。
6. 增加 Emoji 文档错误测试。
7. 生成并提升候选版本。

# 执行过程

已添加简洁根目录 README，并将 Markdown Emoji 校验纳入 SelfForge 验证流程。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. 新增根目录 `README.md`，内容保持简洁。
2. `layout` 将根目录 `README.md` 纳入必需文件和初始化模板。
3. `documentation` 新增 Emoji 检测，Markdown 文档包含 Emoji 时验证失败。
4. `Agents.md` 新增禁止 Emoji 和 README 简洁规则。
5. 测试新增 Emoji 文档错误用例。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，24 个测试全部成功。
4. `cargo run -- validate`：通过，检查 30 个路径。
5. `cargo run -- advance "添加简洁 README 并禁止 Emoji"`：通过，生成候选版本 `v0.1.9`。
6. `cargo run -- cycle`：通过，已将 `v0.1.9` 提升为当前版本。

# 错误总结

本轮未出现新增实现错误。新增错误测试验证 Markdown 文档包含 Emoji 时会被拒绝。

# 评估

版本 `v0.1.9` 已完成最小闭环提升。README 简洁可读，Emoji 禁止规则已经从文档约定变为可测试校验。

# 优化建议

后续可把禁止 Emoji 的范围从 Markdown 扩展到更多用户可见文本，例如 CLI 输出模板和提交信息检查。

# 可复用经验

规则只写在提示词里不够，能自动验证的规则应该进入测试和校验流程。

## v0.1.10

# 版本信息
- 版本号：v0.1.10
- 时间：unix:1777555810
- 父版本：v0.1.9

# 目标

记录 Runtime 执行审计

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 为 Runtime 执行结果设计审计记录目录。
4. 在命令执行结束后写入结构化记录。
5. 增加成功执行和超时执行的记录测试。
6. 执行真实 `run` 命令验证记录落盘。
7. 生成并提升候选版本。

# 执行过程

已为 Runtime 执行增加 `sandbox/runs/run-*` 审计记录，并通过真实 `run` 命令验证记录落盘。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. `ExecutionReport` 新增 `run_dir` 字段。
2. Runtime 执行结束后写入 `report.json`、`stdout.txt`、`stderr.txt`。
3. CLI `run` 输出新增执行记录目录。
4. 测试新增成功执行记录和超时执行记录断言。
5. `Agents.md` 补充 Runtime 执行记录规则。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，24 个测试全部成功。
4. `cargo run -- validate`：通过，检查 30 个路径。
5. `cargo run -- run --current --timeout-ms 5000 -- <selfforge> help`：通过，已写入 `sandbox/runs/run-*`。
6. `cargo run -- advance "记录 Runtime 执行审计"`：通过，生成候选版本 `v0.1.10`。
7. `cargo run -- cycle`：通过，已将 `v0.1.10` 提升为当前版本。

# 错误总结

首次测试中发现路径断言未考虑 canonical workspace 路径，已调整为校验运行记录位于返回的 workspace 下。修复后测试通过。

# 评估

版本 `v0.1.10` 已完成最小闭环提升。Runtime 受控执行现在具备本地审计记录，后续错误分析不再只依赖进程返回值。

# 优化建议

后续可增加运行记录摘要索引，并把关键失败摘要自动追加到 `forge/errors/v0.md`。

# 可复用经验

执行类能力必须同时返回结果和落盘记录；测试应以 Runtime 返回的规范化路径为准。

## v0.1.11

# 版本信息
- 版本号：v0.1.11
- 时间：unix:1777556093
- 父版本：v0.1.10

# 目标

增加 Runtime 运行索引

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 设计 `index.jsonl` 单行摘要。
4. 在每次执行记录落盘后追加索引行。
5. 增加成功执行和超时执行的索引测试。
6. 执行真实 `run` 命令验证索引落盘。
7. 生成并提升候选版本。

# 执行过程

已在 `sandbox/runs/index.jsonl` 中追加 Runtime 运行摘要索引，并通过真实 `run` 命令验证索引可读。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. Runtime 每次执行后追加 `sandbox/runs/index.jsonl`。
2. 索引行包含 `run_id`、版本、程序、参数、退出码、是否超时、输出大小和报告文件路径。
3. 测试新增成功执行索引断言。
4. 测试新增超时执行索引断言。
5. `Agents.md` 补充运行索引规则。

# 测试结果

1. `cargo fmt --check`：首次发现格式差异，执行 `cargo fmt` 后通过。
2. `cargo check`：通过。
3. `cargo test`：通过，24 个测试全部成功。
4. `cargo run -- validate`：通过，检查 30 个路径。
5. `cargo run -- run --current --timeout-ms 5000 -- <selfforge> help`：通过，`index.jsonl` 已追加运行摘要。
6. `cargo run -- advance "增加 Runtime 运行索引"`：通过，生成候选版本 `v0.1.11`。
7. `cargo run -- cycle`：通过，已将 `v0.1.11` 提升为当前版本。

# 错误总结

本轮未出现功能错误。格式检查发现测试代码换行差异，已通过 `cargo fmt` 修复。

# 评估

版本 `v0.1.11` 已完成最小闭环提升。Runtime 执行记录现在既有单次详情目录，也有可快速扫描的索引文件。

# 优化建议

后续可基于索引实现失败查询命令，或把失败运行自动汇总到 `forge/errors/v0.md`。

# 可复用经验

单次记录和索引是互补结构：目录适合深查，JSON Lines 适合增量追加和快速扫描。

## v0.1.12

# 版本信息
- 版本号：v0.1.12
- 时间：unix:1777556437
- 父版本：v0.1.11

# 目标

查询 Runtime 运行索引

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在 Runtime/Supervisor 层实现运行索引查询。
4. 新增 CLI `runs` 命令。
5. 增加运行索引查询测试。
6. 执行真实 `runs` 命令验证输出。
7. 生成并提升候选版本。

# 执行过程

已新增 `runs` 只读查询入口，可从 `sandbox/runs/index.jsonl` 列出最近运行记录。候选版本已通过 cycle 提升为当前版本。

# 代码变更

1. 新增 `RunIndexEntry` 结构。
2. Runtime 新增 `list_runs(version, limit)`。
3. Supervisor 新增 `list_runs` 门面。
4. CLI 新增 `runs [--current|--candidate|--version VERSION] [--limit N]`。
5. README 和 `Agents.md` 补充 `runs` 查询入口。
6. 测试验证运行索引可查询。

# 测试结果

1. `cargo fmt --check`：首次发现格式差异，执行 `cargo fmt` 后通过。
2. `cargo check`：通过。
3. `cargo test`：通过，24 个测试全部成功。
4. `cargo run -- validate`：通过，检查 30 个路径。
5. `cargo run -- runs --current --limit 3`：通过，可读取最近运行记录。
6. `cargo run -- advance "查询 Runtime 运行索引"`：通过，生成候选版本 `v0.1.12`。
7. `cargo run -- cycle`：通过，已将 `v0.1.12` 提升为当前版本。

# 错误总结

本轮未出现功能错误。格式检查发现新代码换行差异，已通过 `cargo fmt` 修复。

# 评估

版本 `v0.1.12` 已完成最小闭环提升。Runtime 执行历史现在既能落盘，也能通过 CLI 快速查看。

# 优化建议

后续可继续增加失败筛选参数，例如 `runs --failed`，用于快速定位异常执行。

# 可复用经验

索引读取应留在 Runtime/Supervisor 层，CLI 只负责参数解析和展示，避免业务逻辑散落在入口。

## v0.1.13

# 版本信息
- 版本号：v0.1.13
- 时间：unix:1777556860
- 父版本：v0.1.12

# 目标

筛选失败 Runtime 运行记录

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 设计可扩展的运行查询过滤结构。
4. 在 Runtime 和 Supervisor 中实现失败与超时过滤。
5. 在 CLI 中新增 `runs --failed` 与 `runs --timed-out`。
6. 增加成功、失败、超时和边界测试。
7. 执行真实失败命令并验证筛选输出。
8. 生成并提升候选版本。

# 执行过程

已新增运行索引过滤能力，并通过真实失败命令确认 `runs --failed` 可以读取非零退出记录。候选版本已通过 `cycle` 提升为当前稳定版本。

# 代码变更

1. 新增 `RunQuery` 查询结构，支持最近记录、失败记录和超时记录。
2. `RunIndexEntry` 新增 `is_failed` 判断，失败包含非零退出和超时。
3. Runtime 新增 `query_runs(version, query)`，并保留 `list_runs(version, limit)` 兼容入口。
4. Supervisor 新增 `query_runs` 门面。
5. CLI `runs` 新增 `--failed` 与 `--timed-out` 参数。
6. README 和 `Agents.md` 补充失败筛选命令和规则。

# 测试结果

1. `cargo fmt`：已执行并统一格式。
2. `cargo check`：通过。
3. `cargo test`：通过，25 个测试全部成功。
4. `cargo run -- run --current --timeout-ms 5000 -- <selfforge> unknown-command-for-runtime-filter`：通过，生成非零退出运行记录。
5. `cargo run -- runs --current --failed --limit 5`：通过，可读取失败记录。
6. `cargo run -- advance "筛选失败 Runtime 运行记录"`：通过，生成候选版本 `v0.1.13`。
7. `cargo run -- cycle`：通过，已将 `v0.1.13` 提升为当前版本。
8. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮未出现功能错误。文档记录过程中曾发现任务段落插入到旧版本记录中间，已移动到正确位置。

# 评估

版本 `v0.1.13` 已完成最小闭环提升。Runtime 运行历史现在不仅可查看，也能快速筛出失败执行，为后续自动错误归档提供基础。

# 优化建议

后续可将 `runs --failed` 的结果转换为结构化错误草稿，追加到 `forge/errors/v0.md`，但需要保持人工可审阅。

# 可复用经验

查询能力应先沉入 Runtime/Supervisor，再由 CLI 暴露；文档更新要使用足够精确的上下文，避免把新版本记录插入旧版本段落。

## v0.1.14

# 版本信息
- 版本号：v0.1.14
- 时间：unix:1777557374
- 父版本：v0.1.13

# 目标

归档失败 Runtime 运行记录

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在应用层实现失败运行错误归档。
4. 新增 CLI `record-error` 命令。
5. 增加错误归档成功、重复、边界和错误测试。
6. 修正同一 major 工作区运行索引跨小版本混读问题。
7. 执行真实失败运行并归档。
8. 生成并提升候选版本。

# 执行过程

已新增 `record-error` 归档入口，可将最近失败运行或指定运行编号追加到 `forge/errors/v0.md`。候选版本已通过 `cycle` 提升为当前稳定版本，并通过真实失败运行完成归档验证。

# 代码变更

1. 新增应用层 `ErrorArchive`、`ErrorArchiveReport` 和 `ErrorArchiveError`。
2. CLI 新增 `record-error [--current|--candidate|--version VERSION] [--run-id RUN_ID] [--stage TEXT] [--solution TEXT]`。
3. 错误归档写入当前 major 的 `forge/errors/v0.md`，不会创建小版本错误文件。
4. Runtime 运行索引查询新增版本过滤，避免同一 major 工作区内混入其他小版本记录。
5. README 和 `Agents.md` 补充错误归档命令和规则。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，28 个测试全部成功。
4. `cargo run -- advance "归档失败 Runtime 运行记录"`：通过，生成候选版本 `v0.1.14`。
5. `cargo run -- cycle`：通过，已将 `v0.1.14` 提升为当前版本。
6. `cargo run -- run --current --timeout-ms 5000 -- <selfforge> unknown-command-for-error-archive`：通过，生成失败运行记录。
7. `cargo run -- record-error --current --stage "Runtime 受控执行验证" --solution "这是功能验证用失败运行，归档能力已验证，后续无需修改程序行为。"`：通过，已追加错误归档。
8. `cargo run -- runs --current --failed --limit 5`：通过，仅显示当前版本失败运行。
9. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮发现运行索引查询会读取同一 major 工作区内其他小版本记录，已在 Runtime 查询中按 `entry.version` 过滤并补充测试。真实验证用失败运行已通过 `record-error` 追加到错误归档。

# 评估

版本 `v0.1.14` 已完成最小闭环提升。SelfForge 现在能从失败运行进入错误归档，执行审计和错误追踪之间形成了更完整的连接。

# 优化建议

后续可增加错误归档状态更新能力，例如将已修复的错误小节标记为已解决，并记录对应验证命令。

# 可复用经验

共享 major 工作区时，索引文件可以共享，但查询结果必须保留小版本边界；应用层负责认知归档，Runtime 只负责执行和记录。

## v0.1.15

# 版本信息
- 版本号：v0.1.15
- 时间：unix:1777557750
- 父版本：v0.1.14

# 目标

标记错误归档已解决

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在应用层实现错误解决状态更新。
4. 新增 CLI `resolve-error` 命令。
5. 增加成功解决、重复解决和缺失错误记录测试。
6. 执行真实错误解决命令并验证幂等。
7. 生成并提升候选版本。

# 执行过程

已新增 `resolve-error` 入口，可定位已归档运行错误并更新“是否已解决”为已解决，同时记录验证依据。候选版本已通过 `cycle` 提升为当前稳定版本，并用真实错误记录验证解决和幂等行为。

# 代码变更

1. `ErrorArchive` 新增 `resolve_run_error(version, run_id, verification)`。
2. 新增 `ErrorResolutionReport`。
3. `ErrorArchiveError` 新增已归档错误缺失和结构异常错误类型。
4. CLI 新增 `resolve-error [--current|--candidate|--version VERSION] --run-id RUN_ID [--verification TEXT]`。
5. README 和 `Agents.md` 补充错误解决命令和规则。

# 测试结果

1. `cargo fmt`：已执行并统一格式。
2. `cargo check`：通过。
3. `cargo test`：通过，30 个测试全部成功。
4. `cargo run -- advance "标记错误归档已解决"`：通过，生成候选版本 `v0.1.15`。
5. `cargo run -- cycle`：通过，已将 `v0.1.15` 提升为当前版本。
6. `cargo run -- resolve-error --version v0.1.14 --run-id run-1777557380568972400-000 --verification "v0.1.15 已通过 cargo test 与 cargo run -- validate，错误归档能力验证完成"`：通过，已标记错误为已解决。
7. 再次执行同一 `resolve-error`：通过，确认不会重复修改。
8. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮未出现功能错误。上一轮用于验证错误归档的运行错误已通过 `resolve-error` 标记为已解决。

# 评估

版本 `v0.1.15` 已完成最小闭环提升。错误记录现在支持从发现、归档到解决关闭的基本生命周期。

# 优化建议

后续可增加错误列表查询能力，用于查看未解决错误和已解决错误，避免手工扫描整个聚合文档。

# 可复用经验

对 Markdown 聚合文档做状态更新时，必须用稳定标题定位目标小节，并让重复执行保持幂等。

## v0.1.16

# 版本信息
- 版本号：v0.1.16
- 时间：unix:1777558440
- 父版本：v0.1.15

# 目标

查询错误归档列表

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在应用层实现错误列表查询。
4. 新增 CLI `errors` 命令。
5. 增加未解决、已解决、空限制和版本过滤测试。
6. 执行真实错误列表查询命令。
7. 生成并提升候选版本。

# 执行过程

已新增 `errors` 只读查询入口，可按版本、解决状态和数量限制查询已归档运行错误。候选版本已通过 `cycle` 提升为当前稳定版本，并通过真实命令验证已解决和未解决查询。

# 代码变更

1. 新增 `ArchivedErrorEntry` 错误列表条目。
2. 新增 `ErrorListQuery` 查询条件。
3. `ErrorArchive` 新增 `list_run_errors(version, query)`。
4. CLI 新增 `errors [--current|--candidate|--version VERSION] [--limit N] [--open] [--resolved]`。
5. README 和 `Agents.md` 补充错误查询命令和规则。

# 测试结果

1. `cargo fmt`：已执行并统一格式。
2. `cargo check`：通过。
3. `cargo test`：通过，31 个测试全部成功。
4. `cargo run -- advance "查询错误归档列表"`：通过，生成候选版本 `v0.1.16`。
5. `cargo run -- cycle`：通过，已将 `v0.1.16` 提升为当前版本。
6. `cargo run -- errors --version v0.1.14 --resolved --limit 5`：通过，可读取已解决错误。
7. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。
8. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮未出现功能错误。

# 评估

版本 `v0.1.16` 已完成最小闭环提升。错误记录现在支持归档、解决和查询，基本错误生命周期更完整。

# 优化建议

后续可将错误查询结果用于 `advance` 的前置检查：存在未解决错误时要求先修复或显式记录继续原因。

# 可复用经验

只读查询能力应从聚合文档解析，不应为了方便查询创建额外索引；状态过滤要保持版本边界。

## v0.1.17

# 版本信息
- 版本号：v0.1.17
- 时间：unix:1777558865
- 父版本：v0.1.16

# 目标

阻断未解决错误进化

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 在 `advance` 开始阶段查询当前版本未解决错误。
4. 存在未解决错误时返回明确错误并保持状态不变。
5. 保持无未解决错误时的原有推进行为。
6. 增加未解决错误阻断测试。
7. 执行真实 `errors --current --open` 和 `advance` 验证。
8. 生成并提升候选版本。

# 执行过程

已将未解决错误前置检查接入 `SelfForgeApp::advance`。当前版本无未解决错误时，真实 `advance` 和 `cycle` 已将候选版本提升为当前稳定版本。

# 代码变更

1. `SelfForgeApp::advance` 新增未解决错误检查。
2. `MinimalLoopError` 新增 `ErrorArchive` 和 `OpenErrors` 错误类型。
3. `app` 与 `lib` 导出 `MinimalLoopError`。
4. `Agents.md` 补充 `advance` 前置检查规则。

# 测试结果

1. `cargo fmt --check`：通过。
2. `cargo check`：通过。
3. `cargo test`：通过，32 个测试全部成功。
4. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。
5. `cargo run -- advance "阻断未解决错误进化"`：通过，生成候选版本 `v0.1.17`。
6. `cargo run -- cycle`：通过，已将 `v0.1.17` 提升为当前版本。
7. `cargo run -- validate`：通过，检查 30 个路径。

# 错误总结

本轮测试阶段发现一处 Rust 模式匹配字段绑定写法错误，已修正后重新运行测试通过。

# 评估

版本 `v0.1.17` 已完成最小闭环提升。系统现在会在进化前检查未解决错误，降低带病生成候选版本的风险。

# 优化建议

后续可增加显式的人工豁免机制，但默认仍应保持阻断策略。

# 可复用经验

安全前置检查应进入应用用例层，而不是分散在 CLI；测试中需要同时验证阻断结果和状态未被修改。

## v0.1.18

# 版本信息
- 版本号：v0.1.18
- 时间：unix:1777559770
- 父版本：v0.1.17

# 目标

新增进化前置检查命令

# 计划（Plan）

1. 读取最近版本记忆、任务记录和持久化状态。
2. 在应用层设计只读 `PreflightReport`。
3. 新增 `SelfForgeApp::preflight`，汇总状态、布局验证、候选版本和未解决错误。
4. 新增 CLI `preflight` 命令，输出进化前风险摘要。
5. 增加干净状态、候选状态和未解决错误状态测试。
6. 执行格式、编译、测试和真实命令验证。
7. 通过 `advance` 和 `cycle` 完成候选生成与提升。

# 执行过程

已新增显式 `preflight` 前置检查入口。该入口只读取状态并执行验证，不修改 `state/state.json`，可在进化前显示当前版本、候选版本、验证路径数量、未解决错误数量和是否可继续进化。本轮真实闭环已将 `v0.1.18` 提升为当前稳定版本。

# 代码变更

1. `SelfForgeApp` 新增 `preflight` 方法。
2. 新增 `PreflightReport`，包含当前版本、当前工作区、状态、候选版本、验证路径、未解决错误和可进化标记。
3. `MinimalLoopError` 新增 `Forge` 分支，用于承载布局验证错误。
4. CLI 新增 `preflight` 命令。
5. `app` 与 `lib` 导出 `PreflightReport`。
6. `README.md` 与 `Agents.md` 补充 `preflight` 使用规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，35 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.17` 可进化。
6. `cargo run -- advance "新增进化前置检查命令"`：通过，生成候选版本 `v0.1.18`。
7. `cargo run -- cycle`：通过，已将 `v0.1.18` 提升为当前版本。
8. `cargo run -- preflight`：通过，提升后显示 `v0.1.18` 可进化。
9. `cargo run -- validate`：通过，检查 30 个路径。
10. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

本轮未发现功能错误。测试和真实闭环均通过。

# 评估

版本 `v0.1.18` 已完成最小闭环提升。系统现在具备显式进化前置检查能力，`advance` 前的风险状态更容易被人和后续自动流程读取。

# 优化建议

后续可将 `preflight` 报告扩展为结构化输出格式，用于自动代理在进入 `advance` 前进行机器判定。

# 可复用经验

只读检查入口应放在应用用例层，并保证不修改状态；CLI 只负责展示结果。前置检查应优先复用已有的验证和错误查询能力，避免产生平行规则。

## v0.1.19

# 版本信息
- 版本号：v0.1.19
- 时间：unix:1777560286
- 父版本：v0.1.18

# 目标

新增 AI 提供商环境变量配置

# 计划（Plan）

1. 读取最近版本记忆、任务记录和持久化状态。
2. 设计 AI 提供商配置注册表，集中管理 OpenAI、DeepSeek、Gemini。
3. 从环境变量读取 API Key、模型和基础地址，并只记录密钥来源变量名。
4. 支持 `SELFFORGE_AI_PROVIDER` 指定提供商；未指定时按已配置密钥自动选择。
5. 新增 CLI `ai-config` 只读检查命令。
6. 增加自动选择、指定提供商、Gemini 双变量优先级、未知提供商错误测试。
7. 执行格式、编译、测试、前置检查、AI 配置检查、候选生成、提升和验证。

# 执行过程

已新增 AI 提供商配置层，支持通过环境变量识别 OpenAI、DeepSeek 和 Gemini。`ai-config` 可以显示提供商是否就绪、选中来源、模型、基础地址、协议和请求路径，但不会输出真实 API Key。本轮真实闭环已将 `v0.1.19` 提升为当前稳定版本。

# 代码变更

1. 新增 `src/app/ai_provider.rs`，包含 `AiProviderRegistry`、`AiConfigReport`、`AiProviderStatus` 和 `AiConfigError`。
2. `SelfForgeApp` 新增 `ai_config` 应用入口。
3. CLI 新增 `ai-config` 命令。
4. 支持 `OPENAI_API_KEY`、`DEEPSEEK_API_KEY`、`GEMINI_API_KEY` 和 `GOOGLE_API_KEY`。
5. 支持 `SELFFORGE_AI_PROVIDER` 指定 `openai`、`deepseek` 或 `gemini`。
6. 支持提供商模型和基础地址环境变量覆盖。
7. 新增 `.env.example`，并在 `.gitignore` 中忽略真实 `.env`。
8. `README.md` 与 `Agents.md` 补充 AI 配置规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，39 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.18` 可进化。
6. `cargo run -- ai-config`：通过，无密钥时显示未就绪且不输出密钥。
7. 使用临时 `SELFFORGE_AI_PROVIDER=deepseek` 与 `DEEPSEEK_API_KEY` 执行 `cargo run -- ai-config`：通过，显示选中 DeepSeek 且不输出密钥值。
8. `cargo run -- advance "新增 AI 提供商环境变量配置"`：通过，生成候选版本 `v0.1.19`。
9. `cargo run -- cycle`：通过，已将 `v0.1.19` 提升为当前版本。
10. `cargo run -- preflight`：通过，提升后显示 `v0.1.19` 可进化。
11. `cargo run -- validate`：通过，检查 30 个路径。
12. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

本轮未发现功能错误。测试和真实闭环均通过。

# 评估

版本 `v0.1.19` 已完成最小闭环提升。系统已经具备主流 AI API 的安全配置入口，为后续 Agent 调用、模型路由和执行计划生成打下基础。

# 优化建议

后续可在配置层之上增加统一 AI 调用接口，先支持非流式文本生成，再逐步扩展工具调用、结构化输出和流式响应。

# 可复用经验

密钥处理必须与日志、状态和文档隔离；可审计输出只记录密钥来源变量名和是否存在。AI 提供商差异应集中在注册表中，避免后续 Agent 功能重复编码不同厂商规则。

## v0.1.20

# 版本信息
- 版本号：v0.1.20
- 时间：unix:1777561348
- 父版本：v0.1.19

# 目标

新增 AI 统一请求构建

# 计划（Plan）

1. 读取最近版本记忆、任务记录和 AI 配置实现。
2. 新增统一 AI 请求规格结构。
3. 基于 OpenAI、DeepSeek、Gemini 的协议差异构建请求地址、认证头摘要和 JSON 请求体。
4. 新增 `SelfForgeApp::ai_request` 应用入口。
5. 新增 CLI `ai-request [prompt]`，输出脱敏请求摘要。
6. 增加 OpenAI、DeepSeek、Gemini、缺少密钥、空提示词测试。
7. 执行格式、编译、测试、前置检查、请求构建检查、候选生成、提升和验证。

# 执行过程

已在 AI 配置层之上新增统一请求构建能力。系统现在可以根据当前选中提供商构建 OpenAI Responses、DeepSeek Chat Completions 和 Gemini GenerateContent 请求规格，并通过 `ai-request` 输出脱敏摘要。本轮真实闭环已将 `v0.1.20` 提升为当前稳定版本。

# 代码变更

1. 新增 `AiRequestSpec`，包含提供商、模型、协议、方法、地址、认证头名、密钥变量名、内容类型和 JSON 请求体。
2. 新增 `AiRequestError`，覆盖配置错误、缺少提供商、缺少 API Key 和空提示词。
3. `AiProviderRegistry` 新增 `build_text_request_env` 和 `build_text_request_with`。
4. `SelfForgeApp` 新增 `ai_request` 应用入口。
5. CLI 新增 `ai-request [prompt]` 命令。
6. `README.md` 与 `Agents.md` 补充 AI 请求构建规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，44 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.19` 可进化。
6. `cargo run -- ai-config`：通过，无密钥时显示未就绪且不输出密钥。
7. 使用临时 Gemini 配置执行 `cargo run -- ai-request "生成一个最小测试计划"`：通过，输出脱敏请求摘要。
8. 使用临时 DeepSeek 配置执行 `cargo run -- ai-request "分析一个错误"`：通过，输出脱敏请求摘要。
9. `cargo run -- advance "新增 AI 统一请求构建"`：通过，生成候选版本 `v0.1.20`。
10. `cargo run -- cycle`：通过，已将 `v0.1.20` 提升为当前版本。
11. `cargo run -- preflight`：通过，提升后显示 `v0.1.20` 可进化。
12. `cargo run -- validate`：通过，检查 30 个路径。
13. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

本轮未发现功能错误。测试和真实闭环均通过。

# 评估

版本 `v0.1.20` 已完成最小闭环提升。系统现在具备从环境变量配置到统一请求规格生成的基础链路，下一步可以在此基础上实现受控 HTTP 执行。

# 优化建议

后续可新增真实 AI HTTP 执行层，但必须保持超时控制、错误归档、密钥脱敏和测试隔离。

# 可复用经验

请求构建层应先于网络执行层落地。这样可以独立测试不同厂商的协议差异，并防止真实 API Key、提示词和响应内容过早进入日志或状态文件。

## v0.1.21

# 版本信息
- 版本号：v0.1.21
- 时间：unix:1777561661
- 父版本：v0.1.20

# 目标

新增 AI 响应文本归一化

# 计划（Plan）

1. 读取最近版本记忆、任务记录和 AI 请求构建实现。
2. 新增统一 AI 文本响应结构。
3. 新增 AI 响应解析错误类型。
4. 基于 OpenAI、DeepSeek、Gemini 的不同协议解析响应文本。
5. 增加 OpenAI `output_text`、OpenAI `output` 数组、DeepSeek `choices`、Gemini `candidates` 测试。
6. 增加无效 JSON、缺失文本和空文本错误测试。
7. 执行格式、编译、测试、前置检查、候选生成、提升和验证。

# 执行过程

已在 AI 请求构建层之后新增响应文本归一化能力。系统现在可以从 OpenAI Responses、DeepSeek Chat Completions 和 Gemini GenerateContent 的不同 JSON 响应结构中抽取统一文本结果。本轮真实闭环已将 `v0.1.21` 提升为当前稳定版本。

# 代码变更

1. 新增 `AiTextResponse`，包含提供商、模型、协议、文本和原始响应字节数。
2. 新增 `AiResponseError`，覆盖无效 JSON、缺失文本和空文本。
3. `AiProviderRegistry` 新增 `parse_text_response`。
4. OpenAI Responses 支持解析 `output_text` 和 `output[].content[].text`。
5. DeepSeek 支持解析 `choices[0].message.content`。
6. Gemini 支持解析 `candidates[].content.parts[].text`。
7. `Agents.md` 补充 AI 响应解析集中化规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，51 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.20` 可进化。
6. `cargo run -- ai-config`：通过，无密钥时显示未就绪且不输出密钥。
7. 使用临时 OpenAI 配置执行 `cargo run -- ai-request "响应解析前置检查"`：通过，输出脱敏请求摘要。
8. `cargo run -- advance "新增 AI 响应文本归一化"`：通过，生成候选版本 `v0.1.21`。
9. `cargo run -- cycle`：通过，已将 `v0.1.21` 提升为当前版本。
10. `cargo run -- preflight`：通过，提升后显示 `v0.1.21` 可进化。
11. `cargo run -- validate`：通过，检查 30 个路径。
12. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

本轮未发现功能错误。测试和真实闭环均通过。

# 评估

版本 `v0.1.21` 已完成最小闭环提升。系统已经具备从 AI 配置、请求构建到响应文本归一化的基础链路。

# 优化建议

后续可新增真实 AI HTTP 执行层，执行完成后直接复用 `parse_text_response` 得到统一文本结果。

# 可复用经验

响应解析必须显式处理缺失文本和空文本，不能静默返回空结果继续推进 Agent 流程。厂商响应差异应继续集中在 AI 提供商模块内。

## v0.1.22

# 版本信息
- 版本号：v0.1.22
- 时间：unix:1777562602
- 父版本：v0.1.21

# 目标

改进 AI 环境变量诊断

# 计划（Plan）

1. 读取用户反馈、最近版本记忆和 AI 请求错误实现。
2. 改进缺少可用提供商时的错误提示，明确 PowerShell 当前会话写法。
3. 改进指定提供商但缺少 API Key 时的错误提示，明确对应密钥变量名。
4. 补充错误消息测试。
5. 更新 README 和 Agents.md 的 PowerShell 环境变量示例。
6. 执行格式、编译、测试、前置检查、AI 配置检查、请求构建检查、候选生成、提升和验证。

# 执行过程

已根据用户反馈改进 AI 环境变量诊断。现在只设置 provider 但未设置 `DEEPSEEK_API_KEY` 时，`ai-request` 会明确提示 PowerShell 当前会话中应执行的 `$env:DEEPSEEK_API_KEY="你的密钥"` 写法，并建议先运行 `cargo run -- ai-config` 检查。本轮真实闭环已将 `v0.1.22` 提升为当前稳定版本。

# 代码变更

1. `AiRequestError::MissingProvider` 文本现在包含 PowerShell 当前会话示例、`SELFFORGE_AI_PROVIDER` 和支持的密钥变量。
2. `AiRequestError::MissingApiKey` 文本现在按提供商列出应设置的 API Key 环境变量。
3. 新增 provider 到 API Key 环境变量的内部辅助函数。
4. README 新增 PowerShell 当前会话配置示例。
5. Agents.md 新增 Windows PowerShell 环境变量规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，53 个测试全部成功。
5. `cargo run -- preflight`：通过，提升前显示 `v0.1.21` 可进化。
6. `cargo run -- ai-config`：通过，无密钥时显示未就绪且不输出密钥。
7. 使用 `SELFFORGE_AI_PROVIDER=deepseek` 但不设置 `DEEPSEEK_API_KEY` 执行 `ai-request`：按预期失败，并明确提示设置 `DEEPSEEK_API_KEY`。
8. 使用临时 `DEEPSEEK_API_KEY` 执行 `ai-request "你好"`：通过，输出脱敏请求摘要。
9. `cargo run -- advance "改进 AI 环境变量诊断"`：通过，生成候选版本 `v0.1.22`。
10. `cargo run -- cycle`：通过，已将 `v0.1.22` 提升为当前版本。
11. `cargo run -- preflight`：通过，提升后显示 `v0.1.22` 可进化。
12. `cargo run -- validate`：通过，检查 30 个路径。
13. `cargo run -- errors --current --open --limit 5`：通过，当前版本无未解决错误。

# 错误总结

用户反馈的体验问题已修复：原错误提示无法指导 PowerShell 当前会话环境变量写法，也没有清楚指出缺少 `DEEPSEEK_API_KEY`。本轮已通过错误消息和测试固化修复。

# 评估

版本 `v0.1.22` 已完成最小闭环提升。AI 配置失败时的诊断更清晰，能直接指导用户检查当前进程可见环境变量。

# 优化建议

后续可增加 `ai-config --explain` 或类似结构化诊断输出，但当前先保持 CLI 简洁。

# 可复用经验

Windows PowerShell 中，环境变量必须使用 `$env:变量名="值"` 才会传递给当前会话启动的 `cargo run` 子进程；只写类 bash 形式不会生效。AI 配置错误必须直接指出当前进程看到了什么、缺少什么。

## v0.1.23

# 版本信息
- 版本号：v0.1.23
- 时间：unix:1777563140
- 父版本：v0.1.22

# 目标

自动读取 .env AI 配置

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 生成或复用 major 工作区与聚合归档文件。
4. 追加候选版本记忆、任务、错误和版本记录。
5. 验证候选版本布局和中文文档规范。
6. 将 state/state.json 更新为 candidate_prepared。

# 执行过程

已按 major 聚合规则生成候选记录，未创建新的小版本工作区目录。

# 代码变更

待最终验证后补充。

# 测试结果

待最终验证后补充。

# 错误总结

待最终验证后补充。

# 评估

候选版本生成完成后仍需通过验证与提升流程才能成为当前版本。

# 优化建议

继续减少小版本碎片化文件和目录。

# 可复用经验

小版本记录应追加到 major 聚合文件，避免目录数量随 patch 增长。
## v0.1.23 最终记录

# 版本信息
- 版本号：v0.1.23
- 时间：2026-04-30
- 父版本：v0.1.22

# 目标

让 `ai-config` 和 `ai-request` 自动读取项目根目录 `.env`，解决用户在 `.env` 中配置 DeepSeek 后仍提示没有可用 AI 提供商的问题。

# 计划（Plan）

1. 读取当前状态、历史记忆和 AI Provider 实现。
2. 在 AI Provider 模块中实现项目 `.env` 读取和解析。
3. 保持真实进程环境变量优先于 `.env`。
4. 让 `SelfForgeApp::ai_config` 和 `SelfForgeApp::ai_request` 统一使用项目根目录配置。
5. 增加 `.env` 读取、覆盖优先级、缺失 `.env` 容错和密钥脱敏测试。
6. 更新 README、`.env.example` 和 Agents.md。
7. 执行格式化、编译、测试、预检、验证、候选生成和提升。

# 执行过程

已新增项目环境查找器，读取当前进程环境变量和项目根目录 `.env`。真实环境变量优先，`.env` 作为本地补充配置。已验证在未显式设置 PowerShell 环境变量时，`cargo run -- ai-config` 能从 `.env` 识别 DeepSeek，`cargo run -- ai-request "你好"` 能生成脱敏请求摘要。

# 代码变更

1. `AiProviderRegistry` 新增 `inspect_project`、`inspect_project_with`、`build_text_request_project` 和 `build_text_request_project_with`。
2. 新增内部 `ProjectEnv` 解析器，支持空行、注释、`export KEY=VALUE`、单引号、双引号和未加引号行内注释。
3. `SelfForgeApp::ai_config` 与 `SelfForgeApp::ai_request` 改为读取项目根目录 `.env`。
4. README 重写为简洁中文说明，并补充 `.env` 自动读取规则。
5. `.env.example` 改为中文示例，默认展示 DeepSeek 配置。
6. Agents.md 补充 `.env` 规则、环境变量优先级和密钥脱敏约束。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，57 个测试全部成功。
5. `cargo run -- ai-config`：通过，已从 `.env` 识别 DeepSeek，未输出密钥值。
6. `cargo run -- ai-request "你好"`：通过，生成脱敏请求摘要。
7. `cargo run -- preflight`：通过，显示 `v0.1.23` 可进化。
8. `cargo run -- advance "自动读取 .env AI 配置"`：通过，生成候选版本 `v0.1.23`。
9. `cargo run -- cycle`：通过，已将 `v0.1.23` 提升为当前稳定版本。
10. `cargo run -- validate`：通过，检查 30 个路径。
11. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。

# 错误总结

用户反馈的问题已解决：之前 AI 配置只读取进程环境变量，未读取项目根目录 `.env`，导致 `.env` 中的 DeepSeek 配置不可见。本轮已改为应用层统一读取项目配置。

# 评估

版本 `v0.1.23` 完成最小闭环提升。AI 配置入口更符合本地开发习惯，同时仍保持密钥不进入日志、状态、文档和请求摘要。

# 优化建议

下一步可以在当前请求构建能力之上增加受控 HTTP 执行层，但必须继续保持超时、脱敏、错误归档和可测试边界。

# 可复用经验

本地配置应由应用层统一合并，真实进程环境变量优先，`.env` 只作为本地补充。密钥值只能用于请求执行，不得进入可审计文本输出。

## v0.1.24

# 版本信息
- 版本号：v0.1.24
- 时间：unix:1777564176
- 父版本：v0.1.23

# 目标

显示 AI 响应文本

# 计划（Plan）

1. 读取最近版本记忆和持久化状态。
2. 验证当前稳定版本仍可运行。
3. 生成或复用 major 工作区与聚合归档文件。
4. 追加候选版本记忆、任务、错误和版本记录。
5. 验证候选版本布局和中文文档规范。
6. 将 state/state.json 更新为 candidate_prepared。

# 执行过程

已按 major 聚合规则生成候选记录，未创建新的小版本工作区目录。

# 代码变更

待最终验证后补充。

# 测试结果

待最终验证后补充。

# 错误总结

待最终验证后补充。

# 评估

候选版本生成完成后仍需通过验证与提升流程才能成为当前版本。

# 优化建议

继续减少小版本碎片化文件和目录。

# 可复用经验

小版本记录应追加到 major 聚合文件，避免目录数量随 patch 增长。
## v0.1.24 最终记录

# 版本信息
- 版本号：v0.1.24
- 时间：2026-04-30
- 父版本：v0.1.23

# 目标

让 `cargo run -- ai-request "提示词"` 默认发起真实 AI 请求并显示响应文本，而不是只显示脱敏请求摘要。

# 计划（Plan）

1. 读取当前状态、历史记忆和 AI 请求结构。
2. 新增受控 HTTP 执行报告和错误类型。
3. 在执行阶段读取 API Key，避免进入请求摘要、状态和日志。
4. 使用非流式 JSON 请求调用 AI 提供商，并设置超时。
5. 复用现有响应文本归一化解析。
6. 保留 `ai-request --dry-run` 作为脱敏请求预览模式。
7. 补充 HTTP 成功、失败状态、解析失败和密钥脱敏测试。
8. 执行格式化、编译、测试、真实请求验证、候选提升和归档。

# 执行过程

已将 `ai-request` 默认路径改为受控 HTTP 执行，命令会输出提供商、模型、协议、状态码、响应字节数和归一化响应文本。`ai-request --dry-run` 继续输出脱敏请求摘要。已使用 DeepSeek 配置执行真实请求，返回状态码 200，并显示响应文本 `SelfForge 响应测试通过`。

# 代码变更

1. 新增 `AiExecutionReport`、`AiRawHttpResponse` 和 `AiExecutionError`。
2. 新增 `AiProviderRegistry::execute_text_request_project` 和可测试的 `execute_text_request_project_with`。
3. 新增受控 HTTP 发送函数，使用 `reqwest` blocking 客户端、JSON 请求和超时控制。
4. HTTP 失败响应摘要会脱敏 API Key。
5. `SelfForgeApp` 新增真实 AI 请求执行入口，并保留 `ai_request_preview`。
6. CLI `ai-request` 默认显示响应文本，新增 `--dry-run` 与 `--timeout-ms` 参数。
7. README 和 Agents.md 已更新真实请求与脱敏预览规则。

# 测试结果

1. `cargo fmt`：通过。
2. `cargo fmt --check`：通过。
3. `cargo check`：通过。
4. `cargo test`：通过，60 个测试全部成功。
5. `cargo run -- ai-request --dry-run "提示词"`：通过，显示脱敏请求摘要。
6. `cargo run -- ai-request --timeout-ms 60000 "请只回复：SelfForge 响应测试通过"`：通过，状态码 200，并显示响应文本。
7. `cargo run -- preflight`：通过，显示 `v0.1.24` 可进化。
8. `cargo run -- advance "显示 AI 响应文本"`：通过，生成候选版本 `v0.1.24`。
9. `cargo run -- cycle`：通过，已将 `v0.1.24` 提升为当前稳定版本。
10. `cargo run -- validate`：通过，检查 30 个路径。
11. `cargo run -- errors --current --open --limit 5`：通过，无未解决错误。

# 错误总结

用户反馈的问题已解决：旧版 `ai-request` 只做请求构建，不执行 HTTP 请求，因此无法显示响应文本。本轮已补上执行层并保留预览模式。

# 评估

版本 `v0.1.24` 完成 AI API 最小真实调用闭环。配置读取、请求构建、HTTP 执行、响应解析和 CLI 输出已串起来，为后续 Agent 自动规划与执行提供基础能力。

# 优化建议

下一步可以将 AI 请求执行接入 Agent 计划生成流程，但必须先加入请求记录脱敏摘要、错误归档和可控重试策略。

# 可复用经验

真实 HTTP 执行应放在应用统一能力层，不应散落在 CLI。API Key 只允许在内存中用于认证头，所有用户可见输出和归档只记录变量名、状态码和脱敏摘要。
