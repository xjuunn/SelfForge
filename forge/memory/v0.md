# v0 记忆记录

# 记录规则

- 本文件是热记忆文件，只保留近期完整记忆，供 `memory-context`、`memory-insights` 和 Agent 默认读取。
- 久远完整记忆迁移到 `forge/memory/archive/v0.md`，只在审计、追溯或人工复盘时读取。
- 同一 major 只维护一个热记忆文件和一个冷归档文件，禁止为小版本创建独立记忆文件。

## 压缩记忆索引

- 热记忆保留：5 条。
- 冷归档完整记忆：54 条。
- 最新热记忆：v0.1.66。
- 最早热记忆：v0.1.61。
- 归档文件：`forge/memory/archive/v0.md`。

## v0.1.65

# 版本信息
- 版本号：v0.1.65
- 时间：unix:1777678800
- 父版本：v0.1.64

# 目标

把来源任务草案审计编号继续贯通到补丁审计记录，形成从任务草案审计、补丁草案到补丁审计的结构化追踪链路。

# 计划（Plan）

1. 巡检当前状态、开放错误和近期记忆。
2. 记录本轮任务计划。
3. 扩展补丁审计记录和摘要结构，新增来源任务草案审计编号。
4. 在补丁审计流程中从补丁草案记录继承来源编号。
5. 更新 CLI 输出，显示来源审计编号。
6. 补充来源编号持久化测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检、记忆压缩和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.64 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- errors --current --open --limit 10` 确认没有开放错误。
3. 已通过 `cargo run -- memory-insights --current --limit 5` 确认本轮目标为继续贯通来源追踪。
4. 已新增补丁审计记录和摘要的 `source_task_audit_id` 可选字段。
5. 已让 `agent-patch-audit` 从补丁草案记录继承来源任务草案审计编号。
6. 已更新补丁审计完成、列表和单条查询输出。
7. 已将当前版本线索提升为 `v0.1.65`。

# 代码变更

1. 更新 `src/app/agent/patch_audit.rs`，为补丁审计记录和摘要新增可选来源任务草案审计编号。
2. 更新 `src/app/minimal_loop.rs`，在补丁审计记录生成时继承补丁草案来源编号。
3. 更新 `src/main.rs`，让补丁审计相关 CLI 输出显示来源审计编号。
4. 更新 `src/lib.rs`，新增补丁审计来源编号持久化测试。
5. 更新 `README.md`、`Agents.md`、`forge/tasks/v0.md`、`forge/errors/v0.md`、`forge/versions/v0.md`、`forge/memory/v0.md` 和 `state/state.json`。

# 测试结果

1. `cargo check` 通过。
2. `cargo test ai_patch_audit_persists_source_task_audit_id` 通过。
3. `cargo test ai_patch_audit` 通过，4 个测试全部通过。
4. `cargo fmt --check` 通过。
5. `cargo test` 通过，182 个测试全部通过。
6. `git diff --check` 通过。
7. `cargo run -- validate` 通过，检查 30 个路径。
8. `cargo run -- preflight` 通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
9. `cargo run -- errors --current --open --limit 10` 返回无开放错误。
10. `cargo run -- agent-patch-audits --current --limit 5` 通过，当前版本暂无真实补丁审计记录。
11. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 54 条。
12. `cargo run -- agent-work-init --current --threads 3 "补丁审计来源追踪后续协作"` 通过，协作任务板复用到 `v0.1.65`。

# 错误总结

本轮暂未发现新增未解决错误。新增功能已覆盖补丁审计继承来源编号的记录、摘要和读取路径，普通旧审计记录保持向后兼容。

# 评估

v0.1.65 把来源任务草案审计编号从补丁草案继续带入补丁审计记录。系统的自动进化链路更容易从审计结果反查最初的任务草案审计依据。

# 优化建议

下一步应把来源任务草案审计编号继续贯通到补丁预演记录，形成任务草案审计、补丁草案、补丁审计和补丁预演的连续追踪链路。

# 可复用经验

1. 追踪字段需要在每个阶段显式继承，不能假设后续阶段能通过草案编号间接推断。
2. 列表摘要和单条记录必须同时保留关键追踪字段，否则查询层会丢失审计上下文。
3. 追踪增强应保持可选字段兼容，避免旧记录阻断查询。

## v0.1.64

# 版本信息
- 版本号：v0.1.64
- 时间：unix:1777677600
- 父版本：v0.1.63

# 目标

检查未完成任务并收尾，让补丁草案记录显式保存来源任务草案审计编号，便于从补丁草案反查任务草案、审计记录和后续源码覆盖链路。

# 计划（Plan）

1. 巡检当前状态、开放错误、任务队列和近期记忆。
2. 记录本轮任务计划。
3. 扩展补丁草案预览、记录和摘要结构，新增来源任务草案审计编号。
4. 在 `--from-task-audit` 链路中写入来源审计编号。
5. 更新 CLI 输出，显示来源审计编号。
6. 补充来源编号持久化测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检、记忆压缩和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.63 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- errors --current --open --limit 10` 确认没有开放错误。
3. 已通过 `cargo run -- memory-insights --current --limit 5` 确认任务 19 是当前最明确未完成任务。
4. 已新增 `source_task_audit_id`，并保持旧补丁草案记录向后兼容。
5. 已让 `agent-patch-draft --from-task-audit TASK_AUDIT_ID` 在预览、成功记录和失败记录中携带来源审计编号。
6. 已更新补丁草案 CLI 预览、完成、列表和单条查询输出。
7. 已将当前版本线索提升为 `v0.1.64`。

# 代码变更

1. 更新 `src/app/agent/patch_draft.rs`，为补丁草案记录和摘要新增可选来源任务草案审计编号。
2. 更新 `src/app/minimal_loop.rs`，为补丁草案预览新增来源编号，并在审计驱动入口中填充。
3. 更新 `src/main.rs`，让补丁草案相关 CLI 输出显示来源审计编号。
4. 更新 `src/lib.rs`，新增来源编号持久化测试。
5. 更新 `README.md`、`Agents.md`、`forge/tasks/v0.md`、`forge/errors/v0.md`、`forge/versions/v0.md`、`forge/memory/v0.md` 和 `state/state.json`。

# 测试结果

1. `cargo check` 通过。
2. `cargo test ai_patch_draft_from_task_audit_persists_source_audit_id` 通过。
3. `cargo test ai_patch_draft_preview_from_task_audit` 通过，3 个测试全部通过。
4. `cargo fmt --check` 通过。
5. `cargo test` 通过，181 个测试全部通过。
6. `git diff --check` 通过。
7. `cargo run -- validate` 通过，检查 30 个路径。
8. `cargo run -- preflight` 通过，当前版本 `v0.1.64`，未解决错误 0，可进化。
9. `cargo run -- errors --current --open --limit 10` 返回无开放错误。
10. `cargo run -- agent-patch-drafts --current --limit 5` 通过，当前版本暂无真实补丁草案记录。
11. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 53 条。
12. `cargo run -- agent-work-init --current --threads 3 "补丁草案来源追踪收尾协作"` 通过，协作任务板复用到 `v0.1.64`。

# 错误总结

本轮暂未发现新增未解决错误。新增功能已覆盖来源编号持久化路径，普通补丁草案记录保持向后兼容。

# 评估

v0.1.64 补齐了任务草案审计到补丁草案记录之间的反向追踪能力。后续审计、预演和源码覆盖流程可以从结构化字段追溯来源，不必依赖文本目标猜测。

# 优化建议

后续若继续进化，可把来源任务草案审计编号继续贯通到补丁审计、预演、应用和源码覆盖记录，形成更完整的端到端追踪链路。

# 可复用经验

1. 追踪链路应使用结构化字段，不应依赖 Markdown 文本中的自然语言描述。
2. 新增追踪字段必须使用可选字段并保持旧记录兼容。
3. 收尾类任务也要经过完整测试和归档，避免把小改动变成不可审计的暗改。

## v0.1.63

# 版本信息
- 版本号：v0.1.63
- 时间：unix:1777676545
- 父版本：v0.1.62

# 目标

让 `agent-patch-draft` 显式接收已批准的任务草案审计记录，自动使用批准目标生成补丁草案，并拒绝未审计或未批准草案链路。

# 计划（Plan）

1. 读取最近 5 个版本记忆、当前状态和开放错误。
2. 编写本轮任务文档。
3. 新增应用层审计驱动补丁草案预览和执行入口。
4. 更新 CLI 参数解析，支持 `--from-task-audit TASK_AUDIT_ID`。
5. 补充批准、阻断和缺失审计测试。
6. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
7. 执行格式化、测试、验证、预检、记忆压缩和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.62 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标为已批准任务草案审计驱动补丁草案入口。
3. 已新增应用层 `ai_patch_draft_preview_from_task_audit` 和 `ai_patch_draft_from_task_audit`。
4. 已新增内部批准目标读取逻辑，审计缺失、未批准或批准目标为空时返回明确错误。
5. 已更新 CLI，`agent-patch-draft` 支持 `--from-task-audit TASK_AUDIT_ID`。
6. 已补充已批准审计生成预览、阻断审计拒绝和缺失审计错误三类测试。
7. 已将当前版本线索提升为 `v0.1.63`。
8. 已执行完整测试、系统验证、前置检查、开放错误查询、记忆压缩和多 AI 协作任务板复用检查。

# 代码变更

1. 更新 `src/app/minimal_loop.rs`，新增从任务草案审计读取批准目标并复用现有补丁草案流程的应用入口。
2. 更新 `src/main.rs`，为 `agent-patch-draft` 增加 `--from-task-audit` 参数和互斥目标校验。
3. 更新 `src/lib.rs`，新增审计驱动补丁草案预览的批准、阻断和缺失记录测试。
4. 更新 `README.md`、`Agents.md`、`forge/tasks/v0.md`、`forge/errors/v0.md`、`forge/versions/v0.md`、`forge/memory/v0.md` 和 `state/state.json`。

# 测试结果

1. `cargo check` 通过。
2. `cargo test ai_patch_draft_preview_from_task_audit` 通过，3 个测试全部通过。
3. `cargo fmt --check` 通过。
4. `cargo test` 通过，180 个测试全部通过。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.63`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
8. 缺失任务草案审计编号的 CLI 负向验证通过，命令会按预期返回明确错误并停止。
9. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 52 条。
10. `cargo run -- agent-work-init --current --threads 3 "审计驱动补丁草案后续协作"` 通过，协作任务板复用到 `v0.1.63`。
11. `cargo run -- memory-insights --current --limit 5` 通过。
12. `git diff --check` 通过。

# 错误总结

本轮暂未发现新增未解决错误。新增功能的错误路径已通过测试覆盖：缺失任务草案审计记录会返回明确错误；阻断审计会拒绝生成补丁草案；已批准审计会把批准目标写入补丁草案提示词。

# 评估

v0.1.63 将任务草案审计和补丁草案生成连接起来。系统现在可以从已批准审计记录进入补丁草案流程，同时拒绝未批准记录和直接目标混用，自动进化链路的闸门更完整。

# 优化建议

下一步应让补丁草案记录显式保存来源任务草案审计编号，便于从补丁草案反查任务草案、审计记录和后续源码覆盖链路。

# 可复用经验

1. 新入口应复用成熟流程，而不是复制一套 AI 补丁生成逻辑。
2. 从审计记录继续执行时，必须以审计记录中的批准目标为唯一目标，禁止再混入直接用户目标。
3. 审计驱动入口要在调用 AI 前失败得足够早，避免无效请求和无意义草案记录。

## v0.1.62

# 版本信息
- 版本号：v0.1.62
- 时间：unix:1777675786
- 父版本：v0.1.61

# 目标

实现基于源码覆盖下一任务草案的审计闸门，把任务草案验证为可执行计划后，再允许进入后续补丁草案或源码覆盖流程。

# 计划（Plan）

1. 读取最近 5 个版本记忆、当前状态和开放错误。
2. 编写本轮任务文档。
3. 新增源码覆盖任务草案审计存储模块。
4. 新增应用层审计流程，检查草案完整性、目标版本、验收检查和后续命令。
5. 新增 CLI 命令和帮助信息。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检、记忆压缩和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.61 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标为任务草案审计闸门。
3. 已新增 `AiPatchSourceTaskAuditStore`、`AiPatchSourceTaskAuditRecord`、`AiPatchSourceTaskAuditSummary`、`AiPatchSourceTaskAuditFinding` 和审计状态。
4. 已新增应用层 `ai_patch_source_task_audit`，读取任务草案并生成批准或阻断审计记录。
5. 已新增 `agent-patch-source-task-audit`、`agent-patch-source-task-audits` 和 `agent-patch-source-task-audit-record`。
6. 已补充批准审计、阻断审计和缺失任务草案记录三类测试。
7. 已将当前版本线索提升为 `v0.1.62`。

# 代码变更

1. 新增 `src/app/agent/patch_source_task_audit.rs`，提供 major 级任务草案审计记录存储、索引、读取和中文错误信息。
2. 更新 `src/app/minimal_loop.rs`，新增任务草案审计应用用例、报告结构、错误类型、审计发现生成和 Markdown 报告生成。
3. 更新 `src/main.rs`，新增任务草案审计 CLI 命令、参数解析、查询和读取输出。
4. 更新 `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出新增类型。
5. 更新 `README.md`、`Agents.md`、`forge/tasks/v0.md`、`forge/errors/v0.md`、`forge/versions/v0.md`、`forge/memory/v0.md` 和 `state/state.json`。

# 测试结果

1. `cargo check` 通过。
2. `cargo test ai_patch_source_task_audit` 通过，3 个测试全部通过。
3. `cargo test` 通过，177 个测试全部通过。
4. `cargo fmt --check` 通过。
5. `cargo run -- validate` 通过。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.62`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
8. `cargo run -- agent-patch-source-task-audits --current --limit 5` 通过，当前版本暂无真实源码覆盖任务草案审计记录。
9. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 51 条。
10. `cargo run -- agent-work-init --current --threads 3 "任务草案审计后续协作"` 通过，协作任务板版本为 `v0.1.62`。
11. `cargo run -- memory-insights --current --limit 5` 通过。
12. `git diff --check` 通过。

# 错误总结

本轮暂未发现新增未解决错误。新增功能的错误路径已通过测试覆盖：缺失任务草案记录会返回明确错误；状态或检查不完整的任务草案会生成已阻断审计；完整任务草案会生成已批准审计和后续命令。

# 评估

v0.1.62 把任务草案继续推进到审计闸门层。系统现在可以把下一步意图先变成草案，再用结构化审计判断是否允许进入补丁生成流程，自动进化链路更符合受控升级边界。

# 优化建议

下一步应让 `agent-patch-draft` 能显式接收已批准的任务草案审计记录，自动使用批准目标生成补丁草案，并在缺少审计时拒绝来自任务草案链路的继续执行。

# 可复用经验

1. 闸门记录应只做判定和归档，不应同时执行下一阶段动作。
2. 任务草案进入补丁生成前必须检查目标版本、验收检查和后续命令，避免把不完整草案传给 AI。
3. 阻断记录也有价值：它能明确告诉后续 Agent 应该修复草案，而不是继续生成补丁。

## v0.1.61

# 版本信息
- 版本号：v0.1.61
- 时间：unix:1777675026
- 父版本：v0.1.60

# 目标

实现基于源码覆盖 cycle 后续总结的下一任务草案归档，把总结中的下一目标和下一任务转换为可审计草案，避免 AI 直接把建议变成真实修改。

# 计划（Plan）

1. 读取最近 5 个版本记忆、当前状态和开放错误。
2. 编写本轮任务文档。
3. 新增源码覆盖下一任务草案存储模块。
4. 新增应用层任务草案流程，读取后续总结并按来源状态生成继续进化或修复任务。
5. 新增 CLI 命令和帮助信息。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检、记忆压缩和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.60 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标为 cycle 后续总结到任务草案。
3. 已新增 `AiPatchSourceTaskDraftStore`、`AiPatchSourceTaskDraftRecord`、`AiPatchSourceTaskDraftSummary` 和任务草案状态。
4. 已新增应用层 `ai_patch_source_task_draft`，读取 cycle 后续总结并生成可审计任务草案。
5. 已新增 `agent-patch-source-task-draft`、`agent-patch-source-task-drafts` 和 `agent-patch-source-task-draft-record`。
6. 已补充提升总结草案、阻断总结修复草案和缺失总结记录错误三类测试。
7. 已将当前版本线索提升为 `v0.1.61`。
8. 验证中发现 `state/state.json` 被 PowerShell 写入 UTF-8 BOM 后无法解析，已改用无 BOM UTF-8 重写并复验通过。

# 代码变更

1. 新增 `src/app/agent/patch_source_task_draft.rs`，提供 major 级任务草案记录存储、索引、读取和中文错误信息。
2. 更新 `src/app/minimal_loop.rs`，新增任务草案应用用例、报告结构、错误类型和 Markdown 报告生成。
3. 更新 `src/main.rs`，新增任务草案 CLI 命令、参数解析、查询和读取输出。
4. 更新 `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出新增类型。
5. 更新 `README.md`、`Agents.md`、`forge/tasks/v0.md`、`forge/errors/v0.md`、`forge/versions/v0.md`、`forge/memory/v0.md` 和 `state/state.json`。

# 测试结果

1. `cargo check` 通过。
2. `cargo test ai_patch_source_task_draft` 通过，3 个测试全部通过。
3. `cargo test` 通过，174 个测试全部通过。
4. `cargo fmt --check` 通过。
5. `cargo run -- validate` 通过。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.61`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
8. `cargo run -- agent-patch-source-task-drafts --current --limit 5` 通过，当前版本暂无真实源码覆盖下一任务草案记录。
9. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 50 条。
10. `cargo run -- agent-work-init --current --threads 3 "源码覆盖任务草案后续协作"` 通过，协作任务板版本为 `v0.1.61`。
11. `git diff --check` 通过。

# 错误总结

本轮没有新增未解决错误。新增功能的错误路径已通过测试覆盖：缺失后续总结记录会返回明确错误；阻断来源总结会生成修复导向的任务草案；已提升来源总结会生成下一轮 patch 级自我升级任务草案。本轮出现一个已解决文件编码问题：`state/state.json` 被写入 UTF-8 BOM 后解析失败，已用无 BOM UTF-8 重写并复验通过。

# 评估

v0.1.61 把 cycle 后续总结继续推进到任务草案层。系统现在能先把下一步意图固化为可审计草案，再交给后续补丁生成和源码覆盖流程，自动自我升级链路更克制、更可解释。

# 优化建议

下一步应实现基于任务草案的审计闸门，把任务草案验证为可执行计划后，再允许进入 `agent-patch-draft` 或后续源码覆盖流程。

# 可复用经验

1. 总结、草案、真实任务必须分层：总结保存事实和建议，草案保存待审计意图，真实任务只能由后续闸门批准后生成。
2. 状态文件必须使用无 BOM UTF-8 写入；涉及 JSON 状态的批量更新后，应立即运行 `preflight` 验证解析和布局。
3. 自动化越靠近真实修改，越需要独立记录和只读输入，避免一步跨过审计边界。

## v0.1.65 结构拆分最终记忆

# 版本信息
- 版本号：v0.1.65
- 时间：2026-05-04
- 父版本：v0.1.64

# 目标

降低单文件职责负担，先把 `src/lib.rs` 中的大型内联测试模块拆分为独立测试目录，让库入口回到模块声明和公共导出职责。

# 计划（Plan）

1. 读取最近 5 条热记忆和经验摘要。
2. 先迁移 `src/lib.rs` 的内联测试模块。
3. 继续把测试按职责拆成多个子模块，避免把体量转移到另一个单文件。
4. 运行格式化、完整测试、布局验证和前置检查。
5. 写入中文归档并保持当前 major 聚合策略。

# 执行过程

1. 已读取最近 5 条热记忆，确认近期任务强调小步、可回滚和完整验证。
2. 已把 `src/lib.rs` 中的测试迁出，并将公共 `pub use app::{...}` 放在测试模块声明之前。
3. 已新增 `src/tests/mod.rs` 和 8 个按主题拆分的测试文件。
4. 首次测试发现共享补丁测试辅助函数被归入单个子模块，已提升到 `src/tests/mod.rs` 后复验通过。

# 代码变更

1. `src/lib.rs` 现在只保留库模块声明、公共导出、版本常量和 `mod tests;`。
2. `src/tests/mod.rs` 保存测试共享导入、临时目录辅助函数、清理函数和测试子模块声明。
3. `src/tests/agent_core.rs` 保存 Agent 注册、工具、任务板、会话和演进相关测试。
4. `src/tests/patch_draft_flow.rs` 和 `src/tests/patch_source_flow.rs` 分别保存补丁草案流程和源码覆盖流程测试。
5. 其余测试按 AI 提供商、应用闭环、布局版本、记忆和运行错误归档拆分。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test` 通过，182 个测试全部通过。
3. `cargo run -- validate` 通过，检查 30 个路径。
4. `cargo run -- preflight` 通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
5. `cargo run -- errors --current --open` 返回无开放错误。

# 错误总结

本轮没有新增未解决错误。拆分过程中出现一次测试辅助函数作用域问题，已在开发阶段通过提升共享辅助函数解决，并通过完整测试复验。

# 评估

本轮没有改变业务行为，但显著降低了 `src/lib.rs` 的职责和体量。测试仍然可按原有名称运行，只是路径变成更清晰的主题模块。

# 优化建议

下一步应继续拆分 `src/app/minimal_loop.rs` 和 `src/main.rs`。优先把补丁流程、Agent 会话流程和 CLI 参数解析分别迁到更小的模块，避免应用层和 CLI 层继续膨胀。

# 可复用经验

1. 拆大文件时应优先选择低行为风险的机械迁移，并立即运行完整测试。
2. 测试拆分不能只把体量搬到另一个单文件，应同步按业务主题分组。
3. 共享测试辅助函数应放在父测试模块中，避免子模块之间形成横向依赖。

## v0.1.65 补丁范围模块拆分最终记忆

# 版本信息
- 版本号：v0.1.65
- 时间：2026-05-04
- 父版本：v0.1.65

# 目标

继续降低 `src/app/minimal_loop.rs` 的单文件职责负担，先把补丁写入范围解析、保护路径检查和协作冲突审计迁移到独立子模块。

# 计划（Plan）

1. 读取近期热记忆和经验摘要，确认下一步拆分目标。
2. 定位补丁审计写入范围辅助函数和调用点。
3. 新增 `patch_scope` 子模块并迁移低耦合逻辑。
4. 保持应用流程调用不变，修复迁移后的可见性问题。
5. 执行完整测试、布局验证、前置检查和开放错误查询。

# 执行过程

1. 已读取最近 5 条热记忆和经验摘要，确认本轮继续拆分 `src/app/minimal_loop.rs`。
2. 已执行前置检查，当前版本 `v0.1.65` 未解决错误为 0，可继续进化。
3. 已新增 `src/app/minimal_loop/patch_scope.rs`。
4. 已将补丁范围解析、路径规范化、受保护路径判断、协作任务冲突审计和范围比较函数迁入子模块。
5. 首次测试发现 Markdown 标题解析仍被父模块补丁预演逻辑复用，已改为包内可见函数并重新导入。

# 代码变更

1. `src/app/minimal_loop.rs` 新增 `mod patch_scope` 并导入补丁范围审计函数。
2. `src/app/minimal_loop/patch_scope.rs` 保存 `PatchWriteScopeAudit`、写入范围提取、写入范围审计、受保护根路径计算、路径规范化和冲突检测逻辑。
3. 父模块移除了约 280 行补丁范围辅助实现，继续保留应用用例流程。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test` 通过，182 个测试全部通过。
3. `cargo fmt --check` 通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open` 返回无开放错误。
7. `git diff --check` 通过。

# 错误总结

本轮没有新增未解决错误。迁移后出现一次已解决编译错误：`markdown_heading_title` 被迁到子模块后仍需被父模块调用，已通过 `pub(super)` 可见性修复并由完整测试复验。

# 评估

本轮没有改变补丁审计行为，但让补丁写入范围相关职责从应用闭环主文件中独立出来，后续可以继续按补丁流程、Agent 会话流程和应用验证流程拆分。

# 优化建议

下一步应继续拆分 `src/app/minimal_loop.rs` 中的补丁预演、补丁应用或 Agent 会话流程，并尽量每次只迁移一个低耦合职责块。

# 可复用经验

1. 拆分辅助函数时要先查清是否存在跨流程复用，避免把共享工具误判为单一流程私有函数。
2. 对大文件做机械迁移时，先选择边界清楚的纯函数群，测试失败时只修复可见性和导入问题。
3. 结构拆分也应完整写入任务、错误、版本和记忆归档，保持后续进化可追溯。

## v0.1.65 协作队列模型拆分最终记忆

# 版本信息
- 版本号：v0.1.65
- 时间：2026-05-04
- 父版本：v0.1.65

# 目标

继续降低单文件职责负担，把 `src/app/agent/coordination.rs` 中的数据模型和错误展示职责迁移到独立子模块，让协作队列主文件聚焦流程控制。

# 计划（Plan）

1. 读取近期热记忆、经验摘要、开放错误和前置检查结果。
2. 查询并领取协作任务板中的应用层任务。
3. 新增协作队列模型子模块，迁移模型、报告和错误类型。
4. 修复迁移后的可见性和导出问题。
5. 执行完整测试、布局验证、前置检查和开放错误查询。

# 执行过程

1. 已读取最近 5 条热记忆和经验摘要，确认本轮继续小步拆分低耦合职责块。
2. 已通过前置检查，当前版本 `v0.1.65` 未解决错误为 0，可继续进化。
3. 已领取协作任务板 `coord-002-application`，写入范围覆盖 `src/app/agent/`。
4. 已新增 `src/app/agent/coordination/model.rs`。
5. 已将协作队列状态、任务、事件、报告、错误和显示实现迁入模型子模块。
6. 已完成协作任务板任务，任务板记录本轮领取和完成事件。

# 代码变更

1. `src/app/agent/coordination.rs` 新增 `mod model`，并重新导出协作队列公共类型。
2. `src/app/agent/coordination/model.rs` 保存 `AgentWorkQueue`、`AgentWorkTask`、`AgentWorkEvent`、报告结构、`AgentWorkError` 和相关显示实现。
3. `AgentWorkCoordinator` 的根路径字段和默认租约常量改为父模块可见，确保父模块继续实现协调器流程。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test` 通过，182 个测试全部通过。
3. `cargo fmt --check` 通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open` 返回无开放错误。
7. `cargo run -- memory-context --current --limit 1` 已确认本轮最终记忆可被读取。
8. `git diff --check` 通过。

# 错误总结

本轮没有新增未解决错误。迁移后出现一次已解决编译错误：协调器类型和默认租约常量从父模块迁走后可见性不足，已通过重新导出和 `pub(super)` 可见性修复，并由完整测试复验。

# 评估

本轮没有改变协作队列行为，但把模型和错误展示职责从 1000 行以上的主文件拆出，`coordination.rs` 从 1034 行降到 798 行，后续可以继续拆分队列模板、锁文件存储或冲突检测逻辑。

# 优化建议

下一步应继续拆分 `src/app/agent/coordination.rs` 中的默认任务模板、提示词生成或锁文件读写逻辑，进一步让主文件只保留高层用例流程。

# 可复用经验

1. 模型拆分时要注意结构体方法实现仍可能留在父模块，字段可见性要随实现位置调整。
2. 默认值函数和常量通常跟 serde 模型同属一层，但父模块若仍参与构造，也需要明确包内边界。
3. 任务板领取、完成记录本身会修改协作队列文件，提交前应把该审计痕迹纳入差异检查。

## v0.1.65 协作 CLI 收口最终记忆

# 版本信息
- 版本号：v0.1.65
- 时间：2026-05-04
- 父版本：v0.1.65

# 目标

继续完成项目收口，补齐协作任务板 CLI 的 README 使用入口，复核协作队列测试覆盖，并完成剩余协作任务板归档。

# 计划（Plan）

1. 读取近期热记忆、经验摘要、开放错误和前置检查结果。
2. 领取 CLI 任务，补齐 README 协作命令入口。
3. 领取测试任务，复核既有协作队列测试覆盖并运行定向测试和完整测试。
4. 领取归档任务，写入中文任务、错误、版本和记忆记录。
5. 执行完整验证和差异检查后提交。

# 执行过程

1. 已读取最近 5 条热记忆和经验摘要，确认继续小步收口。
2. 已通过前置检查，当前版本 `v0.1.65` 未解决错误为 0，可继续进化。
3. 已领取 `coord-003-cli` 并补齐 README 协作任务板命令。
4. 已执行 `cargo run -- agent-work-status --current`，确认协作任务板 CLI 输出仍展示领取任务提示词和租约状态。
5. 已领取 `coord-004-tests`，确认现有测试已覆盖单线程领取、多线程不重复领取、依赖阻断、范围冲突、错误完成、释放和租约清理。
6. 已领取 `coord-005-review-archive` 并追加中文归档。

# 代码变更

1. `README.md` 常用命令新增 `agent-work-init`、`agent-work-claim`、`agent-work-complete`、`agent-work-release` 和 `agent-work-reap` 示例。
2. `workspaces/v0/artifacts/agents/coordination/work-queue.json` 记录 CLI、测试和归档任务的领取与完成事件。
3. `forge/tasks/v0.md`、`forge/errors/v0.md`、`forge/versions/v0.md` 和 `forge/memory/v0.md` 追加本轮收口记录。

# 测试结果

1. `cargo run -- validate` 通过，检查 30 个路径。
2. `cargo run -- agent-work-status --current` 通过。
3. `cargo test agent_work` 通过，12 个协作队列测试全部通过。
4. `cargo test` 通过，182 个测试全部通过。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open` 返回无开放错误。
7. `cargo run -- memory-context --current --limit 1` 已确认本轮最终记忆可被读取。
8. `git diff --check` 通过。

# 错误总结

本轮没有新增未解决错误。旧任务板写入范围要求测试任务只修改 `src/lib.rs`，但测试已经按职责拆分到 `src/tests/agent_core.rs`；本轮没有把测试回塞到库入口，而是复核现有覆盖并运行定向测试。

# 评估

协作任务板剩余核心任务已经完成，README 能直接展示完整协作命令链路；协作队列测试覆盖仍然有效，项目当前稳定版本保持可验证、可进化状态。

# 优化建议

下一步应初始化新的结构拆分任务板，明确允许创建 `src/cli/` 或 `src/bin/` 子模块，再继续拆分 `src/main.rs` 的参数解析和输出格式化逻辑。

# 可复用经验

1. 任务板写入范围过时时，不应为了形式满足而破坏已经完成的文件拆分，应通过验证和归档说明原因。
2. 文档收口也要跑 CLI 状态命令，确保 README 中补充的入口确实存在。
3. 完成一组协作任务后，应立即写入归档，避免任务板事件和 forge 记忆脱节。

## v0.1.65 协作任务板重开能力最终记忆

# 版本信息
- 版本号：v0.1.65
- 时间：2026-05-04
- 父版本：v0.1.65

# 目标

打通已完成协作任务板进入下一轮任务的入口，让 `agent-work-init` 可以显式重开全部完成的队列，同时保护未完成队列不被覆盖。

# 计划（Plan）

1. 读取近期热记忆、经验摘要、开放错误和前置检查结果。
2. 确认旧任务板全部完成且普通领取无可领取任务。
3. 重新领取应用层任务，新增完成队列重开能力。
4. 重新领取 CLI 任务，接入 `--reset-completed` 参数。
5. 验证未完成队列重开会被拒绝，运行协作队列定向测试和布局验证。
6. 重新领取归档任务，写入中文记录。

# 执行过程

1. 已确认 `agent-work-init` 默认只复用旧任务板，`agent-work-claim` 在全完成队列上没有可领取任务。
2. 已新增 `AgentWorkCoordinator::initialize_with_reset_completed`，保留原有初始化行为。
3. 已新增 `AgentWorkError::QueueNotCompleted`，用于拒绝未完成队列重开。
4. 已新增 `SelfForgeApp::init_agent_work_queue_with_reset_completed`。
5. 已在 CLI 中接入 `agent-work-init --reset-completed`，并同步 README 与帮助文本。
6. 已验证在队列尚未全部完成时使用 `--reset-completed` 会返回明确错误。

# 代码变更

1. `src/app/agent/coordination.rs` 支持全部完成队列重开，重开时保留既有事件并追加 `restart` 事件。
2. `src/app/agent/coordination/model.rs` 新增 `QueueNotCompleted` 错误。
3. `src/app/minimal_loop.rs` 新增应用层重开入口。
4. `src/main.rs` 新增 `--reset-completed` 参数解析和输出动作识别。
5. `README.md` 的协作任务板初始化示例加入 `--reset-completed`。

# 测试结果

1. `cargo test agent_work` 通过，12 个协作队列测试全部通过。
2. `cargo run -- validate` 通过，检查 30 个路径。
3. 未完成队列重开验证通过：命令返回明确错误，未覆盖任务板。
4. `cargo fmt --check` 通过。
5. `cargo test` 通过，182 个测试全部通过。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open` 返回无开放错误。
8. `git diff --check` 通过。

# 错误总结

本轮解决了一个流程阻断：旧任务板全部完成后无法开启下一轮任务。新增重开能力采用显式参数和完成状态检查，避免隐式覆盖仍在进行的任务。

# 评估

系统现在可以在完成一轮协作后通过 `agent-work-init --reset-completed` 开启下一轮协作目标，为继续拆分 `src/main.rs` 或其他大文件提供任务板边界。

# 优化建议

下一步应在本轮提交后使用 `agent-work-init --reset-completed --threads 3 "拆分 src/main.rs CLI 结构"` 开启新的任务板，再按新任务边界拆分 CLI 参数解析和输出格式化。

# 可复用经验

1. 调度系统本身也需要可演进入口，完成队列不能成为后续工作的死端。
2. 重开任务板必须是显式动作，并且只允许在所有任务完成后执行。
3. 对调度能力的修复应先验证拒绝路径，确保不会覆盖活跃协作任务。

## v0.1.65 分支协作流程规范最终记忆

# 版本信息
- 版本号：v0.1.65
- 时间：2026-05-04
- 父版本：v0.1.65

# 目标

建立从独立任务分支到 `master` 合并再删除分支的强制流程，让多个 AI 可以用独立分支、独立工作树和协作任务板同步修改代码，同时保持审计、验证和回滚边界清晰。

# 计划（Plan）

1. 确认 `master` 干净、当前版本可进化且没有开放错误。
2. 创建本轮任务分支 `codex/branch-workflow-rules`。
3. 读取近期热记忆和经验摘要。
4. 重开并领取协作任务板任务。
5. 更新 `Agents.md`，写入分支隔离、合并验证和删除分支规则。
6. 执行格式检查、测试、布局验证、前置检查和开放错误查询。
7. 写入任务、错误、版本和记忆归档。
8. 在任务分支提交，合并回 `master`，通过验证后删除任务分支。

# 执行过程

1. 已读取最近 5 条热记忆和经验摘要，确认重开任务板和结构化追踪是当前可复用经验。
2. 已通过前置检查，当前版本 `v0.1.65` 未解决错误为 0。
3. 已从干净 `master` 创建任务分支 `codex/branch-workflow-rules`。
4. 已重开协作任务板并领取架构任务。
5. 已在 `Agents.md` 中新增分支隔离最高原则。
6. 已把开发流程改为包含创建分支、分支提交、合并回 `master`、合并后验证和删除分支。
7. 已把多 AI 协作规则改为要求独立任务分支、独立 worker 和独立物理工作树。
8. 已新增“分支与合并规范”章节，约束分支命名、同步、验证、合并、冲突处理和删除条件。
9. 已完成协作任务板中架构、应用、CLI、测试和归档任务的收口。

# 代码变更

1. `Agents.md` 增加分支隔离原则。
2. `Agents.md` 更新开发流程步骤。
3. `Agents.md` 更新多 AI 协作任务板规则。
4. `Agents.md` 新增分支与合并规范章节，并顺延后续章节编号。
5. `workspaces/v0/artifacts/agents/coordination/work-queue.json` 记录本轮任务板重开、领取和完成事件。
6. `forge/tasks/v0.md`、`forge/errors/v0.md`、`forge/versions/v0.md` 和 `forge/memory/v0.md` 追加本轮归档。

# 测试结果

1. `cargo run -- validate` 通过，检查 30 个路径。
2. `cargo fmt --check` 通过。
3. `git diff --check` 通过。
4. `cargo test` 通过，182 个测试全部通过。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open` 返回无开放错误。

# 错误总结

本轮没有新增未解决错误。文档规则变更未触发中文 Markdown 审计失败，也未影响 Rust 测试。

# 评估

分支流程现在进入最高原则、开发流程、多 AI 协作规则和专门的 Git 合并章节，后续 AI 操作可以按固定命令顺序执行，减少直接在 `master` 写入、分支遗留、合并后不验证和多个 AI 共用工作树带来的风险。

# 优化建议

后续可以在 CLI 中增加只读检查命令，用于提示当前分支、任务编号和协作任务板状态是否一致，进一步减少人工执行分支流程时的顺序错误。

# 可复用经验

1. 多 AI 并行时，任务板只能管理逻辑写入范围，Git 分支和独立工作树才能隔离物理文件修改。
2. 合并后必须在 `master` 再验证，否则任务分支验证无法覆盖合并冲突或主干变化。
3. 分支删除必须绑定 `master` 验证成功，验证失败时保留分支便于修复和追溯。

## v0.1.65 分支流程只读检查命令最终记忆

# 版本信息
- 版本号：v0.1.65
- 时间：2026-05-04
- 父版本：v0.1.65

# 目标

实现 `branch-check` 只读检查命令，让 AI 在线程写入前能检查当前 Git 分支、基础分支、协作任务板、worker 和任务编号是否一致，降低多 AI 并行修改时的流程错误。

# 计划（Plan）

1. 从最新 `master` 创建任务分支。
2. 读取近期热记忆和经验摘要。
3. 重开协作任务板并领取任务。
4. 在 `Agents.md` 中定义 `branch-check` 只读边界。
5. 在应用层实现分支检查报告和阻断判断。
6. 在 CLI 中接入命令解析和中文输出。
7. 增加定向测试并执行完整验证。
8. 写入 major 聚合归档，提交、合并和推送。

# 执行过程

1. 已确认 `master` 与远端同步，当前版本无开放错误。
2. 已创建任务分支 `codex/branch-check-command`。
3. 已重开协作任务板，目标为“实现分支流程只读检查命令”。
4. 已在 `Agents.md` 中补充 `branch-check` 的只读规则、允许的任务板审计改动和阻断项。
5. 已在 `SelfForgeApp` 中新增 `branch_check`，读取当前分支、基础分支、未提交路径、协作任务板和任务领取关系。
6. 已在 CLI 中新增 `branch-check`，支持 `--worker`、`--task`、`--base` 和版本参数。
7. 已在 README 中补充常用命令示例。
8. 已新增测试覆盖 `master` 阻断和已领取任务分支允许写入的路径。

# 代码变更

1. `src/app/minimal_loop.rs` 新增 `BranchCheckReport`、`BranchCheckError`、Git 只读检查辅助函数和 `SelfForgeApp::branch_check`。
2. `src/app/mod.rs` 和 `src/lib.rs` 重新导出分支检查类型。
3. `src/main.rs` 新增 `branch-check` 命令、参数解析和中文报告输出。
4. `src/tests/app_loop.rs` 新增 2 个分支检查测试和 Git 测试夹具。
5. `README.md` 和 `Agents.md` 同步补充分支检查入口。
6. `workspaces/v0/artifacts/agents/coordination/work-queue.json` 记录本轮任务板事件。
7. `forge/tasks/v0.md`、`forge/errors/v0.md`、`forge/versions/v0.md` 和 `forge/memory/v0.md` 追加本轮记录。

# 测试结果

1. `cargo test branch_check` 通过，2 个定向测试全部通过。
2. `cargo fmt --check` 通过。
3. `git diff --check` 通过。
4. `cargo test` 通过，184 个测试全部通过。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open` 返回无开放错误。

# 错误总结

本轮没有新增未解决错误。开发中发现初版 Git 状态读取会因为整体 `trim` 丢失第一条状态路径首字母，已改为保留原始 Git 输出，仅在具体字段处裁剪，并通过 CLI 试运行复验。

# 评估

`branch-check` 把上一轮写入文档的分支规则转成可执行的只读检查入口。它不会代替任务领取或 Git 合并，但能在 AI 开始写入前暴露常见阻断项，包括仍在 `master`、基础分支不可读、分支落后、非任务板改动、任务未被指定 worker 领取和分支名缺少任务编号。

# 优化建议

后续可以让 `agent-work-claim` 输出建议的分支名，或增加只读 `branch-check --suggest`，把任务编号、目标和 worker 组合成稳定分支名，进一步减少分支命名不一致。

# 可复用经验

1. 只读检查命令必须避免副作用，不能顺手领取任务或切换分支。
2. 协作任务板文件是允许的审计改动，否则领取任务后检查会被自己的任务事件阻断。
3. 解析 Git 状态时不能对整段输出做整体裁剪，否则会破坏首行 porcelain 状态列。

## v0.1.65 分支命名建议最终记忆

# 版本信息
- 版本号：v0.1.65
- 时间：2026-05-04
- 父版本：v0.1.65

# 目标

为 `branch-check` 增加 `--suggest` 参数，让 AI 能通过只读命令获得规范任务分支名建议，减少分支名与任务编号不一致的问题。

# 计划（Plan）

1. 从最新 `master` 创建任务分支。
2. 读取近期热记忆和经验摘要。
3. 重开协作任务板并领取任务。
4. 在 `Agents.md` 中定义 `branch-check --suggest` 只读边界。
5. 在应用层实现分支名建议生成。
6. 在 CLI 中接入 `--suggest` 参数和中文输出。
7. 增加定向测试并执行完整验证。
8. 写入 major 聚合归档，提交、合并和推送。

# 执行过程

1. 已确认 `master` 与远端同步，当前版本无开放错误。
2. 已创建任务分支 `codex/branch-check-suggest`。
3. 已重开协作任务板，目标为“增加分支命名建议”。
4. 已在 `Agents.md` 中补充 `branch-check --suggest` 的只读规则和建议来源优先级。
5. 已在 `BranchCheckReport` 中新增 `suggested_branch` 和 `suggested_branch_source`。
6. 已在应用层实现分支名建议生成：显式任务编号优先，其次 worker 已领取任务，最后任务板下一条待处理任务。
7. 已在 CLI 中新增 `--suggest` 参数，并输出建议分支和来源。
8. 已在 README 中更新常用命令示例。
9. 已新增测试覆盖显式任务编号生成建议分支。

# 代码变更

1. `src/app/minimal_loop.rs` 新增分支建议字段、建议结构和分支名规范化函数。
2. `src/main.rs` 新增 `branch-check --suggest` 参数解析和输出。
3. `src/tests/app_loop.rs` 新增分支建议测试。
4. `README.md` 和 `Agents.md` 同步补充 `--suggest` 入口。
5. `workspaces/v0/artifacts/agents/coordination/work-queue.json` 记录本轮任务板事件。
6. `forge/tasks/v0.md`、`forge/errors/v0.md`、`forge/versions/v0.md` 和 `forge/memory/v0.md` 追加本轮记录。

# 测试结果

1. `cargo test branch_check` 通过，3 个定向测试全部通过。
2. `cargo run -- branch-check --suggest --worker ai-1 --task coord-002-application` 已输出建议分支。
3. `cargo fmt --check` 通过。
4. `git diff --check` 通过。
5. `cargo test` 通过，185 个测试全部通过。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，当前版本 `v0.1.65`，未解决错误 0，可进化。
8. `cargo run -- errors --current --open` 返回无开放错误。

# 错误总结

本轮没有新增未解决错误。分支建议保持只读，不会创建分支、切换分支或修改任务板。

# 评估

`branch-check --suggest` 把分支命名规范转成可执行提示，能在任务领取和分支创建之间提供稳定名称。建议来源会在输出中显示，便于审计分支名来自显式任务、worker 已领取任务还是任务板下一任务。

# 优化建议

后续可以把 `branch-check --suggest` 与协作任务板状态输出联动，在 `agent-work-status` 中显示每个待处理任务的建议分支名，但仍保持只读和不创建分支。

# 可复用经验

1. 分支名建议应优先来自结构化任务编号，而不是自然语言目标。
2. 只读建议命令要显示来源，方便审计和排错。
3. 建议命令可以辅助人工或 AI 决策，但不能替代 Git 分支创建命令。

## v0.1.65 分阶段 PR 流程本地阶段记忆

# 版本信息
- 版本号：v0.1.65
- 时间：2026-05-04
- 父版本：v0.1.65

# 目标

建立更适合多轮 AI 协作的企业级本地分支流程：相关任务组在同一分支中持续本地提交和微调，只有用户确认任务组完成后才提升版本、统一推送并创建 Pull Request。

# 计划（Plan）

1. 明确每次用户消息不等于任务组完成。
2. 明确未提升版本的阶段性本地提交不携带版本号。
3. 明确每次 PR 才对应一次小版本提升。
4. 增加任务板压缩能力，降低 AI 读取噪声。
5. 增加任务板阻断能力，保留旧任务审计但防止误领取。
6. 处理当前任务板旧待办并执行完整本地验证。
7. 本地提交阶段性成果，暂不 push、不创建 PR、不提升版本。

# 执行过程

1. 在分支 `codex/coord-009-batched-pr-flow` 中继续本地开发。
2. 更新 `Agents.md`、`git-pr.md` 和 `testing-output.md`，形成分阶段提交和最终 PR 规则。
3. 新增 `agent-work-compact --keep-events N`，压缩已完成任务提示词和旧事件。
4. 新增 `agent-work-block TASK_ID --reason TEXT`，将过期任务标记为已阻断。
5. 使用阻断命令处理 4 个旧默认待办任务。
6. 使用压缩命令将任务板事件控制在近期事件范围。
7. 所有阶段性提交均未携带版本号，因为本阶段没有提升版本。

# 代码变更

1. `src/app/agent/coordination.rs` 新增任务板压缩和阻断逻辑。
2. `src/app/agent/coordination/model.rs` 新增压缩报告和阻断错误类型。
3. `src/app/minimal_loop.rs` 新增应用层入口。
4. `src/main.rs` 新增 `agent-work-compact` 和 `agent-work-block` 命令。
5. `src/tests/agent_core.rs` 新增任务板压缩和阻断测试。
6. `README.md` 和 `agent-coordination.md` 补充中文命令说明。
7. `work-queue.json` 压缩旧事件并阻断过期旧任务。

# 测试结果

1. `cargo test agent_work_compact` 通过。
2. `cargo test agent_work_block` 通过。
3. `git diff --check` 通过。
4. `cargo fmt --check` 通过。
5. `cargo test` 通过，189 个测试全部通过。
6. `cargo run -- validate` 通过。
7. `cargo run -- preflight` 通过。
8. `cargo run -- errors --current --open` 返回无开放错误。

# 错误总结

本阶段没有新增未解决错误。旧默认待办任务不是运行错误，而是协作队列上下文遗留；已通过受控阻断命令处理，避免后续 AI 误领取。

# 评估

分阶段 PR 流程现在更符合节省 GitHub Actions 资源和多轮 AI 微调的要求。任务板压缩降低了热调度文件的上下文成本，任务板阻断保留审计同时避免旧任务继续参与调度。

# 优化建议

后续可在任务组最终收束前增加只读收束检查命令，确认待领取为 0、已领取为 0、开放错误为 0、验证全部通过，再允许提升版本和创建 PR。

# 可复用经验

1. 本地阶段性提交和最终版本提升提交必须使用不同格式。
2. 热调度文件应有压缩机制，避免历史 prompt 和事件无限增长。
3. 过期任务应阻断而不是删除，这样既能避免误领取，又能保留审计链路。
4. 只有用户确认任务组完成后才触发远端 CI，能显著减少无效 GitHub Actions 消耗。

## v0.1.66 分阶段 PR 流程最终收束记忆

# 版本信息
- 版本号：v0.1.66
- 时间：2026-05-04
- 父版本：v0.1.65

# 目标

在用户确认任务组收束后，将分阶段本地提交成果提升为 patch 版本，统一推送一次并创建 Pull Request，PR 正文关联 `Refs #1` 和 `https://github.com/xjuunn/SelfForge/issues/1`。

# 计划（Plan）

1. 领取最终 PR 收束任务。
2. 将项目版本从 `v0.1.65` 提升到 `v0.1.66`。
3. 补充 v0 major 聚合归档。
4. 执行格式化、测试、布局验证、前置检查和开放错误查询。
5. 以一次最终版本提交完成本地收束，然后统一 push 并创建 PR。

# 执行过程

1. 已在 `codex/coord-009-batched-pr-flow` 中追加并领取 `coord-014-finalize-pr`。
2. 已更新 `Cargo.toml`、`Cargo.lock`、`README.md`、`src/lib.rs` 和 `state/state.json` 的版本线索。
3. 已将本次最终收束记录追加到 `forge/tasks/v0.md`、`forge/memory/v0.md`、`forge/errors/v0.md` 和 `forge/versions/v0.md`。

# 代码变更

1. 本次最终收束只做版本、状态和归档更新。
2. 前序本地提交已完成分阶段 PR 规则、任务板压缩命令和任务板阻断命令。

# 测试结果

1. `git diff --check` 通过。
2. `cargo fmt --check` 通过。
3. `cargo test` 首次发现测试夹具仍期望旧版本，修正后 189 个测试全部通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.66`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open` 返回无开放错误。

# 错误总结

本次最终收束没有新增未解决错误。版本提升后的测试夹具期望已同步更新并通过完整测试复验。

# 评估

该流程把多轮本地微调和最终远端 CI 明确分离，符合节省 GitHub Actions 资源和企业级 PR 审计的要求。

# 优化建议

后续可以增加只读收束检查命令，在版本提升前自动确认任务板无待领取、无已领取、无开放错误且验证命令齐备。

# 可复用经验

1. 任务组内的阶段提交不携带版本号，最终版本提升提交和 PR 标题才携带版本号。
2. 远端分支只在最终收束时 push 一次，避免每次对话微调都触发 CI。
3. PR 正文负责连接 GitHub Issue，最终回复负责补充 PR、合并和分支删除状态。

## v0.1.66 终态任务板重开修复记忆

# 版本信息
- 版本号：v0.1.66
- 时间：2026-05-04
- 父版本：v0.1.66

# 目标

让协作任务板在旧任务全部处于已完成或已阻断终态时，可以使用 `agent-work-init --reset-completed` 重开下一轮协作。

# 计划（Plan）

1. 复现含已阻断任务的终态队列无法重开问题。
2. 修改终态判断，保持待领取和已领取任务仍然阻断重开。
3. 补充测试、文档和归档。

# 执行过程

已创建分支 `codex/coord-015-terminal-queue-reset`，追加并领取 `coord-015-terminal-queue-reset`，开始修复队列重开终态判断。

# 代码变更

`src/app/agent/coordination.rs` 将重开条件从全部 `Completed` 调整为全部终态，终态包含 `Completed` 和 `Blocked`。

# 测试结果

1. `cargo test agent_work_reset_completed` 通过，2 个定向测试全部通过。
2. `git diff --check` 通过。
3. `cargo fmt --check` 首次提示新增测试需要格式化；执行 `cargo fmt` 后复验通过。
4. `cargo test` 通过，191 个测试全部通过。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.66`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open` 返回无开放错误。

# 错误总结

本阶段发现的问题是协作队列终态判断过窄，不是数据损坏；已通过定向测试和完整测试验证。

# 评估

该修复让阻断任务保留审计价值的同时，不会阻塞下一轮协作队列重开。

# 优化建议

后续可以把任务板状态输出增加“可重开”只读提示，减少人工判断。

# 可复用经验

已阻断任务是终态任务，应参与审计保留，但不应被视为活跃任务。

## v0.1.66 协作任务组收束检查命令记忆

# 版本信息
- 版本号：v0.1.66
- 时间：2026-05-04
- 父版本：v0.1.66

# 目标

在任务组最终版本提升、统一 push 和 Pull Request 之前，提供一个只读命令确认任务板已经收束。

# 计划（Plan）

1. 复用协作任务板状态读取能力统计任务状态。
2. 复用错误归档读取能力统计开放错误。
3. 新增 CLI 展示、测试、中文文档和 v0 聚合归档。

# 执行过程

已在 `codex/coord-015-terminal-queue-reset` 中追加并领取 `coord-016-finalize-check`，实现 `agent-work-finalize-check` 只读收束检查。

# 代码变更

1. `src/app/minimal_loop.rs` 新增 `AgentWorkFinalizeCheckReport` 和 `agent_work_finalize_check`。
2. `src/main.rs` 新增命令入口和中文输出。
3. `src/tests/agent_core.rs` 新增 3 个收束检查测试。

# 测试结果

1. `cargo test agent_work_finalize_check` 通过。
2. `git diff --check` 通过。
3. `cargo fmt --check` 通过。
4. `cargo test` 通过，194 个测试全部通过。
5. `cargo run -- validate`、`cargo run -- preflight` 和 `cargo run -- errors --current --open` 均通过。

# 错误总结

本阶段没有新增未解决错误。当前任务完成后，`agent-work-finalize-check --current` 已报告任务板无待领取、无已领取、无开放错误，可收束。

# 评估

该命令把“是否可以进入最终 PR 阶段”变成可执行、可审计的只读检查，减少人工误判和提前 push 的风险。

# 优化建议

后续最终收束流程可以在版本提升前自动调用该检查，并在不可收束时输出任务编号列表。

# 可复用经验

收束检查应只读取任务板和错误归档，不应顺手完成任务、修改状态或触发版本提升。

## v0.1.66 无关后续任务分支规则记忆

# 版本信息
- 版本号：v0.1.66
- 时间：2026-05-04
- 父版本：v0.1.66

# 目标

明确后续任务与当前分支完全无关时的企业级分支处理方式，避免把无关改动塞进同一个 PR。

# 计划（Plan）

1. 领取规则文档任务。
2. 更新入口规则和 Git/PR 细分规则。
3. 更新回复输出规则。
4. 补充 v0 聚合归档并执行验证。

# 执行过程

已追加并领取 `coord-017-unrelated-task-pr-rule`，将无关任务自动收束当前分支和新建独立分支的规则写入文档。

# 代码变更

本阶段仅修改中文规则文档和聚合归档，没有修改 Rust 源码。

# 测试结果

1. `git diff --check` 通过。
2. `cargo fmt --check` 通过。
3. `cargo run -- validate` 通过，检查 30 个路径。
4. `cargo run -- preflight` 通过，当前版本 `v0.1.66`，未解决错误 0，可进化。
5. `cargo run -- errors --current --open` 返回无开放错误。

# 错误总结

本阶段暂未发现新增未解决错误。

# 评估

该规则让“继续当前分支”与“先收束并另开分支”有明确判断边界，减少跨任务污染和大型混合 PR。

# 优化建议

后续可以让 `branch-check` 输出当前任务与分支任务组的相关性提示。

# 可复用经验

完全无关任务需要通过小版本 PR 切断上下文，再从最新 `master` 创建新分支。

## v0.1.67 分支任务组最终收束记忆

# 版本信息
- 版本号：v0.1.67
- 时间：2026-05-04
- 父版本：v0.1.66

# 目标

将当前分支中围绕分阶段 PR 流程的后续修复和规则补充收束为一个 patch 版本，并通过一次 Pull Request 进入 `master`。

# 计划（Plan）

1. 确认任务板可收束且远端 `master` 无落后。
2. 将项目版本提升到 `v0.1.67`。
3. 同步测试期望和 v0 聚合归档。
4. 执行完整本地验证。
5. 创建最终版本提交，统一 push 并创建 PR。

# 执行过程

已确认当前分支领先 `origin/master` 3 个本地阶段提交，任务板无待领取、无已领取、无开放错误。已追加并领取 `coord-018-finalize-v067`，开始最终版本提升。

# 代码变更

1. `v0.1.67` 收束包含终态任务板重开修复。
2. `v0.1.67` 收束包含 `agent-work-finalize-check` 只读收束检查命令。
3. `v0.1.67` 收束包含无关后续任务先收束当前分支、再另开分支的规则。
4. 最终收束同步版本文件、状态文件和测试版本期望。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，194 个测试全部通过。
3. `git diff --check` 通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.67`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open` 返回无开放错误。
7. `cargo run -- agent-work-finalize-check --current` 通过，任务板无待领取、无已领取、无开放错误，可收束。

# 错误总结

本次最终收束暂未发现新增未解决错误。

# 评估

该版本把当前分支的本地阶段成果整理成一个可审计的 patch PR，符合“相关任务本地多次提交、最终统一 push 一次”的规范。

# 优化建议

后续可以把最终收束流程封装为命令，自动执行版本提升、收束检查、PR 正文生成和分支删除确认。

# 可复用经验

最终 PR 阶段才提升版本；阶段性提交不携带版本号，最终版本提交和 PR 标题携带新版本号。

## v0.1.67 任务板状态建议分支名记忆

# 版本信息
- 版本号：v0.1.67
- 时间：2026-05-04
- 父版本：v0.1.67

# 目标

让协作任务板状态输出直接展示活跃任务的建议分支名，便于 AI 从同一事实源获取分支命名建议。

# 计划（Plan）

1. 新建任务分支并领取协作任务。
2. 在 `agent-work-status` 输出中追加活跃任务建议分支名。
3. 补充 CLI 单元测试、README、Git/PR 规则和 v0 聚合归档。
4. 执行本地验证。

# 执行过程

已创建 `codex/coord-019-status-branch-suggestions`，追加并领取 `coord-019-status-branch-suggestions`。实现保持只读，仅改变 CLI 展示。

# 代码变更

1. `src/main.rs` 新增任务编号到 `codex/` 分支名的只读格式化逻辑。
2. `agent-work-status` 仅对 `Pending` 和 `Claimed` 任务显示建议分支名。
3. `README.md` 和 `git-pr.md` 补充说明该提示只读。

# 测试结果

1. `cargo fmt --check` 首次提示测试断言需要格式化；执行 `cargo fmt` 后复验通过。
2. `cargo test` 通过，194 个库测试和 2 个 CLI 单元测试全部通过。
3. `git diff --check` 通过。
4. `cargo run -- validate`、`cargo run -- preflight` 和开放错误查询均通过。

# 错误总结

本阶段没有新增未解决错误。唯一处理项是格式化提示，已通过 `cargo fmt` 修复。

# 评估

任务板状态现在能直接给出活跃任务分支建议，减少 AI 在创建分支前重复猜测任务编号和分支名。

# 优化建议

后续可以让任务板状态输出支持 `--active-only`，在队列很长时只展示待领取和已领取任务。

# 可复用经验

分支建议应来自结构化任务编号，并明确保持只读，不替代实际 Git 分支创建操作。

## v0.1.68 任务板状态建议分支名最终收束记忆

# 版本信息
- 版本号：v0.1.68
- 时间：2026-05-04
- 父版本：v0.1.67

# 目标

将任务板状态建议分支名功能作为独立小版本完成最终收束，创建 Pull Request 并关联 Issue #1。

# 计划（Plan）

1. 完成功能实现、测试和规则文档。
2. 追加最终收束任务。
3. 提升 patch 版本到 `v0.1.68`。
4. 同步测试期望和 v0 聚合归档。
5. 执行完整验证并创建 PR。

# 执行过程

已追加并领取 `coord-020-finalize-v068`，将版本线索从 `v0.1.67` 提升到 `v0.1.68`，并同步测试期望。

# 代码变更

1. `agent-work-status` 显示待领取和已领取任务的建议分支名。
2. 新增 CLI 单元测试覆盖活跃任务显示和任务编号清洗。
3. 最终收束同步版本、状态、测试期望和 v0 聚合归档。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，194 个库测试和 2 个 CLI 单元测试全部通过。
3. `git diff --check` 通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.68`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open` 返回无开放错误。
7. `cargo run -- agent-work-finalize-check --current` 通过，任务板无活跃任务并且无开放错误。

# 错误总结

本次最终收束暂未发现新增未解决错误。

# 评估

该功能让任务板继续成为多 AI 协作的单一事实源，AI 可以从状态输出中直接获得结构化分支建议。

# 优化建议

后续可以增加任务板状态过滤参数，减少已完成历史任务对输出的干扰。

# 可复用经验

任务状态输出可以提供只读辅助信息，但不能执行 Git 操作或隐式改变任务板。
## v0.1.68 任务板活跃过滤与大文件拆分本地阶段记忆

# 版本信息
- 版本号：v0.1.68
- 时间：2026-05-04
- 父版本：v0.1.67

# 目标

减少长任务板输出噪音，并把新增 CLI 展示逻辑从过大的 `src/main.rs` 拆分到独立模块。

# 计划（Plan）

1. 为当前任务创建独立分支并领取协作任务。
2. 拆分 `agent-work-status` 状态输出模块。
3. 增加 `--active-only` 只读过滤参数。
4. 更新中文文档和架构规则。
5. 执行测试、验证和本地阶段归档。

# 执行过程

已创建 `src/cli/agent_work_status.rs` 和 `src/cli/mod.rs`，将任务板状态输出、租约展示、分支建议、过滤参数解析和相关测试移出 `src/main.rs`。同时补充 `forge/architecture/agent-rules/code-architecture.md`，明确文件过大时必须拆分。

# 代码变更

1. `agent-work-status --active-only` 仅展示待领取和已领取任务明细。
2. 默认 `agent-work-status` 仍展示完整任务板。
3. 任务统计仍基于完整队列，避免过滤后丢失审计上下文。
4. CLI 状态输出逻辑迁移到 `src/cli/agent_work_status.rs`。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，194 个库测试和 5 个 CLI 单元测试全部通过。
3. `git diff --check` 通过。
4. `cargo run -- validate`、`cargo run -- preflight` 和开放错误查询通过。

# 错误总结

首次定向测试发现 `ForgeState` 测试夹具字段类型应为 `Option<String>`，已修正并复验通过。

# 评估

本阶段同时改善了任务板可读性和 CLI 入口文件体积增长风险，符合“文件过大自动拆分”的架构要求。

# 优化建议

后续自我进化循环应作为独立任务组实现，优先放入应用层模块，CLI 只保留一键入口。

# 可复用经验

当新增功能命中大入口文件时，应先拆出领域模块，再添加新能力；这样测试可以围绕小接口编写，后续维护成本更低。

## v0.1.69 任务板活跃过滤最终收束记忆

# 版本信息
- 版本号：v0.1.69
- 时间：2026-05-04
- 父版本：v0.1.68

# 目标

将任务板活跃过滤与大文件拆分规则作为独立 patch 版本收束，避免与后续自我进化循环任务混在同一分支。

# 计划（Plan）

1. 完成本地功能和模块拆分验证。
2. 追加最终收束任务。
3. 提升 patch 版本到 `v0.1.69`。
4. 同步测试期望和 v0 聚合归档。
5. 执行完整验证并创建 PR。

# 执行过程

已领取并完成 `coord-022-finalize-v069`，将版本从 `v0.1.68` 提升为 `v0.1.69`，并把任务板版本同步到当前稳定版本。

# 代码变更

1. `agent-work-status --active-only` 纳入 `v0.1.69`。
2. CLI 状态展示逻辑保持在 `src/cli/agent_work_status.rs`。
3. 大文件拆分规则写入 `Agents.md` 和 `forge/architecture/agent-rules/code-architecture.md`。
4. 版本、状态和测试期望完成最终同步。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 首次发现 `app_loop` 版本期望需同步，修复后全部通过。
3. `git diff --check` 通过。
4. `cargo run -- validate`、`cargo run -- preflight`、开放错误查询和任务组收束检查通过。

# 错误总结

最终收束阶段没有新增未解决错误；测试夹具少后移一位已修正。

# 评估

该版本让长任务板查询更适合多 AI 协作，同时建立了入口文件继续增长时必须拆分模块的规范。

# 优化建议

后续自我进化循环应使用独立任务分支，从应用层实现可恢复的一键循环，避免在 CLI 入口堆叠编排逻辑。

# 可复用经验

无关的大目标进入前，先把当前小任务组收束成独立版本 PR，可以保持 PR 范围清晰并节省后续排障成本。
