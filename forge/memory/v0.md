# v0 记忆记录

# 记录规则

- 本文件是热记忆文件，只保留近期完整记忆，供 `memory-context`、`memory-insights` 和 Agent 默认读取。
- 久远完整记忆迁移到 `forge/memory/archive/v0.md`，只在审计、追溯或人工复盘时读取。
- 同一 major 只维护一个热记忆文件和一个冷归档文件，禁止为小版本创建独立记忆文件。

## 压缩记忆索引

- 热记忆保留：5 条。
- 冷归档完整记忆：43 条。
- 最新热记忆：v0.1.54。
- 最早热记忆：v0.1.50。
- 归档文件：`forge/memory/archive/v0.md`。

## v0.1.54

# 版本信息
- 版本号：v0.1.54
- 时间：unix:1777639360
- 父版本：v0.1.53

# 目标

实现补丁候选应用后的验证执行记录，把格式化、测试、系统验证和预检命令的执行结果关联到同一条候选应用记录。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 扩展候选应用记录结构，保存验证状态和命令执行明细。
4. 新增候选应用验证执行流程。
5. 新增 CLI 命令。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.53 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已为补丁候选应用记录新增验证状态、验证时间和命令执行明细。
4. 已新增 `agent-patch-verify`，按白名单命令执行验证并回写同一条应用记录。
5. 已保证验证命令不使用隐式 shell 包装，未知命令会在执行前失败并写入失败验证明细。
6. 已补充成功、失败、阻断跳过和未知命令四条测试路径。
7. 已将当前版本线索提升为 `v0.1.54`。

# 代码变更

1. `src/app/agent/patch_application.rs` 扩展验证状态、验证执行记录和记录更新能力。
2. `src/app/minimal_loop.rs` 新增补丁候选应用验证用例、命令白名单、执行器和 Markdown 回写逻辑。
3. `src/main.rs` 新增 `agent-patch-verify` 命令、参数解析和验证结果输出。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出验证相关类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.54`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test ai_patch_verify` 通过，4 个测试全部成功。
3. `cargo test` 通过，150 个测试全部成功。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 43 条。
6. `cargo run -- agent-work-init --current --threads 3 "补丁候选验证后续协作"` 通过，协作任务板版本为 `v0.1.54`。
7. 记忆压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。
8. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-applications --current --limit 5` 和 `git diff --check` 均通过。

# 错误总结

本轮暂无未解决错误。开发中曾出现一次新增字段初始化遗漏，已补齐默认验证状态后通过定向测试。

# 评估

v0.1.54 把候选应用从“写入镜像”推进到“验证可审计”。系统现在能把每条验证命令的结果回写到候选应用记录中，为后续真实源码覆盖和版本提升提供机器可读证据。

# 优化建议

下一步应实现补丁验证通过后的受控源码覆盖准备记录，先生成覆盖计划、差异摘要和回滚清单，再允许进入真实源码覆盖。

# 可复用经验

验证执行必须与应用记录绑定，避免只在终端或聊天中保存结果。真实写入前的每一道闸门都应有结构化状态，且失败命令要保留退出码、超时和输出摘要。

## v0.1.53

# 版本信息
- 版本号：v0.1.53
- 时间：unix:1777646400
- 父版本：v0.1.52

# 目标

实现预演通过后的候选工作区受控应用流程，让 AI 补丁先写入候选工作区镜像目录，并保留结构化记录、验证命令和回滚提示。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增补丁候选应用存储模块。
4. 新增候选准备、预演状态校验、路径边界检查和镜像写入能力。
5. 新增应用后候选布局验证和回滚提示记录。
6. 新增 CLI 命令。
7. 补充测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.52 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiPatchApplicationStore`、`AiPatchApplicationRecord`、`AiPatchApplicationFile` 和候选应用状态。
4. 已新增 `agent-patch-apply`、`agent-patch-applications` 和 `agent-patch-application-record`。
5. 已实现候选版本准备或复用、预演状态校验、代码块镜像写入、候选布局验证和回滚提示记录。
6. 已保证候选应用只写入 `workspaces/v0/source/patch-applications/` 镜像目录，不直接覆盖仓库源码。
7. 已补充成功应用、阻断失败预演、阻断非法路径和复用已有候选四个测试。
8. 已将当前版本线索提升为 `v0.1.53`。

# 代码变更

1. `src/app/agent/patch_application.rs` 新增候选应用存储、索引、记录、摘要和错误类型。
2. `src/app/minimal_loop.rs` 新增补丁候选应用用例、镜像写入和验证记录逻辑。
3. `src/main.rs` 新增补丁候选应用 CLI。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出补丁候选应用类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.53`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test ai_patch_apply` 通过，4 个测试全部成功。
3. 首次 `cargo test` 发现 14 个版本生命周期断言仍停留在上一轮版本线索，已修复；新增非法路径阻断时曾出现变量遮蔽编译错误，已修复。
4. 再次 `cargo test` 通过，146 个测试全部成功。
5. `cargo fmt --check` 通过。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，当前版本 `v0.1.53`，未解决错误 0，可进化。
8. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
9. `cargo run -- agent-patch-applications --current --limit 5` 通过，当前版本暂无真实补丁候选应用记录。
10. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 42 条。
11. `cargo run -- agent-work-init --current --threads 3 "补丁候选应用后续协作"` 通过，协作任务板版本为 `v0.1.53`。
12. 记忆压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。
13. 最终复验 `cargo test`、`cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-applications --current --limit 5` 和 `git diff --check` 均通过。

# 错误总结

本轮出现两个已解决错误：版本生命周期测试断言滞后一位，以及非法路径阻断修正中的变量遮蔽编译错误。修复后无未解决错误。

# 评估

v0.1.53 让 AI 补丁进入候选工作区应用阶段，但仍然不覆盖仓库源码。系统现在具备草案、审计、预演、候选镜像应用四级闸门，权限扩大是可追踪、可阻断、可回滚的。

# 优化建议

下一步应实现候选应用后的验证执行记录，把格式化、测试、验证和预检命令的执行结果关联到候选应用记录。

# 可复用经验

候选应用应优先写入镜像目录，并保留与预演、审计、草案的链路。真正覆盖源码前，必须先把验证结果和回滚方案结构化。

## v0.1.52

# 版本信息
- 版本号：v0.1.52
- 时间：unix:1777642800
- 父版本：v0.1.51

# 目标

实现补丁草案审计通过后的受控应用预演流程，让 AI 补丁在真实写入源码前先生成可审计的中文预演报告和结构化记录。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增补丁应用预演存储模块。
4. 新增草案代码块提取和预演 Markdown 生成逻辑。
5. 新增应用层预演、查询和读取能力。
6. 新增 CLI 命令。
7. 补充测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.51 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiPatchPreviewStore`、`AiPatchPreviewRecord`、`AiPatchPreviewChange` 和预演状态。
4. 已新增 `agent-patch-preview`、`agent-patch-previews` 和 `agent-patch-preview-record`。
5. 已实现从补丁草案 `代码草案` 章节提取代码块，并按审计通过的写入范围生成预演变更摘要。
6. 已保证预演只写入 major 工作区产物目录，不直接修改源码。
7. 已补充预演成功、审计失败阻断和缺少代码块阻断测试。
8. 已将当前版本线索提升为 `v0.1.52`。

# 代码变更

1. `src/app/agent/patch_preview.rs` 新增补丁预演存储、索引、记录、摘要和错误类型。
2. `src/app/minimal_loop.rs` 新增补丁预演应用用例、代码块提取和 Markdown 生成逻辑。
3. `src/main.rs` 新增补丁预演 CLI。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出补丁预演类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.52`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test ai_patch_preview` 通过，3 个测试全部成功。
3. 首次 `cargo test` 发现 14 个版本生命周期断言仍停留在上一轮版本线索，已修复。
4. 再次 `cargo test` 通过，142 个测试全部成功。
5. `cargo fmt --check` 通过。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，当前版本 `v0.1.52`，未解决错误 0，可进化。
8. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
9. `cargo run -- agent-patch-previews --current --limit 5` 通过，当前版本暂无真实补丁预演记录。
10. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 41 条。
11. `cargo run -- agent-work-init --current --threads 3 "补丁应用预演后续协作"` 通过，协作任务板版本为 `v0.1.52`。
12. 记忆压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。
13. 最终复验 `cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `git diff --check`，均通过。

# 错误总结

本轮出现一个已解决测试错误：版本生命周期测试断言滞后一位。修复后无未解决错误。

# 评估

v0.1.52 将 AI 补丁从“草案”和“审计”推进到“可应用预演”，但仍保持严格克制：预演只生成中文报告和 JSON 记录，不直接修改源码。系统因此多了一道真实写入前的可审计缓冲层。

# 优化建议

下一步应实现预演通过后的候选工作区受控应用流程，应用前继续检查写入范围，应用后立即执行格式化、测试、验证和回滚准备。

# 可复用经验

扩大 AI 写代码权限时，应把权限拆成草案、审计、预演、候选应用、验证提升几个小闸门。每个闸门都要有独立记录，并且失败时也要留下阻断原因。

## v0.1.51

# 版本信息
- 版本号：v0.1.51
- 时间：unix:1777639200
- 父版本：v0.1.50

# 目标

实现 AI 自我升级执行后的中文总结报告，让真实自我升级审计记录可以进一步沉淀为便于复盘、查询和后续规划的中文 Markdown 报告。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增自我升级总结报告存储模块。
4. 新增应用层报告生成、查询和读取能力。
5. 在 `agent-self-upgrade` 成功路径自动生成报告。
6. 新增 CLI 命令。
7. 补充测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.50 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiSelfUpgradeSummaryStore`、`AiSelfUpgradeSummaryRecord` 和总结报告状态。
4. 已新增报告 Markdown 生成逻辑，覆盖目标、计划、代码变更、测试结果、错误信息、审计记录和下一步建议。
5. 已新增 `agent-self-upgrade-report`、`agent-self-upgrade-reports` 和 `agent-self-upgrade-report-record`。
6. 已将 `agent-self-upgrade` 真实成功路径接入自动报告生成，并在成功输出中显示报告编号和 Markdown 路径。
7. 已补充成功审计和失败审计的报告测试。
8. 已将当前版本线索提升为 `v0.1.51`。

# 代码变更

1. `src/app/agent/self_upgrade_summary.rs` 新增 major 级报告存储、记录、摘要和错误类型。
2. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出总结报告类型。
3. `src/app/minimal_loop.rs` 新增报告生成、查询和读取流程，并在自我升级成功后自动创建报告。
4. `src/main.rs` 新增报告命令，并在真实自我升级输出中展示报告记录。
5. `src/lib.rs` 扩展自我升级测试，并将版本期望前移到 `v0.1.51`。
6. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.51`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test ai_self_upgrade` 通过，6 个测试全部成功。
3. 首次 `cargo test` 发现 14 个版本生命周期断言仍停留在上一轮版本线索，已修复。
4. 再次 `cargo test` 通过，139 个测试全部成功。
5. `cargo fmt --check` 通过。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，当前版本 `v0.1.51`，未解决错误 0，可进化。
8. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
9. `cargo run -- agent-self-upgrade-reports --current --limit 5` 通过，当前版本暂无真实自我升级总结报告。
10. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 40 条。
11. `cargo run -- agent-work-init --current --threads 3 "自我升级总结报告后续协作"` 通过，协作任务板版本为 `v0.1.51`。
12. 记忆压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。
13. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-self-upgrade-reports --current --limit 5` 和 `git diff --check`，均通过。

# 错误总结

本轮出现一个已解决测试错误：版本生命周期测试断言滞后一位。修复后无未解决错误。

# 评估

v0.1.51 补齐了自我升级审计后的中文复盘层。系统现在不仅能记录结构化审计，还能生成面向人和后续 Agent 的总结报告，便于快速理解本次自我升级做了什么、如何验证、是否有风险以及下一步应该做什么。

# 优化建议

下一步应实现补丁草案审计通过后的受控应用预演流程，让 AI 生成的补丁先在候选工作区或沙箱中形成可审计变更，再进入测试和验证。

# 可复用经验

结构化审计适合机器查询，中文总结适合人类复盘和 Agent 规划。两者应通过记录编号互相关联，但不要重复保存敏感提示词或密钥。

## v0.1.50

# 版本信息
- 版本号：v0.1.50
- 时间：unix:1777635903
- 父版本：v0.1.49

# 目标

实现 AI 补丁草案的差异审计和冲突检查能力，在补丁应用前解析目标写入范围，阻断受保护路径和多 AI 活跃写入范围冲突。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增补丁审计存储模块。
4. 新增草案写入范围解析、路径边界检查和协作冲突检查。
5. 新增应用层审计、查询和读取能力。
6. 新增 CLI 命令。
7. 补充测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.49 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标是补丁审计和冲突检查。
3. 已新增 `AiPatchAuditStore`、`AiPatchAuditRecord`、`AiPatchAuditFinding` 和审计状态。
4. 已新增 `SelfForgeApp::ai_patch_audit`、审计记录列表和单条读取能力。
5. 已新增 `agent-patch-audit`、`agent-patch-audits` 和 `agent-patch-audit-record`。
6. 已实现草案 `允许写入范围` 解析、仓库相对路径检查、受保护路径检查和活跃协作任务冲突检查。
7. 已将当前版本线索提升为 `v0.1.50`。

# 代码变更

1. `src/app/agent/patch_audit.rs` 新增补丁审计存储、索引、记录和发现结构。
2. `src/app/minimal_loop.rs` 新增补丁审计应用用例、写入范围解析和冲突检查。
3. `src/main.rs` 新增补丁审计 CLI。
4. `src/app/mod.rs` 和 `src/lib.rs` 导出补丁审计类型。
5. `README.md` 和 `Agents.md` 补充命令和约束。
6. `Cargo.toml`、`Cargo.lock` 和 `state/state.json` 已更新到 `v0.1.50`。

# 测试结果

1. `cargo test ai_patch_audit` 通过，3 个针对性测试全部成功。
2. 首次 `cargo test` 失败，原因是 14 个版本生命周期测试仍断言上一轮版本关系；已修正为 `v0.1.50 -> v0.1.51`。
3. 再次 `cargo test` 通过，139 个测试全部成功。
4. `cargo fmt --check` 通过。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.50`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
8. `cargo run -- agent-patch-audits --current --limit 5` 通过，当前版本暂无真实补丁审计记录。
9. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 39 条。
10. `cargo run -- agent-work-init --current --threads 3 "补丁审计后续协作"` 通过，协作任务板版本为 `v0.1.50`。

# 错误总结

本轮暂无未解决错误。已解决测试断言版本偏移问题，新增错误测试覆盖受保护路径和活跃写入范围冲突。

# 评估

v0.1.50 给 AI 补丁草案增加了第一道应用前闸门：草案即使生成成功，也必须先通过写入范围审计，才能进入后续应用和验证流程。

# 优化建议

下一步应实现自我升级执行后的中文总结报告，把目标、计划、代码变更、测试、错误、审计记录和下一步建议聚合成一次可读的闭环结果。

# 可复用经验

在允许 AI 应用补丁前，必须先把写入范围、受保护边界和并行协作冲突变成结构化审计记录；审计失败应保留证据并阻断继续推进，而不是依赖人工在聊天记录中判断。
