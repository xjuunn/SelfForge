# v0 记忆记录

# 记录规则

- 本文件是热记忆文件，只保留近期完整记忆，供 `memory-context`、`memory-insights` 和 Agent 默认读取。
- 久远完整记忆迁移到 `forge/memory/archive/v0.md`，只在审计、追溯或人工复盘时读取。
- 同一 major 只维护一个热记忆文件和一个冷归档文件，禁止为小版本创建独立记忆文件。

## 压缩记忆索引

- 热记忆保留：5 条。
- 冷归档完整记忆：29 条。
- 最新热记忆：v0.1.40。
- 最早热记忆：v0.1.36。
- 归档文件：`forge/memory/archive/v0.md`。

## v0.1.40 最终记忆

# 版本信息
- 版本号：v0.1.40
- 时间：unix:1777628573
- 父版本：v0.1.39

# 目标

解决长期 Markdown 记忆无限增长的问题，新增热记忆压缩、冷归档和进化后的自动压缩能力。

# 计划（Plan）

1. 读取最近记忆和当前状态。
2. 编写任务文档。
3. 在记忆模块实现压缩归档用例。
4. 在 CLI 增加 `memory-compact`。
5. 让 `agent-evolve` 在提升成功后自动压缩热记忆。
6. 补充单元、边界和错误测试。
7. 执行真实命令验证、候选提升、文档归档和提交。

# 执行过程

先实现可重复执行的压缩函数，将 `forge/memory/v0.md` 中超出保留窗口的完整小节迁移到 `forge/memory/archive/v0.md`。随后将命令接入 CLI，并把 `agent-evolve` 的成功提升流程扩展为自动执行热记忆压缩。

# 代码变更

1. `src/app/memory.rs` 新增记忆压缩报告、错误类型、热记忆渲染和冷归档渲染。
2. `src/app/minimal_loop.rs` 新增应用层压缩入口，并在 `agent-evolve` 成功提升后自动压缩热记忆。
3. `src/main.rs` 新增 `memory-compact` 命令和输出。
4. `src/lib.rs` 新增压缩迁移、幂等执行、错误参数和自动压缩相关测试。
5. `Agents.md` 与 `README.md` 补充记忆压缩规则和命令。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，111 个测试全部成功。
3. `cargo run -- memory-compact --current --keep 8` 通过，首次将 25 条旧记忆迁入冷归档。
4. `cargo run -- memory-insights --current --limit 5` 通过，只读取热记忆文件中的近期来源。
5. `cargo run -- agent-evolve "新增记忆压缩冷归档能力"` 通过，当前稳定版本提升为 `v0.1.40`。

# 错误总结

本轮没有未解决错误。并行执行两个 `cargo run` 时出现 Cargo 文件锁等待提示，命令最终成功，不构成系统错误。

# 评估

v0.1.40 将记忆系统从“单文件持续追加”推进到“热记忆加冷归档”的结构，既保留审计追溯能力，又降低默认 AI 上下文读取成本。自动压缩接入 `agent-evolve` 后，后续进化不会继续让热记忆无限增长。

# 优化建议

下一步应继续完善 Agent 多步自动执行，让系统在受控工具范围内自动推进多个计划步骤，并在遇到需要外部输入或验证失败时暂停、归档并给出清晰原因。

# 可复用经验

记忆读取和记忆压缩要分离：默认读取只访问热记忆，冷归档只服务审计和复盘。进化流程中的维护动作应放在应用用例层，由 `agent-evolve` 统一编排，避免 CLI 或 Agent 业务逻辑各自实现一套规则。

## v0.1.39 最终记忆

# 版本信息
- 版本号：v0.1.39
- 时间：unix:1777627839
- 父版本：v0.1.38

# 目标

新增 Agent 会话自动步进能力，让已有会话可以按计划推进下一条待执行步骤，并把工具调用结果写回会话记录。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 会话执行结构。
2. 编写本轮任务计划。
3. 新增 Agent 步进请求、结果和错误结构。
4. 在应用层实现下一待执行步骤选择和工具选择策略。
5. 为 `agent-step` 增加 CLI 入口。
6. 补充正常步进、缺少 Runtime 命令、Runtime 运行关联和无待执行步骤测试。
7. 执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.38 可继续进化，开放错误为 0。
2. 已新增 `AgentStepExecutionRequest` 和 `AgentStepExecutionReport`。
3. 已新增应用层 `execute_next_agent_step`，可选择下一条待执行步骤并按已绑定工具执行。
4. 已新增 `agent-step` 命令，支持安全工具自动输入、Runtime 命令输入和 AI 提示词输入。
5. 已通过真实会话 `agent-session-1777627829933565900-000` 验证连续步进，步骤 3 生成运行记录 `run-1777627831264923400-000`。
6. 已通过 `agent-evolve "新增 Agent 会话自动步进能力"` 将候选提升为 v0.1.39。

# 代码变更

1. `src/app/agent/tool_invocation.rs` 新增 Agent 步进请求和结果结构。
2. `src/app/minimal_loop.rs` 新增 `execute_next_agent_step`、步骤工具选择策略和 `AgentStepExecutionError`。
3. `src/main.rs` 新增 `agent-step` 命令与参数解析。
4. `src/app/mod.rs`、`src/app/agent/mod.rs` 和 `src/lib.rs` 导出新的步进类型。
5. `src/lib.rs` 新增 4 个步进测试，覆盖成功步进、Runtime 输入缺失、Runtime 运行关联和无待执行步骤。
6. `README.md` 和 `Agents.md` 补充自动步进命令与规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，108 个测试全部成功。
3. `cargo run -- agent-start "验证 Agent 自动步进"` 通过，创建会话 `agent-session-1777627829933565900-000`。
4. `cargo run -- agent-step agent-session-1777627829933565900-000 --limit 2` 连续两次通过，完成步骤 1 和步骤 2。
5. `cargo run -- agent-step agent-session-1777627829933565900-000 -- target/debug/selfforge.exe --help` 通过，完成步骤 3 并记录 Runtime 运行。
6. `cargo run -- agent-evolve "新增 Agent 会话自动步进能力"` 通过，候选 v0.1.39 已提升。
7. `cargo run -- validate` 通过，检查 30 个路径。
8. `cargo run -- preflight` 通过，当前版本 v0.1.39，未解决错误 0，可进化为是。
9. `cargo run -- errors --current --open --limit 5` 返回无记录。
10. 已确认没有创建 `workspaces/v0.1.39` 或 `forge/*/v0.1.39.md`。

# 错误总结

本轮没有未解决错误。开发阶段 `cargo fmt --check` 提示 rustfmt 排版调整，已运行 `cargo fmt` 并复验通过。

# 评估

v0.1.39 让 Agent 会话从“记录计划和工具”推进到“可以执行下一步计划”，自我迭代闭环更进一步。系统仍保持克制：需要外部输入的 Runtime 和 AI 工具不会自动猜测输入，而是明确阻断或要求调用方提供。

# 优化建议

下一步应实现受控的多步执行命令，让系统可以在安全工具范围内自动推进多个步骤，并在遇到 Runtime、AI 或错误阻断时暂停并生成可审计的待办。

# 可复用经验

自动执行必须以会话中的待执行步骤为唯一来源，不能重新推断计划；缺少必要输入时保持步骤待执行，比伪造成功结果更安全。

## v0.1.38 最终记忆

# 版本信息
- 版本号：v0.1.38
- 时间：unix:1777627078
- 父版本：v0.1.37

# 目标

新增 Agent Tool 统一调用协议，优化 Agent 代码架构，让工具从计划绑定推进到可校验、可分发、可审计的应用层调用能力。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent Tool 模块。
2. 编写本轮任务计划。
3. 新增独立的 Agent Tool 调用协议模块，保持配置和执行分离。
4. 在应用层实现工具调用分发，禁止 CLI 直接解析业务逻辑。
5. 为 `agent-tool-run` 增加最小可用命令入口。
6. 补充工具调用成功、未授权、输入错误和 Runtime 关联测试。
7. 执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.37 可继续进化，开放错误为 0。
2. 已新增 `AgentToolInvocation`、`AgentToolInvocationInput` 和 `AgentToolInvocationReport`，将工具调用协议从工具配置中拆分出来。
3. 已新增应用层 `invoke_agent_tool`，统一校验 Agent 工具绑定并按工具类型分发执行。
4. 已新增 `agent-tool-run` 命令，CLI 只负责参数解析和结果展示。
5. 已覆盖 `memory.context`、`memory.insights`、`agent.session`、`runtime.run`、`ai.request` 请求预览和 `forge.archive` 状态查询。
6. 已通过 `agent-evolve "新增 Agent Tool 统一调用协议"` 将候选提升为 v0.1.38。

# 代码变更

1. `src/app/agent/tool_invocation.rs` 新增工具调用输入和结果结构。
2. `src/app/minimal_loop.rs` 新增 `invoke_agent_tool` 和 `AgentToolInvocationError`，集中处理工具权限校验、输入校验和执行分发。
3. `src/main.rs` 新增 `agent-tool-run` 命令和参数解析。
4. `src/app/mod.rs`、`src/app/agent/mod.rs` 和 `src/lib.rs` 导出新的工具调用类型。
5. `src/lib.rs` 新增工具调用协议测试，覆盖成功调用、未绑定拒绝、Runtime 运行关联和自定义工具缺失执行器。
6. `README.md` 和 `Agents.md` 补充工具调用命令与架构规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，104 个测试全部成功。
3. `cargo run -- agent-tool-run memory.insights --agent architect --limit 2` 通过。
4. `cargo run -- agent-tool-run forge.archive --agent archivist` 通过。
5. `cargo run -- agent-tool-run runtime.run --agent builder --session agent-session-1777627070452815300-000 --step 3 -- target/debug/selfforge.exe --help` 通过，生成运行记录 `run-1777627071037892500-000`。
6. `cargo run -- agent-tool-run agent.session --agent archivist --all --limit 3` 通过。
7. `cargo run -- agent-evolve "新增 Agent Tool 统一调用协议"` 通过，候选 v0.1.38 已提升。
8. `cargo run -- validate` 通过，检查 30 个路径。
9. `cargo run -- preflight` 通过，当前版本 v0.1.38，未解决错误 0，可进化为是。
10. `cargo run -- errors --current --open --limit 5` 返回无记录。
11. 已确认没有创建 `workspaces/v0.1.38` 或 `forge/*/v0.1.38.md`。

# 错误总结

测试阶段发现两个已解决问题：版本提升后 3 个测试断言仍停留在旧的下一个期望版本，已修正为 `v0.1.40` 或 `v0.1.41`；真实 CLI Runtime 工具验证时，首次 PowerShell 正则未能解析会话编号，已改用直接匹配会话标识并复验通过。本轮没有未解决错误。

# 评估

v0.1.38 将 Agent Tool 从“可配置、可绑定”推进到“可调用、可校验、可审计”，代码边界更清晰：工具定义在 Agent 模块，工具执行在应用用例层，CLI 不承载业务逻辑。这让后续多 Agent 自动执行计划具备了稳定入口。

# 优化建议

下一步应实现 Agent 执行计划的自动步进器，让会话可以按计划步骤自动选择工具、执行工具、记录结果，并在失败时自动转入错误归档和回滚流程。

# 可复用经验

工具执行必须先校验 Agent 绑定，再校验输入类型；自定义工具没有执行器时要显式失败。版本提升后，涉及“下一个候选版本”的测试断言需要按语义重新计算，不能只做机械替换。

## v0.1.37 最终记忆

# 版本信息
- 版本号：v0.1.37
- 时间：unix:1777625855
- 父版本：v0.1.36

# 目标

新增可插拔 Agent Tool 配置能力，让 Agent 可以通过内置工具、动态配置和显式绑定自由组合工具，并让计划和会话显示工具绑定结果。

# 计划（Plan）

1. 读取当前状态、最近记忆与 Agent 计划结构。
2. 编写本轮任务计划。
3. 新增 Agent Tool 结构、动态配置读取和初始化能力。
4. 将工具绑定接入 `agent-plan` 和 Agent 会话步骤。
5. 补充工具默认分配、动态配置、错误配置和计划绑定测试。
6. 执行格式化、严格测试、真实命令验证、进化提升、归档和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.36 可继续进化，开放错误为 0。
2. 已新增 `AgentToolDefinition`、`AgentToolConfig`、`AgentToolReport` 和相关错误类型。
3. 已新增 `agent-tools` 命令，可查询内置工具、动态配置和 Agent 工具分配，也可初始化 `tool-config.json`。
4. 已让 `agent-plan` 输出每个步骤的工具列表。
5. 已让 Agent 会话步骤持久化工具绑定，并通过 `agent-session` 展示。
6. 已通过 `agent-evolve "新增可插拔 Agent 工具配置"` 将候选提升为 v0.1.37。

# 代码变更

1. `src/app/agent/tools.rs` 新增可插拔工具定义、配置读取、默认工具、绑定验证和配置初始化。
2. `src/app/agent/types.rs` 和 `src/app/agent/session.rs` 为计划步骤和会话步骤新增 `tool_ids`。
3. `src/app/minimal_loop.rs` 将工具报告接入计划生成与会话创建。
4. `src/main.rs` 新增 `agent-tools` 命令，并更新 `agent-plan`、`agent-session` 的工具输出。
5. `src/lib.rs` 新增工具系统测试。
6. `README.md` 和 `Agents.md` 补充工具系统命令与规则。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，99 个测试全部成功。
3. `cargo run -- agent-tools --init` 通过，已创建 `workspaces/v0/artifacts/agents/tool-config.json`。
4. `cargo run -- agent-tools` 通过，显示 6 个内置工具和 5 个 Agent 分配。
5. `cargo run -- agent-plan --limit 3 "验证工具配置进入计划"` 通过，计划步骤显示工具绑定。
6. `cargo run -- agent-evolve "新增可插拔 Agent 工具配置"` 通过，候选 v0.1.37 已提升。
7. `cargo run -- agent-session --version v0.1.36 agent-session-1777625855927865900-000` 通过，会话步骤显示工具绑定。
8. 已确认没有创建 `workspaces/v0.1.37` 或 `forge/*/v0.1.37.md`。
9. `cargo run -- validate` 通过，检查 30 个路径。
10. `cargo run -- preflight` 通过，当前版本 v0.1.37，未解决错误 0，可进化为是。
11. `cargo run -- errors --current --open --limit 5` 返回无记录。
12. `cargo run -- agent-plan --limit 2 "验证工具配置进入计划"` 通过，计划继续显示工具绑定和记忆经验。

# 错误总结

测试阶段发现机械版本替换误改了 `Cargo.lock` 中依赖 `tracing-core` 的版本号，已恢复依赖版本并复验通过。动态配置测试初版未创建父目录，已补齐夹具目录创建。本轮没有未解决错误。

# 评估

v0.1.37 建立了 Agent Tool 的可插拔配置基础，Agent 计划从“按能力分工”进一步扩展为“按能力和工具组合执行”，为后续真实工具执行、AI 工具接入和多 Agent 编排打下了可维护边界。

# 优化建议

下一步应实现工具执行协议，让工具定义不仅能进入计划，还能由 Agent 步骤通过统一接口调用 Runtime、AI 请求、记忆读取和归档能力。

# 可复用经验

动态配置必须先做结构化验证，再进入计划；工具绑定结果应写入计划和会话，避免执行阶段重新推断导致审计不一致。

## v0.1.36 最终记忆

# 版本信息
- 版本号：v0.1.36
- 时间：unix:1777624840
- 父版本：v0.1.35

# 目标

为 Agent 会话持久化计划上下文快照，让会话记录包含记忆来源与经验摘要，提升计划可解释性和后续审计能力。

# 计划（Plan）

1. 读取当前状态、最近记忆和 Agent 会话结构。
2. 编写本轮任务计划。
3. 新增可选会话计划上下文字段，保持旧会话兼容。
4. 在应用层将 `memory-insights` 转换为会话快照。
5. 更新 `agent-start`、`agent-advance`、`agent-evolve`、`agent-verify` 和 `agent-session`。
6. 补充正常、边界和错误测试。
7. 执行格式化、严格测试、进化提升、验证和提交。

# 执行过程

1. 已通过 `preflight` 确认 v0.1.35 可继续进化，开放错误为 0。
2. 已新增 `AgentSessionPlanContext` 和 `AgentSessionMemoryInsight`。
3. 已让应用层在会话创建后附加计划上下文快照。
4. 已让 `agent-session` 输出计划依据摘要和来源版本。
5. 已通过 `agent-evolve "持久化 Agent 会话计划上下文"` 将候选提升为 v0.1.36。

# 代码变更

1. `src/app/agent/session.rs` 新增会话计划上下文快照，并保持旧会话文件兼容。
2. `src/app/minimal_loop.rs` 将 `memory-insights` 转换为会话快照，并接入 `agent-start`、`agent-advance`、`agent-evolve` 和 `agent-verify`。
3. `src/main.rs` 在 `agent-start` 和 `agent-session` 输出中展示计划依据。
4. `src/lib.rs` 新增计划上下文持久化、缺失记忆错误和旧会话兼容测试。
5. `README.md`、`Agents.md`、`Cargo.toml` 和 `Cargo.lock` 更新到 v0.1.36 相关规则与版本。

# 测试结果

1. `cargo fmt --check` 通过。
2. `cargo test` 通过，95 个测试全部成功。
3. `cargo run -- agent-start "验证计划上下文持久化"` 通过，输出计划依据摘要。
4. `cargo run -- agent-session agent-session-1777624812019886500-000` 通过，展示来源版本 v0.1.35 到 v0.1.31。
5. `cargo run -- agent-evolve "持久化 Agent 会话计划上下文"` 通过，候选 v0.1.36 已提升。
6. `cargo run -- agent-session --version v0.1.35 agent-session-1777624840044233700-000` 通过，进化会话包含计划依据。
7. 已确认没有创建 `workspaces/v0.1.36` 或 `forge/*/v0.1.36.md`。
8. `cargo run -- validate` 通过，检查 30 个路径。
9. `cargo run -- preflight` 通过，当前版本 v0.1.36，未解决错误为 0，可进化为是。
10. `cargo run -- errors --current --open --limit 5` 返回无记录。

# 错误总结

测试阶段发现初始化记忆归档可能为空白可读但没有可提取的小节，已通过显式测试夹具修正断言。本轮没有未解决错误。

# 评估

v0.1.36 让 Agent 会话不仅保存计划和执行结果，还保存计划依据，计划、记忆、执行和审计之间的连接更完整。

# 优化建议

下一步应让自动进化流程在候选验证前自动执行一组受控验证命令，并把 Runtime 运行证据绑定到同一 Agent 会话中。

# 可复用经验

会话文件中的历史依据应保存为摘要和引用，避免复制完整记忆正文；新增字段必须使用可选结构并提供旧文件兼容测试。
