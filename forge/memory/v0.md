# v0 记忆记录

# 记录规则

- 本文件是热记忆文件，只保留近期完整记忆，供 `memory-context`、`memory-insights` 和 Agent 默认读取。
- 久远完整记忆迁移到 `forge/memory/archive/v0.md`，只在审计、追溯或人工复盘时读取。
- 同一 major 只维护一个热记忆文件和一个冷归档文件，禁止为小版本创建独立记忆文件。

## 压缩记忆索引

- 热记忆保留：5 条。
- 冷归档完整记忆：47 条。
- 最新热记忆：v0.1.58。
- 最早热记忆：v0.1.54。
- 归档文件：`forge/memory/archive/v0.md`。

## v0.1.58

# 版本信息
- 版本号：v0.1.58
- 时间：unix:1777643224
- 父版本：v0.1.57

# 目标

实现基于源码覆盖提升衔接记录的候选生成准备流程，让系统在衔接记录就绪后可以受控生成或复用下一候选版本，并继续执行验证、记录和回滚检查。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增源码覆盖候选准备记录存储模块。
4. 新增应用层候选准备流程，检查提升衔接记录、当前状态、开放错误和候选版本。
5. 无候选时调用受控版本状态机生成候选，目标候选已存在时复用并验证布局。
6. 新增 CLI 命令。
7. 补充单元、边界和错误测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.57 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiPatchSourceCandidateStore`、`AiPatchSourceCandidateRecord` 和源码覆盖候选准备状态。
4. 已新增 `agent-patch-source-candidate`、`agent-patch-source-candidates` 和 `agent-patch-source-candidate-record`。
5. 已实现候选准备前置检查：提升衔接记录必须已就绪，当前稳定版本必须一致，下一候选版本必须符合 patch 规则，当前版本不能存在开放错误。
6. 已实现候选生成与复用：无候选时调用 `prepare_next_version`，目标候选已存在且匹配时复用并验证布局。
7. 已实现阻断记录：提升衔接未就绪、开放错误、版本不一致或已有不同候选时写入已阻断候选准备记录。
8. 已将当前版本线索提升为 `v0.1.58`。

# 代码变更

1. `src/app/agent/patch_source_candidate.rs` 新增源码覆盖候选准备存储、索引、记录、摘要和错误类型。
2. `src/app/minimal_loop.rs` 新增源码覆盖候选准备用例、状态检查、候选生成或复用逻辑和 Markdown 报告生成。
3. `src/main.rs` 新增源码覆盖候选准备 CLI。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出候选准备类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.58`。
6. `Agents.md` 和 `AGENTS.md` 补充源码覆盖候选准备规则。

# 测试结果

1. `cargo check` 通过。
2. `cargo test ai_patch_source_candidate` 通过，4 个测试全部成功。
3. `cargo fmt --check` 通过。
4. `cargo test` 通过，165 个测试全部成功。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.58`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
8. `cargo run -- agent-patch-source-candidates --current --limit 5` 通过，当前版本暂无真实源码覆盖候选准备记录。
9. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 47 条。
10. `cargo run -- agent-work-init --current --threads 3 "源码覆盖候选准备后续协作"` 通过，协作任务板版本为 `v0.1.58`。
11. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-candidates --current --limit 5` 和 `git diff --check` 均通过。

# 错误总结

本轮未出现未解决错误。候选准备相关定向测试一次通过。

# 评估

v0.1.58 将源码覆盖成功后的衔接记录继续接入候选状态机。系统现在能够基于就绪衔接记录受控生成或复用下一候选版本，并把状态变化、候选验证摘要和后续验证命令写入独立记录。

# 优化建议

下一步应实现基于源码覆盖候选准备记录的自动候选验证与 cycle 衔接，把候选准备、预检、cycle 结果和回滚原因写入同一条后续审计链路。

# 可复用经验

候选生成是状态变更，必须先有独立衔接记录和开放错误检查。已有候选版本不应重复生成，应优先验证并复用；存在不同候选时必须阻断，避免多个自动升级链路争用同一个状态文件。

## v0.1.57

# 版本信息
- 版本号：v0.1.57
- 时间：unix:1777642531
- 父版本：v0.1.56

# 目标

实现源码覆盖执行成功后的版本提升衔接记录，把源码覆盖执行记录、验证结果、建议提交信息和下一候选版本线索串成同一条可追溯链路。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增源码覆盖提升衔接记录存储模块。
4. 新增应用层提升衔接用例，成功执行生成就绪记录，失败或证据不足生成阻断记录。
5. 新增 CLI 命令。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.56 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiPatchSourcePromotionStore`、`AiPatchSourcePromotionRecord` 和源码覆盖提升衔接状态。
4. 已新增 `agent-patch-source-promotion`、`agent-patch-source-promotions` 和 `agent-patch-source-promotion-record`。
5. 已实现就绪检查：源码覆盖执行必须已覆盖、验证通过、未回滚、覆盖文件非空且验证运行记录存在。
6. 已实现阻断检查：回滚执行、验证证据缺失或执行未成功时会生成已阻断衔接记录。
7. 已将建议提交信息和下一候选版本线索写入同一条衔接记录，命令本身不修改状态文件、不生成候选版本、不执行真实提交。
8. 已将当前版本线索提升为 `v0.1.57`。

# 代码变更

1. `src/app/agent/patch_source_promotion.rs` 新增源码覆盖提升衔接存储、索引、记录、摘要和错误类型。
2. `src/app/minimal_loop.rs` 新增源码覆盖提升衔接用例、就绪检查和 Markdown 报告生成。
3. `src/main.rs` 新增源码覆盖提升衔接 CLI。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出提升衔接类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.57`。
6. `Agents.md` 和 `AGENTS.md` 补充源码覆盖提升衔接规则。

# 测试结果

1. `cargo check` 通过。
2. `cargo test ai_patch_source_promotion` 首次失败 1 个测试断言，原因是前置候选应用流程本来会准备候选版本；已改为验证衔接命令前后状态不变。
3. 修正后 `cargo test ai_patch_source_promotion` 通过，3 个测试全部成功。
4. `cargo fmt` 和 `cargo fmt --check` 通过。
5. `cargo test` 通过，161 个测试全部成功。
6. `cargo run -- validate` 通过，检查 30 个路径。
7. `cargo run -- preflight` 通过，当前版本 `v0.1.57`，未解决错误 0，可进化。
8. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
9. `cargo run -- agent-patch-source-promotions --current --limit 5` 通过，当前版本暂无真实源码覆盖提升衔接记录。
10. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 46 条。
11. `cargo run -- agent-work-init --current --threads 3 "源码覆盖提升衔接后续协作"` 通过，协作任务板版本为 `v0.1.57`。
12. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-promotions --current --limit 5` 和 `git diff --check` 均通过。

# 错误总结

本轮出现一个已解决测试断言问题：测试误以为衔接命令执行前候选版本应为空。实际前置候选应用流程已经准备候选版本，正确验证目标是衔接命令不改变状态。修正后复测通过。

# 评估

v0.1.57 把真实源码覆盖执行后的链路继续向版本提升推进了一步。系统现在可以把覆盖结果、验证证据、建议提交信息和下一候选版本线索保存为独立记录，为后续自动候选生成和受控提交提供可审计入口。

# 优化建议

下一步应实现基于源码覆盖提升衔接记录的候选生成准备流程，让系统在衔接记录就绪后可以受控生成下一候选版本，并继续执行验证、记录和回滚检查。

# 可复用经验

真实源码覆盖成功后仍不应直接进入版本提升。需要先生成衔接记录，把执行证据、验证证据和提交建议固定下来；自动化越接近真实提交，越需要更小的闸门和更清晰的状态不变性验证。

## v0.1.56

# 版本信息
- 版本号：v0.1.56
- 时间：unix:1777670276
- 父版本：v0.1.55

# 目标

实现源码覆盖准备通过后的受控源码覆盖执行记录，让系统可以在真实覆盖源码前后保留执行级备份、验证结果和回滚证据。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增源码覆盖执行记录存储模块。
4. 新增应用层执行流程，校验准备记录状态、目标路径、镜像文件和目标文件新鲜度。
5. 执行覆盖前写入执行级备份，覆盖后运行固定验证命令。
6. 验证失败或写入失败时按执行级备份回滚。
7. 新增 CLI 命令。
8. 补充单元、边界和错误测试。
9. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
10. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.55 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiPatchSourceExecutionStore`、`AiPatchSourceExecutionRecord`、`AiPatchSourceExecutionFile` 和源码覆盖执行状态。
4. 已新增 `agent-patch-source-execute`、`agent-patch-source-executions` 和 `agent-patch-source-execution-record`。
5. 已实现执行前新鲜度校验，目标文件在准备后变化时会阻断。
6. 已实现执行级备份、源码覆盖写入、验证命令执行和验证失败自动回滚。
7. 已补充成功覆盖、未准备计划阻断、验证失败回滚和陈旧目标阻断四条测试路径。
8. 已将当前版本线索提升为 `v0.1.56`。

# 代码变更

1. `src/app/agent/patch_source_execution.rs` 新增源码覆盖执行存储、索引、记录、摘要和错误类型。
2. `src/app/minimal_loop.rs` 新增源码覆盖执行用例、目标新鲜度校验、执行级备份、验证和回滚逻辑。
3. `src/main.rs` 新增源码覆盖执行 CLI。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出源码覆盖执行类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.56`。

# 测试结果

1. `cargo test ai_patch_source_execute` 首次失败 1 个断言，原因是陈旧目标阻断文案先命中“字节数已变化”，测试断言只匹配“过期”。
2. 修正断言后，`cargo test ai_patch_source_execute` 通过，4 个测试全部成功。
3. `cargo test` 通过，158 个测试全部成功。
4. `cargo fmt --check` 通过。
5. `cargo run -- validate` 通过，检查 30 个路径。
6. `cargo run -- preflight` 通过，当前版本 `v0.1.56`，未解决错误 0，可进化。
7. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
8. `cargo run -- agent-patch-source-executions --current --limit 5` 通过，当前版本暂无真实源码覆盖执行记录。
9. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 45 条。
10. `cargo run -- agent-work-init --current --threads 3 "源码覆盖执行后续协作"` 通过，协作任务板版本为 `v0.1.56`。
11. 记忆压缩后再次执行 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-source-executions --current --limit 5` 和 `git diff --check` 均通过。

# 错误总结

本轮出现一个已解决测试断言问题：陈旧目标阻断的实现行为正确，但测试文案匹配过窄。已改为匹配“已变化”，复测通过。

# 评估

v0.1.56 把补丁流程推进到真实源码覆盖执行阶段。系统现在可以在执行前防止过期计划，在执行后保留验证结果，并在失败时按执行级备份回滚。

# 优化建议

下一步应实现源码覆盖执行成功后的版本提升衔接记录，把覆盖执行记录、验证结果、提交信息和下一候选生成串成同一条可追溯链路。

# 可复用经验

真实覆盖源码时，准备级备份只能证明计划生成时的状态；执行级备份才是回滚的直接依据。执行前必须重新检查目标文件是否变化，执行后必须立即验证并把失败回滚写入同一条记录。

## v0.1.55

# 版本信息
- 版本号：v0.1.55
- 时间：unix:1777669147
- 父版本：v0.1.54

# 目标

实现补丁验证通过后的受控源码覆盖准备记录，让系统在真实覆盖仓库源码前先生成覆盖计划、差异摘要、回滚备份和阻断证据。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 新增源码覆盖准备记录存储模块。
4. 新增应用层准备流程，校验候选应用状态和验证状态。
5. 生成差异摘要、目标文件信息和回滚清单。
6. 新增 CLI 命令。
7. 补充单元、边界和错误测试。
8. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
9. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.54 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已新增 `AiPatchSourcePlanStore`、`AiPatchSourcePlanRecord`、`AiPatchSourcePlanFile` 和覆盖准备状态。
4. 已新增 `agent-patch-source-plan`、`agent-patch-source-plans` 和 `agent-patch-source-plan-record`。
5. 已实现候选应用已应用状态、验证通过状态和镜像文件存在性的前置检查。
6. 已实现目标文件差异摘要和回滚备份生成，准备阶段不覆盖仓库源码。
7. 已补充成功准备、未验证阻断、验证失败阻断和镜像缺失阻断四条测试路径。
8. 已将当前版本线索提升为 `v0.1.55`。

# 代码变更

1. `src/app/agent/patch_source_plan.rs` 新增源码覆盖准备存储、索引、记录、摘要和错误类型。
2. `src/app/minimal_loop.rs` 新增源码覆盖准备用例、前置状态检查、差异摘要和回滚备份逻辑。
3. `src/main.rs` 新增源码覆盖准备 CLI。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出源码覆盖准备类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.55`。

# 测试结果

1. `cargo test ai_patch_source_plan` 通过，4 个测试全部成功。
2. `cargo test` 通过，154 个测试全部成功。
3. `cargo fmt --check` 通过。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- preflight` 通过，当前版本 `v0.1.55`，未解决错误 0，可进化。
6. `cargo run -- errors --current --open --limit 5` 返回无开放错误。
7. `cargo run -- agent-patch-source-plans --current --limit 5` 通过，当前版本暂无真实源码覆盖准备记录。
8. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 44 条。
9. `cargo run -- agent-work-init --current --threads 3 "源码覆盖准备后续协作"` 通过，协作任务板版本为 `v0.1.55`。
10. 记忆压缩后再次执行 `cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5` 和 `cargo run -- agent-patch-source-plans --current --limit 5` 均通过。
11. 最终复验 `cargo fmt --check`、`cargo run -- validate`、`cargo run -- preflight` 和 `git diff --check` 均通过。

# 错误总结

本轮暂无未解决错误。定向测试和完整测试均一次通过。

# 评估

v0.1.55 把补丁流程推进到真实源码覆盖前的准备闸门。系统现在可以在覆盖前保存目标文件快照、展示差异摘要并明确回滚动作，为后续受控覆盖执行提供可审计前提。

# 优化建议

下一步应实现源码覆盖准备通过后的受控源码覆盖执行记录，在执行覆盖后立即运行格式化、测试、系统验证和预检，并在失败时使用回滚备份恢复。

# 可复用经验

真实源码覆盖前必须先生成独立准备记录。准备记录既要说明将要改什么，也要保存如何回退；只有候选应用已验证通过，才允许进入覆盖准备阶段。

## v0.1.54

# 版本信息
- 版本号：v0.1.54
- 时间：unix:1777639360
- 父版本：v0.1.53

# 目标

实现补丁候选应用后的验证执行记录，把格式化、测试、系统验证和预检命令的执行结果关联到同一条候选应用记录。

# 计划（Plan）

1. 读取最近 5 个版本记忆和当前状态。
2. 编写本轮任务文档。
3. 扩展候选应用记录结构，保存验证状态和命令执行明细。
4. 新增候选应用验证执行流程。
5. 新增 CLI 命令。
6. 补充单元、边界和错误测试。
7. 更新 README、`Agents.md`、任务、错误、版本和记忆归档。
8. 执行格式化、测试、验证、预检和提交。

# 执行过程

1. 已通过 `cargo run -- preflight` 确认 v0.1.53 可继续进化，未解决错误为 0。
2. 已通过 `cargo run -- memory-insights --current --limit 5` 读取近期经验，确认本轮目标与后续任务队列一致。
3. 已为补丁候选应用记录新增验证状态、验证时间和命令执行明细。
4. 已新增 `agent-patch-verify`，按白名单命令执行验证并回写同一条应用记录。
5. 已保证验证命令不使用隐式 shell 包装，未知命令会在执行前失败并写入失败验证明细。
6. 已补充成功、失败、阻断跳过和未知命令四条测试路径。
7. 已将当前版本线索提升为 `v0.1.54`。

# 代码变更

1. `src/app/agent/patch_application.rs` 扩展验证状态、验证执行记录和记录更新能力。
2. `src/app/minimal_loop.rs` 新增补丁候选应用验证用例、命令白名单、执行器和 Markdown 回写逻辑。
3. `src/main.rs` 新增 `agent-patch-verify` 命令、参数解析和验证结果输出。
4. `src/app/agent/mod.rs`、`src/app/mod.rs` 和 `src/lib.rs` 导出验证相关类型。
5. `Cargo.toml`、`Cargo.lock`、`README.md` 和 `state/state.json` 提升到 `v0.1.54`。

# 测试结果

1. `cargo fmt` 通过。
2. `cargo test ai_patch_verify` 通过，4 个测试全部成功。
3. `cargo test` 通过，150 个测试全部成功。
4. `cargo run -- validate` 通过，检查 30 个路径。
5. `cargo run -- memory-compact --current --keep 5` 通过，热记忆保留 5 条，冷归档共 43 条。
6. `cargo run -- agent-work-init --current --threads 3 "补丁候选验证后续协作"` 通过，协作任务板版本为 `v0.1.54`。
7. 记忆压缩后再次执行 `cargo run -- validate` 和 `cargo run -- preflight` 均通过。
8. 最终复验 `cargo fmt --check`、`cargo test`、`cargo run -- validate`、`cargo run -- preflight`、`cargo run -- errors --current --open --limit 5`、`cargo run -- agent-patch-applications --current --limit 5` 和 `git diff --check` 均通过。

# 错误总结

本轮暂无未解决错误。开发中曾出现一次新增字段初始化遗漏，已补齐默认验证状态后通过定向测试。

# 评估

v0.1.54 把候选应用从“写入镜像”推进到“验证可审计”。系统现在能把每条验证命令的结果回写到候选应用记录中，为后续真实源码覆盖和版本提升提供机器可读证据。

# 优化建议

下一步应实现补丁验证通过后的受控源码覆盖准备记录，先生成覆盖计划、差异摘要和回滚清单，再允许进入真实源码覆盖。

# 可复用经验

验证执行必须与应用记录绑定，避免只在终端或聊天中保存结果。真实写入前的每一道闸门都应有结构化状态，且失败命令要保留退出码、超时和输出摘要。
